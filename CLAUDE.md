# Memory

## Me
Justin Schmidt, justinschmidtsales@gmail.com. Building "Sales Overlay" as a personal side project (NOT for Net Revenue LLC). Not a developer — relies entirely on Claude for all code. No external dev will be hired until the product is generating revenue — Phase 2 will be built by Justin + Claude incrementally.

## Project: Sales Overlay
Real-time AI sales coaching teleprompter for high-ticket closers. Electron desktop app. Listens to live sales calls, transcribes in real time, displays a transparent overlay bar at top of screen showing the closer what to say next.

**Target vertical:** High-ticket coaching/consulting closers.
**Important:** No Net Revenue client content (SSI, GlobalBanks) — only universal sales framework structure.

## Tech Stack
| Tech | Purpose |
|------|---------|
| **Electron 41** | Desktop app, two windows (control panel + overlay) |
| **Deepgram Nova-2** | Real-time STT via raw WebSocket (SDK v5 broken, using raw WS) |
| **Anthropic Claude Sonnet** | Live teleprompter AI — always tells closer what to say next |
| **Supabase + pgvector** | Knowledge base / RAG for sales frameworks |
| **electron-audio-loopback** | Native system audio capture — no drivers needed, user just grants Screen Recording permission |
| **Chart.js 4 (CDN)** | Donut charts on the /dashboard coaching view. Loaded via `cdn.jsdelivr.net`. If the CDN goes down the dashboard center numbers still render — only the rings fail. No npm dependency. |

## Key Files
| File | What |
|------|------|
| `src/main/index.js` | Main Electron process, IPC, session lifecycle, suggestion polling timer. v1.1.2: session-start logic extracted into `startSessionNow(clientId)` so device-check commit/cancel paths can both kick off a session without duplicating the original `ipcMain.on('start-session')` body. Original IPC entry point still works — it just calls the named function. v1.1.8: `startZoomWatcher()` added (3s pgrep CptHost loop); auto-fires `ipcMain.emit('stop-session', {})` when a Zoom meeting ends while a Scout session is active, so all teardown runs identically to a manual Stop. |
| `src/renderer/overlay/overlay.html` | Overlay bar — stacking prompt display (60% opacity for old prompts, light gray text) |
| `src/renderer/control/index.html` | Control panel UI with audio device selectors |
| `src/renderer/control/control.js` | Audio capture, device enumeration, session controls |
| `src/ai/claude.js` | ClaudeCoach — delivery detection, backchannel filtering, question-first matching, prospect response gate, closer-active protection, anti-repetition (themes + exact text), objection bypass |
| `src/ai/prompts.js` | System prompt (live teleprompter mode — follow the conversation, not a script), suggestion prompt builder with 5 params (transcript, objection, KB, memory, history) |
| `src/ai/objections.js` | 4 framework + 6 secondary objections, prospect-only detection, strict fuzzy matching (4+ word triggers, 2+ distinctive words, edit distance ≤1) |
| `src/ai/call-memory.js` | Rolling summary every 8 turns (max_tokens: 500), 7-stage detection with progression guards |
| `src/ai/knowledge-base.js` | Supabase search, client filtering, framework phase lookup |
| `src/ai/script-parser.js` | Claude API parses uploaded .txt scripts into KB entries |
| `src/preload.js` | contextBridge IPC (includes logToMain for debug) |
| `scripts/seed-frameworks.js` | 65+ KB entries for sales frameworks |
| `scripts/clear-and-reseed.js` | Wipes and reseeds Supabase KB |
| `backend/lib/session-analytics.js` | Pure-ish aggregation helpers shared by `/me/*` and `/admin/*` analytics routes. `computeAnalytics(admin, userId, from, to)` returns outcome distribution + objection breakdown for the dashboard. `loadSessionObjections(admin, sessionId)` for per-call drill. `loadObjectionsByType(admin, userId, objectionId, from, to)` for the third-level drill (joins session_objections to call_sessions so each event carries prospect_name + session_outcome). Refactored from inline route code so the same logic powers both caller-scoped and admin-scoped queries. |
| `backend/routes/me.js` | Caller-scoped routes (`/me/*`). Holds the canonical `OBJECTION_LABEL_MAP` (10 labels → id + framework + framework_rebuttal text denormalized from `src/ai/objections.js`). Owns the extraction prompts: outcome inference from post_call_summary, per-objection coaching narrative generation, prospect name extraction from intro transcript, cross-session pattern recommendations. Routes export `_computeCoachingPatterns` so `admin.js` can dispatch to it for cross-user views. |
| `backend/web/dashboard.html` | Coaching dashboard with KB management section. Users upload winning calls, offer docs, landing pages. Admins see full KB management. Uses Chart.js via CDN. |
| `backend/web/coaching.html` | v1 coaching page — route still exists at `/coaching`. Kept for reference; not linked from nav. |
| `backend/web/connect.html` | `GET /connect` — the Zoom app-submission **Direct Landing URL** (install method "From Your Site"). Client-side session gate: signed-in → `/dashboard#connect` (the `#account` Connections section, scrolled to + Zoom row pulsed); signed-out → `/login?return=%2Fdashboard%23connect`. Zoom requires this URL to let a logged-in user connect Zoom and send a logged-out user to log in first. The `#connect` hash is handled in dashboard.html's `applyHashToState`/`focusConnections`. |
| `backend/lib/safe-return-path.js` | Open-redirect guard for the post-login `?return=` redirect (only same-origin absolute paths pass; off-site/scheme/protocol-relative/control-char values fall back). CANONICAL copy — the identical function is inlined into `login.html` (parses+uses `return`) and the logic is trivially mirrored in `connect.html` (only builds the encoded `return`). Contract: `backend/test/safe-return-path.test.js`. Keep the three in sync. |

## Sales Frameworks in KB
- **Identity Shifting** objection methodology (Isolate > Binary Identity > Historical Pattern > Mirror Reality > Identity Choice)
- **SPAIN** discovery (Situation > Pain > Accountability > Implications > Needs Payoff)
- **V-L-F-A-R** dialogue (Validate > Label > Frame > Ask > Repeat)
- 4 main objection frameworks: Money (8 phases), Spouse (8), Think About It (10), No Time (6)
- 7 call stages: Introduction, Set, Discovery, Transition, Pitch, Close, Objection Handling

## Current Architecture Decisions

### Prompt Flow (the core loop)
1. Prompt appears in overlay (statement + question)
2. System listens for the QUESTION portion only (extracts questions via `?` detection)
3. Closer reads/paraphrases the question → delivery detected (35% fuzzy match on meaningful words)
4. System WAITS for prospect to respond (prospect response gate)
5. Prospect speaks → "Prospect responded — ready for next prompt"
6. Prospect finishes (800ms silence — v1.0.7; was 1.5s in v1.0.6) → Claude API generates next suggestion
7. New prompt appears below, old prompt fades to 60% opacity (still readable)

### Key Protections
- **Closer-active guard** — a closer counts as "active" iff ALL three: (1) they've spoken at least once, (2) their last non-backchannel turn was within the last 5 seconds, and (3) they spoke MORE RECENTLY than the prospect (floor-handoff detection, added v1.0.7). When active, all timeouts and new prompts are frozen — no prompt pops up mid-sentence. Condition (3) is the fix for v1.0.6's thrash where the guard kept blocking for up to 5s after the prospect had already responded (observed cycle jo6buv: 4,284ms of post-prospect blocking). Backchannels don't stamp the closer's timer.
- **Backchannel filtering** — short filler words (Yeah, Right, Okay, Mhmm, etc. — 3 words or fewer) do NOT reset the closer-active timer, do NOT count toward the 4-turn auto-advance, and do NOT accumulate in delivery detection. Without this, saying "Yeah" every few seconds would permanently block new prompts.
- **Prospect response gate** — after delivery, system waits for prospect to actually answer before generating next prompt. 45s safety valve (only fires when closer is silent).
- **Prospect-finished-speaking check** — waits `this.prospectSilenceMs` (v1.0.7: 800ms; was 1500ms in v1.0.6) after prospect's last final transcript before firing next suggestion, so it doesn't fire mid-answer. Stacked on top of Deepgram's ~500ms endpointing delay = ~1300ms total effective silence window.
- **Suggestion polling timer** — `getSuggestion()` is called every 1.5s (in addition to on each transcript) so natural pauses in conversation still trigger prompts even when nobody is speaking.
- **Question-first delivery detection** — splits suggestion into statement vs question parts, only listens for the question. Closer can skip/use the statement mirror freely.
- **Delivery fallbacks** — 4 non-backchannel closer turns = auto-advance **if** at least `this.turnAutoAdvanceMinElapsedMs` (v1.0.8: 4000ms; was a per-pair ≥2s gap in v1.0.7) have elapsed since the FIRST closer turn after the suggestion appeared (`this.firstCloserSpeechAfterSuggestion`). Deepgram chunking protection preserved: one continuous sentence split into 4 sub-second chunks arrives within ~1-2s total, well under the 4s floor. The v1.0.7 per-pair gap false-negatived on fluid conversations where real closer bursts arrive <2s apart. Separately: 30s of total silence = auto-advance (Check 3, in `getSuggestion()`). Both still require prospect response after.
- **Objections bypass everything** — objection detection fires immediately regardless of delivery/prospect gates (they're urgent).

### Anti-Repetition System
- **Exact text history** — last 8 suggestions tracked, passed to Claude as "ALREADY SUGGESTED"
- **Theme/angle tracking** — last 5 unique themes tracked by headline keyword overlap (50% match = same theme). Passed as "ANGLES ALREADY EXPLORED"
- **System prompt rules** — explicit instructions to never revisit answered topics, go deeper on answers or pivot

### Overlay Display
- **50% screen width, horizontally centered**, positioned at `y: display.workArea.y` (just below macOS menu bar — NOT y:0 which goes behind it)
- **Auto-resizing window** — `ResizeObserver` on `.bar` sends `resize-overlay` IPC to main on every content change; main calls `setBounds` with `Math.max(44, height)`. This means clicks below the bar pass through naturally without needing `setIgnoreMouseEvents`. Do NOT use `setIgnoreMouseEvents` for click-through — auto-resize is the correct approach.
- **Control panel hidden** (`show: false` in createControlWindow) — audio capture runs there invisibly; receives `trigger-start-audio` / `trigger-stop-audio` IPC from main when overlay buttons are pressed
- **Overlay controls** — Scout logo + brand name (drag handle) center-left, Start (green) / Stop (red) / X buttons top-right. Start triggers `startSession('generic')`, Stop triggers `stopSession()`, X triggers `quitApp()`
- **Button visibility** — managed exclusively via inline `style.display` (`'flex'`/`'none'`). CSS classes carry appearance only, never `display:none`. Initial states set on HTML elements. All transitions go through `setSessionState(active)`.
- **Drag** — brand area (logo + name) is `-webkit-app-region: drag`; buttons are `-webkit-app-region: no-drag`
- **Stacking prompts** — new prompts appear below, old ones fade to 60% opacity with #aaa text color
- **Max 2 visible** (v1.0.8; was 3 in v1.0.7) — oldest removed when 3rd appears. Controlled by `MAX_VISIBLE_PROMPTS` in `overlay.html`. Sticky top row and ResizeObserver IPC both handle the smaller count automatically.
- **Stage badges** — color-coded (blue default, red objection, green close, amber transition)
- **Rounded bottom corners** (`border-radius: 0 0 10px 10px`)

### Discovery Tracker
- **Separate window** — top-right corner of screen (x = screen.width - 190, y = 0), 190px wide
- **Hidden until session starts** — appears on Start, hides + resets on Stop
- **7 checkboxes** — Finances/Budget, Willing to Do the Work, Pain Identified, Goals/Vision, Timeline/Urgency, Decision Maker Confirmed, Why Now
- **Auto-checks via call memory** — after each rolling summary (every 8 turns), Claude detects which items have been covered and checks them off. Once checked, never unchecked.
- **Live financial details under Finances row** — when the prospect shares specific numbers (income, savings, budget, cashflow, debt, credit), Claude extracts them into `discovery.financeDetails` (array of short strings capped at 8 entries). The details render as a green-bar-accented list directly under the Finances checkbox, and are also passed into `getContext()` so the suggestion engine can reference them during money objections. Dedupes on text, never drops entries mid-call.
- **No extra API cost** — detection + financial extraction are added to the existing call memory summary prompt, not a separate call
- **Auto-resizes** to content height via `resize-discovery` IPC (same pattern as overlay) — resize fires after every update so the panel grows as details are added

### Audio & Transcription
- **System audio loopback** — prospect audio captured natively via electron-audio-loopback (no BlackHole/virtual drivers needed). User grants Screen Recording permission.
- **Deepgram multichannel** — channels=2, channel 0 = CLOSER (mic), channel 1 = PROSPECT (system loopback)
- **Auto-selects preferred mic** — Elgato Wave:1 auto-selected over webcam mics

### Rate Limiting
- 5s min interval + 2 turn minimum between Claude API calls
- Renderer logs piped to terminal via `logToMain` IPC + `renderer-log` handler

### Architecture Decisions
- **Voyage embeddings must stay server-side** — was found as broken dead code when the desktop app called them directly (no VOYAGE_API_KEY in desktop env). All embedding generation happens on Railway via `POST /kb/search`.
- **KB search diversity** — fetch `matchCount+3` results from Supabase, guarantee at least 1 seeded framework entry in final result set. Three-tier priority: `learned_pattern` → user uploads → seeded frameworks.
- **Auto-approve all KB uploads** — no pending/approval queue at current scale. Trickle-down scoping handles visibility: owner uploads → global, admin → team, closer → personal.
- **Post-call summarization is non-blocking** — session UI clears immediately, summary arrives async. Fires after session ends, never blocks teardown.
- **Transcript tag warning** — the prospect/closer dialogue is under tag `[deepgram]` (NOT `[memory]`, which holds CallMemory's internal logs). Any future analytics work that needs transcripts must filter on `[deepgram]` + `Transcript (final)`.

## Current Status: What Works
- Deepgram real-time transcription of both speakers (closer mic + system audio loopback)
- Claude API generating next-line suggestions as live teleprompter
- Overlay bar with stacking prompts and question-first delivery detection
- Prospect response gate — next prompt waits for prospect to answer
- Closer-active protection — never interrupts mid-sentence; clears the moment the prospect speaks more recently than the closer (v1.0.7 floor-handoff fix)
- Backchannel filtering — "Yeah/Right/Okay" don't block the system
- Suggestion polling timer — prompts fire during natural pauses, not just on new transcripts
- Knowledge base search with framework phases
- Objection detection (prospect speech only, strict fuzzy matching with edit distance)
- Anti-repetition (exact suggestion history + theme/angle tracking)
- Script upload + parse infrastructure (untested by user)
- Call memory with rolling summaries (500 max_tokens, fence-stripped JSON)
- Overlay with drag handle, Scout logo, Start/Stop/X buttons — buttons confirmed working
- Sticky top row — Stop/X always visible even when prompts stack below
- Discovery Tracker — side panel top-right, 7 auto-checking items driven by call memory + live financial detail extraction under the Finances row
- Audio device enumeration and dual-input UI
- Device check panel (v1.1.2) — mic selector with live RMS level meter appears when Start is clicked. Closer confirms mic before session starts. Skip available for experienced users. Probe runs entirely in the control window (mic-only `getUserMedia` + lightweight ScriptProcessor emitting leftRMS via `audio-level-update` IPC every audio buffer). Five new IPC channels relay overlay ↔ control through main: `device-check-start` / `device-check-ready` / `audio-level-update` / `device-check-commit` / `device-check-cancel`. Commit and cancel both fire `startSessionNow('generic')` after relaying to control.
- Post-call summary (v1.1.1) — fires async after session ends. Five sections: STAGE REACHED, DISCOVERY COMPLETE, WHAT WENT WELL, AREAS TO IMPROVE, NEXT STEP. Auto-saves to `call_sessions.post_call_summary`. Overlay panel with single Dismiss button — no outcome buttons. Migration: `backend/migrations/005_post_call_summary.sql`. Post-call summarization is non-blocking — session UI clears immediately, summary arrives async.
- Knowledge Base upload system (backend-only, no desktop build needed) — users upload content via /dashboard or /admin KB section. Three ingestion types: URL fetch, PDF parse, paste text. Voyage AI embeddings server-side (voyage-3-lite, 512-dim, VOYAGE_API_KEY on Railway). Transcript-aware chunking for `winning_call` category (5 speaker turns per chunk, 2-turn overlap, falls back to fixed word chunking if <4 turns detected). Three-tier diversity enforcement in search: `learned_pattern` → user uploads → seeded frameworks. Trickle-down scoping: owner uploads → global, admin → team, closer → personal. Migration 006 added `uploaded_by`, `scope`, `source_label` columns to `knowledge_base` and updated `match_knowledge` RPC with `p_user_id` and `p_admin_id` params for scoped visibility. New routes: `POST /kb/upload`, `GET /kb/list`, `DELETE /kb/:source_label`, `POST /kb/search`, `POST /kb/store-patterns`, `GET /kb/entries/:label`. KB search now routes through proxy (`kb.search()` → `/kb/search`) instead of direct Voyage call from desktop (was broken — no key in desktop env).
- **Adaptive Learning Engine (DESIGNED, NOT YET BUILT)** — architecture designed: after every call, Claude Sonnet extracts up to 5 high-signal moments from the transcript (`POST /proxy/extract-patterns`). Each moment stored as `category: learned_pattern`, `scope: personal` in `knowledge_base` via `POST /kb/store-patterns`. Pattern extraction fires async after post-call summary is delivered to overlay — user never waits. Requires 10+ turn minimum to skip test calls. `buildContext()` renders learned patterns as `[LEARNED PATTERN: label]` in Claude context. Promotion path to team scope supported by existing schema with no changes. KB diversity update: `learned_pattern` gets a guaranteed slot alongside seeded frameworks. Admin can change scope from personal to team (schema already supports it). Awaiting implementation. **Display surface (SHIPPED as pattern-learning v1):** the Coaching Dashboard **Performance Summary** panel is the intended home for learned patterns. Today it's `GET /me/performance-synthesis` (+ `/admin/…/:user_id`) — one cached Claude synthesis per (user, range) comparing WIN-class vs LOSS-class calls, in two evidence-linked blocks (WHAT'S WORKING / WHAT TO IMPROVE). Each insight is `{claim, data, quote, clip_url, call_id}` — the SAME shape the learned_pattern extraction above will feed, so that pipeline plugs into this display with no UI change. Cached in `objection_synthesis_cache` with `synthesis_type='performance'` (migration 017; the cache table now holds both `objections` and `performance` synthesis — same set-hash invalidation + credit-tolerant `available:false`). Evidence (`quote`/`clip_url`/`call_id`) is always resolved from real DB moments by id, never LLM-invented.
- Signed + notarized .dmg via electron-builder using Apple Developer ID (identity `1CD4B87D…`, team `8QN5Y29R27`). **Current release: v1.1.2** (arm64 + x64, both notarized + stapled). Architecture baseline (since v1.0.4): the desktop app ships no secret API keys — all Claude + Deepgram traffic routes through the Railway backend's `/proxy/*` endpoints using short-lived ephemeral Deepgram keys. Supabase URL + anon key (both public per Supabase's own design) are hardcoded in `src/config.js`. Historical packaging milestones: v1.0.0 initial; v1.0.1 added macOS app icon; v1.0.2 fixed packaged app login; v1.0.3 attempted but the DMG had three bugs (broken logo, dead Start button, invisible onboarding wizard) all tracing to missing `.env` in the asar — v1.0.3's DMGs remain in `~/sales-overlay/dist/` alongside v1.0.4 (harmless, different filenames). v1.0.4 release notes are on GitHub Releases. v1.1.x = role-based dashboard, script upload, call boundary detection, post-call summary, device check.
- App bundles ship with the Scout icon (`build/icon.icns` referenced via `build.mac.icon` in `package.json`)
- GitHub repo at github.com/scoutsystems-js/sales-overlay (PAT lives in `API Keys.md` only — never in committed files)
- Auto-updater (electron-updater) pointed at GitHub Releases — `latest-mac.yml` + DMGs + blockmaps uploaded per release. electron-updater is lazy-required inside `initAutoUpdater()` with try/catch so a missing module never crashes the app.
- GitHub Releases: v1.0.0 initial, v1.0.1 macOS icon, v1.0.2 packaged-login fix (arm64), v1.0.3 attempted (three packaging bugs — never published), v1.0.4 (no shipped API keys, JWT refresh, proxy routing), v1.0.5 (cloud logging via SessionLogger), v1.0.6 (auto-updater 30-min polling + backend transient-failure retry + error truncation), v1.0.7 (ZIP target so macOS auto-update works + latency pass: Haiku for live suggestions, 800ms prospect-silence gate, closer-active floor-handoff), v1.0.8 (delivery gate tuning + overlay max visible prompts reduced 3→2), v1.1.0 (role-based dashboard system: /dashboard for users, /admin for admins+owners with user management; v1.1.0 Features 1+2), v1.1.1 (Feature 3 script upload + Feature 4 call boundary detection + Feature 5 post-call summary), **v1.1.2 current release** (Feature 6: device check panel — mic level meter and device selection before session start). Releases published via `gh release create` (gh CLI installed as of v1.1.0).
- Project synced to iCloud Drive (`~/Library/Mobile Documents/com~apple~CloudDocs/sales-overlay/`) with node_modules and dist as `.nosync` symlinks
- Supabase user_profiles table live — stores onboarding wizard data (niche, offer, pricing, payment URLs) with RLS scoped to auth.uid(). Migration at backend/migrations/001_user_profiles.sql.
- Onboarding Wizard — 5-screen post-login setup (niche, offer, pricing, payment links, qualifications) wired to user_profiles via Supabase. Skip saves partial data, complete sets completed_at to hide wizard on future launches.
- Web login session persistence — backend/routes/auth.js returns full session (access_token, refresh_token, expires_at); login.html stores to localStorage under `scout_session_v1` key and auto-restores on reload
- Cache-Control: no-cache on all HTML files served by backend (prevents browser caching stale login/landing pages after deploys)
- gitleaks pre-commit hook installed via `.pre-commit-config.yaml` — blocks commits containing API keys or secrets before they reach GitHub

## Current Status: What's Being Tuned
1. **Delivery detection sensitivity** — Currently 35% threshold with question-first matching. May need further tuning based on real call testing.
2. **Deepgram transcription quality** — System audio (YouTube/loopback) sometimes garbles badly, which confuses Claude's suggestions. Open issue — no fix yet.

## Current Status: Not Yet Built
- Stripe billing wired end-to-end (SKIP_BILLING still true — auth works; Stripe keys pending; `SKIP_BILLING=true` means all logged-in users get full access until flipped)
- Admin UI to promote learned patterns from personal to team scope (schema already supports it — just needs a PATCH endpoint and UI button)
- Post-call analytics dashboard — charts, aggregations, win rate, objection breakdown, stage timing (Phase 3 per BUILD-PLAN.md, at `/dashboard`). NOTE: the desktop-teleprompter-era Live-Sessions analytics + the `/admin` raw session-log/transcript viewer (v1.1.0 Feature 2) were **retired in A-2.3** — nothing writes `call_sessions` anymore. Part 1 removed the backend routes + `/admin` markup; part 2a removed the dead `dashboard.html` Live-Sessions code; part 2b removed the dead `admin.html` session-viewer JS/CSS. Tables kept (a SQL query still reaches history).
- Optional future nicety (logged, not built): clicking a user row in the `/admin` User Management table could open that user's coaching dashboard via the existing `?user=` pivot (`dashboard.html` already supports admin/owner viewing another user). The row's *old* click behavior — a session-log filter — was removed in A-2.3 part 2b, so rows are currently plain.
- Automated `npm run release` end-to-end with `GH_TOKEN` (gh CLI is now installed and `gh release create` works as of v1.1.0; one remaining piece is wiring `GH_TOKEN` into the build env so electron-builder's `--publish always` flag can fire automatically without a manual asset upload step)
- x64 DMG for v1.0.2 (Apple notarization server returned 500 mid-build; arm64 shipped; rebuild x64 separately when Apple servers recover)

## Resolved Issues (full history)
- **STANDING GOTCHA — cross-origin OAuth severs `window.opener` for good; the connect handoff is OPENER-FREE (2026-07-31, corrected).** Helmet's **default** `Cross-Origin-Opener-Policy: same-origin` severs `window.opener` the moment a popup navigates cross-origin. We DO keep `crossOriginOpenerPolicy: { policy: 'same-origin-allow-popups' }` in `index.js` on purpose — **do NOT harden it back to `same-origin`**. BUT the header alone did **NOT** fix onboarding Connect (proven by re-test: the popup still returned with `window.opener === null` and stranded on `/dashboard`, main window never updated). The opener is dead on return regardless of our header — the cross-origin provider hop severs it from their side. **So the fix does not use the opener at all.** The real design (shipped 2026-07-31): the Zoom/Fathom OAuth callbacks redirect the popup to a tiny **`/connected`** page (never `/dashboard`); it signals the main window OPENER-FREE via `BroadcastChannel('scout-oauth')` **and** a non-persistent `localStorage` write (`scout_oauth_signal`, carries a `ts` the reader freshness-checks and **clears on read** so a stale signal can't fire a spurious "connected" later); it best-effort `window.close()`s and, if blocked, stays a tiny "you're connected, close this window" page (never the full app). The main window (`dashboard.html`) listens on BroadcastChannel + `storage` + a boot read-on-load, **plus** a bounded `/zoom|fathom/status` poll (`startConnectPoll`, 2.5s × 2 min) as a final fallback — three opener-independent update paths. The legacy `postMessage`-close handoff is kept but is now just a bonus for the rare case the opener survives. Files: `web/connected.html`, `GET /connected` in `index.js`, `routes/auth.js` callback redirects, the listeners/poll/`consumeStoredOauthSignal` in `dashboard.html`.
- **Zoom sync — sub-stage 2 BUILT 2026-07-31 (LOCAL, unpushed; RULING REVERSED — build now, don't wait for Marketplace).** Supersedes the prior "connecting Zoom syncs nothing" accepted-risk. Now built end-to-end, mirroring Fathom: `zoom-client.listRecordings` (GET /users/me/recordings — uses the `cloud_recording:read:list_user_recordings` scope, already on the live app since July 24) + `zoom-client.fetchTranscriptVtt` (GET /meetings/{double-encoded uuid}/recordings → pick the TRANSCRIPT file → download the VTT) → `lib/vtt-adapter` → the same `meeting` object the pipeline already consumes; `POST /zoom/sync` + `POST /zoom/sync-all` (cron) map recordings → `fathom_calls` rows with `source='zoom'` and the Zoom instance UUID in `fathom_call_id`, upsert ON CONFLICT DO NOTHING, and auto-analyze the first-sync backlog's newest 20 (the SAME `callIdsToAnalyze`/`FIRST_SYNC_ANALYZE_CAP` imported from `routes/fathom` — first-sync-only; steady-state syncs grade every new call); `analyzeCall` branches on `transcriptSourceFor(callRow)` — `'zoom'` uses `call_connections.getValidAccessToken` (serialized single-flight refresh — mandatory for Zoom's single-use rotating refresh tokens, never bypassed) + the VTT path; everything else is the unchanged Fathom path. Onboarding step 2 for Zoom is now a real **"Sync & grade my recent calls"** button (`syncZoomNow`), replacing the "rolling out" copy. The 2h GitHub-Actions cron (`.github/workflows/fathom-sync.yml`) now has TWO steps — `/fathom/sync-all` then `/zoom/sync-all` (same `CRON_SECRET`; the Zoom step tolerates 503 pre-launch) — pushed `18ebb69` after the repo PAT was granted the `workflow` scope. (Historical gotcha: the default `ghp_` PAT lacks `workflow` scope and GitHub rejects any push that touches `.github/workflows/*` — grant the scope or apply that one file via the web UI.) **Free-plan degrade:** a user with no cloud recordings gets `{ no_recordings:true }` → honest "record a cloud meeting with transcription on" copy, not an error. TDD: mapping, transcript-file pick, UUID double-encode, the first-sync-only cap (`callIdsToAnalyze`), and the source-switch predicate (`test/zoom-sync.test.js`, `test/analysis-source-switch.test.js`, `test/first-sync-cap.test.js`; suite 175). **UNEXERCISED AGAINST REAL DATA (validation honesty):** no live Zoom cloud recording was available (Justin's Zoom is free = zero recordings; Josh can't authorize until Marketplace approval), so `listRecordings`/`fetchTranscriptVtt`/the VTT adapter are unit-tested against synthetic payloads shaped from Zoom's documented API only. Expect a small fix-up round when Josh's first real recording arrives (field-name / VTT-shape confirmation). Prereqs already satisfied (2026-07-31): the list scope is on the live app (all five trimmed scopes confirmed at the July-24 reconnect) and `ZOOM_CLIENT_ID/SECRET` hold the PRODUCTION credentials on Railway (swapped + API-verified July 24) — the feature is live-configured, not env-dark. **Clips/timestamps:** Zoom has NO timestamp deep-link, so review-page `?t=` links degrade (no seek) until clips land in sub-stage 3 — as designed. **TODO still deferred: the Fathom→call_connections cutover** (own contained step after Zoom proves the unified store on real traffic).
- **Create-user made atomic + orphan handling; public signup CLOSED (2026-07-31).** A create could show "already registered" yet leave a half-provisioned account, and an owner adding a rep from Team → Manage members created it UNMANAGED (the "force managed_by=self" only ran for non-owners; `submitAddMember` sent no `managed_by`). Fixes: `submitAddMember` now sends `managed_by = team key`; `POST /admin/users` routes through **`lib/provision-user.js provisionUser`** (TDD'd) — fully succeed or leave nothing new behind: roll back a fresh create's auth user on profile failure, **report** (never silently swallow) an orphan if the rollback delete also fails, return a clean 409 on a real duplicate, and **RECLAIM** an orphan (auth row with no profile → reset password + create profile) so a fresh-looking email that collides with stale debris just works. **DECISION — leave the 6 existing orphan auth rows ALONE (do NOT "tidy" them):** they're likely real abandoned self-signups, and `provisionUser` reclaims them harmlessly on the next create for that email — they self-heal, so deleting them is unnecessary and risky. **Public self-signup is CLOSED:** `POST /auth/signup` now returns 403 invite-only and never calls `supabase.auth.signUp` (`auth.js signupDisabled`, TDD'd in `signup-disabled.test.js`) — FD-2 only hid the tab; the live endpoint contradicted invite-only and was the orphan source. Only prior callers were the dead desktop app + the hidden web tab. **`/admin` gained the full member action set** (Status + Deactivate/Reactivate + Delete, owner-only, same endpoints/gates + zero-history delete rule as Team → Manage members).
- **UI-removal verification must EXERCISE interactive elements, not just check HTTP 200 — `/admin` user-row TypeError (A-2.3, 2026-07-29).** A-2.3 part 1 removed the `/admin` sessions + log-viewer markup and its backend routes, but left the JavaScript wired: the still-live User-Management table rendered every row with `onclick="handleUserRowClick(...)"` → `setUserFilter()` → `document.getElementById('sessionsScope').textContent = …`, and that DOM node was gone, so **every user-row click threw `TypeError: Cannot set properties of null`**. Part 1's post-deploy check ("/admin returns 200") never clicked a row, so the bug shipped to production. Fixed in A-2.3 part 2b: removed the whole dead Section B+C cluster (session list + log detail + transcript view + download), its module state, its CSS, `handleUserRowClick`, and the row `onclick`/`clickable`/`active` wiring — User-Management rows are now plain rows (their dropdowns + buttons stay interactive). **Standing convention: post-deploy verification of ANY UI removal MUST exercise the surviving interactive elements — click a row, open a modal, submit a form — not merely confirm the page returns 200. A 200 page with a dead click handler still ships a broken feature.**
  - **⚠ TESTING A FUNCTION IN ISOLATION AND GREPPING FOR ITS NAME ARE THE SAME CHECK TWICE. Both confirm the code EXISTS. Neither confirms it RUNS. (2026-08-15, 10b's graphs — the CALL SITE was dead.)** A scripted edit spliced the mount line **INSIDE the callback passed to `teamScoreListHtml`**, immediately after `return r.avg_score;` with no closing brace between them — so `content.insertAdjacentHTML('beforeend', repSeriesSectionHtml())` was **unreachable dead code**. It parsed, the suite passed, and **every marker check passed**: the served bundle genuinely contained `repSeriesSectionHtml`, `repSeriesChart`, `REP_LINE_COLORS` and `.rep-graph`, because the definitions were all there. Only the invocation was dead.
  - **THE VERIFICATION THAT FAILED IS THE POINT.** `repSeriesSectionHtml()` was extracted and run headlessly against a fake `Chart`, asserting the plotted series matched SQL exactly. That proved **the function works** — and said nothing about whether anything calls it. The served-bundle grep was not a second, independent check; it was the same one again.
  - **This is the same family as the two missing-column bugs already recorded above** — Part 1b's `GET /fathom/calls/:id` not selecting `section`/`resolution`, and 2b's select missing `id`. In all three the component under test was correct and the thing that *reaches* it was broken, so every check aimed at the component passed.
  - **THE RULE: verification must execute the REAL ENTRY POINT and assert on what it produces.** For a render path that means running the actual render function against real state and asserting the section appears in the resulting output — see `test/team-view-render.test.js`, which executes `renderTeamView` itself. Two properties make it real, and both are worth copying: (a) its DOM stub returns a canvas **only if that id is present in the markup just assigned**, so a chart being built is *proof* the canvas existed at draw time rather than an assumption about ordering; (b) **every ordering anchor is asserted present before it is compared** — the first draft used `if (needsWork !== -1)`, the anchor didn't exist, and that assertion passed vacuously. **A conditional ordering check with a stale anchor is not a check.**
  - **PROVE THE TEST IS NON-VACUOUS BY REINTRODUCING THE DEFECT.** The suite was re-run against the exact broken form: **5 of 8 failed**, and all 8 passed once restored. A regression test written after a bug that has never been shown to fail against that bug is unproven.
  - **AND THEN EXERCISE IT IN A REAL BROWSER — the node test cannot see layout.** The fix was additionally driven in Chrome by loading the REAL `/dashboard` document into an iframe with only the trailing `init();` neutralised, so CSS, layout and Chart.js were all genuine, then calling `renderTeamView()` with injected state. That established what no stub can: the section paints, the line traces the right shape, a rep with a missing week leaves a visible GAP, and — via `getBoundingClientRect` — the graphs sit above *What needs work*, first chart 97% visible at 1512×799, second **entirely below the fold**. **This works UNAUTHENTICATED**, which matters because entering credentials is prohibited: the login page is same-origin, so `fetch('/dashboard')` returns the full document even though navigating there redirects. **Reusable recipe, not a one-off.**
  - **⚠ WHEN PROBING WITH SYNTHETIC STATE, SEPARATE FIXTURE ARTEFACTS FROM REAL DEFECTS.** The probe rendered `undefined/undefined` in the glance tiles purely because the injected `totals` shape did not match what the tiles read — the FIXTURE being wrong, not the product. Reporting it as a bug would have sent someone chasing a defect that does not exist. Say plainly which observations the fixture cannot support.
- **Owner role RLS leak caused onboarding wizard skip + maybeSingle() errors.** `user_profiles` SELECT queries in `src/main/index.js` without `.eq('user_id', userId)` returned all rows for the owner role (which has unrestricted RLS), causing `maybeSingle()` to error on multiple results and `needsOnboardingCheck()` to see another user's completed profile and skip the wizard. Fixed by adding explicit `.eq('user_id', userId)` to all three unscoped SELECT queries: `needsOnboardingCheck()`, `get-profile` IPC handler, and `start-session` `script_summary` fetch. Rule: never rely on RLS alone to scope queries — always include explicit user filters.
- **Transcript turns are tagged `[deepgram]`, NOT `[memory]` 2026-05-25.** Critical finding during v1.1.10 objection-coaching backfill. First-pass classifier prompts kept returning "no transcript content in window" for 9 of 13 objection events despite sessions having hundreds of `[memory]` log lines. Diagnosis: `[memory]` is CallMemory's internal state (`"[memory] Updating call summary…"`, `"[memory] Stage: introduction"`, `"[memory] Key facts: 1"`) — useless to a classifier. Actual transcripts live under `[deepgram]` with shape `[deepgram] Transcript (final) [PROSPECT|CLOSER]: <text>` (38,861 such rows across the corpus). Fix: route filters `tag === '[deepgram]'` and `message.indexOf('Transcript (final)')` to isolate genuine dialogue. Future analytics work that needs transcripts MUST filter on this tag. The fallback (use all window rows when `[deepgram]` is sparse) is the right call for short sessions but `[memory]` is never useful as a primary source.
- **supabase-js default 1000-row pagination bug 2026-05-25.** First version of `extract-objections` did `select('logged_at, message').eq('session_id', X)` without explicit pagination — supabase-js silently caps at 1000 rows. Real sessions are 38-51 minutes with 3,000-7,400 log lines each; any objection in the second half of a call had an empty ±90s transcript window because those rows were never fetched. Fix: per-objection time-windowed query using `.gte('logged_at', windowStart).lte('logged_at', windowEnd)` so only the relevant slice is fetched, no row-count truncation. Rule of thumb: any unbounded `select(...).eq(...)` on a table with potentially-large per-row counts needs explicit `.limit()` or per-page query — never trust the default.
- **JSON parsing fragility on Claude responses 2026-05-25.** `JSON.parse(stripCodeFences(rawText))` was failing 100% of the time on Money Objection classification — Claude occasionally wraps JSON in narrative prose ("Here's my analysis: {...} The closer was…") that strict parsing can't see past. Fix: new `extractFirstJsonObject(text)` helper in `backend/routes/me.js` walks the string brace-by-brace tracking depth + string state to return the first balanced `{...}` block. Tries strict parse first (cheap), bracewalk only on fallback. Applied to both extract-outcome and extract-objections classifier outputs.
- **Supabase CLI workflow for autonomous DDL 2026-05-25.** Before this session, all migrations were applied manually via the Supabase dashboard SQL Editor. After verifying `supabase` CLI v2.95.4 is installed at `/opt/homebrew/bin/supabase` and already logged in (saw 3 projects including Overlay = `vkprybqmryiuwbdwdlpk`), the autonomous flow is: `supabase link --project-ref vkprybqmryiuwbdwdlpk` (one-time per working dir; creates `supabase/.temp/pooler-url` containing the DB password — `supabase/` MUST be gitignored). Then `supabase db query --linked --file backend/migrations/NNN.sql` applies any migration. `supabase db query --linked "<sql>" -o csv` runs ad-hoc queries against prod. Service-role key from `API Keys.md` (the `sb_secret_*` prefix) does NOT work for DDL — only CRUD via the REST API. The Management API (`api.supabase.com/v1/projects/.../database/query`) needs a Personal Access Token (`sbp_*` prefix in old model; under the May 2026 publishable/secret-only model, the CLI is the path of least resistance and uses session auth, not API keys).
- **DMG notarization gap discovered + recovered 2026-05-16 (v1.1.8 release).** `npm run build` with proper Apple creds (APPLE_ID / APPLE_APP_SPECIFIC_PASSWORD / APPLE_TEAM_ID) successfully signs the `.app` bundle, uploads it to Apple as `Scout.zip`, gets `status: Accepted`, and staples the ticket onto the `.app` inside `dist/mac-arm64/Scout.app` and `dist/mac/Scout.app`. **But the DMG container wrapping the .app is never signed or notarized.** `xcrun stapler validate Scout-X.Y.Z-arm64.dmg` returns "does not have a ticket stapled" and `codesign -dv` returns "code object is not signed at all" — even though the `.app` *inside* the DMG passes `spctl -a -v` with `source=Notarized Developer ID`. Same gap silently affected every build since at least v1.1.6 (last verified-shipped) — explains why v1.1.7 was bumped but never released. Was blamed on the multi-line paste bug for three rebuild cycles before the diagnostic `xcrun notarytool history` proved Apple was accepting every submission. **Recovery (no rebuild required):** for each DMG, run `codesign --sign "Developer ID Application: Justin Schmidt (8QN5Y29R27)" --timestamp --options runtime <dmg>`, then `xcrun notarytool submit <dmg> --apple-id ... --password ... --team-id ... --wait` (5-10 min per DMG, both can run in parallel), then `xcrun stapler staple <dmg>`. Then verify with `xcrun stapler validate <dmg> && spctl -a -t open --context context:primary-signature <dmg>`. **Permanent fix (TODO):** investigate electron-builder 25 + `notarize: true` config — `.app` notarization runs but DMG notarization is skipped. Likely needs an `afterAllArtifactBuild` hook or `notarize.tool: "notarytool"` explicit setting. Until fixed, every release requires the manual DMG recovery sequence above. Diagnostic clue for next time: the `.app` bundles in `dist/mac/` and `dist/mac-arm64/` will be fully stapled even when the DMG isn't — that's the fingerprint of this exact bug.
- **Build-release script eliminates multi-line paste bug permanently 2026-05-16.** Multi-line pasting of `APPLE_ID=... APPLE_APP_SPECIFIC_PASSWORD=... APPLE_TEAM_ID=... npm run build` into zsh has caused at least three silent unsigned/unnotarized builds across the project's history. Permanent fix: gitignored `~/sales-overlay/build-release.sh` (mode 0755) wraps the build command with backslash-continued env-var prefix in a single bash invocation. Run as `bash ~/sales-overlay/build-release.sh`. File added to `.gitignore` (committed `2082207`). Contains Apple creds in plaintext — same trust model as `API Keys.md`. If creds rotate, edit the script. Do NOT inline-paste the env-var prefix into the terminal anymore — always use the script.
- v1.1.0 Feature 2 (Role-based dashboard system) complete — /dashboard for users (personal call history, inline log expansion), /admin for admins+owners (three-section layout: user management with role change controls + last-owner protection, session list with user filter, log detail panel). Login now redirects to correct surface by role via /auth/me. Audit trail logs all role changes. Scope violation attempts logged on all protected routes.
- v1.1.0 Feature 2 (Admin session logs page) complete — `/admin` page at `backend/web/admin.html` with two-panel layout (session list + log detail), protected by `requireRole('owner')`. New `backend/routes/admin.js` exposes `GET /admin/sessions` (cross-user list, cursor paginated via `?before=<started_at>`, unfiltered by user_id on purpose — `requireRole` is the gate) and `GET /admin/sessions/:session_id/logs` (up to 2000 rows, `total_count` from Supabase `count: 'exact'` so client shows "Showing X of Y" when capped). Emails batch-fetched once via `admin.auth.admin.listUsers({perPage:1000})` → `{user_id: email}` map for the list route; detail route uses `getUserById` for the single user. Log counts batch-computed per page via one `.in('session_id', ids)` query + JS group-by (swap to Postgres RPC when session_logs scales). Page reuses login.html's session refresh flow (`scout_session_v1` localStorage, `/auth/refresh`); 401 → `/login`, 403 → `/`. Client-side filter chips (`[claude]`, `[memory]`, `[TIMING]`, `Errors only`) + search box; all in-memory over the capped 2000 rows. Pure helpers (`buildUserEmailMap`, `computeCountsBySession`, `computeDurationSeconds`) exported on the router per the `log.js:_validateLogBatch` pattern.
- v1.1.0 Feature 1 (Role system) complete — migration 003 adds role/managed_by columns to user_profiles, current_user_role() security-definer helper, revised RLS policies on user_profiles/call_sessions/session_logs, and requireRole middleware in auth.js. Justin (justinschmidtsales@gmail.com) promoted to owner, Josh (josh@scoutsystems.io) at user. Railway live.
- Overlay used to replace prompts mid-read → fixed with delivery detection + stacking
- Delivery gate permanently blocking → fixed with timeout (30s) + turn (4) auto-advance + prospect response gate
- Delivery detection too sensitive (common words triggering) → fixed with 80+ stop words, 4+ char minimum, 35% threshold
- Delivery detection firing on statement before question asked → fixed with question-first extraction (splits on `?`)
- New prompts popping up while closer still speaking → fixed with closer-active guard (5s) + backchannel filtering
- New prompts popping up before prospect answers → fixed with prospect response gate
- Prompt appearing mid-question (closer mid-sentence) → fixed: maxSecondsBeforeAutoAdvance 20s→30s, 4-turn auto-advance now requires 2s gap between turns, closerActiveThreshold kept at 5s
- Prompt not updating for 4-5 questions → fixed with 1.5s polling timer in main/index.js + backchannel filtering
- Prompt firing mid-prospect-answer → fixed with 1.5s prospect-silence check in getSuggestion()
- "Yeah/Right/Okay" backchannels blocking new prompts → fixed with BACKCHANNEL_WORDS filter + isBackchannel() function
- False objection triggers on closer's speech → fixed with prospect-only guard
- False positive objection detection from fuzzy matching → fixed with strict fuzzy (4+ word triggers, 2+ distinctive 5+ char words, ALL must match, edit distance ≤1)
- Call memory JSON parse failures from markdown code fences → fixed with fence stripping
- Call memory JSON parse failures from truncated responses → fixed by increasing max_tokens from 300 to 500
- Claude repeating exact same suggestion → fixed with suggestion history (last 8)
- Claude repeating same theme/angle with variations → fixed with theme tracking (headline keyword overlap)
- Call stage stuck on introduction → fixed by relaxing stage progression (allow 2-stage skips, more after 30/50 turns)
- Too many API calls → fixed with interval + turn gating
- Overlay was bottom-right popup → rebuilt as full-width top bar
- AI was selective coach → rewritten as always-on teleprompter
- Old prompts too dark to read (30% opacity) → raised to 60% opacity with #aaa text
- desktopCapturer can't capture system audio on macOS → replaced with manual audio device selector + BlackHole → replaced with electron-audio-loopback (zero user setup)
- package.json got overwritten (missing electron/start script) → restored from sales-overlay-backup-2026-04-08.zip
- GitHub push authentication failure → fixed by embedding PAT token directly in remote URL (`https://scoutsystems-js:TOKEN@github.com/...`)
- GitHub 100MB file size limit on dist/ and node_modules/ → fixed with .gitignore + `git rm -r --cached` + fresh `git init`
- Microphone permission loop in packaged app → fixed with `session.defaultSession.setPermissionRequestHandler`, `NSMicrophoneUsageDescription` in package.json extendInfo, and ad-hoc codesign
- iCloud sync corrupting node_modules → fixed with `rm -rf node_modules.nosync && npm install`
- overlay.html deadlocked by iCloud (EDEADLK) → fixed by writing to Downloads and running `cp` in terminal
- codesign "resource fork/detritus" error → fixed with `xattr -cr` before codesign
- dist/ syncing 2.33GB to iCloud → fixed by renaming to `dist.nosync` and creating symlink
- Overlay buttons not clicking (setIgnoreMouseEvents blocking all events) → fixed by removing setIgnoreMouseEvents and auto-resizing window to bar height via `resize-overlay` IPC — window never extends beyond visible dark bar so clicks below pass through naturally
- Stop/X buttons hidden when prompts appeared → fixed by making top-row `position: sticky; top: 0` and removing `bar.scrollTop = bar.scrollHeight`
- electron-builder hanging on build → caused by cleared cache forcing re-download of Electron binaries (~200MB). Fix: run `npm run build:unsigned` and wait 5-10 min. Do NOT use ELECTRON_BUILDER_OFFLINE=true (breaks binary rename).
- Packaged app showed default Electron icon → `~/sales-overlay/build/` was missing `icon.icns` (and entitlements) AND `build.mac.icon` wasn't set in `~/sales-overlay/package.json`. Fixed by `cp`'ing icon + entitlements from the iCloud copy to `~/sales-overlay/build/` and patching `build.mac.icon = "build/icon.icns"`. Confirmed fixed in v1.0.1.
- `~/sales-overlay` (local build dir) can drift from iCloud copy → the iCloud folder has `build/` assets the local dir may be missing. Always verify `build/icon.icns` + `build/entitlements.mac.plist` exist in `~/sales-overlay/build/` before building.
- GitHub push protection blocking `git push` due to secrets in commits → Anthropic API key in old initial commit's `.env:2`, PAT in `CLAUDE.md`. Fix: strip secrets from tracked files, amend commit, click the `unblock-secret` URL GitHub prints for secrets in unreachable old commits, then `git push --force`. Going forward: keep all secrets in `API Keys.md` (gitignored) — NEVER commit them to `CLAUDE.md` or `.env`.
- `gh release create` failed with `command not found: gh` → GitHub CLI not installed on Justin's Mac. **Resolved as of v1.1.0**: gh CLI installed and authenticated, `gh release create v1.1.0 <assets...> --repo scoutsystems-js/sales-overlay --title ... --notes ...` works one-shot. Used to publish v1.1.0 (arm64+x64 DMGs, both ZIPs, all blockmaps, latest-mac.yml). Web UI fallback no longer needed for typical releases.
- Live site (www.scoutsystems.io) rendering as unstyled plain HTML → `website/public/index.html` references `/css/style.css` but the CSS file was never committed to GitHub (only existed in Justin's local `~/sales-overlay/website/public/css/style.css`). Fixed by `git add website/public/css/style.css && git commit && git push` (commit `75bd6a0`). Railway auto-redeployed in ~60s and the site rendered correctly. Root cause was NOT the DNS work — the CSS was missing since the initial website deploy (commit `feba9a9`) and just went unnoticed.
- Namecheap MX records don't live in Advanced DNS → Host Records section → they live in the **Mail Settings** section, which is a separate dropdown farther down the Advanced DNS page. Set Mail Settings to **Custom MX** to enable the MX records table.
- Namecheap ALIAS Record at apex can be flaky with Railway → switched to `URL Redirect Record @ → https://www.scoutsystems.io`. Only works over HTTP (Namecheap's redirect servers don't listen on 443); HTTPS to bare domain times out. Planned fix: migrate DNS to Cloudflare.
- Namecheap "Failed to save record" error when adding SPF TXT `v=spf1 include:_spf.google.com ~all` → the tilde `~` character causes the save to fail in Namecheap's UI. Fix: use `-all` (hard fail) instead of `~all` (soft fail). Functionally stricter but fine for a domain that only sends via Google.
- Google Workspace MX verification failing on first retry even though MX is correct + globally propagated → normal. Google docs: activation can take up to 72 hours. Just retry every ~30 min.
- Railway provisions a **different** `*.up.railway.app` endpoint for each custom domain (e.g., `ujcd5dfv` for the old bare domain, `vdy3qiy5` for the new `www` domain). Both route to the same underlying service. Make sure to update any CNAME records to the NEW endpoint when Railway asks for it — using an old endpoint will 404.
- Packaged app login showed "Something went wrong" → two root causes: (1) `BACKEND_URL` fell back to `http://localhost:3000` when no `.env` present in packaged build; (2) all 8 auth methods (`authLogin`, `authSignup`, `getToken`, `saveToken`, `clearToken`, `checkSubscription`, `openCheckout`, `authSuccess`) were missing from `src/preload.js` contextBridge — calling `undefined()` threw TypeError. Fixed both in v1.0.2: BACKEND_URL default changed to Railway URL, all 8 methods added to contextBridge.
- `Cannot find module 'electron-updater'` crash in packaged app → adding `"node_modules/**/*"` to electron-builder `files` array bypasses smart bundler and produces malformed asar headers (only 2 entries for electron-updater instead of 201). Fix: revert `files` to `["src/**/*", "package.json"]` (smart bundler handles transitive deps automatically) + lazy-require electron-updater inside `initAutoUpdater()` with try/catch so future packaging issues never crash the app. Confirmed fixed: `npx asar list app.asar | grep -c electron-updater` returns 201.
- Overlay showed as thin black line — three compounding causes: (1) overlay.html was missing the entire top control row (Start/Stop/X/logo) — it was only in the hidden control panel; (2) `y: 0` positioned the window behind the macOS menu bar (~24px), so only a sliver of the bar showed below it — fixed by using `y: display.workArea.y`; (3) `trigger-start-audio` / `trigger-stop-audio` IPC was sent from main to the hidden control window but control.js had no listeners — added them.
- Stop button never appeared after pressing Start — CSS had `display:none` on `.btn-stop`; JS used `style.display = ''` which clears the inline style and falls back to the CSS rule (still none). Fix: remove `display:none` from CSS class, set initial state as inline `style="display:none"` on the HTML element, use explicit `'flex'`/`'none'` in JS via `setSessionState(active)`.
- Overlay was rendering but from old packaged app (v1.0.2 DMG) — source changes in the iCloud working directory have no effect on the installed Scout.app. Always test with `npm start` from `~/Library/Mobile\ Documents/com~apple~CloudDocs/sales-overlay/` during dev; only the packaged DMG reflects built changes.
- Installed v1.0.3 DMG showed broken Scout logo, dead Start button, and invisible onboarding wizard — three separate-looking bugs with one shared root cause: `package.json` `build.files` glob was `["src/**/*", "package.json"]` which excludes `.env` and `build/icon.png` from the packaged asar. All testing had been via `npm start` from iCloud dev, which physically has those files on disk — hiding every one of these bugs until v1.0.3 was the first DMG to actually try to use them (v1.0.2's overlay had no logo image and no in-overlay Start button). Full fix in v1.0.4 via architectural refactor: (1) Scout icon moved to `src/assets/icon.png` so it ships via the existing glob, overlay.html updated to `../../assets/icon.png`; (2) `.env` eliminated from packaged app entirely — Supabase URL + anon key (public per Supabase design) hardcoded in `src/config.js`, Deepgram + Anthropic routed through Railway backend proxy via new `src/lib/proxy-client.js`; (3) onboarding wizard now actually runs the Supabase query because `createSupabaseForUser` no longer returns null on missing env vars. Lesson: if an npm-start-only test passes but the packaged app behaves differently, the `files` glob is the first suspect.
- JWT expiry silently ignored — access tokens expired after ~1 hour but the app kept proceeding with stale tokens because: (1) save-token IPC only persisted the access_token and discarded refresh_token + expires_at; (2) there was no expiry check anywhere; (3) `needsOnboardingCheck` fail-open returned false on any Supabase error, masking expired-JWT as "no wizard needed." Fix in v1.0.4: `auth-login`/`auth-signup` IPC now returns the full session; new `ensureFreshToken()` single-choke-point reads stored session, parses exp claim (with 60s grace), refreshes via `/auth/refresh` if expired, or clears session and reopens login if refresh fails. Every token-consuming IPC handler (get-token, auth-success, get-profile, save-profile) + ProxyClient go through it. Helper `decodeJwtPayload()` shared between `isJwtExpired` and `decodeUserIdFromToken` (DRY). `_refreshInFlight` singleton dedupes concurrent refreshes.
- `/proxy/*` routes returned 403 "No active subscription found" for every authenticated user — `requireSubscription` middleware at `backend/middleware/auth.js` was unconditionally querying a `subscriptions` table (which doesn't even exist yet — no migration), ignoring `SKIP_BILLING`. Desktop app's own `check-subscription` IPC already honored `SKIP_BILLING !== 'false'` and returned `{active: true, status: 'beta'}`; the backend's middleware was out of sync. Fix: add `if (process.env.SKIP_BILLING !== 'false') return next();` at top of `requireSubscription`. Unit-tested across unset / 'true' / 'false' — pass-through in the first two, falls through to DB check in the third. When Stripe gets wired, flip `SKIP_BILLING=false` in Railway env vars.
- v1.0.8 live 48-min call analysis showed Scout kept re-asking discovery questions (pain, goals, timeline) long after those topics had been confirmed — AND the Discovery Tracker checkboxes never updated on screen. Two separate root causes, both fixed in v1.0.9: (A) IPC wiring break — `preload.js` never exposed `onDiscoveryUpdate`/`onDiscoveryReset`/`resizeDiscovery` bridges, so `discovery.html` threw TypeError on load and every `discovery-update` IPC from main was dropped. Added the three bridges (commit `569921e`); added `console-message` pipes to `discoveryWindow` + `overlayWindow` so future renderer errors surface to session_logs (commit `a6e679d`). (B) Prompt blindness — CallMemory tracked 7 discovery booleans but `getContext()` only surfaced `financeDetails`; Claude inferred coverage from summary prose and often missed it. SYSTEM_PROMPT also had no rule about what to do once an item was covered. Fix: `getContext()` now emits a `DISCOVERY STATUS` section (`[DONE]` / `[NEEDED]` lines) and SYSTEM_PROMPT includes a 40-word DISCOVERY COMPLETION RULE instructing the engine never to re-ask `[DONE]` items and to advance to TRANSITION/PITCH once all are covered (commit `8aaf813`). Unicode audit: `✓`/`✗` were safe (em-dash + `→` already ship in the same prompt path) but ASCII tags chosen for explicit LLM parseability and session_logs grep-friendliness. Complementary to the existing theme/angle anti-repetition — state-based vs keyword-based, they reinforce.
- v1.0.7 post-latency-fix testing showed delivery gate blocking for 30+ seconds on fluid conversations where the closer spoke in rapid non-verbatim bursts. Root cause: Check 2 (the 4-turn auto-advance in `addTurn`) required the gap between the last two closer turns to be >2s, so rapid real bursts (<2s apart) never tripped it; the gate then had to wait for Check 3 (30s silence in `getSuggestion`). Fix in v1.0.8: replace the per-pair gap with a total-elapsed floor. New field `this.firstCloserSpeechAfterSuggestion` stamps the timestamp of the first non-backchannel closer turn post-suggestion (set once when `turnsSinceSuggestion === 1`, resets to 0 in all three sites where `turnsSinceSuggestion` resets: objection bypass path, Claude API success path, `reset()`). New named constant `this.turnAutoAdvanceMinElapsedMs = 4000` replaces the hardcoded 2000 inline check. Check 2 now fires when closer has 4+ non-backchannel turns AND ≥4000ms elapsed since their first post-suggestion turn. Deepgram chunking protection preserved — one continuous sentence split into 4 sub-second chunks arrives within ~1-2s total, still well under the 4s floor. Check 1 (35% fuzzy match), Check 3 (30s silence), backchannel filter, prospect-response gate, closer-active v1.0.7 floor-handoff all untouched. Also in v1.0.8: `MAX_VISIBLE_PROMPTS` reduced 3→2 in overlay.html per product direction — sticky top row and ResizeObserver handle the smaller count without any other changes. Commits: `d864cfd` delivery gate, `1a792b5` overlay cap, `9462b80` version bump.
- v1.0.6 live testing showed prospect-last-word → prompt-visible latency >10s, target <3s. Added diagnostic instrumentation in v1.0.7-alpha (src/lib/proxy-client.js passthrough of Anthropic `usage`, checkpoint logs `[TIMING]` at t0..t7 + `[TIMING_SUMMARY]` per cycle, gated by `SCOUT_TIMING=1`). First live-test run showed no `[TIMING]` lines despite env var set — shell paste split the env assignment from npm start; fixed by adding an unconditional `[timing-check]` startup log at the top of `src/ai/claude.js` so env-var propagation is always visible. Second live-test revealed three bottlenecks, all fixed in v1.0.7 final: (1) Claude Sonnet averaged 3,372ms per live-suggestion call — swapped `/proxy/suggest` to `claude-haiku-4-5-20251001` (new `CLAUDE_SUGGESTION_MODEL` constant in `backend/config.js`); `/proxy/memory` stays on Sonnet. (2) Prospect-silence gate was 1500ms on top of Deepgram's ~500ms endpointing — lowered to 800ms via new `this.prospectSilenceMs` class field in claude.js constructor. (3) `closerIsActive` was a pure trailing 5s window; it blocked new prompts for up to 5s after the prospect had already responded (cycle jo6buv: 4,284ms post-prospect thrash). Fixed by adding a third conjunction to the existing two (`lastCloserSpeechTime > lastProspectSpeechTime`) — the guard now clears the moment the prospect speaks more recently than the closer. All three protections preserved: mid-sentence protection, backchannel filter, 45s prospect-response safety valve. Three separate commits (`d5784fa` startup log, `b7e2598` silence gate, `537cdc2` floor-handoff). Timing instrumentation stays in for now; Phase B cleanup removes it once latency is validated.
- macOS auto-update never worked from v1.0.0 through v1.0.6 — every installed DMG stayed on its original version indefinitely, requiring manual reinstalls. Root cause: `package.json` `build.mac.target` was `{"target":"dmg","arch":[...]}` (single target), so electron-builder never produced the ZIP files that electron-updater requires for in-place macOS updates. DMGs are for initial install only; auto-update downloads a ZIP and swaps it over the running .app. Without ZIP entries in latest-mac.yml, the updater failed with `Error: ZIP file not provided` at the download step. Only discovered because a user finally checked Scout → About after multiple releases and noticed the version hadn't moved. Fix in v1.0.7: change target to array of two entries — `[{"target":"dmg","arch":[...]},{"target":"zip","arch":[...]}]`. latest-mac.yml's `path:` field now points at the arm64 zip, which is what the updater actually uses. Once everyone is on v1.0.7, every subsequent release auto-installs seamlessly. Rule of thumb: auto-update must be tested by installing a real DMG of the OLD version and watching it catch the NEW release. `npm start` in dev mode does not exercise auto-update at all.
- Every `/proxy/suggest` and `/proxy/memory` call started 404'ing mid-session with `Claude API call failed` — root cause: Anthropic retired the model ID `claude-sonnet-4-20250514` (Sonnet 4.0 from May 2025). Was only discovered via v1.0.5 cloud logging (first real production bug the logs caught). Fix: `backend/config.js` now exports `CLAUDE_MODEL = 'claude-sonnet-4-6'` used by both `/proxy/suggest` and `/proxy/memory`. Also added `formatProxyError(err, serviceLabel)` helper so upstream errors (status code + message) now surface directly in the client JSON body and session_logs instead of being masked by a generic "X API call failed" string — applied to all three proxy catch blocks (`/suggest`, `/memory`, `/deepgram-key`). 14 unit tests covering Anthropic-shaped errors, plain Errors, null, truncation. `src/ai/script-parser.js` model ID also bumped (still uses direct SDK, not proxy — refactor queued for when script upload becomes a real feature). Rule of thumb: when an upstream API breaks, the real error should be one SQL query away. Never silently wrap errors in a generic string — always include the upstream status + message (truncated).
- Deepgram `/proxy/deepgram-key` returned 404 even with API key + project ID set on Railway — the new Deepgram API key created for v1.0.4's backend lived in a different Deepgram project (`b74d3e9b-336c-4d89-87f0-061139970005`) than the one the old key was in (`a0458447-3a65-4098-a46a-3e033900258d`). Backend was hitting `POST /v1/projects/a0458447.../keys` which 404'd because the new key couldn't see that project. Fix: update `DEEPGRAM_PROJECT_ID` on Railway to match whichever project the current key actually lives in. Diagnostic: `curl -H "Authorization: Token $KEY" https://api.deepgram.com/v1/projects` returns the project(s) the key has access to. Also: the key needs `keys:write` scope to mint ephemeral keys — `usage:write` alone returns 403 INSUFFICIENT_PERMISSIONS.
- Signup on live site returned "Database error" on every attempt → root cause was a leftover `on_auth_user_created` trigger on `auth.users` running a `public.handle_new_user()` function that inserted into a `public.profiles` table (singular). That table was scaffolded from a Supabase starter template and never belonged in this project — our actual schema is `public.user_profiles` (populated by the onboarding wizard after login, not by a signup trigger). Trigger failed on every signup and rolled back the `auth.users` insert. Fix: `drop trigger if exists on_auth_user_created on auth.users; drop function if exists public.handle_new_user();` in Supabase SQL Editor. Left the empty `public.profiles` table in place — harmless, can be dropped later if desired. Do NOT re-add an auto-create-profile trigger; `user_profiles` is populated by the onboarding wizard, which is the intended flow. Diagnostic queries used: `select tgname from pg_trigger where tgrelid = 'auth.users'::regclass and not tgisinternal;` and `select * from public.profiles;`.
- electron-builder silently skipped notarization with message `notarize options were unable to be generated` → root cause was multi-line paste of the build command. In zsh, `VAR=value` followed by a newline sets a *local shell variable*, not an exported env var. So `APPLE_ID=... \n APPLE_APP_SPECIFIC_PASSWORD=... \n npx electron-builder` reached electron-builder with ZERO Apple creds in its environment, and `@electron/notarize` silently bailed. DMGs were produced and signed with Developer ID, but `xcrun stapler validate` confirmed no ticket and `spctl` rejected them — unshippable. Fix: always run the env-var prefix and the command on ONE single line. Preferred: `cd ~/sales-overlay && APPLE_ID="..." APPLE_APP_SPECIFIC_PASSWORD="..." APPLE_TEAM_ID="8QN5Y29R27" npm run build` (single line, no newlines). Verify after build with `xcrun stapler validate dist/Scout-X.Y.Z-arm64.dmg` — should say "The validate action worked!" not "does not have a ticket stapled".
- Force-push on origin silently wiped 5 commits of backend code (`de4836d`, `8b5bdb3`, `c6b028e`, `d9e2fe6`, `671d3cc`) → Railway kept running an older cached build while the current `main` was missing `backend/routes/auth.js`, `backend/routes/billing.js`, `backend/routes/proxy.js`, `backend/middleware/auth.js`, `backend/package.json`, `Procfile`, `railway.json`. Login endpoints 404'd, nobody noticed for days because `/health` kept working. Fixed by recovering from git reflog (`git checkout 671d3cc -- <paths>`) and committing restore as `0e669f8`. Prevention: branch protection on `main` (no force-push), `.gitignore` covering all secrets on `main`, pre-commit `gitleaks` hook. See README.md.

### PROSPECT NAMES (stage opened 2026-08-03) — 3a SHIPPED, 3b BUILT
- **The bug:** an EOD row read prospect "Impromptu Zoom Meeting" while that call's own summary said "Katina Goss". There was **no prospect-name column at all** in the Fathom-era schema — `routes/eod.js prospectNameFromTitle` derived it at READ time from `fathom_calls.title` (last `|` segment), and every other surface rendered the raw title. Measured on 83 analyzed calls: **13 titles had no pipe** (so the meeting label showed verbatim — **11 distinct real prospects collapsed into ONE fake prospect**, including a CLOSED call), and **the title is the BOOKED name, not the attendee** — on the 56 calls whose prose reliably names the prospect, **19 (34%) name someone the title does not** (title "Tasha Presberry" ↔ attendee "Jamie Ellis"). This blocks per-prospect close rate: the denominator is corrupt.
- **GOVERNING PRINCIPLE (standing): a WRONG name is worse than NO name.** It silently fabricates a prospect identity that later merges and miscounts, and nobody questions a name that reads plausibly. Device names, phone numbers, hotspot names, generic participant labels and meeting labels resolve to **NULL → "Unknown prospect"**, never a plausible-looking guess. **When in doubt, refuse.**
- **RULINGS:** (1) **couples = ONE prospect** — single name field, combined name ("Alan and Jen"); close rate measures deals. (2) **A prospect is CLOSED if ANY of their calls closed**; else the most recent decided outcome; else open. **Recorded now so 3d inherits it rather than re-deciding.** (3) backfill ALL 83 (free, and it corrects the 19 booked-name mismatches, which matter more than the 13 obvious ones). (4) store `prospect_name_source` + `confidence` but **do NOT surface them in 3a** — they become visible in 3d's merge review where they're actionable.
- **3a SHIPPED 2026-08-03 (`403019b`, migration 031 applied).** `call_analyses.prospect_name` + `_source` (grader|diarized|title|manual) + `_confidence` (high|low) + partial index for 3d. `lib/prospect-name.js` is pure/total: precedence **grader → diarized → cleaned title → NULL**, written at analysis time (worker Phase 6b). EOD reads the column (old helper commented in place); Call Library + review header use it via `callLabel()`.
  - **TWO RULES CAME FROM THE LIVE DRY RUN, NOT FROM REASONING — do not "simplify" them back:**
    - **Never assume the closer talks most.** That heuristic is true on 77/83 but on the other 6 the PROSPECT out-talks the closer, so it returned **the CLOSER as the prospect** (verified: "AF … | Sherrita Hall" is Donna 637 turns + Joshua Pinner 585). The resolver now **REFUSES when it cannot identify the closer**, and the reliable identity is `fathom_connections.fathom_email` → local part ("joshua" ↔ "Joshua Pinner"), passed by the worker. Result: **0 closer leaks across 83**.
    - **Diarization is more ACCURATE but often less COMPLETE than the title** ("Towana Joseph"→"Towana", "Cameel Bernard"→"Ca", "Carolyn Tolbert"→"Carolyn T."). Agreeing sources now yield the **FULLER** name at high confidence (corroboration); on **disagreement diarization wins** (title is the booked name). Losing surnames would break 3d's grouping.
  - Live dry run: **45/48 real calls resolve (94%)**, 0 closer leaks. The 38 refusals are **35 demo-user copies** (no connection email → closer unidentifiable → correctly refused) + 3 genuinely unresolvable. `speaker_closer_name` is NULL on all 83 because the worker hardcodes `recorded_by: null`.
- **⚠ SURFACE DISTINCTION — a DECISION, not an oversight (ruling 2026-08-03).** A prospect NAME and a call LABEL are different things:
  - **Prospect name** (resolved, falls back to raw title): **EOD**, **Call Library**, **call review header**, **needs-work evidence**. Needs-work is included because the original spec for that view was *"July 12th Call with Jim Stone"* — a person is what was asked for.
  - **Raw title, deliberately KEPT**: the **objections feed** and the **team digest**. On those cross-call SCANNING surfaces the title functions as a call label, and its **program prefix (AF vs PS vs IH) carries information a bare name loses**. Do not "finish the job" by switching these.
- **3b BUILT 2026-08-03 (LOCAL/unpushed) — grader emits `prospect_name`; `ANALYSIS_PROMPT_VERSION` v10 → v11-2026-08-03.** The contract **reuses the v7 follow-up-email greeting rule VERBATIM** (transcript-only; null when no name is established; **never the meeting title**) — a rule already proven in production, rather than a second subtly-different one. A couple returns as ONE joined name (ruling 1). **ADDITIVE — scoring/outcome/section prompts UNCHANGED, so NO delta-gate** (same reasoning as v10). The grader name is **fed into the resolver, not written directly**, so precedence + rejection rules + the couples cap still apply. Tests: `grader-v11.test.js` (6). Suite **329**.
- **3c FREE PASS APPLIED 2026-08-03 — 85 rows written, ZERO Claude calls.** Pure `lib/prospect-name.js` over data already stored. Result: 49 named / 36 NULL (35 demo-user copies + 1 real) / **48 distinct prospects, down from 63 fake ones** / **0 closer leaks** / **0 rows anywhere with a meeting label as a prospect**. The originating bug end-to-end: title `"Impromptu Zoom Meeting"` → `prospect_name = "Katina Goss"` (diarized, high, closed). **8 booked-name corrections** landed (`Kay Rapple→Khari`, `Kd Richmond→Lemoine`, `Maria Mercado→Benny`, `Kristien Bayan→Christine`, `Niajail Gulley→Ben Bymaster`, `Majesty→Elodie Simons de Fanti`, `Ken Happle→Aurora Estates`, `Mark-Anthony Rassmann→Forb`) — **not the 19 predicted, and the gap is explained**: the 19 measured "title first-name absent from grader prose", which includes nickname cases (`Towana Joseph` ↔ prose `TJ`) where diarization AGREES with the title and corroboration correctly keeps the fuller title name rather than "correcting" it.
  - **The dry run caught two defects BEFORE any writes** (why the report-before-spending discipline earns its keep): (1) an abbreviated first name beat the fuller title (`Lisa Williams` → `L. Williams`) because containment matches neither way — `sameIdentity` now also accepts exact-surname + initial/prefix first name, while still refusing `Jane Smith` ↔ `John Smith`; (2) 3+ speaker calls refused outright, discarding titles that literally named someone present (`Ty Downey`, `LaTanya Giles` both appear in their own transcripts) — when the title corroborates EXACTLY ONE attendee that identifies the prospect; two matches stay ambiguous and still refuse. Real-call resolution **94% → 98%**, residue 3 → 1.
  - **⚠ LLM RESIDUE TIER — SKIPPED BY RULING (2026-08-03), NOT unfinished.** After the refinements exactly **ONE** real call remains unresolvable (an `Impromptu Zoom Meeting` whose only prospect-side speaker is a device/dial-in id). **One row does not justify a pipeline, and "Unknown prospect" is the CORRECT answer for it** under the governing principle. It will pick up a name for free if that call is ever re-analyzed under v11. Do not build a second backfill tier for it.
- **⚠ CLOSE RATE REDEFINED (Justin's ruling 2026-08-03, SUPERSEDES the decided-only formula): `closed PROSPECTS ÷ TOTAL PROSPECTS`.** His words: *"if 1 prospect takes 3 calls to close that SHOULDN'T count as 3 calls, it's 1 prospect getting closed — if it's multiple follow ups it doesn't matter."* Follow-up calls **collapse into their prospect** and never inflate the denominator; **open prospects DO count as not-closed**. That last part is what **removes the need for an aging rule, a "still open" bucket, or any human judgement about dark deals** — all of which were on the table and are now moot. **DISPLAY:** percentage as the tile value with raw counts beneath ("12 of 37 prospects"), per the house rule that rates always render with their counts.
  - **CONSEQUENCE — 3d-1 is now a PREREQUISITE, not a follow-on.** The rate cannot be computed without `prospects` + `fathom_calls.prospect_id`.
  - **⚠ DO NOT OVER-ATTRIBUTE THE WIN TO THE ENTITY TABLE.** Measured on the live corpus: per-**prospect** rate **40%** vs per-**call** rate **39%** — **one point apart**. **Grouping is NOT what fixed the close rate; the DENOMINATOR REDEFINITION was** (counting open prospects as not-closed took it from 90% → 40%). Almost every prospect currently has exactly one call, so the entity table mainly makes the rate *computable and correct as volume grows* — its contribution widens only as follow-up sequences accumulate. A future session must not mis-prioritise around a belief that grouping delivered the number.
  - **3d-2 CANDIDATE GENERATORS — include the MEETING TITLE (ruling 2026-08-03).** The title is unreliable as a *name* (booked, wrong ~34%) but **reliable as a KEY**: the same booked title across two calls is strong same-prospect evidence **even when the resolved names disagree**. Live proof: `Mark-Anthony Rassmann`'s two calls resolved to `Mark-Anthony` and `Forb` (both grader, high) — no name-based generator can join those, but the identical title does. **Proposals only, never auto-merge.**
  - **CONSEQUENCE — merge quality now drives the headline number directly.** An unmerged `Towana / TJ` inflates the denominator by one and reads the rate **LOW**. Measured sensitivity on the current corpus: **each missed merge ≈ 0.9 points**. This raises the stakes on 3d-2's review surface from "nice UI" to "the thing that makes the headline number correct".
- **3d COMPLETE 2026-08-03/04.** 3d-0 (closer-exclusion on every name path) · 3d-1 (`prospects` + `fathom_calls.prospect_id`, migration 032, exact-match attach at analysis time) · **3d-3 SHIPPED FIRST, deliberately reordered ahead of 3d-2** so the corrected number reached the dashboard before the merge tooling · 3d-2 (merge-review surface).
  - **Close rate live and verified end-to-end on the served page:** backend `fetchProspectCloseRates` → `{closed, total, pct}` fed into the helper extracted from the live HTML → tile `41%`, sub-label `18 of 44 prospects`. `"of decided calls"` is **gone from served markup**; the legacy per-call figure is still emitted by the API for compatibility but rendered nowhere.
  - **Merge/unmerge routes exercised over real HTTP, 14/14** (throwaway account mirroring the Forb shape): proposal → merge → **unmerge**, plus self-merge rejected, double-unmerge rejected, and another user's prospect **403**. The undo property that mattered: a survivor with its OWN calls keeps them — the unmerge re-points only calls whose observed name still keys to the un-merging prospect.
  - **⚠ CALLER-SCOPE LIMITATION (capability gap, NOT a security hole) — belongs with the MANAGER-EXPERIENCE work, not here.** `/me/prospects/*` are scoped to `req.user.id`: a manager **cannot review a rep's prospects**. Nothing leaks — the routes 403 on another user's prospect (verified) — you simply can't act on someone else's. Closing it needs the `/admin` pivot's scope check (`role !== 'owner'` → `managed_by = self`), which is the same pattern `admin.js` already uses in 8 places. **Do not hand-roll a second authorization path inside `/me`** — that is why it was left out rather than approximated.
- **NOT built (superseded — 3d is done):** Historical note — the original 3c plan also listed a second tier (backfill all 83 — free diarized pass, then a cheap LLM pass for the residue: devices, phone numbers, 3+ speakers) and **3d** (prospects entity + per-prospect grouping). **3d hazards already present in live data:** same person different form (Towana Joseph/TJ, Serena Ifeoma/Ify), possible-variant-or-different-person (Kristien Bayan vs Christine), no name at all ("a prospect out of Wilmington"), couples, and 37 single-token names. **Auto-merging on fuzzy name is the one design to argue hardest against** — a wrong merge silently fabricates close-rate numbers and is invisible in the aggregate. Identity must be an ENTITY with human-confirmed merges.

## Brand
- **Name:** Scout Systems
- **Domain:** scoutsystems.io
- **Product name TBD** — "Sales Overlay" is the working title, may rename for launch

## GitHub & Railway Deployment

### Project layout (iCloud root: `~/Library/Mobile Documents/com~apple~CloudDocs/sales-overlay/`)
- `src/` — Electron desktop app source
- `backend/` — Railway-deployed Express backend (auth, billing, API proxy) + landing page
- `backend/web/` — static website served by backend (`index.html` landing, `login.html`, `admin.html`). Folder is `web/` not `public/` because Railpack's Staticfile detector auto-triggers on `public/` and deploys Caddy instead of Node — rename sidesteps it (see comment at `backend/index.js:22-24`).
- `build/` — electron-builder icons + entitlements
- `dist` → `dist.nosync` (symlinked, kept out of iCloud). In `~/sales-overlay/dist/` (local build dir), failed/unshipped build artifacts are moved into `dist/archive/<label>/` instead of deleted — e.g., `dist/archive/failed-build-2026-04-22-unnotarized/` holds the v1.0.3 DMGs that built signed-but-not-notarized due to the env-var paste bug. Pattern: always move failed builds to a dated `archive/` subfolder so electron-builder can write fresh artifacts at the top level of `dist/` without conflicts, and old artifacts remain for forensics.
- `node_modules` — kept local (iCloud sync breaks it; use `.nosync` pattern if re-created)
- `package.json` — Electron app manifest
- `railway.json`, `Procfile` — Railway config
- `API Keys.md` — all secrets in one place (gitignored)
- `.env` — local desktop app env
- `sales-overlay/` (nested folder) — OLD copy of the project, ignore

### GitHub
- **Repo:** https://github.com/scoutsystems-js/sales-overlay
- **Owner:** `scoutsystems-js`
- **Default branch:** `main`
- **Used for:** source for the Electron app AND the Railway backend in one repo; also the electron-builder auto-updater release target
- **PAT (scoutsystems-js):** stored in `API Keys.md` only (NEVER paste into `CLAUDE.md` or any tracked file — GitHub push protection will block the push)
- **Authed remote URL format:** `https://scoutsystems-js:<PAT>@github.com/scoutsystems-js/sales-overlay.git` (substitute the PAT from `API Keys.md` when setting up a new clone)
- **Push command:** `cd ~/Library/Mobile\ Documents/com~apple~CloudDocs/sales-overlay && git add <files> && git commit -m "message" && git push`
- **Auto-deploy:** every push to `main` triggers a Railway redeploy of the backend (~60s)
- **Release workflow (manual, v1.1.8 baseline):**
  1. `cd ~/sales-overlay && npm version <x.y.z> --no-git-tag-version`
  2. `bash ~/sales-overlay/build-release.sh` (~8–10 min). The script is gitignored and contains the APPLE_ID / APPLE_APP_SPECIFIC_PASSWORD / APPLE_TEAM_ID prefix on backslash-continued lines so the env vars always reach electron-builder. **Do NOT inline-paste the env-var prefix into the terminal** — that has caused silent unsigned builds at least three times across the project history.
  3. **Verify .app + DMG notarization separately** — the build will sign + notarize the `.app` reliably, but the DMG container is currently NOT auto-notarized (electron-builder gap, see Resolved Issues). Run: `xcrun stapler validate dist/Scout-X.Y.Z-arm64.dmg && xcrun stapler validate dist/Scout-X.Y.Z.dmg`. If either reports "does not have a ticket stapled," recover with manual sign+notarize+staple per DMG (see the DMG notarization gap entry in Resolved Issues for the exact commands). Until the electron-builder config is fixed, expect to run the manual recovery on every release.
  4. `git add package.json package-lock.json && git commit -m "Bump to vX.Y.Z" && git push` (from iCloud, not local build dir)
  5. Publish via `gh release create vX.Y.Z dist/Scout-X.Y.Z-arm64.dmg dist/Scout-X.Y.Z-arm64.dmg.blockmap dist/Scout-X.Y.Z-arm64-mac.zip dist/Scout-X.Y.Z-arm64-mac.zip.blockmap dist/Scout-X.Y.Z.dmg dist/Scout-X.Y.Z.dmg.blockmap dist/Scout-X.Y.Z-mac.zip dist/Scout-X.Y.Z-mac.zip.blockmap dist/latest-mac.yml --repo scoutsystems-js/sales-overlay --title "vX.Y.Z" --notes "..."`. Web UI fallback no longer needed (gh CLI installed since v1.1.0).
  6. Done. Auto-updater picks up the release for existing installs; the website's `/download` endpoint resolves the new DMG automatically within 5 minutes (cache TTL). Never need to update a hardcoded URL.

### Railway
- **CLI access (2026-07-23): a Railway PROJECT token lives in `API Keys.md`** (project caring-empathy/production). Usage: `RAILWAY_TOKEN=<token> railway <logs|status|variables> --service sales-overlay`. Project tokens always fail `railway whoami` — that's expected, not an auth problem. The interactive `railway login` OAuth is expired and non-TTY shells can't renew it; the token path needs no login state.
- **Outbound SMTP (25/465/587/2525) is BLOCKED on this plan tier** — email must go over HTTPS (Resend). A "hanging" outbound integration that times out in exactly 2 minutes is the signature of a blocked port + a default transport timeout.
- **Service public URL:** https://sales-overlay-production.up.railway.app
- **Custom domain:** www.scoutsystems.io is the canonical live URL — CNAME `www` in Namecheap → `vdy3qiy5.up.railway.app` (Railway provisioned a new endpoint when `www` was added as a custom domain; the old `ujcd5dfv` target is no longer used). Bare `scoutsystems.io` uses a Namecheap **URL Redirect Record** → `https://www.scoutsystems.io` (permanent 301). Known limitation: Namecheap URL Redirects don't serve HTTPS, so `https://scoutsystems.io` times out (HTTP 000) — users typing the bare domain with HTTPS-first browsers hit an SSL error. Permanent fix requires migrating DNS to Cloudflare for CNAME flattening + edge SSL at the apex.
- **Builder:** NIXPACKS (configured via `railway.json`)
- **Build command:** `cd backend && npm install`
- **Start command:** `cd backend && node index.js` (same in `Procfile`)
- **Node version:** `>=20.0.0` (per `backend/package.json` engines)
- **Health check:** `GET /health` returns `{"status":"ok","service":"Scout Systems Backend"}`
- **Routes served by backend:**
  - `/` → landing page (`backend/web/index.html`)
  - `/login` → web login page (`backend/web/login.html`)
  - `/admin` → admin + owner dashboard (`backend/web/admin.html`; user management with role change controls, cross-user session list, log detail; admin API at `backend/routes/admin.js`)
  - `/dashboard` → v2 donut-led coaching dashboard (`backend/web/dashboard.html`). Three donuts + Coach Summary + Recent Calls + drill-downs. No role redirect — works for everyone (admin/owner can pivot to another user via `?user=<id>` query param). v1 list-heavy version archived at `dashboard-v1.html.archived`.
  - `/coaching` → v1 coaching page (`backend/web/coaching.html`) — superseded by dashboard v2; route kept for backward compat, not linked from nav.
  - `/auth/*` → signup / login / verify / `me` (Supabase-backed, `backend/routes/auth.js`; `/auth/me` returns `{user_id, email, role}` and is the source of truth for post-login redirect)
  - `/me/*` → caller-scoped endpoints (`backend/routes/me.js`):
    - `GET /me/sessions` — call list with prospect_name + outcome_source enrichment
    - `GET /me/sessions/:id/logs` — raw session_logs rows
    - `GET /me/sessions/:id/objections` — per-call objection events with coaching narratives
    - `POST /me/sessions/:id/extract-outcome` — Claude infers win/loss/follow_up from post_call_summary
    - `POST /me/sessions/:id/extract-objections` — mines session_logs, classifies + generates coaching narrative per objection event
    - `POST /me/sessions/:id/extract-prospect-name` — Claude pulls prospect's first name from intro transcript
    - `GET /me/analytics?from=&to=` — donut data (outcome distribution + objection breakdown by type)
    - `GET /me/objections?objection_id=&from=&to=` — every event of one objection type, joined with session metadata for the third-level drill
    - `GET /me/coaching/patterns?from=&to=` — Claude-generated 3-5 cross-session coaching patterns
  - `/admin/*` → cross-user routes for admin/owner (`backend/routes/admin.js`). Mirror of `/me/*` extraction + analytics + coaching/patterns + objections-by-type routes, scope-checked: admins limited to `user_profiles.managed_by = self`, owners unrestricted. Plus the cross-user user management + session list from v1.1.0.
  - `/billing/*` → Stripe checkout + webhook (`backend/routes/billing.js`; `/billing/webhook` uses raw body)
  - `/proxy/*` → API proxy (`backend/routes/proxy.js`, keeps Deepgram/Anthropic keys server-side)
  - `/download` → 302 redirects to the latest Mac universal .dmg from GitHub Releases (`backend/routes/download.js`). Variants: `/download/mac` (universal, same), `/download/mac/arm64` (Apple Silicon only), `/download/latest-mac.yml` (auto-updater manifest passthrough), `/download/version` (JSON: current tag, name, published date, release URL). Resolves the asset URL from the GitHub API at request time with a 5-minute in-memory cache — the download button on login.html points to `/download` and never has to change when a new version ships. Optional `GITHUB_TOKEN` env var lifts the 60/hr unauthenticated rate limit to 5000/hr if we ever need it.
  - `/health` → health check

### Railway environment variables (set in Railway → Variables)
Keys themselves live in `API Keys.md` (gitignored). Variable names set in Railway:
```
DEEPGRAM_API_KEY
ANTHROPIC_API_KEY
VOYAGE_API_KEY                   # server-side embeddings for KB search
SUPABASE_URL           (https://vkprybqmryiuwbdwdlpk.supabase.co — not secret)
SUPABASE_ANON_KEY      (public key, safe to reference)
SUPABASE_SERVICE_ROLE_KEY
STRIPE_SECRET_KEY                # add when Stripe is wired
STRIPE_WEBHOOK_SECRET            # add when webhook is configured
STRIPE_PRICE_ID                  # $197/month price ID
DEEPGRAM_PROJECT_ID              # for Deepgram usage proxy
FATHOM_CLIENT_ID                 # Fathom OAuth (v2.0 pivot)
FATHOM_CLIENT_SECRET             # Fathom OAuth (v2.0 pivot)
FATHOM_REDIRECT_URI=https://sales-overlay-production.up.railway.app/auth/fathom/callback
FATHOM_STATE_SECRET              # HMAC-SHA256 key for the signed OAuth state JWT in /auth/fathom/*. Generate with `openssl rand -hex 32`. Treat as a database password — never commit. Rotating it invalidates any in-flight OAuth attempts but does NOT affect already-stored Fathom tokens.
PORT=3000
BACKEND_URL=https://sales-overlay-production.up.railway.app
SKIP_AUTH=true                   # flip to false once auth is tested
CRON_SECRET                      # shared secret for POST /fathom/sync-all (auto-sync cron). Generate with `openssl rand -hex 32`. Set on BOTH the web service and the cron service. Without it, /fathom/sync-all returns 503.
```

### Domain / DNS (Namecheap → Railway + Google Workspace)
- Registrar: Namecheap
- **Advanced DNS records:**
  - `CNAME www` → `vdy3qiy5.up.railway.app` (Railway www endpoint)
  - `TXT _railway-verify.www` → `railway-verify=0cff3f73…` (Railway www domain verification)
  - `TXT _railway-verify` → `railway-verify=622b22b1…` (stale — from the old bare-domain Railway setup, safe to delete)
  - `URL Redirect @` → `https://www.scoutsystems.io` (Permanent 301 Unmasked — HTTP-only, no HTTPS)
  - `TXT @` → `google-site-verification=Rbg5oSHK…` (Google Workspace domain ownership)
  - `TXT @` → `v=spf1 include:_spf.google.com -all` (SPF; used `-all` because Namecheap's UI choked on the tilde `~`)
  - `TXT _dmarc` → `v=DMARC1; p=none; rua=mailto:justin@scoutsystems.io`
- **Mail Settings section** (separate from Host Records — MX lives here in Namecheap):
  - `MX @` → `smtp.google.com` priority `1` (Google Workspace single-record format, 2023+)
- SSL cert (Let's Encrypt) auto-issues from Railway once TXT verifies
- **HTTPS apex limitation:** Namecheap URL Redirect Records don't listen on port 443 so `https://scoutsystems.io` times out. Fix requires migrating DNS to Cloudflare (planned — see Future Plans).

### Google Workspace (scoutsystems.io)
- **Plan:** Business Starter, $7/user/month, 14-day free trial active
- **Admin console:** https://admin.google.com (logged in as `justin@scoutsystems.io`)
- **Users to create after Gmail activates:** `justin@`, `James@`, `Josh@` (support@ added later)
- **Domain verification:** completed (TXT record at `@`)
- **Gmail activation:** in progress — DNS is correct and globally propagated (verified via `dig` from Google + Cloudflare DNS), but Google's verifier hasn't re-checked yet. Google docs note activation can take up to 72 hours. Retry every ~30 min until it flips to "verified."
- **Deliverability:** SPF + DMARC in place. DKIM must be generated in admin.google.com → Apps → Google Workspace → Gmail → Authenticate Email AFTER Gmail activates, then TXT at `google._domainkey` added to Namecheap.

### Electron app publish target (auto-updater)
- Publish provider: `github`
- Owner/repo: `scoutsystems-js/sales-overlay`
- Current release: **v1.1.2** (arm64 + x64). Desktop app ships with zero secret API keys — all Claude + Deepgram traffic proxied through Railway. Architecture baseline from v1.0.4 still holds: JWT auto-refresh via `/auth/refresh`, proxy-minted 10-min ephemeral Deepgram keys, and SKIP_BILLING beta pass-through on `requireSubscription` middleware.
- Releases published via `gh release create v<X.Y.Z> <assets...> --repo scoutsystems-js/sales-overlay --title "v<X.Y.Z>" --notes "..."` (gh CLI installed and authenticated as of v1.1.0). Fully automated `npm run release` (build + publish in one command via electron-builder's `--publish always`) needs `GH_TOKEN` set in the build env — not yet wired but the gh CLI prerequisite is no longer blocking.

## Strategic Pivot — Scout v2.0

### Product Vision: "Jarvis for closers"

Scout v2.0 is an **AI operating system for high-ticket sales teams** — not a single feature, a stack of capability layers that compound as they ship. The original live-teleprompter product stays as an optional feature; the new core is post-call intelligence and CRM-level automation.

**Three capability layers, shipped in order:**

**Layer 1 — Call Intelligence** (building now)
- Fathom OAuth + auto-sync (Phase 1 — **COMPLETE**)
- Post-call analysis pipeline with 5 section grades
- Call library dashboard with timestamp-linked clips
- Objection intelligence dashboard

**Layer 2 — CRM Integration** (after call analysis proven)
- GHL (GoHighLevel) and Close.io to start
- HubSpot and others added later
- Read AND write: Scout updates pipeline stages, logs call activities, manages contact data
- Pipeline updates after every analyzed call
- Curated follow-up cadences per deal
- Beginning-of-day pipeline prioritization list
- Beginning-of-day overnight recap

**Layer 3 — Proactive Intelligence** (after CRM)
- BOD report: prioritized call list for the day
- Overnight recap: what changed while closer was offline
- Curated follow-ups: Scout suggests next touch per deal
- Follow-up email generation after every call

### Build Phases (in order)
- **v1.2.0 — Fathom OAuth + auto-sync** — **COMPLETE** (shipped to prod 2026-06-15 via commits `00c9630` / `c39954d` / `2f3abc4` / `df5fb4e`; migration 009 applied)
- **v1.3.0 — Post-call analysis pipeline** — **COMPLETE & PROVEN ON REAL CALLS** (2026-07-17: Josh's real sales calls analyzed end-to-end — section grades, highlights timeline, one-thing, and follow-up emails all validated on production data). Layer 1 step 2 / step 3 combined release:
  - Migration 010 (`call_analyses` + `call_highlights`)
  - Transcript normalizer (HH:MM:SS strings + numeric seconds → single numeric-seconds representation)
  - Section grader (Claude call — Intro / Discovery / Pitch / Objection Handling / Close)
  - Highlight extractor (Claude call — 5-8 moments with type + quote + observation)
  - Auto-trigger after sync completes (async; closer never waits)
  - Call library dashboard page
  - Call review page with highlights timeline as the lead section
  - **Proven in production (2026-07-17):** the OAuth transcript path (`/recordings/{id}/transcript`), the `findMeeting` bypass (metadata from the stored `fathom_calls` row + direct transcript fetch), the reanalyze/batch-limit retry flow, and the `recorded_by[]` sync filter were all shipped and validated against Josh's live Fathom workspace. See "Fathom API Constraints (learned in production)" below.

#### Shipped 2026-07-18 → 2026-07-19 (v1.3.1 partial + v1.4.0 objection intelligence, pulled forward)
- **Default Coaching Dashboard (analytics2).** All roles land on `/dashboard` (admin one click away). Fathom-era analytics from `call_analyses` + `call_highlights` via `/me/analytics2` (+ `/admin/analytics2/:user_id`): Calls-analyzed / Avg-Score / Objections(distinct-calls) donuts + computed coach summary (section averages with `n` + latest `one_things`; **no LLM on load**). Old `call_sessions` analytics moved behind a **"Live Sessions"** toggle. Per-donut **filtered drill-downs** (`/fathom/calls?filter=analyzed|objections&sort=score`; admin pivot via `/admin/fathom-calls/:user_id`).
  - **Close Rate donut deferred** — replaced by Avg Score until `outcome` is populated (needs re-analysis).
- **v1.4.0 Objection Intelligence — PARTIALLY SHIPPED.** Schema + prompts + view are live; synthesis + populated data are **pending on Anthropic credits**.
  - **Shipped:** migrations 012 + 013 (see Architecture below); the section grader infers `outcome`; the highlight extractor emits `objection_category` + `resolution` + `closer_response`; the **Objections view** (nav) — `/me/objections-intel` (+ `/admin/objections-intel/:user_id`): metrics row (total / handle-rate / handled·partial·unhandled), per-category breakdown, and a feed of objection moments with prospect quote + observation + resolution badge + the closer's own handling line + a **Fathom clip link** (`recording_url?t=seconds`). Works on existing highlights today.
  - **Pending (credit-gated, as of 2026-07-19 the Anthropic balance is exhausted):** the **re-analysis pass** that populates `objection_category` / `resolution` / `closer_response` / `outcome` (currently null / all `uncategorized`); the **grounded ISOLATE→REFRAME→OVERCOME synthesis** (LLM + cache — currently a placeholder section in the view); and the outcome **manual-override** UI on the review page.
  - **Taxonomy as actually built:** a **single** category layer — `fear` / `logistical` / `timing` / `partner` (money-phrased objections classified as `fear` unless a genuine logistical payment constraint appears; encoded in the extractor prompt; `partner` requires the observation to cite the prospect's framing). No second "surface" layer. `outcome` is a **4-value** enum (`closed` / `follow_up` / `lost` / `no_show`) with `outcome_source` (`inferred`/`manual`/`crm`) — no confidence/evidence fields.

#### 2026-07-21 — Auth diagnostics shipped; Issue-1 root cause identified (fix pending overnight data)
- **Overnight logout RECURRED despite the scout-auth.js hardening.** Root cause found: **dashboard.html carries its OWN parallel auth layer that overrides scout-auth.js** — a naive `refreshSessionIfNeeded()` (treats transient failures as fatal, bypasses the cross-tab lock via a raw `/auth/refresh`) used at boot, plus ~15 inline `if (res.status === 401) { clearSession(); location.replace('/login') }` handlers that nuke the session on any 401 — including the ones scout-auth deliberately returns WITHOUT clearing on a transient failure. So the transient-tolerance + cross-tab lock are neutralized by the page. The same page-level pattern exists in admin.html. **Fix (deferred until overnight beacon data confirms):** delete the page's naive refresh + 401 handlers and route everything through `window.ScoutAuth`.
- **Persistent auth diagnostics (shipped, NOT the fix).** `scout-auth.js`: a localStorage RING BUFFER `scout_auth_diag_v1` (≤200 events, survives reloads; read via `scoutAuthDump()`), a BEACON to **`POST /auth/diag`** (unauthenticated; logs `[auth-diag]` to Railway) on failures + logout triggers (`refresh-fatal-cleared`, `refresh-transient-kept`, `redirect-to-login`, `boot-no-session`), and a **boot snapshot** logging UA + which auth keys were present BEFORE any refresh — distinguishes "refresh failed" from "storage already empty" (Safari-ITP idle eviction). dashboard.html: its `clearSession()` beacons `page-clearSession` with a stack trace (identifies which of the ~15 handlers fired), `refreshSessionIfNeeded()` beacons its failure mode, and `init()` redirects beacon `page-init-redirect` with a reason. `ScoutAuth.diag = {push,beacon,snapshot,dump,clear,tab}`. **Cron ruled out** (touches only `fathom_connections`, never Supabase auth). `auth.audit_log_entries` is empty (GoTrue audit logging off) — check Supabase Dashboard → Logs → Auth for server-side revocation evidence. Known suspect to confirm via UA in beacons: **Safari ITP** clears localStorage after ~7 days idle (and is more aggressive on some setups).

#### Shipped 2026-07-21 — verdict extended to follow_up (grader v5)
- **`ANALYSIS_PROMPT_VERSION='v5-2026-07-21'`.** The review-page verdict now covers `follow_up` calls too (the majority — ~60% of Josh's corpus). The grader produces `why_outcome` for `closed`/`lost`/`follow_up`; only `no_show`/null stay null. For `follow_up` the reason is the **primary BLOCKER** keeping it open (deal alive, not lost), paired with `one_thing` as the **next move** that advances it. Persist gate `isWinLoss` → `producesVerdict` (adds follow_up). Frontend `whyOutcomeState()` adds `follow_up → {kind:'pending'}`; renders an amber "Why this call hasn't closed yet" section (`.review-why.pending`, `--mid`) with "The move / One thing to do next". `no_show` still hidden. **Root-cause note:** the earlier "Towana Joseph shows no verdict" was NOT a bug — it was a `follow_up` correctly getting a null verdict under v4; v5 is the product change that gives follow_ups a verdict. The v5 bump makes all 38 v4 calls outdated → the "Update analyses (38 outdated)" button returns → re-grade populates follow_up verdicts.

#### Shipped 2026-07-20
- **Persistent web login — overnight-logout fix (`scout-auth.js`).** Confirmed Supabase auth settings: single-session OFF, time-box never, inactivity never, refresh-token rotation ON with **compromised-token (reuse) detection ON**, reuse interval ~10s; access-token TTL 60 min. Two root causes fixed: **(1) transient failures were treated as fatal** — `refreshSession` now only clears the session on a definitive HTTP 400/401; network errors / 5xx / 429 keep the session so a wake-time blip can't log you out. **(2) cross-tab refresh race** (the prime suspect given reuse detection) — two tabs waking overnight both replayed the same rotating refresh token; the one landing outside the 10s window tripped reuse detection and revoked the whole family. Refreshes are now serialized ACROSS tabs via `navigator.locks` (falls back to a localStorage lock with stale-takeover); every waiter re-reads localStorage first (adopts a token another tab already rotated) so only ONE network refresh happens per rotation. On a 401 the interceptor passes the stale access token so a sibling-tab rotation is adopted with no network call; a final localStorage re-read happens before any redirect to `/login`. Wake triggers (`visibilitychange`/`focus`/`online`) supplement the 4-min timer (frozen during sleep). Always-on console instrumentation prefixed `[scout-auth <ISO>]` for morning diagnosis. **Repro/test:** leave the dashboard open in TWO tabs overnight.
- **Fathom auto-sync cron + always-available re-grade (Phase 1.5).** The per-user sync core is factored into `syncUserCalls(admin, userId, conn)`, reused by the authed `GET /fathom/sync` and a new **`POST /fathom/sync-all`** (shared-secret `X-Cron-Secret: CRON_SECRET`, NOT requireAuth — cron has no session). `sync-all` iterates every `fathom_connections` row with `fathom_email` set, syncs each, and logs+continues on per-user failure. The `synced_unanalyzed` holdback is respected for free (ON CONFLICT DO NOTHING → held rows never re-insert → never dispatched; NEW calls arrive `pending` and analyze). **Cron mechanism LIVE: GitHub Actions scheduled workflow** — `.github/workflows/fathom-sync.yml` runs `0 */2 * * *` (+ `workflow_dispatch`), POSTing `/fathom/sync-all` with the `CRON_SECRET` repo secret. Chosen over Railway-native cron because the Railway CLI was unauthenticated at setup and GH Actions needs no second Railway service; `gh secret set CRON_SECRET` is done, secret also stored in gitignored `API Keys.md` under "## Cron / Auto-sync". **STILL REQUIRED:** set the SAME `CRON_SECRET` value on the Railway **web service** env — until then `/fathom/sync-all` returns 503 and the workflow fails (that failure is the signal). Alternative trigger kept: Railway native cron on a dedicated service running `node backend/scripts/cron-sync.js` (same schedule) — either hits the same endpoint. **Re-grade availability:** `GET /fathom/status` now returns `outdated_count` = **all `call_analyses.status='done'` rows for the user whose `prompt_version != CURRENT`** (NULL counts; held calls carry `status='synced_unanalyzed'` so they're excluded for free — `outdatedCallIds()` no longer joins `fathom_calls.sync_status`, which under-counted after a deploy-race; see Session Learnings 2026-07-20), and **`POST /fathom/update-analyses`** resets that batch (`fathom_calls.sync_status` + `call_analyses.status` → `pending`) and fires the analyze loop. Dashboard shows **"⟳ Update analyses (N outdated)"** when `pending_count===0 && outdated_count>0`. `ANALYSIS_PROMPT_VERSION` is now exported from the worker so the route can compare. **Requires:** set `CRON_SECRET` in Railway (web + cron service) and configure the cron service.
- **First-sync analysis cap = newest 20 — FIRST SYNC ONLY (ruling 2026-07-31, FD-4 Option B; scope corrected 2026-07-31).** On the FIRST sync (connection has no `last_sync_at` yet) auto-analysis is capped to the newest `FIRST_SYNC_ANALYZE_CAP` (=20) of the backlog; the rest insert as `sync_status='pending'` for the Update-analyses backfill. On EVERY subsequent (steady-state) sync, ALL new calls are auto-analyzed. `fathom.js` `callIdsToAnalyze(newRows, lastSyncAt, cap)` is the selector (TDD'd in `first-sync-cap.test.js`); Zoom imports the SAME function. **Why first-sync-only:** "analyze everything on connect" is unbounded — a customer with years of history would auto-fire hundreds of Claude analyses on connect. But capping EVERY run (the original FD-4 implementation, fixed 2026-07-31) was wrong: a single busy day / manual sync / cron-gap-recovery bringing >20 NEW calls in one run would silently leave the remainder ungraded (they'd sit `pending`, only reachable via backfill). Steady-state windows are naturally bounded by real call volume, so analyzing all of them is safe and correct. The **Update-analyses dropdown (7d/30d/all-time, dry-run count + confirm)** remains the deliberate path for the first-sync backlog tail. `orderBatchIds` stays ordering-only; batch SIZE is the caller's (first-sync cap 20 via `callIdsToAnalyze`, or re-grade `limit` default 10). **Zoom mirrors this exactly (sub-stage 2, shipped 2026-07-31).**
- **"Why this call closed / didn't close" on the call review page (grader v4).** `ANALYSIS_PROMPT_VERSION='v4-2026-07-20'`. For win-class (`closed`) and loss-class (`lost`) calls the section grader now emits `why_outcome {reason, quote, timestamp_seconds}` — the single most decisive cause of the outcome anchored to one transcript moment — plus `one_thing_timestamp_seconds`. For a **lost** call, `one_thing` is required to be the direct antidote to `why_outcome`, so they render as **cause → correction**. `why_outcome` is forced null for `follow_up`/`no_show`; timestamps are clamped to the recording (`boundTs`, mirrors the highlight bound). **Schema:** migration 018 adds `why_outcome` / `why_quote` / `why_timestamp_seconds` / `one_thing_timestamp_seconds` to `call_analyses` — **one** `why_outcome` column (not `why_closed`+`why_not_closed`), interpreted by the call's outcome. **Review page:** new section between header and highlights — loss-class "Why this call didn't close" (red accent: cause + quoted moment + ▶ clip, then the paired "One thing to do differently" beneath it, footer duplicate suppressed); win-class "Why this call closed" (green accent + clip, `one_thing` stays in the highlights footer); other/no-outcome hidden. **Graceful degradation:** pre-v4 analyses have `why_outcome=null` → section hidden, `one_thing` renders standalone as before. **No forced re-analysis** — the 9 pending + future calls populate the full section naturally. `/fathom/calls/:id` select now returns `outcome` + the four new fields.
- **Live Sessions toggle RETIRED (DORMANT).** The Coaching / Live Sessions toggle is hidden in ALL ranges regardless of data — the Live Sessions view is desktop-teleprompter-era and fully retired from the dashboard until the teleprompter returns. `renderModeToggleHtml()` early-returns `''`; the former toggle markup is commented in place directly beneath it. The view logic (`renderOverview`'s `'sessions'` branch, `hasLiveSessions()`, `setOverviewMode()`) is kept intact but unreachable — nothing sets `overviewMode='sessions'` anymore. To revive: delete the early return in `renderModeToggleHtml()`. (Superseded the 2026-07-20 "auto-hide when zero sessions" behavior — now hidden unconditionally.)

#### v1.4 Team Layer — COMPLETE (2026-07-22: digest live + accepted; EOD Report arc accepted)
Design approved with 5 mods: (1) role migration has a **staged, tested rollback**; (2) manager daily digest is **CRON-generated** for everyone-with-reps after the morning sync (not on-first-view); (3) **owner-as-manager** — `managed_by` may point at a `manager` OR `owner`; **"has assigned reps" (not role)** grants the manager-view experience; owner-with-reps lands on their own team by default; owner team-picker = My team | each manager's team | All users; (4) a **hard post-data-flip verification checkpoint** (owner / manager / user / owner-with-reps) before any UI; (5) rest as designed — manual creation w/ temp password, pivot tightening (managers→own reps, owners→anyone), account-lock rules, permissions matrix (+ owner-with-reps row).
- **Stage 1 (migration 019, applied):** role `admin`→`manager` (backup table + tested `019_rollback.sql`; the test caught that `user_profiles_role_check` only allowed owner/admin/user → migration now drops→flips→re-adds `(owner,manager,user)`). Added `billing_status` (trial|active|past_due|canceled, default trial) + `no_self_manage` check. RLS 3 policies `admin`→`manager`. Prod flip was a no-op (0 admins).
- **Stage 2 (backend refactor):** admin.js 11 `requireRole` gates + 8 inline pivot scope checks (`role==='admin'` → `role!=='owner'`, so any non-owner is scope-checked to `managed_by=self`; owner passes platform-wide) + `ALLOWED_ROLES`; kb.js team-scope/uploader/upload-scope key on `manager` (the `match_knowledge` RPC is relationship-based, already correct).
- **Post-data-flip checkpoint: ALL 21 CHECKS PASSED** (route-level scope + Postgres RLS) across owner / owner-with-reps / manager / user, using ephemeral test identities (created + cleaned up). Manager sees only self+reps (RLS 2 rows; `/admin/users` self+rep; pivot to non-rep/other-mgr-rep → 403); user → 403 on admin routes, RLS self-only (1 row); owner/owner-with-reps → all + pivot anywhere.
- **Stage 3 (Manager view) — SHIPPED, STOPPED for visual review 2026-07-21.** `/team/*` (manager+owner), rep set resolved+permission-checked server-side; `lib/team-analytics.js` (per-rep cards + team totals + trend arrows + week/month/quarter buckets) + `lib/team-synthesis.js` (team recommendations + Call Highlights of the Week — 3 lanes, cache types `team`/`highlights`, credit-tolerant, evidence resolved from real rows w/ rep attribution). Frontend: **Team** nav (manager+owner; Admin link now **owner-only**), `#team` route, owner team-picker, per-rep cards → pivot drill-through. Digest panel = LIVE (see Stage 6 below). Verified live end-to-end. **DEMO TEAM (cleanable):** 3 reps `demo-{ava,ben,cara}@scout-demo.dev` assigned to Josh, with 15 of Josh's analyzed calls **copied** (new ids — non-destructive). To remove after review: delete those users + their copied `fathom_calls`/`call_analyses`/`call_highlights` (all `demo-`-prefixed `fathom_call_id`).
- **Stage 6 (Manager daily digest) — LIVE + ACCEPTED (2026-07-22).** `lib/team-digest.js`: one digest per user-with-assigned-reps (has-reps NOT role — owner-with-reps included) covering the previous **America/New_York** calendar day (DST-safe via Intl; tested incl. both transition days). Rides `objection_synthesis_cache` as `synthesis_type='digest'` keyed `(manager, from_ts=date@00:00Z, to_ts=nextDate@00:00Z, set_hash)`; the hash folds in analyses + **call ids** (so synced-but-unanalyzed days don't collide with true zero-call days) + kbHash. Cache hit = no Claude call → the every-2h cron is idempotent per day. Zero-call day = deterministic "quiet day" digest, cached, zero Claude spend. Generation: fire-and-forget pass after the `/fathom/sync-all` loop (per-manager error-isolated — a digest failure can never fail or block sync, same degrade rule as KB) + owner-only **`POST /team/digest/run`** `{date?}` for on-demand testing. Read: `GET /team/digest?date=YYYY-MM-DD` (default ET-yesterday) now also returns `recent_dates` (last 7 stored days) which the panel renders as history chips. Panel renders date/quiet badge, stat tiles, summary, ≤3 notable moments (rep-attributed, clip links resolved from real rows only), and a "Today's focus" accent box. Grader untouched — ANALYSIS_PROMPT_VERSION stays v6. Tests: `backend/test/team-digest.test.js` (ET math, cache-key convention, hash discipline, quiet shape).
- **Stage 7 (EOD Report + grader v7) — SHIPPED + ACCEPTED (2026-07-22; migrations 021 applied, delta-gated).** (a) **Migration 021**: `call_analyses.cash_collected` numeric(12,2) NOT NULL DEFAULT 0 (grader-owned, versioned with the prompt) + `eod_edits` table (user_id, fathom_call_id, field CHECK'd to the 4 EOD fields, value text, UNIQUE per user+call+field; RLS no policies) — **MUST be applied to Supabase BEFORE the v7 code deploys** (the worker upserts cash_collected; unknown column ⇒ analyses error). (b) **Grader v7** (`v7-2026-07-22`, one commit): cash_collected extraction (explicit-only, zero-default, never inferred — server-side sanitizeCashCollected: strict number, 0–$1M, else 0) + follow_up_email **greeting contract** (transcript-established name only, omit entirely when unclear, meeting title explicitly forbidden as a source). (c) **`/fathom/update-analyses` now dispatches pending ∪ outdated** (one batch covers manually-reset rows + stale versions; dashboard button shows when outdated>0, count = outdated+pending, replaces the reanalyze button in that state) — per the combined-reanalysis ruling. (d) **EOD Report view** (`#eod`, all users): GET /eod?date= (ET-day, digest-lane helpers) + PUT /eod/edit (ownership-checked, field-allowlisted); render rule = **override if present else analysis value** — analysis upserts can never touch eod_edits; inline editing, day picker, copy-for-Slack plain text (prospect/outcome/link/cash/summary order). Tests: grader-v7.test.js + eod.test.js (suite 25).
- **Stage 8 (grader v8: transaction-evidence cash + payment structure + first-person EOD summary) — SHIPPED + ACCEPTED (2026-07-22; migration 022 applied, delta-gated incl. flip-rate control arms; note: the Stage-8 Payment select/Slack line described below was later REMOVED by the composed-outcome product ruling — see the EOD Payment row entry above).** (a) **Migration 022**: `call_analyses.payment_structure` (text NOT NULL DEFAULT 'none_stated', CHECK paid_in_full|payment_plan|bnpl|none_stated) + `call_analyses.eod_summary` (text, NULL = pre-v8) + `eod_edits` CHECK gains 'payment_structure' — apply BEFORE the v8 deploy. (b) **Grader v8** (`v8-2026-07-22`, one commit): cash extraction now actively hunts transaction evidence (card runs, deposits, charge confirmations, Affirm/Klarna/BNPL approvals, plan first-payments) with **explicit per-structure cash rules** — PIF = full amount charged; payment_plan = ONLY what was collected on the call, never total contract value; **bnpl = full financed amount (intentional semantic addition, Justin-ruled)**; never-fabricate unchanged. `payment_structure` closed-only (server-side coupling: `sanitizePaymentStructure(v, outcome)` forces none_stated for non-closed). `eod_summary`: 2-4 sentence FIRST-PERSON closer-voice Slack-ready summary, losses stated plainly, no coaching language, no third-person narration; `overall_summary` (coaching voice) untouched — consumers verified disjoint (eod.js is the only shared read point, now forked). (c) **EOD view**: summary prefill = `eod_summary || overall_summary` (pre-v8 fallback), payment structure as a constrained select (4 values, route-enforced allowlist), Slack copy gains 'Payment: <label>' after Cash, skipped when none_stated. Edits still win over both. Tests: grader-v8.test.js + eod.test.js updates (suite 34).
- **EOD Payment row REMOVED by product ruling (2026-07-22) — do not re-add.** `payment_structure` stays extracted, stored, and edit-protected (eod_edits allowlist unchanged), but it no longer renders as its own EOD row or Slack line. Instead the OUTCOME prefill composes it server-side (routes/eod.js `outcomePrefill`): closed + known structure → "Closed - PIF" | "Closed - Payment plan" | "Closed - Financed"; plain outcome otherwise; outcome stays free-text editable and stored edits win untouched. **Logged for the next grader bump:** extract a short human payment label (pay count / provider, e.g. "3 pay", "Affirm") so labels compose as "Closed - 3 Pay" with no editing.
- **Batch scope ruling (2026-07-22): Update-analyses batches are the 20 MOST-RECENT calls, not full sweeps.** Server selects newest-first by `call_date` (pending block ahead of outdated — `orderBatchIds` in routes/fathom.js, test-pinned; `outdatedCallIds` itself still orders by analyzed_at and is count-only for `/fathom/status`). Frontend sends `limit: 20`. Older calls stay on their old prompt versions INTENTIONALLY — new calls pick up the current prompt at sync time; the button's outdated count still shows the full number, and that is by design, not a bug.
- **NEXT HARDENING PASS — also queued (2026-07-22):** shared constants module for the payment-structure allowlist (currently triplicated: migration 022 CHECK, analysis-worker, eod.js+frontend — each pinned by tests, but single-source is cleaner). Justin-approved as hardening-pass scope, not stage scope. **Add (2026-07-31): the `routes/zoom` → `routes/fathom` import coupling** — Zoom's sync imports `FIRST_SYNC_ANALYZE_CAP` / `pickNewestForAnalysis` / `callIdsToAnalyze` from `routes/fathom` to guarantee ONE shared cap rule. Accepted for now (the shared rule is the point), but the clean home for those (plus the payment-structure allowlist) is a small shared module (`lib/sync-cap.js` or similar) so a route file doesn't import another route file. Fold into this hardening pass.
- **Manual-outcome-override protection — IMPLEMENTED (confirmed 2026-07-31; was logged as a TODO 2026-07-22).** `analyzeCall` is NOT a blind upsert: before writing, it reads the existing row's `outcome_source`, and when `='manual'` it FREEZES `outcome` / `outcome_source` / `outcome_set_at` / `outcome_set_by` (read-before-write guard, `manualLocked` in analysis-worker.js ~L885). The frozen `effectiveOutcome` also drives `close_score` (Thread 2) + `payment_structure`. So a re-analysis / backfill can NEVER re-stamp a human-set outcome — this is why the v9→v10 30-day backfill is safe. (Confirmed against live data 2026-07-31: 0 rows have `outcome_source='manual'` — 182 null + 135 inferred — so nothing is even at risk yet, but the guard holds structurally for when an outcome-edit UI lands.) The EOD edit layer (`eod_edits`) separately shields the EOD view's outcome field. The v7 delta-gate note (borderline `lost→follow_up` flicker ~1-in-6 on re-analysis) is why the guard matters: inferred outcomes CAN move on re-grade — only manual ones are frozen.
- **BUILD PLAN (ordered, ratified 2026-07-22 close-out):**
  1. **Welcome emails — COMPLETE + ACCEPTED (2026-07-23, end-to-end verified in production logs: `invite_sent` → set-password completed by the test user).** Final shape: manual creation (or the owner-only per-row **Resend welcome email** button / `POST /admin/users/:user_id/welcome-email`) mints a one-time Supabase **recovery-type** action link (`generateLink` — invite-type rejects existing users; the mint REJECTS any link whose redirect_to was silently swapped for the Site URL, so a broken allowlist fails loudly instead of mailing localhost) → email via **Resend HTTPS API** (native fetch to api.resend.com, hard 10s AbortController bound, env `RESEND_API_KEY`, sender constant `Justin <justin@scoutsystems.io>`, subject "Your Scout login") → **`/set-password`** (login-styled; parses the verify-redirect fragment incl. the error branch; friendly no-timeframe expired/used message; min 10 + confirm; `POST /auth/set-password` = bearer-verified `getUser` → `updateUserById`; writes `scout_session_v1`; relative redirect to `/dashboard` → Connect Fathom card). Console: `invite_sent` hides the temp-password box; `failed`/`not_configured` shows it — the temp password remains the permanent fallback, one create flow, presentation branch only. Isolation contract throughout: email can never fail/hang creation (10s worst case); no token/credential/key in any log, reasons scrubbed. **History (why the shape):** v1 temp-password email never shipped; scope-upgraded to the invite flow; nodemailer/Google-SMTP transport hung 2 min in-request because **Railway blocks outbound SMTP (25/465/587/2525) on this plan tier — never put SMTP back on Railway**; swapped to Resend over HTTPS. Supabase Auth URL config (Site URL `https://www.scoutsystems.io`, `/set-password` allowlisted, OTP expiry 24h) is DONE. **Minor open items:** delete retired `WELCOME_SMTP_USER`/`WELCOME_SMTP_PASS`/`WELCOME_FROM_NAME` from Railway Variables (unused; batch in one edit — each var change redeploys); optional `WELCOME_FROM` constant → config.js. Tests: welcome-email.test.js (copy-exact, config matrix, isolation, 10s-bound, scrub; suite 45).
  2. **Stage 5 account page + billing seam — BUILT 2026-07-23, LOCAL COMMITS, awaiting approval to push (migration 023 applies pre-deploy).** `#account` view (signed-in email in the top bar is the entry link): **Profile** (name self-edit via `PATCH /me/account`, allowlisted first/last 1-60 chars, FRESH `managed_by` check per mutation → 403 — the managed lock's single source of truth is the column, same as kb.js; email read-only "contact your admin"), **Password** (`POST /auth/change-password` — requires CURRENT-password verification via a throwaway anon-client signInWithPassword, generic 401 on mismatch, no password material logged; EXEMPT from the managed lock by ruling — personal security ≠ business config; the recovery-token path `/auth/set-password` stays exempt from current-password, the token is the proof), **Offer** (link to `#kb` for unmanaged users — never a second editor; desktop-era user_profiles offer columns stay off the page entirely), **Billing** (badge from canonical `billing_status` + plan line when `billing_plan` set + provider-keyed action slot: dead-ends to a contact-admin mailto until `billing_provider` is non-NULL, where the portal link goes later). **Migration 023 (billing seam)**: `billing_provider` (CHECK stripe|whop, NULL = manual/admin-managed), `billing_external_customer_id`, `billing_external_subscription_id`, `billing_synced_at`, `billing_plan` (deliberately NO CHECK — plans in motion; pricing page will sell single_user at $199/mo, team undecided). `billing_status` remains canonical — future webhooks map INTO it. **Dormant desktop-era `routes/billing.js` + STRIPE_* vars left untouched — the future provider-integration stage decides replace-vs-revive.** Tests: account.test.js (name validation + payload shape; suite 47).
  3. Onboarding + landing page "front door" pass. **Landing page COMPLETE + ACCEPTED (2026-07-23):** the marketing page is done and live (green accent, product-matched dark theme, mailto CTAs). **REMAINING front-door scope stays QUEUED for this item:** (a) onboarding sequence for a first-run user, (b) forced password change on first login, (c) self-serve forgot/reset flow — (b)+(c) are the §5 permanent-credential gap. (d) PREREQUISITE for Zoom Marketplace beta-share (logged 2026-07-24): a hosted **Privacy Policy URL** and **Terms of Use URL** are REQUIRED by Zoom and NEITHER PAGE EXISTS yet — drafting + two simple hosted pages is a work item for this front-door arc (or sooner if beta-submission timing demands it). Zoom also needs a Support URL + Documentation URL (add/use/remove). Landing-page build detail: `backend/web/index.html` fully replaced — the dead teleprompter marketing is gone, new copy sells the call-grading product (hero / how-it-works / 4 features / 2 pricing cards / closing CTA). Self-contained: inlines the PRODUCT token block (dashboard theme — blue --accent, --border-strong/--bg-field) instead of the legacy green `/css/style.css`, so there's zero visual seam crossing `/` → `/login` → `/dashboard`. All CTAs are `mailto:justin@scoutsystems.io` (subjects "Scout access" / "Scout team waitlist") — no self-serve signup (matches the invite-only reality). Log-in link unchanged; the login-state nav script (retargeted) sends logged-in visitors to `/dashboard` instead of the mailto CTA. Zoom/Meet carry a muted "coming soon" tag in the connect step. Meta description + OG title/description added (no OG image — no brand asset exists yet). **Front-door pass still OWES (logged, not built): forced password change on first login + self-serve forgot/reset flow** (the §5 permanent-credential gap). **Cleanup item: `backend/web/download.html` is now ORPHANED** (dead desktop-app page, no inbound links after the landing rebuild; legacy green style.css only survives for it) — safe to delete in a future cleanup pass along with `/css/style.css` if nothing else references them. **Must include (logged 2026-07-23): forced password change on first login + a self-serve forgot/reset flow.** Today NOTHING forces a change and no reset UI exists anywhere — welcome emails therefore carry a PERMANENT credential (the §5 recon gap). The welcome email already says "please change your password after your first login"; this item makes that sentence true.
  4. Slack connect for EOD auto-post.
  5. **Zoom as a recording source — IN PROGRESS (Google Meet ruled OUT of scope: free @gmail can't record, restricted Drive scopes → annual CASA, no timestamp deep-link).** Zoom-only, clips stay first-class via download→ffmpeg-cut→discard-source→store-clips. 4 sub-stages: (1) schema + OAuth [sub-stage 1 BUILT 2026-07-23, local], (2) sync + VTT adapter → grades end-to-end, (3) clip extraction + inline player, (4) webhook + polling + connect UI. See the Zoom section under Recording Source Architecture.
  Hardening pass list stays as logged: shared constants module, payment-label extraction riding the next grader bump. (Manual-outcome-override protection is DONE — see the entry above.)

- **Call Review Context — Part 1a (data) BUILT 2026-07-31 (LOCAL, unpushed; Justin's ruling: section-tag in-extractor, backfill last 30 days, inline two-group UI).** Spec: clicking a section score on the Call Review page breaks it into that section's own good/bad highlights (Part 1b UI; the Add-to-KB button inside is Part 2, not built). Part 1a ships the data layer: (1) the highlight extractor now emits a **`section`** per moment (intro/discovery/pitch/objection/close) — riding the EXISTING extractor call, no extra Claude call; (2) **migration 028** adds `call_highlights.section` text NULLABLE + CHECK (NULL or the five sections) — nullable because old rows / pre-backfill calls legitimately have none; (3) `lib/highlight-section.js` holds `VALID_HIGHLIGHT_SECTIONS` + `sanitizeSectionValue` (used by `sanitizeHighlights`, coerces bad/missing → null, never drops a moment) + `highlightGroup(h)` → 'good'|'bad' (the two-bucket split: strong/rapport/buying + handled-objection = good; missed/disqualify + partial/unhandled/unknown-objection = bad) — TDD'd in `highlight-section.test.js` (8). (4) **`ANALYSIS_PROMPT_VERSION` v9→v10-2026-07-31** (additive extractor field; grader scoring/outcome prompts UNCHANGED, so no delta-gate). Highlights are DELETE+INSERT on re-analysis, so backfilled calls get fresh section-tagged rows. **BACKFILL PLAN (reviewed step, NOT auto): last 30 days only** via `POST /fathom/update-analyses` `scope:'30d'` — est. ~134 calls ≈ ~268 Claude calls (grader+extractor; extractor-only re-run isn't possible); exact count from a `dry_run:true` after deploy. Older calls stay on v9 with no section tags → Part-1b UI falls back to the section-notes prose (graceful, required). Manual outcomes survive (guard above; 0 exist anyway). **Sequence when approved: apply migration 028 → push → deploy-verify → backfill dry-run (report count) → confirm → execute. Part 1b (UI) is separate, after 1a approval.** **Also fixed here (data-safety, applies to ALL analyses Fathom+Zoom): highlight persistence was DELETE-then-INSERT (a failed/empty extract wiped a call's highlights). Now `persistHighlights` (analysis-worker.js, TDD'd in `persist-highlights.test.js`, 5) inserts the new set FIRST, then deletes exactly the prior rows — a failed/empty extract or a failed insert keeps the existing highlights untouched (critical for a 134-call backfill where a mid-run hiccup is realistic). If the final old-row cleanup fails, the new set is already saved and dupes self-heal next run — never a wipe.**
  - **BACKFILL DROPPED (Justin's ruling 2026-08-01) — not worth further time.** The 30-day retroactive re-grade stalled repeatedly at ~38/101 and was abandoned. **Rationale (accepted):** section tags land automatically on ALL new analyses at **zero marginal cost** (the extractor already emits `section` — proven on real data: on the 37 that did complete, all five values present, close 130 / discovery 41 / pitch 33 / objection 23 / intro 14, 36/37 calls tagged), and **untagged older calls fall back cleanly** to the section-notes prose in the Part-1b UI. The backfill was retroactive polish only. So older calls stay on v9/v8/v5 with no section tags — that's fine, by design. (Snapshot of pre-run per-call outcomes preserved at `~/Desktop/scout-backfill-snapshot-2026-07-31.csv` if a shift report is ever wanted; not pursued.)
  - **Why the backfill kept stalling — TWO causes, both LOGGED (2026-08-01, report-only, not fixed):** (1) **Deploys during the drain** killed the fire-and-forget analysis loop (see the standing "never push during a drain" rule) — a Part-1b API-fix push redeployed Railway mid-drain. (2) **UI mismatch — what shipped ≠ what was specified.** SPEC: a 7d/30d/all-time **scope dropdown with a dry-run count + confirm** as THE Update-analyses control. SHIPPED: that dropdown DOES exist (`updateOutdatedAnalysesNow` → `/fathom/update-analyses` scope, dashboard.html ~L2993) — but it renders **only when `outdated_count > 0`**. When `outdated===0 && pending>0`, the control that renders instead is **"⟳ Analyze next 10"** (`reanalyzeFathomNow` → `/fathom/reanalyze`, **newest 10, `limit:10`, NO scope, NO dry-run**, dashboard.html ~L2987). Because a scope run resets its batch to `status='pending'` (dropping them out of `outdated_count`), the visible control flips to "Analyze next 10" mid-backfill — which only ever re-analyzes the newest 10 pending per click and never sweeps the older pending tail. That's the mismatch that made completing the backfill by clicking impractical. **Not fixed — logged for a future UI pass** (e.g. always expose the scope dropdown whenever `outdated+pending>0`, or make "Analyze next N" honor a scope). **Stuck-row cleanup done 2026-08-01:** 2 rows were stranded at `status='processing'` from the dead drains → reset to `pending` (both `call_analyses` + `fathom_calls`); 0 remain stuck.

- **Call Review Context — Part 1b (UI) BUILT 2026-07-31 (LOCAL, unpushed).** Clicking a section grade card now expands into that section's highlights split into **"What worked"** / **"What to fix"** (dashboard.html `gradeCardHtml`, using `sectionBreakdown` + `highlightGroup` mirrored inline from `lib/highlight-section.js`). Each row REUSES the existing `highlightEntryHtml` (timestamp `?t=` jump — Fathom seeks, Zoom opens without seeking — speaker, quote, observation). The section **notes are kept, demoted** to a one-line summary above the groups. **GRACEFUL FALLBACK (the common case — only backfilled calls have tags):** a section with no tagged highlights shows today's notes prose cleanly (no empty state, no group headers); a section with tags but one empty group renders only the non-empty group (never an empty column). **Part 2's "Add to KB" button is NOT built** — each highlight row is the natural per-item slot for it. TDD: `sectionBreakdown` branch/group logic (`highlight-section.test.js`, 12), the inline-mirror-can't-drift guard (`section-breakdown-mirror.test.js`, 2), and the render assembly per branch (`grade-card-render.test.js`, 4). Suite 198. Live pixel render on a real tagged vs untagged review page is the post-deploy exercise step (needs a Josh-authenticated review page). **SHIPPED + live-verified 2026-07-31 (`9962bfc` + fix `838b192`):** verified on prod (throwaway user + seeded tagged/untagged calls) — tagged section shows both groups with working `?t=` links, untagged falls back to notes prose, collapsed unchanged. The live verify CAUGHT a gap: `GET /fathom/calls/:id` didn't SELECT `section`/`resolution`, so the client never got the tags — fixed + guarded (`review-highlights-select.test.js`). Backfill STALLED at 38/101 (fire-and-forget drain died — a push during the drain redeploys Railway and kills it; complete it with `update-analyses scope=30d` AFTER all pushes are done).

- **Coaching glance tiles — handle-rate + avg-score trend BUILT 2026-08-01 (LOCAL, unpushed; push AFTER the backfill completes — independent of it).** (1) The **"Objection moments" count tile → "Objection handle rate" %** over TRUE objections only (logistical + disqualifications excluded). **Recon ruling: the "true objection" split is the What-needs-work card's LLM bucket classification (`lib/team-needs-work.js`), NOT the stored `objection_category`** — so to make the tile and the card agree, the tile REUSES `state.needsWork` (already loaded on overview boot, cached, `loadNeedsWork()`), and **triggers NO LLM of its own** (boot-fetch trim intact). `objectionHandleRate(needsWork)` sums `detail.buckets` handled/total; **degrades to "—"** when the classification isn't available (loading/error) or there are zero true objections — never falls back to counting all moments. Subtext shows raw counts ("12 of 31 handled"); still drills to `goObjections()`. (2) **Avg-score tile gains a period-over-period trend** (green ▲ / red ▼ + delta %) vs the immediately-preceding equal window — **reuses the team view's prior-window machinery** (`team-analytics._aggregateWindow` over `[from - span, from]`), attached as `avg_score.prior_mean` in `computeCallAnalytics` (so `/me/analytics2` + the `/admin` pivot both get it). No prior data → **no arrow** (not a misleading 0%); prior ≤ 0 → division-guarded. Uses the existing `trendArrow` styling. TDD: `tile-metrics.test.js` (11 — handle-rate incl. div-by-zero/no-data, trend incl. no-prior/div-by-zero) + `tile-metrics-mirror.test.js` (2 — the inline dashboard copies can't drift from `lib/tile-metrics.js`). Suite 212. Live pixel verify is the post-deploy step.

- **KNOWLEDGE BASE Part 2 — five rulings set 2026-08-03; sub-stage 2a (team-scope ownership + promotion) BUILT, migration 029 APPLIED to prod, LOCAL/unpushed.**
  - **RULINGS (standing).** (1) **Harvested material NEVER reaches the grader in v1** — coaching/review surfaces only. If it ever does, it goes in a SEPARATE labeled block framed *"examples of this closer's past execution — evidence of capability, NOT the standard to grade against"*, **never inside SELLING CONTEXT** (whose prompt says *"do not penalize approaches this material explicitly endorses"* — correct for an offer doc, rubric-capture for harvested call moments). **The exclusion is enforced by TWO independent filters in `lib/selling-context.js`, and this is deliberate, not incidental: (a) `fetchSellingContext` filters `category='user_upload'`, which `learned_pattern` rows are not; (b) `GRADER_CATEGORIES = ['script','offer_document','objection_framework','case_study']` excludes `learned_pattern` (it isn't in `SYNTHESIS_CATEGORIES` either). Harvested entries therefore MUST be written with the `category` COLUMN = `'learned_pattern'` — that single choice is what keeps them out of grading. Do not "fix" either filter without re-opening ruling 1.** (2) The existing exclusion of a **managed rep's personal KB from their own grading STAYS**; offer/script governance is the manager's — ruling 1 means no conflict in v1. (3) **Per-section caps ~2/section/call** on the harvest so `close` can't swamp discovery+objection; **do NOT exclude close** — the extractor flags on merit, independent of the synthetic close score. (4) Auto-population gates on **`outcome='closed'` ALONE, NOT `cash_collected > 0`** — the grader records cash by payment structure, so a **payment-plan close with nothing due at signing legitimately shows zero cash**; that filter would drop real wins. Manual outcome tagging makes the closed tag trustworthy on its own. (5) A **MANAGER** clicking the Add-to-KB button on a rep's call **promotes into the TEAM KB — the button IS the promotion path, no separate curation queue**; a rep/owner clicking on their own call writes to their own KB; **auto-extraction always goes to the rep's OWN KB**.
  - **2a BUILT.** **Migration 029** adds `knowledge_base.team_owner_id uuid` + partial index + recreated `match_knowledge` (signature/returns unchanged). **Why:** migration 006 defined a team row as `scope='team' AND uploaded_by = manager`, conflating **provenance** with **visibility** — so promoting a REP's entry was impossible (flipping `scope` left `uploaded_by` on the rep → matched nobody; the only workaround rewrote `uploaded_by`, destroying "this came from Ava's Mar-14 call"). Now `uploaded_by` = provenance (never rewritten), `team_owner_id` = team key. `COALESCE(team_owner_id, uploaded_by)` keeps every pre-029 row's exact old meaning → non-breaking, backfill was a no-op (0 team rows existed).
  - **Four duplicated scope predicates collapsed into ONE** (`lib/kb-scope.js`): the RPC, `lib/kb-visibility.js` (now a re-export shim, old body commented in place), the inline filter in `routes/kb.js /kb/search`, and the team branch of `lib/selling-context.js` (now keys on `TEAM_KEY_COLUMN`). **The SQL copy can't import JS**, so `test/kb-scope-sql-mirror.test.js` guards it two ways: **(a) textual** — migration 029 must contain `KB_VISIBILITY_SQL` verbatim; **(b) semantic** — the SQL clause is transpiled to JS and evaluated against the same fixtures as the predicate, asserting row-for-row agreement (plus a non-vacuous-pass pin). Same pattern as `section-breakdown-mirror` / `tile-metrics-mirror`.
  - **Writers must stamp the key:** `/kb/upload` sets `team_owner_id` for manager uploads (identical to pre-2a there, since manager `p_admin_id` = own id); `/kb/list`'s managed-reader `.or()` gained a `team_owner_id` branch — **without it promoted rows were filtered out of the candidate set before the predicate ever saw them**; three `select()`s gained the column.
  - **`PATCH /kb/:source_label/scope`** `{scope:'team'|'personal'}` — `manage`-gated, `resolvePromotion` exported for unit test. Never writes `uploaded_by`; demote clears the team key (leaving it set would silently republish on a later re-promote); `'global'` is deliberately unreachable (would publish a rep's material platform-wide). Dashboard: **Promote to team / Team ✓** toggle on each KB row, manager+owner only (`kbCanPromote` — an unmanaged user can manage their own KB but has no team to promote INTO).
  - **§2 cost estimate CHALLENGED AND CONFIRMED — team scope had NEVER been exercised against a single live row** (0 team rows; org is 3 owners + 5 users + **no managers**, so every upload landed `global` via owner→global). Proven end-to-end on the real team (manager `8c952cc0` + 4 reps) with a genuinely promoted row (`uploaded_by`=rep, `team_owner_id`=manager): uploader sees it **1**, teammate sees it **1**, other team **0**, teamless caller **0**, legacy-shape row still visible to team **1**, legacy cross-team **0**. **Counterfactual proven: the same row scored 0 under the pre-029 predicate and 1 under the new one.** Test rows deleted (0 remain); KB total unchanged at 770. Suite **239**.
  - **2a SHIPPED + live-verified 2026-08-03 (`18631b3` pushed).** Post-deploy exercised, not just a 200: served `/dashboard` carries `kbCanPromote`/`setKbScope`/`kb-promote-btn`, and `PATCH /kb/:label/scope` returns **401 (mounted + gated)** where it returned 404 pre-deploy. Health 200; analysis counts untouched (no drain was running — 0 rows `processing`, last analysis 2 days prior).
  - **⚠ TWO SEPARATE SURFACES — DO NOT "CONSOLIDATE" THEM (ruling, 2026-08-03).** They are different actions on different objects and both are intentional:
    - **Call-review row button** (`POST /kb/from-highlight`, 2b) — **CREATES** a new KB entry from a call moment. Manager acting on a rep's call → team KB (ruling 5).
    - **KB-list toggle** (`PATCH /kb/:source_label/scope`, 2a) — **RE-SCOPES an entry that already exists**, personal ↔ team.
    A future session will notice both say "promote to team" and try to merge them. It must not: one is a create-from-source, the other is a scope change on existing rows, and collapsing them would either lose the create path or make re-scoping impossible for uploads that never came from a call.
  - **2b BUILT 2026-08-03 (migration 030 APPLIED to prod, LOCAL/unpushed) — the manual "Add to Knowledge Base" button in the Call Review section-breakdown rows.**
    - **`POST /kb/from-highlight` takes IDS ONLY** (`{fathom_call_id, highlight_id}`) and **re-reads the highlight server-side**; client-supplied quote/observation is never trusted into a store that may one day feed grading. Mismatched call/highlight pairs are rejected (they would skew the dedupe key onto the wrong call). Gate is **`protect`, not `manage`** — ruling 5 lets a MANAGED rep add from their own call, and `requireKbAccess` deliberately 403s managed reps; authority over *other* users' calls is enforced by `getVisibleUploaderIds`.
    - **RULING 1 enforcement is the entry shape:** category COLUMN `'learned_pattern'` (filter (a): `fetchSellingContext` only takes `category='user_upload'`) + `metadata.category='call_moment'` (filter (b): absent from `GRADER_CATEGORIES` **and** `SYNTHESIS_CATEGORIES`). **Both filters are deliberate and redundant on purpose** so that "fixing" one in isolation can't silently open the loop. Pinned by a test in `kb-entry.test.js` that reads the real category lists.
    - **Migration 030** adds `source_fathom_call_id` / `source_section` (CHECK'd to the five sections) / `source_quote_hash` + a **PARTIAL unique index** `(uploaded_by, source_fathom_call_id, source_section, source_quote_hash) WHERE source_quote_hash IS NOT NULL`. **Partial is load-bearing:** all 770 pre-existing rows have a NULL hash and a full index would have collapsed them into one conflicting bucket. `uploaded_by` (the KB owner) is the first key component, so a rep's personal copy and the team copy legitimately coexist while a second add into the SAME KB is refused — **this is what makes the button idempotent with 2d's auto-population by construction** (2d writes with `uploaded_by`=the rep; a rep re-adding the same moment produces an identical key). A `23505` is returned as `{ok:true, duplicate:true}`, not an error.
    - **NOT the highlight id, and this is the whole point:** `persistHighlights` is insert-new-then-delete-old, so **every re-analysis reissues fresh `call_highlights.id`s for the same moments**; an id-keyed index would review as correct and silently readmit duplicates on the next re-grade. **KNOWN LIMIT stated honestly:** the content hash survives id reissue but NOT the model *re-wording* a quote on re-extraction — `normalizeQuote` absorbs case/whitespace/wrapping-quote/trailing-punctuation drift, but a genuinely reworded quote is a new entry. Semantic near-duplicate detection would need embedding-similarity dedupe; out of scope for 2b.
    - **`GET /kb/saved-moments/:fathom_call_id`** returns already-saved highlight IDs so rows render as **In KB ✓**. Hashing stays server-side deliberately — returning hashes for the browser to recompute would have meant a **fifth mirrored copy of `normalizeQuote`**, the exact duplication 2a spent its budget retiring. Degrades to "everything unsaved" on any failure, which is safe because re-adding is idempotent.
    - **`routes/fathom.js` `/calls/:id` highlights select gained `id`** — without it the client has nothing to send and every button is inert. Same failure class as the Part-1b missing-`section` bug; pinned by a new assertion in `review-highlights-select.test.js`.
    - **`lib/kb-entry.js`** holds the pure pieces (`normalizeQuote`, `quoteHash`, `buildEntryContent`, `resolveEntryTarget`) — 2d reuses them verbatim. `quoteHash('')` returns **null**, never a hash of empty string: a blank hash would bucket every quote-less moment together and swallow legitimate adds.
    - **Live-verified** against the real DB: duplicate **blocked**, same moment into a different KB **allowed**, same quote in a different section **allowed**, two NULL-hash rows **allowed**. Test rows deleted; KB total unchanged at 770. Suite **256**.
  - **NO cap on the MANUAL button (ruling, 2026-08-03) — the ~2/section cap is a 2d concern ONLY.** Ruling 3's cap exists because **auto-population is indiscriminate**; a human clicking each individual moment has already made that judgment call, and capping the manual path would block a genuinely exceptional call from being fully captured. Do not "unify" the two paths under one counter.
  - **2c SETTLED 2026-08-03 (decision block — three rulings, one guard built).**
    - **(i) kbHash guard — BUILT.** `test/kb-hash-guard.test.js`. The cache-invalidation cost was previously avoided only as a **side effect** of ruling 1's category choices, and side effects don't survive future edits. Two layers: **structural** (`call_moment` absent from `GRADER_CATEGORIES` **and** `SYNTHESIS_CATEGORIES`; harvested rows' category COLUMN ≠ `user_upload`) and **behavioural** (drives the REAL `fetchSellingContext` against a fake Supabase holding a harvested row beside a genuine offer doc, asserting the harvested row lands in neither `contextText` **nor `kbHash`** — including at 25-row scale). Failure messages name ruling 1 and state the consequence. **Guard proven to actually fire** by deliberately breaking each filter: breaking either ALONE trips its structural test while the behavioural ones stay green (**the two filters are genuinely independent — one break does not open the loop**); breaking BOTH trips all five. **Why kbHash specifically matters: it invalidates `objection_synthesis_cache`, so if harvested rows ever entered the included set, every closed call would force a paid synthesis regeneration.**
    - **(ii) Write path — 2d calls `lib/kb-entry.js` DIRECTLY, no internal HTTP hop.** An HTTP call inside the analysis worker would add auth, network, and failure modes to a pipeline already fragile enough to die on a redeploy. `POST /kb/from-highlight` is now a **thin wrapper** over the same `buildMomentRow` + `insertMoment` — **ONE insert implementation, two callers.** Also extracted `getVoyageEmbedding` → **`lib/voyage.js`** (the worker needs embeddings; a second copy in the worker would have been new duplication — old body commented in place in `routes/kb.js`).
    - **(iii) Surfacing — DELIBERATELY NOT BUILT, and this is NOT an oversight.** Harvested moments currently land in the KB list under "My call moments" with an expand panel — **storage, not coaching**. Surfacing gets built **together with the queued section-drilldown** (clicking a Coach-summary section bar → aggregate breakdown for that section across the period), where *"Discovery is your weakest section — here are your best discovery moments"* is a clean section-level match needing **no new tagging**. **Do not bolt on a separate surface before then** — it would be thrown away when the drilldown lands.
  - **2d BUILT 2026-08-03 (LOCAL/unpushed) — auto-population of closed calls into the rep's OWN KB.** `lib/kb-harvest.js`, wired into `analysis-worker.js` as **Phase 7b**, immediately after `persistHighlights`.
    - **Fire-and-forget, NOT awaited** — the drain already dies on a Railway redeploy (stranded batches twice); adding ~2–10 sequential Voyage round-trips inline would lengthen the window where a deploy kills a call mid-analysis. **Zero new Claude calls** — it files moments the extractor already produced.
    - **Gate = `shouldHarvest(effectiveOutcome)`, i.e. `outcome === 'closed'` ALONE (ruling 4).** Uses the **manual-override-aware** `effectiveOutcome`, so a human-tagged outcome drives it exactly as it drives `close_score`/`payment_structure`. **NOT `cash_collected > 0`:** the grader records cash by payment structure (v8: `payment_plan` = only what was collected ON the call), so **a plan close with nothing due at signing legitimately shows zero** and that filter would systematically drop plan closes. `shouldHarvest` takes only the outcome so cash cannot leak in via a later edit (arity is test-pinned).
    - **Filter + cap (ruling 3):** good-group (`highlightGroup`) + section-tagged + non-blank quote, **capped at `HARVEST_SECTION_CAP = 2` per SECTION per call**. Live data motivating it: on section-tagged closed calls the good-group split is close 62 / pitch 14 / discovery 11 / objection 6 / intro 6 — **uncapped, close would be ~63% of every harvest**, and close is the section whose score is synthetically 100. **Close is CAPPED, never EXCLUDED** — the extractor flags on merit independent of that score. **The manual button stays UNCAPPED** (see the ruling above).
    - **Writes to the REP'S OWN KB** (`scope:'personal'`, `team_owner_id:null`, `uploaded_by` = the rep) — which is precisely what makes it **idempotent with the manual button by construction**: both paths build through `buildMomentRow`, producing an identical dedupe key, so whichever runs second is a no-op. **Order does not matter.** Test-pinned by comparing the two paths' keys directly.
    - **Same entry shape as 2b** (`learned_pattern` / `call_moment`) so **ruling 1 holds automatically with nothing extra to remember**; `metadata.source = 'auto_closed_call'` distinguishes it from `'manual_add'`.
    - **Degrades silently** — `insertMoment` swallows its own errors and the worker `.catch()`es the pass. A KB write can never fail or stall an analysis (same rule `fetchSellingContext` follows). A null embedding still writes the row (keyword-searchable).
    - Suite **277**. TDD'd: the gate, the cap, and idempotency-against-a-manual-add (`test/kb-harvest.test.js`, 16).
    - **IN-SITU VERIFIED 2026-08-03 (15/16) against a REAL closed call** (`06c8d9a5…`, Josh, "AF Sober Living Riches | Amanda Law"). Phase 7b **fired**: 5 rows harvested, per-section `{discovery:2, intro:1, close:2}` — **cap held** (close capped 4→2), every row `learned_pattern` + `call_moment` + `auto_closed_call` + `scope:personal` + `uploaded_by`=the rep, dedupe keys populated, **the analysis itself returned `{status:'done', overall_score:70, highlights_count:6}` undisturbed** by the non-awaited call, score unchanged 70→70, outcome still `closed`, `sync_status` back to `processed`. **Rows KEPT** — genuine material from a real closed call in the correct rep's KB, not test residue. **Why in-situ mattered: Phase 7b is fire-and-forget with a swallowed catch, so a broken 2d is INVISIBLE — no error, no log, no symptom, just an empty KB nobody questions for weeks. A silent-by-design path must be observed firing at least once.**
    - **VOYAGE 429 DEFECT — FIXED 2026-08-03 by BATCHING (local/unpushed).** `lib/voyage.js` gains **`getVoyageEmbeddings(texts)`** — ONE request for a whole call's moments, replacing N sequential singles; `harvestClosedCall` builds every row first, then embeds in a single batch. **Batching, not delays:** a delay would only slow the pass, still rate-limit at scale, and — the decisive point — **would not remove the systematic bias**, since `selectHarvestMoments` preserves chronological order so the dropouts are always the late-call sections. **Contract (test-pinned): output length ALWAYS equals input length; mapping uses the response's `index` field, NEVER array order** (trusting order would silently attach the wrong vector to the wrong moment — a corruption invisible until search results stopped making sense); partial failure degrades per-item to null, total failure to all-nulls, never throws, never returns a short array. **A batch error can never lose the harvest** — every row still writes, just unembedded and keyword-searchable. `summary.unembedded` is now logged per call. Tests: `voyage-batch.test.js` (11: single-request, index-mapping, missing/malformed/out-of-range indices, 429, network throw, malformed body, no-key, degenerate input) + 3 harvest-level wiring tests driving `harvestClosedCall` with a stubbed fetch (one-request, partial-failure, total-429). Suite **291**. **The 2 pre-fix null-embedding rows were BACKFILLED via the new batch path (both `close` moments — confirming the predicted bias); 5/5 harvested rows now embedded, 0 remaining.** **QUEUED (deliberately NOT folded into this stage): `/kb/upload` still embeds chunk-by-chunk sequentially** — a 20-chunk document is 20 single requests, exposed to the identical 429. `getVoyageEmbeddings` drops straight in.
      - **Why it is LOW URGENCY today:** embeddings are **not on the grading path at all**. `fetchSellingContext` is a bulk SQL dump filtered by category + scope under a 5000-char cap — **it never reads `embedding` and never calls `match_knowledge`**. So a chunk that failed to embed still reaches the grader normally. The ONLY thing that degrades is the **`match_knowledge` / `POST /kb/search` similarity path, which today serves the DORMANT desktop teleprompter** — i.e. nothing a live user touches.
      - **⚠ THE TRIGGER THAT MAKES IT LOAD-BEARING — the surfacing work using similarity search.** The moment the queued **section-drilldown** surfacing (see 2c(iii)) retrieves harvested moments or uploaded material by embedding similarity rather than by section/scope filters, an unembedded chunk becomes **permanently invisible to the feature that exists to find it** — silently, with no error. **Fix `/kb/upload` BEFORE that retrieval ships, not after.** This is a stated precondition, not a nice-to-have: the failure mode is invisible-by-design, exactly like the Phase 7b one that required an in-situ run to catch.
    - **⚠ ORIGINAL DEFECT REPORT (kept for context) — VOYAGE 429s under the harvest loop.** `harvestClosedCall` fires N **sequential** Voyage embeds in a tight loop. On the 5-moment run, **3 succeeded then #4 and #5 returned HTTP 429** → those rows stored with `embedding: null`. The degrade path worked as designed (row still written, still keyword-searchable), **but the bias is not random: `selectHarvestMoments` preserves chronological order, so the LATE-CALL sections — `close` above all — are systematically the ones left unembedded**, and those are exactly the moments a "how do I close" semantic search should surface. Live state: 5 harvested rows, **2 without embeddings**. Resolved by the batching fix above.
    - **STILL UNPROVEN INDEPENDENTLY: that the DEPLOYED Railway container runs Phase 7b.** The in-situ run invoked `analyzeCall` directly against prod (same committed code, clean tree) rather than over HTTP, because the only closed section-tagged calls belong to `josh@scoutsystems.io` and `/fathom/reanalyze` is `requireAuth` on `req.user.id` with no admin variant — an HTTP trigger would have meant minting a login session for another person's account. **That was the right call and should stay the norm.** The container-side proof is **the first genuinely new closed call**, which costs nothing to wait for — do not manufacture it. 2d gates on `outcome='closed'` alone (ruling 4) with ~2-per-section caps (ruling 3), writes to the rep's OWN KB, and reuses `lib/kb-entry.js` unchanged.

#### Roadmap update — Team & Account layer (design notes, set 2026-07-21)
These are **design notes, not specs** — a full design session happens per-feature before build. This re-sequences the numbering below: **v1.4.0 is now the Team & Account layer**; CRM integration (previously v1.4.0/v1.4.5) and Slack connect shift to **v1.5.0**.

**v1.4.0 — Team & Account layer:**
- **EOD Report (PULL FORWARD — build FIRST).** Per-call, editable report: prospect name, outcome, Fathom link, **cash collected**, brief summary. *Cash collected is NEW: the grader extracts the payment amount from the transcript ($0 default when none stated)* — needs a `call_analyses` column (e.g. `cash_collected numeric default 0`) + a prompt field + a bump. Output in a **copy-paste format for Slack**; the **one-click Slack send lands in v1.5**. Editable = the closer can correct fields before copying/sending.
- **Manager view** (for a role that has assigned reps — reuses the existing `user_profiles.managed_by`): team + individual stats; trends (weekly / monthly / quarterly); team-level recommendations; **"Call Highlights of the Week"** (best objection handling / challenger moments / pain excavation — clips + short summary, **reuses the synthesis + highlight infrastructure** already built for Performance Summary / Objections); a **daily manager digest** (in-app panel first; email/Slack later).
- **Admin (owner) console:** list all users with **email, role (user / manager / admin), billing status** — all editable and persisted.
- **User account page:** login info, billing, subscription, offer info. **Self-serve ONLY when the user is NOT assigned to a manager;** when managed, it's **locked (view-only) with a "managed by X" notice**.
- **Role naming decision (document + implement mapping):** today's `admin` role **with** `managed_by` assignments = **"manager"**; `owner` = **platform admin**. So the surfaced role set is **user / manager / admin(=owner)**, mapped onto the existing `role` + `managed_by` columns (no new enum unless a design session says otherwise).

**v1.5.0 (existing items stay):** CRM integration (GHL + Close.io — read then act, diff-then-confirm on writes); Slack connect (EOD one-click send + manager digests).

#### Revised roadmap (next milestones, in priority order — set 2026-07-18)
- **v1.3.1 — Dashboard rewire + UX fixes** (NEXT):
  1. **Coaching Dashboard becomes the default landing view for ALL roles** including admin/owner (the admin panel stays one click away, no longer the landing page for admins/owners).
  2. **Rewire the coaching dashboard data source** from the desktop-teleprompter era to the Fathom era. Donuts + Coach summary currently aggregate `call_sessions`; they must aggregate `call_analyses` + `call_highlights`:
     - Calls donut = analyzed calls count (`call_analyses`)
     - Objections donut = `call_highlights WHERE type='objection'`
     - Close rate = from analysis outcomes
     - Coach summary = aggregated from section grades + `one_thing` patterns
     - Drill-downs link to call review pages
  3. **Persistent login sessions (website + desktop):** extend Supabase session TTL + refresh-token persistence so users stay logged in indefinitely (already flagged high priority — see Future Plans).
- **v1.4.0 — CRM Integration Phase A (read):**
  - "Connect CRM" button: GHL and Close.io via OAuth popup (same pattern as Fathom)
  - Read + display: contacts, pipeline stages, deal values alongside call data
  - Match analyzed calls to CRM contacts
- **v1.4.5 — CRM Integration Phase B (act):**
  - Scout as assistant: send SMS/email follow-ups (using the generated follow-up drafts), add notes to contacts, update pipeline stages
  - **All write actions require user confirmation before executing** (diff-then-confirm — see CRM Integration Notes below)
- **v1.5.0 — Notifications & reporting:**
  - "Connect Slack" button — Scout posts EOD reports to a channel
  - EOD report generation (per the existing Phase 5 spec)
  - BOD prioritization + overnight recap (Layer 3 proactive intelligence)
- **Carried forward — slot under the milestones above where they fit:**
  - **Objection intelligence dashboard** — mostly SHIPPED as the Objections view (see "Shipped" above). Remaining: the grounded ISOLATE→REFRAME→OVERCOME synthesis and the re-analysis that populates categories/resolution — both credit-gated.
  - **Zoom / Google Meet as additional recording sources** — the pipeline is already source-agnostic (see Recording Source Architecture); do after CRM.
  - **Teleprompter section-mode** (prompt only during objection/close sections) — see Teleprompter (Future Architecture Note); unscheduled, architecture preserved.

### Fathom API Constraints (learned in production)
Hard-won during the v1.3.0 rollout against Josh's real workspace. The
fathom-typescript SDK (v0.0.41) is the source of truth for the API surface.
- **OAuth apps cannot use `include_transcript=true` / `include_summary=true` on `/meetings`** — the SDK documents them "Unavailable for OAuth connected apps" and they return no transcript (HTTP 400 territory). Fetch the transcript separately via **`GET /recordings/{recording_id}/transcript`** instead.
- **Transcript shape:** `/recordings/{id}/transcript` returns `{ transcript: [...] }`; each turn has `speaker.display_name`, `text`, and an `HH:MM:SS` `timestamp` string (the normalizer converts to numeric seconds).
- **No identity endpoint — `/me` does not exist.** The entire API is 8 endpoints: `/meetings`, `/meeting_types`, `/recordings/{id}/summary`, `/recordings/{id}/transcript`, `/team_members`, `/teams`, `/webhooks`, `/webhooks/{id}`. The OAuth grant uses `scope=public_api` (not OIDC), so the token response carries no identity either.
- **`recorded_by` identity ≠ login email.** Fathom stamps recordings with a workspace-assigned recorder email that can differ entirely from the user's login. Josh logs in as `josh@scoutsystems.io` but his recordings are `recorded_by = joshua@soberlivingriches.com`. The `fathom_connections.fathom_email` column (migration 011) holds the **`recorded_by` value**, confirmed by the user via the identity-options picker — which lists distinct `recorded_by` emails pulled from one unfiltered page of real meetings so the user *selects* theirs. **Never guess the login email** (it cost several wrong-value cycles here).
- **`recorded_by[]=<email>` is a server-side owner filter** (literal brackets, one per email, exact-match on email). WITHOUT it, `/meetings` returns the ENTIRE team workspace's recordings — there is no implicit per-user scoping on an OAuth token. Note: `recorded_by=` WITHOUT brackets is silently ignored by Fathom (returns the default unfiltered page), so the brackets are load-bearing.
- **`created_after` with an epoch/1970 value returns zero** regardless of other filters. OMIT the param entirely when `last_sync_at` is NULL (first sync / post-reset) — never default to `new Date(0)`. `_fetchMeetingsPage` already appends `created_after` only when truthy.
- **Single-use refresh tokens rotate on every refresh** — each refresh returns a NEW refresh_token that MUST be persisted immediately in the same UPDATE, or the connection bricks on the next refresh.
- **Errored calls are recoverable:** reset `fathom_calls.sync_status='pending'` (+ `call_analyses.status='pending'`) and re-run via **`POST /fathom/reanalyze`** (batch-limited: default 10, max 50, most-recent first). It does NOT re-sync from Fathom — it re-analyzes already-stored rows.

### Session Learnings (Fathom analysis rollout, 2026-07)
- **Sequential analysis is ~30–60s per call.** The post-sync analysis loop is fire-and-forget and **does not survive Railway restarts/redeploys** — an interrupted drain leaves rows stuck at `status='processing'` that need a manual reset back to `'pending'`. Keep batches bounded (that's what the reanalyze `limit` is for).
- **A prompt change MUST ship with its version-constant bump in the SAME commit.** If `ANALYSIS_PROMPT_VERSION` lags the prompt content it stamps, every downstream system lies *coherently*: the DB stamp, the outdated-count, and the "Update analyses (N)" button all agree with each other and all agree with the wrong answer, so nothing looks broken. Rule: editing `buildSectionGraderPrompt` (or any dispatched prompt) and bumping `ANALYSIS_PROMPT_VERSION` are one atomic change. When verifying a bump, probe the *content that will run*, not just the constant (read the deployed grader prompt / confirm a re-graded call carries the new fields), and confirm the count query returns the expected N against live data.
- **Supabase `.or()` / `.neq()` null-logic must be verified against live data (2nd silent-null bug in a week).** In Postgres, `.neq('prompt_version','v4')` is NULL (excluded) for NULL rows, so a naive `.neq` count silently drops pre-versioning rows; `.or('prompt_version.neq.v4,prompt_version.is.null')` is the correct-but-fragile form. `outdatedCallIds()` deliberately SIDESTEPS this — it fetches `status='done'` rows and filters in JS (`prompt_version !== currentVersion`, where JS `!==` treats NULL as outdated), provably correct with no `.or()`. If the count ever moves back into a SQL filter, verify it against a dataset containing NULL prompt_versions first.
- **Deploy-race — SECOND occurrence (2026-07-20).** A re-analysis batch dispatched during a redeploy runs against the *outgoing* server, so calls get graded under the OLD prompt version (or the loop dies mid-drain). It's insidious because the rows come back `status='done'` and look fine — only the `prompt_version` stamp reveals they're stale. **Working as designed: the `prompt_version` column made it detectable** (a "v3 done" row after a v4 deploy is unambiguous). **Mitigation for future prompt bumps — deploy FIRST, verify (`/health` 200 + a version probe such as `/fathom/status`.`prompt_version` or `/dashboard.html` carrying the new marker), THEN dispatch batches.** Never `git push` while a reanalyze/update-analyses drain is running. Corollary bug fixed same day: the "Update analyses (N outdated)" count must be defined on `call_analyses.status='done'` + `prompt_version != CURRENT` ALONE — an earlier version joined `fathom_calls.sync_status='processed'`, which under-counted because a deploy-race can leave a done+old analysis on a call whose `sync_status` drifted to `pending`/`error`, letting it escape the count (held calls are excluded for free since they carry `status='synced_unanalyzed'`, not `done`).
- **Diagnostic principle: replay the pipeline stage-by-stage with counts** (`fetched → mapped → upserted`) before assuming where data is lost. In the "sync returns zero" investigation, replaying the exact fetch with Josh's token showed `FETCHED=0` at stage 1 (wrong `recorded_by` email) vs `FETCHED=200 / MAPPED=200` for the correct email — proving there was no downstream "leak" and the only variable was the identity value. Instrument the failing stage; don't guess it.
- **Duplicate highlights root cause (2026-07-22) was CONCURRENT dispatch, not blind insert.** The Jeremiah Thompson Jul 14 call showed 15 highlights (two full sets) — but analyzeCall's delete-before-insert had existed since 2026-06-24, and only 1 of 62 highlight-bearing calls was affected. The tell: the two insert batches' `created_at` were **1 second apart** — two overlapping analyzeCall runs (the three fire-and-forget dispatch loops in routes/fathom.js share no lock), not serial re-analysis stacking (which delete-first already handles; that would also have hit every re-analyzed call). Diagnostic rule: group suspect rows by `created_at` batch before blaming the persist path — batch spacing distinguishes concurrency races (seconds apart) from serial stacking (dates apart).
- **Fix: atomic per-call claim in analyzeCall (`claimAnalysisRun`).** Replaces the blind `'processing'` upsert. One conditional UPDATE claims the row unless another run holds a *fresh* claim (row-level atomicity arbitrates concurrent updates); when no `call_analyses` row exists, an INSERT claims it and the `fathom_call_id` unique index makes the loser fail cleanly. `analyzed_at` doubles as the claim timestamp while processing. Staleness window `CLAIM_STALE_MS` = **10 min**, sized from measured drain data (per-call p50 50s / p95 64s / max 247s) — a live run is never preempted, and a crashed run (redeploy mid-drain) self-heals after the window instead of needing the manual `'processing'`→`'pending'` reset. A run that loses the claim returns `{status:'skipped', reason:'claim_held_by_another_run'}` — logged, no status change, no Claude spend (this also halves worst-case double-spend that a DB unique constraint alone would not have prevented). The `.or()` filter explicitly includes `status.is.null` and `analyzed_at.is.null` per the silent-null lesson above.
- **Backend now has a real test harness:** `npm test` in `backend/` runs `node --test 'test/*.test.js'` (Node's built-in runner, no new dependency). First suite: `test/claim-analysis.test.js` (claim/lose/insert-claim/fail-closed matrix against a faked supabase chain). Note: `node --test test/` (bare directory arg) misbehaves under the iCloud working dir — use the quoted glob form.
- **`knowledge_base` category schema (Item A, 2026-07-22):** the top-level `category` COLUMN is the row-type discriminator (`user_upload` / `learned_pattern` / seeded-framework values) and must never be rewritten to a content category; the *content* category lives in `metadata->>'category'` (allowlist in kb.js `KB_UPLOAD_CATEGORIES`). The "Josh Winning Call Jackie Ustache" upload (20 chunks, global) had `metadata.category='offer_document'`, so `fetchSellingContext` was injecting a raw call transcript into the grader's SELLING CONTEXT as offer material — fixed data-side (`metadata.category`→`'winning_call'`, column untouched); kbHash shifted so synthesis caches invalidate naturally. Platform scan found no other mislabeled winning-call uploads.
- **one_thing double-render on follow_up reviews (Item D, 2026-07-22)** — grader v5 (d87c97c) added follow_up/pending to the verdict section's one_thing pairing ("The move"), but the highlights-footer suppression still keyed on `kind === 'loss'` alone, so follow_up reviews rendered the identical one_thing twice. Fixed by extracting `verdictKindShowsOneThing(kind)` (dashboard.html) as the single predicate both the verdict pairing and the footer suppression key on — they can no longer drift. Win-class unchanged (footer only). Guarded by `backend/test/review-one-thing.test.js`, which extracts the predicate from the HTML and asserts both call sites use it (inline browser script — no module exports — so the test is textual-extraction by design).
- **follow_up_email greeting names are Claude's unguided inference (Item C, 2026-07-22).** The grader prompt never states the prospect's name and never includes the meeting title, so the greeting comes from names spoken in the transcript. Verified: Tasha Presberry's call greeting "Jamie" is CORRECT — the attendee self-identifies as "Jamie Ellis"; the meeting *title* is the misleading artifact (booked-name ≠ attendee). Spot-checks: transcript-grounded names correct (Rochelle, Dana); with no name signal the model emits junk ("Hey First" on the Fathom Demo call). Proposed prompt contract (NOT yet implemented — batch with the next ANALYSIS_PROMPT_VERSION bump): *greet with the name the prospect is actually called in the transcript; if unclear, no name*. Do NOT switch to meeting-title names — that would have greeted the absent "Tasha".

### Analysis schema & outcome/objection pipeline (migrations 012–013)
- **Migration 012:** `call_analyses.outcome` (`closed`/`follow_up`/`lost`/`no_show`), `outcome_source` (`inferred`/`manual`/`crm`), `outcome_set_at` (+ `(user_id, outcome)` index); `call_highlights.objection_category` (`fear`/`logistical`/`timing`/`partner`), `objection_handled` (bool, superseded by `resolution`).
- **Migration 013:** `call_highlights.resolution` (`handled`/`partial`/`unhandled`), `closer_response` (the closer's verbatim handling line — the grounding quote for synthesis; the Fathom clip link is derived at render time from `recording_url` + `timestamp_seconds`).
- **Outcome is folded into the section grader** — it's one field in the grader's JSON output, so the pipeline stays **two** Claude calls (grader + highlight extractor), NOT three. `outcome_source='inferred'`. **TODO when the manual-override UI lands:** the blind `call_analyses` upsert must stop clobbering a `'manual'` outcome (read-before-write or a DB guard) — noted in `analyzeCall`.
- **Coaching synthesis caching (DESIGNED, not built):** the grounded ISOLATE→REFRAME→OVERCOME synthesis is to be cached per **(user, date range, analysis-set hash)** so it regenerates only when the underlying analyzed set changes. Currently a placeholder in the Objections view (credit-gated).
- **Objections-only re-extraction is NOT possible.** The highlight extractor runs inside the full `analyzeCall` pipeline (fetch transcript → grade → extract). Any taxonomy/category change (e.g. adding a category, adjusting the money→fear rule) requires a **full re-analysis** of the call, not a cheap objection-only re-pass. Budget re-analysis accordingly (2 Claude calls/call × N calls; Josh ≈ 200).

### Grading calibration + prompt-version discipline
- **Grader is calibrated, v3 (2026-07-19).** v1/v2 graded against a perfect-call ideal and scored demoralizingly low (Josh: ~51 overall, close 53, on a closer who closes ~31% — strong for high-ticket). v3 adds to `buildSectionGraderPrompt`: (a) an **anchored rubric** (85-100 exceptional / 70-84 strong = a good working closer's typical call / 55-69 adequate / 40-54 weak / <40 failed; reserve sub-50 for genuine failures), (b) **domain context** (25-35% close rate is strong; advancing or appropriately disqualifying a prospect is a success, not a failure), (c) **ranking-integrity** instruction (anchor shifts the scale, not the discrimination — stronger sections must still outscore weaker ones). **Empirical calibration is deferred** until 50+ outcome-tagged calls exist (then anchor the bands to this closer's own win/loss score distributions rather than a fixed rubric). **Shipped result** (Josh's 20 calls, v2→v3): blended overall 51→62; win-class (closed) 65→**71**, middle (follow_up) 46→60, loss-class (lost) 25→**49** — wins land in 65-75, losses in 40-50, ranking preserved (pitch/intro top, discovery bottom, same as v2). The Coaching Dashboard **Avg Score donut carries a "Wins X · Others Y" sub-stat** (`analytics2.avg_score.win_mean`/`other_mean`) so win quality is visible instead of buried in the blend — a low blended average is then legible as composition (many non-closes), not harshness.
- **Delta gate REDEFINED (Justin's ruling, 2026-07-22).** The gate measures **overall score only — median of 3 dry-runs per sample, 3 samples** (closed/follow_up/lost), threshold ~5 points, plus a **hard stop if the outcome flips on any run**. Per-section movement is REPORTED but does not gate — measured noise floor on byte-identical prompts was up to ±14/section while overall stayed within ±3 (2026-07-22 measurement). Tooling: scratchpad delta_check.js implements exactly this.
- **Validation methodology — 3-sample delta check before any batch re-analysis.** When changing the grader prompt, dry-run the new prompt against 3 representative calls (one win-class, one middle/follow_up, one loss-class) using their stored `transcript_stored`, and compare new vs old section+overall scores WITHOUT persisting. Acceptance: overall lift into sane ranges, win-class visibly above loss-class, section ranking preserved. Only then bump the version and re-analyze.
- **prompt_version discipline.** `ANALYSIS_PROMPT_VERSION` (analysis-worker.js) is bumped on EVERY grader/extractor prompt change and stamped on every `call_analyses` row. `SELECT prompt_version, count(*) ... GROUP BY prompt_version` instantly separates current from stale analyses — the guard that makes "did the re-analysis actually run the new prompt?" a one-query answer. Any taxonomy/scale change requires a full re-analysis (the extractor + grader run together inside `analyzeCall`; there is no partial re-grade).



### ⚠ GRADER NOISE PROFILE — a standing constraint on what can be validated (measured 2026-08-10)
**Measured on ONE call, repeated runs of the SAME UNCHANGED prompt:**
- **Per-section noise floor: ±14 points.** (Already documented from the 2026-07-22 delta work; re-confirmed.)
- **Some sections are BIMODAL, not noisy-around-a-mean.** Live example — intro returned `[42,42,42,42,58]` on one prompt and `[58,58,42,58,58]` on another. Same two modes, different winner. **A median over a bimodal distribution reports a 16-point "shift" when all that changed is which mode won 3 of 5 runs.** Reading medians alone will manufacture effects that do not exist.
- **Baseline drift ~6 points BETWEEN ROUNDS on identical input.** The unchanged v11 prompt on the same call gave overall `[51,50,50,50,50]` med **50** in one round and `[57,57,53,56,53]` med **56** in the next. Tight WITHIN a round, unstable ACROSS rounds — so tight distributions are NOT evidence of a real effect.
- **THE RULE: any change whose intended effect is smaller than that CANNOT be validated by score deltas.** It needs a **directly checkable structured field** instead — have the grader answer a question you can verify by reading the transcript, rather than hoping a score moves.
- **How this was learned (3 failed attempts, do not repeat them):** trying to encode "credit any conversational route" as grader wording. Attempt 1 (one-sided "credit by ANY route") → +7 overall, read as general leniency. Attempts 2–3 (two-sided, with qualification enforcement) → targeted section moved **0** while an UNRELATED section moved +16. Three attempts, three different failure modes ⇒ the lever was wrong, not the wording. **A "+7 reproducible, not noise" claim made on tight within-round distributions was later shown to be partly baseline drift** — which is exactly the trap this constraint exists to prevent.

### SELLING CONTEXT — the orphaned onboarding material (wired 2026-08-10)
- **THE FINDING:** `user_profiles` held `offer`, `qualifications` and `script_raw`, written by the **old onboarding wizard** and the desktop script upload. `fetchSellingContext` reads `knowledge_base` ONLY, so **the grader had never seen any of it** — every grade to date was scored against generic standards while the closer's real criteria sat one table away. Josh's stored `qualifications`, verbatim: **"10k saved, not living paycheck to paycheck, 640 or above credit score"**, plus a **14,721-char** `script_raw`. (An earlier recon wrongly concluded "the criteria are not written down anywhere" — they were, in a column the grader doesn't read. The tell that should have caught it: the best closer-side moment in the corpus is *"correct me if I'm wrong, you're not living paycheck to paycheck"*, which maps word-for-word onto a stored criterion.)
- **NO PROMPT BUMP — deliberately.** `analysis-worker.js` is untouched and `ANALYSIS_PROMPT_VERSION` stays **v11**. The SELLING CONTEXT block already existed; only its INPUT changed. Bumping would mark every analysis outdated for no reason under the **new-calls-only ruling**. **History keeps its generic grades; the corpus corrects itself as new calls arrive. Do NOT re-analyse to "fix" old grades.**
- **⚠ BUDGET SPLIT — `lib/selling-budget.js` (the part that had to be right).** The old selection was a single 5,000-char pool filled breadth-first with `break outer` on the first chunk that didn't fit. Measured on Josh's real material: the offer's first chunk took **3,348 of 5,000**, the script's first chunk needed ~3,000 and couldn't fit the remaining 1,652 — so **all 14,721 script characters were dropped silently**. Uploading a script would have appeared to do nothing. Replaced with **lanes + reserves + redistribution**: priority `qualifications(600) → offer(900) → script(1500) → kb uploads(1500)`, phase 1 fills reserves in priority order, phase 2 round-robins the remainder so long lanes absorb slack from short/empty ones. Whole chunks only. Profile material is chunked at **180 words** (not the KB's 500) because a 1,500-char reserve cannot accept a 3,000-char chunk — that granularity mismatch WAS the bug.
- **Result for Josh: 0 → 4,500 chars.** `qualifications 137 (1/1) · offer 544 (1/1) · script 3,809 (4/16 chunks)`. All three criteria visible to the grader.
- **Substance guard:** `usableProfileField` requires ≥15 chars AND more than one token. The demo accounts store `offer = "Ava"/"Ben"/"Cara"` (names left by seeding) — those must never reach the grader as an offer. Verified excluded; `justinschmidtsales`' genuine 26-char offer is kept.
- **`kbHash` now covers profile material too**, so editing the offer/criteria invalidates cached syntheses exactly as a KB upload does.
- **Only Josh has substantive material** (offer 506 / quals 69 / script 14,721). `justinschmidtsales` has a 26-char offer; everyone else has nothing.
- **Expected effect, measured before landing:** across 79 calls the criteria are covered unevenly — credit 72%, savings 78%, **paycheck-to-paycheck only 15%**, all three only **14%**, none **11%**. Discovery already tracks coverage *implicitly*: 0 criteria → avg **36**, 1 → **54**, 2 → **56**, 3 → **60**. With the bar explicit, expect **discovery to move DOWN on average** (34% of calls cover ≤1 criterion) and the spread to widen. That is grades becoming MORE accurate, not worse — the old ones flattered because the bar was unknown.

### PER-CALL COACHING INTELLIGENCE — 7a/7b/7c SHIPPED, 7d BUILT (2026-08-12)
- **The pattern: ask a direct question, store CHECKABLE evidence, wire it to nothing until it has been read.** This is the `qualification_covered` shape extended, adopted after three attempts to express the same intent as grader WORDING died against the ±14 noise floor. **None of these fields touch a score**, and guard tests fail if one ever does.
- **7a (gate)** — proved v14's verbatim contract fixed the evidence leg before anything was built on it: `qualification_covered.evidence` reconstruction **17% → 89%** in an A/B, and **3/3 vs v13's 0/3** on real cron-analysed production rows.
- **7b (`lib/coaching-areas.js`)** — derives each rep's discovery AREAS from their own offer/criteria/script. **Silence is the default path: 1 of 8 live profiles derives areas, 7 are silent and cost no model call.** See the two-bars ruling below.
  - **⚠ AREA KEYS ARE JOIN KEYS AND DERIVATION IS NON-DETERMINISTIC.** The same material produced `income_goal_and_gap` then `income_goal_and_motivation`, `prior_attempts` then `previous_attempts`. Since any material edit re-derives, drifting keys would silently orphan every tagged moment — surfacing as *"no examples yet"*, not as a bug. **Re-derivations are shown the keys already in use** (reuse measured 3/6 → 6/6). Cached on `materialHash` in `objection_synthesis_cache` (`synthesis_type='coaching_areas'`) with a **fixed sentinel** for from_ts/to_ts — those are NOT NULL and sit in the unique index, so a "now" value would mean the cache never hits. An EMPTY derivation is never cached (a transient 429 would otherwise pin the silence in place).
- **7c (migration 036)** — `coverage` (per area: established on this call, by ANY conversational route, with a verbatim line) + `prospect_context` (≤3 attributes the prospect stated about themselves). **Evidence is verified at WRITE TIME by `quote-locate`, never trusted from the model.**
- **7d (migration 037, v16)** — `what_mattered`: the highest-priority UNCOVERED area, naming a structured `area_key` with the prospect's own PROVEN quote as the reason. **Never free text** — free text can't be checked, can't be filtered against the 7e example library, and reads plausibly whether or not it's right. Accepted only when the area exists for this rep, is marked uncovered, AND the reason reconstructs as the PROSPECT's words; any failure stores NULL.
- **⚠ GRADER OUTPUT TOKEN BUDGET IS NOW THE BINDING CONSTRAINT.** `GRADER_MAX_TOKENS` 4500. Measured twice on the six LONGEST calls (1645-2123 turns): worst case **3,211/4,500 (71%)**, **0 unparseable in 24 runs**, ~1,289 spare. **Re-measure on the longest calls before adding ANY further grader field** — an unparseable response fails the ENTIRE analysis, and this cap was already raised once (3000→4500) for exactly that. **Beware the naive marginal-cost number: run-to-run variance on identical input is ~300 tokens, LARGER than 7d's measured +162**, so treat small deltas as noise.
- **⚠ ROLE-INVERTED CALLS (the recorded user is the one being SOLD TO).** Live case: the closer's own disclosures (*"I own a primary residence"*, *"I have cash on hand"*) were counted as covered PROSPECT ground. **The `role_inverted` detector is a WEAK FLAG, NOT THE PROTECTION** — it keys on the model citing closer lines as prospect attributes, and it fired on one run of a call and not the next. A deterministic alternative (closer question share: inverted 39% vs normal 51-63%) is promising but **only 2 corpus calls have role-labelled turns**, too few to set a threshold. **What actually protects the output is the verification chain**: `what_mattered` came out null on the inverted call in both runs, because closer words cannot pass a proven-prospect-quote test. Do not describe the flag as the safety mechanism.
  - **⚠ THIS IS A DATA PROBLEM, NOT A CODE PROBLEM — do not "fix" it by tuning the detector.** Reliable inversion detection needs labelled examples that do not exist yet: only calls analysed under **v13+** carry role-labelled turns, so the corpus of calibratable calls is 2 and grows only as new calls are analysed. **Justin's ruling 2026-08-12: do NOT ship a threshold justified by n=2.** Revisit when enough v13+ calls have accumulated to separate the distributions honestly — until then the verification chain carries it, and that is sufficient.

### HIGHLIGHT TAXONOMY v17 — objection / risk_signal / barrier (shipped 2026-08-12, `e31c9cb`)
- **THE PROBLEM:** Scout was detecting the right moments and filing them under the wrong names. On one real call **three moments were tagged `objection` and only ONE was**, and the best catch on the call — prior financial losses signalling skepticism that would resurface at close — was filed as **`rapport_moment`**. Justin called that catch the essence of the product. The detection was right; **the vocabulary was too small to hold it.**
- **JUSTIN'S GOVERNING DEFINITION:** *"a true objection only happens after you drop price and ask for the close."*
- **`objection`** narrowed to post-price/post-ask resistance · **`risk_signal`** (new) attitudinal doubt that could kill the deal later · **`barrier`** (new) concrete external constraint · **`rapport_moment`** restricted to genuine connection so disclosures of fear stop landing there. Migration 038 widened the CHECK; **no historical row was rewritten**.

- **⚠ THE DECISION TEST — the rule that separates objection from barrier. DO NOT REPHRASE IT.**
  > **Can the prospect change it by deciding differently?** If yes → **OBJECTION**. If no → **BARRIER**.
  - *"I may be able to get to the $4,800, but probably not today"* → **OBJECTION**. Nothing external stops them; they could choose otherwise and are choosing not to.
  - A lender approving only **$5,000** against a $9,800 price, a spouse who must sign, funds locked until a date → **BARRIER**. They cannot decide their way out.
  - **MEASURED PROGRESSION on the canonical $4,800 case, 3 runs each: post-price timing 1/3 → "fact reported vs position taken" 2/3 → the decision test 3/3.** The first two phrasings were not wrong, they were *unoperationalisable*: both moments are post-price, so timing cannot separate them.
  - **EVIDENCE IT GENERALISES rather than pattern-matches the examples:** the model produced a barrier that appears nowhere in the prompt — *"I couldn't pull it until after the 26th anyway"* — which is precisely the funds-locked-until-a-date case. It applied the rule; it did not copy a phrase.
- **⚠ THE SHAPE LESSON, now proven twice — STATE THE OPERATION, NOT THE ADJECTIVE.** This is the same failure and the same fix as **v14**, where `closer_response` already said *"quoted verbatim"* and still failed reconstruction 67% of the time until the prompt described the OPERATION (copy a contiguous span from one line). Here, "resistance" and "obstacle" are adjectives a model can agree with and still misapply; *"could they decide their way out of it?"* is a test it can run. **When a prompt rule underperforms, do not intensify the adjective — find the operation, and put the worked examples in.**
- **⚠ QUESTIONS ARE NOT AUTOMATICALLY RESISTANCE (Justin's sales-manager lens).** *"Just because a prospect asked a question doesn't mean it's an objection."* Manner and timing discriminate: a genuine logistical question (*"how does the price break down"*) is **not resistance at all**; a demanding early price question (*"just hurry up and tell me the price"*) is a red flag → `risk_signal`, with the observation required to cite the wording showing manner. **Where the wording gives no evidence of manner, the prompt defaults AWAY from resistance.** Under this rule the 37:25 cost question moved objection → missed_opportunity → **buying_signal**.
- **⚠ NO PRECEDENCE RULE between prospect-intent types and closer-failure types — DECIDED, NOT OVERLOOKED (ruling 2026-08-12).** One type wins per moment and **the model chooses**. Justin's reasoning: *forcing `missed_opportunity` to outrank `buying_signal` would relabel every unanswered question as a closer failure, which is worse than the occasional judgement call.* The types are not mutually exclusive in principle — the extractor proved it can emit both on one call — but no ordering is imposed. **Do not add one "for consistency".**
- **⚠ SELECTION VARIANCE IS NOT MISLABELLING — never conflate them again.** The extractor emits only **5–8 moments** from a call that may run 66 minutes, so any given example is often **NOT PICKED** at all; across three runs one of Justin's five examples never surfaced. That is pre-existing selection variance, **not** a taxonomy failure. A validation harness must report NOT PICKED separately from MISLABEL — the first harness conflated them and produced four false FAILs.

### ⚠ DATA-OP CHECKPOINTS PIN TO VERIFIED IDS, NEVER A FIXED COUNT (ruling 2026-08-12)
**When the target set of a destructive data op can legitimately change between planning and execution, a "stop unless the count is N" checkpoint will halt on the system working correctly.**
- **What happened:** a cleanup of speaker-anchoring violations was authorised as *"delete the 10, stop if it isn't 10"*. By execution time it was **6** — because the `violatesProspectAnchor` guard had removed 4 on its own as those calls were re-analysed. The script halted and deleted nothing. **The halt was correct behaviour against a flawed checkpoint**, and it cost a round trip.
- **⚠ AN OUTDATED-SCOPE RE-ANALYSIS DOES REACH OLD CALLS.** A wrong assumption made in the same block: that rows on old prompt versions (v4/v9/v11…) can never self-heal under new-calls-only. **They can** — `/fathom/update-analyses` with a scope dispatches exactly those outdated calls, and a human clicking reanalyze will clean them without anyone touching the database. **Do not plan a manual data fix around "these rows are unreachable" without checking whether a re-analysis path covers them.**
- **THE CORRECT SHAPE for a destructive data op:**
  1. Select with **the same predicate the production guard uses** — never a hand-written SQL variant, so the deletion cannot disagree with the code.
  2. **PRINT the rows** and confirm each genuinely satisfies the rule.
  3. **Delete exactly those printed IDS.** Not the predicate re-evaluated at delete time, and not "everything matching" — the set may have moved between the read and the write.
  4. **Re-count to zero**; if any id fails to delete or the recount is non-zero, stop and report.
- **NEVER run a destructive data op while a drain is active.** `persistHighlights` inserts and deletes highlight rows continuously during analysis, so the target set is moving. Observed live: a paged scan saw 1,218 rows while SQL saw 1,217 seconds later. Wait for `processing = 0` **AND** `pending` stable across several checks — a serial batch has gaps where nothing is "processing" yet the run is far from done.

### ⚠ THE CALL REVIEW SHOWS **CALL HIGHLIGHTS ONLY** — THREE PANELS WERE REMOVED FROM THE RENDER PATH, BUT THE DATA STILL WRITES (ruling 2026-08-14)
**Justin, on production:** *"It just replaced the old section with a new section. I just want the call highlights and this is basically it — I think we just keep over complicating things here."*
- **Removed from the render path (commented in place, NOT deleted):** `renderWhatMatteredHtml` (7d, *"The question that mattered here"*), `renderRiskSignalsHtml` (8b, *"Risk signals captured on this call"*), `renderBarrierTraceHtml` (9a, *"A signal you didn't qualify on"*).
- **⚠ THIS IS A RENDERING DECISION, NOT A DATA ONE. DO NOT READ THESE REMOVALS AS THE FEATURES BEING ABANDONED.** `what_mattered`, `coverage`, `prospect_context` and `barrier_trace` **all keep being written** to `call_analyses`, and `selectMissedCuePair` still runs. Any of it can be folded into a Call Highlights row later **with no re-analysis and no backfill** — the data will already be there.
- **WHAT SURVIVED, because it improves Call Highlights itself rather than adding to it:** the **handling badge inline with the type chip** (`Engaged with` / `Moved past` / `No response`), the **"You said"** line beneath the quote, the **missing type labels** (`risk_signal` and `barrier` were rendering as raw snake_case), **bigger full-strength type text**, and the **barrier tightening**.
- **Only `risk_signal`, `barrier`, `objection` are badged** (`HANDLING_TYPES`). Badging every type alike is what made the removed panel feel cluttered.
- **THE LESSON:** three separate stages each added a section rather than improving the one the user actually reads. **When a user asks for something to be better, improving the existing surface beats adding a new one — a new panel reads as more complexity even when its content is correct.**

### ⚠ HELD: "A CHALLENGE IS ENGAGEMENT" NEEDS A CONFRONTS-vs-STEERS DISTINCTION (held 2026-08-14, work recorded so it is not lost)
v23 broadened `handling='addressed'` to count a challenge as engagement — the mirror of the v18 warmth fix, prompted by Scout scoring *"It's taking you a year to finally reach out to us?"* as *"acknowledged, then moved past"* when Justin called it a phenomenal response.
- **⚠ IT IS UNVALIDATED AND MAY OVER-APPLY.** The moment Justin praised was **not picked in any of 3 runs**, so his case is untested. A regression check on a borderline case came back **`deflected | deflected | addressed`** — flipping in 1 of 3.
- **THE DISTINCTION THE WORDING IS MISSING — a question mark is not the discriminator:**
  - *"It's taking you a year to finally reach out to us?"* **CONFRONTS** → engagement.
  - *"But that's set aside for business, right?"* **STEERS THE PROSPECT TO AGREE** → not a dig. (Justin's own 9a missed-cue read treats this exact line as a **miss**.)
- **Held at Justin's call for stabilising, not more tuning.** When it resumes, both live examples above are the worked pair — the pattern that has settled every previous boundary here.

### ⚠ 9a MISSED CUE — PAIRED IN THE WORKER, WITH NO MODEL DECLINE STEP (rulings 2026-08-13)
**The feature:** an unaddressed cue early, a concrete obstacle later. *"You had a signal here and didn't qualify on it — this is where the time went."*
- **⚠ THE ARCHITECTURAL LESSON, WHICH GENERALISES.** This shape is DEFINED by the extractor's own output (`risk_signal` + `handling` + timestamps). I first put the field in the GRADER — because the extractor returns a bare JSON array and the grader is the only call with a call-level output slot. **An OUTPUT-SHAPE constraint drove an INFORMATION design decision, and put a judgement where its evidence wasn't.** Result: the grader re-derived it blind and **linked on 0 of 8 calls that demonstrably contain the shape**, while pairing the same 8 calls from stored moments yields **14 pairs across 5 of them**. **RULE: when a field needs data from a model's own output, the WORKER is the home — not the other model.**
- **⚠ NO MODEL "DECLINE" STEP, AND DO NOT RESTORE ONE AS A SAFETY MEASURE.** The claim is two FACTS, both already established: the prospect raised this (a typed `risk_signal` whose quote is proven as their words), and the closer did not dig (`handling` = deflected/ignored, **judged at CAPTURE with the transcript in view**). It never claims the obstacle was *caused* by the miss, and the panel refuses that reading outright (*"not proof the deal was winnable"*). **The judgement was already made where the evidence was; asking a second model to re-make it blind is precisely what produced 0/8.**
- **SETTLED CONSTANTS — do not re-litigate:**
  - **`MIN_CUE_GAP_SECONDS = 120`**, derived not rounded: degenerate pairs (the "obstacle" is the same moment restated) top out at **101s**, the next genuine pair is at **183s**, so any value in that empty band excludes exactly the degenerate cluster and no real pair.
  - **A SECTION constraint (cue in discovery, obstacle at close) was measured and REJECTED** — it halves eligible calls (20 → 10) and discards coachable pitch→close, intro→close and discovery→objection shapes.
  - **ONE pair per call: the EARLIEST cue with its FIRST qualifying obstacle.** Measured 44 pairs across 20 calls but only **24 distinct cues** — the multiplication is one cue against several later obstacles, so showing all is repetition, not coaching. The cue anchors the story; the *first* consequence proves it bit, the last would inflate the gap.
- **Both quotes must be `speaker_verified`** — the panel quotes both as the prospect's words.
- **Shape (b) — the closer DID dig and the prospect genuinely had nothing — is DEFERRED, not dropped.** `handling='addressed'` is far too weak a proxy: sampled, only 1 of 6 resembled a real dig; the rest were reframes and flattery (*"the whole thing is reverse engineering the concept behind risk"*, *"you are at least intelligent because…"*). It needs a purpose-built signal **plus** a ruling on the KB harvest gate, which admits `outcome='closed'` ALONE — and **5 of the 6 calls carrying shape (b) did not close.**

### ⚠ SPEAKER-ANCHORING GUARD — `violatesProspectAnchor` (shipped 2026-08-12)
**`barrier`, `risk_signal` and `objection` describe the PROSPECT'S position and cannot be anchored on a CLOSER-spoken line.** Enforced at WRITE TIME in `lib/analysis-worker.js`, **not** by a prompt instruction — a prompt line here would be the adjective-not-operation trap this project has hit three times.
- **THE LIVE BUG IT EXISTS FOR:** two moments typed `barrier` carried `speaker=CLOSER` and quoted the closer's OWN finances — *"Dude, I just burnt through $100,000… my dad, I'm going to talk to him about being a silent partner"* — while their observations read **"Prospect disclosed no liquid capital."** **Speaker attribution was CORRECT.** The extractor ignored it when interpreting, turning the closer's rapport-building into the prospect's barrier. Justin read it as a one-off caused by Josh relating to prospects; **the mechanism is systematic and would corrupt the coverage map, `what_mattered`, and any 8c/8d tracing**, all of which read these types as statements about the prospect.
- **⚠ APPLIED TWICE, AND BOTH ARE NECESSARY:** once in `sanitizeHighlights` on the model's CLAIMED speaker, and again **after write-time verification on the PROVEN speaker** — because verification can correct `PROSPECT → CLOSER` *after* sanitisation, so checking the claim alone lets a corrected row through. Removing either is a regression.
- **⚠ IT REJECTS RATHER THAN RECLASSIFIES — this is the ruling, not laziness.** **The observation is written from the same mistaken premise as the type.** Retyping the row (say to `rapport_moment`) would keep prose asserting the prospect said something they never said, and the sanitizer cannot rewrite an observation. Inventing a replacement label it cannot justify is the wrong-label-worse-than-none failure. **Dropping one moment out of the 5–8 the extractor emits is the cheap, safe action.**
- **⚠ THE SCOPE IS A DELIBERATE RULING, NOT AN OVERSIGHT.** Exactly three types are guarded. `buying_signal` and `disqualify_signal` are prospect-side too, but **a closer voicing a buying signal (*"so you'd want to start in January?"*) is normal reflection, not corruption** — guarding it would delete legitimate moments. **Widening the list is a DECISION, not a tidy-up.** Pinned by a test so it cannot be widened casually.
- **Proven in a live run:** the guard independently dropped a `barrier` anchored on *"We can do $5,000 with Clarity. Do you guys happen to have…"* — the closer talking.

### ⚠ A SEARCH PREFIX CAN SILENTLY REWRITE THE WRONG PROMPT BLOCK (self-caught 2026-08-12)
**The extractor prompt contains TWO `- closer_response:` instructions** — one in the objection block, one in the risk_signal/barrier block. A scripted edit matching the prefix `'- closer_response: the closer'` matched **both**, replaced the **first** (the objection one), and left the intended target untouched.
- **This class of error does not fail loudly.** The prompt still parses, the suite still passes, and the only symptom is objections quietly acquiring risk-signal wording. It was caught by reading the file after the edit, not by a test.
- **Both instructions are now pinned by a test** asserting there are exactly two `- closer_response:` lines and that the objection one still says *"answering this objection"*. **When scripting an edit against a prompt with repeated field names, match on the FULL distinguishing line or edit by index — and verify which one you changed.**

### ⚠ THERE ARE TWO TOKEN CAPS — CHECK WHICH ONE YOUR FIELD LANDS IN (recorded 2026-08-12)
**Every analysis makes TWO Claude calls with SEPARATE caps. Budgeting a new field against the wrong one means reasoning from a number that does not apply to it.**
- **`GRADER_MAX_TOKENS = 4500`** — the section grader. Carries the scores, notes, outcome, `one_thing`, `follow_up_email`, `why_outcome`, `qualification_covered`, and the 7c/7d coaching fields (`coverage`, `prospect_context`, `what_mattered`). **Measured worst case across the six longest calls: 3,211 / 4,500 (71%), ~1,289 spare, 0 unparseable in 24 runs.**
- **`HIGHLIGHT_MAX_TOK = 3000`** — the highlight extractor. Carries `quote`, `observation`, `section`, `type`, and the objection/risk/barrier sub-fields (`objection_surface`, `resolution`, `closer_response`, `handling`). **Measured worst case: 908 / 3,000 (30%), ~2,092 spare, 0 unparseable in 12 runs.**
- **⚠ THE EXTRACTOR CAP HAD NEVER BEEN MEASURED UNTIL 8a (2026-08-12).** Every truncation note before that — including the 3000→4500 bump — was about the GRADER. If you are adding an extractor field, none of those numbers apply to you.
- **⚠ THE CODE COMMENT ON `HIGHLIGHT_MAX_TOK` IS A ~3× OVERESTIMATE.** It says *"8 highlights × ~300 tokens each"* = 2,400, implying only 600 spare. Actual worst-case usage is **868**. **Do not budget against the comment; measure.** The extractor has far more room than it appears, and the grader far less.
- **The rule stays: measure on the SIX LONGEST CALLS before adding any field, never on the average.** A truncated response fails the whole analysis for that call — the grader loses everything, the extractor loses the entire highlight set. And beware small deltas: run-to-run variance on identical input is ~300 tokens on the grader, larger than most single-field costs.

### ⚠ 8c/8d (BARRIER → UNCOVERED-GROUND CAUSAL LINK) IS HELD ON DATA, NOT UNFINISHED (ruling 2026-08-12)
**The feature:** *"the lender only approved $5,000 — and financial qualification was never established in discovery, which is where that would have surfaced."* Closest thing yet to Justin's vision. **Designed, deliberately NOT built.**
- **WHY IT IS HELD — measured, not suspected.** Uncovered areas per call, on every call that currently has a coverage map: **0, 1, 1.** With exactly ONE uncovered area **the link is forced, not judged** — every barrier on that call gets attributed to the single available gap regardless of relevance. A spouse-approval barrier would trace to `financial_qualification` purely because nothing else was there, and it would read as a confident causal discovery. **A forced link that reads as a discovery is precisely the failure this project keeps designing against.**
- **UNBLOCK CONDITION:** coverage maps get richer as v15+ calls accumulate and reps' area lists exercise more ground. Revisit when a meaningful share of calls carry **2+ uncovered areas**, so the model is actually choosing between candidates rather than naming the only one. **This is a DATA problem with a due date, the same shape as the role-inversion detector — do NOT read it as unfinished work and do not "fix" it by building the link anyway.**
- **WHEN IT IS BUILT, THE MACHINERY ALREADY EXISTS.** Item 2 is structurally **7d with a different anchor quote**: `resolveWhatMattered` already enforces area-exists → area-uncovered → cited-quote-reconstructs. Swap the prospect's self-description for the barrier's quote (already `speaker_verified`) and the validation chain is unchanged. No new validator.
- **Also required at that point:** the model must be able to DECLINE, the prompt must say that an available gap is not the same as a causal one, and the wording must read as an observation the closer can judge — **never as a finding**. Causation is not provable here; only the scaffolding is.
- **Asymmetry worth remembering:** this needs derived areas, so it is silent for **7 of 8 live profiles**. Item 1 (risk-signal handling) needs none and works for every rep — which is why 8a/8b went first.

### ⚠ OBJECTION HANDLE RATE WILL DRIFT FOR WEEKS — WHAT IS HEALTHY vs WHAT IS BROKEN (recorded 2026-08-12)
**Under new-calls-only, any 30/90-day window blends v16-and-earlier (broad `objection`) with v17+ (narrow) rows. The number WILL fall. That is the metric becoming truer, not a regression. A future session reading a lower number must not chase it as a bug.**
- **BASELINE captured the day v17 shipped:** 30-day window = **303 objection-typed moments, 85 handled, 28% raw handled rate**. (The dashboard tile is computed from `team-needs-work` buckets — true-objection buckets only — so it is a related but not identical number; the raw figure above is the cheap proxy, computable with no Claude call.)
- **HEALTHY drift — expect all of these:**
  - Rate **falls toward the 21–32% post-price band**. Measured pre-v17 by section: discovery **45%**, intro 33%, close 32%, pitch 25%, objection **21%**. Early concerns are handled far more often because they are easier; removing them is the point.
  - **Denominator roughly HALVES.** 153 of 322 pre-v17 objection moments (48%) were post-price by section proxy.
  - The **What-needs-work** card hits its *insufficient* state **occasionally** — `MIN_BUCKET = 6`, and fewer objections per surface bucket means marginal buckets legitimately drop below it. That is the guardrail working.
- **BROKEN — these are real failures, investigate:**
  - The **tile blanks** (`objectionHandleRate` returns null → "—"). It only does that when total is 0. With ~150 objections expected per 30 days post-narrowing, a blank tile means objections stopped being captured at all — check that the extractor is still emitting `type='objection'` and that migration 038's CHECK did not reject writes.
  - The What-needs-work card sits at *insufficient* **persistently rather than occasionally** — that means the narrowing over-fired and almost nothing is being typed `objection`.
  - The rate falls **below ~20%** and keeps going. The post-price floor measured pre-v17 was 21%; materially below that suggests true objections are being mis-typed as `barrier`, i.e. the decision test has inverted somewhere.

### ⚠ `coverage` AND `qualification_covered` HANDLE A MISSING QUOTE DIFFERENTLY — ON PURPOSE (ruling 2026-08-12)
- **`sanitizeQualificationCovered` FLIPS `true` → `false`** when the model claims coverage without a supporting quote. Correct **there**, because that field **drives nothing** — it is measurement only, so under-reporting is free and over-reporting would corrupt the measurement.
- **`sanitizeCoverage` does NOT flip.** It keeps `covered: true` and withholds the unprovable quote (`evidence: null`, `evidence_verified: false`).
- **WHY THE DIFFERENCE IS LOAD-BEARING:** 7d ranks **UNCOVERED** areas into *"the question that mattered"*. Flipping an unsupported claim to `false` would **manufacture a gap and coach the rep on a fiction** — telling them they missed ground they actually covered. Claiming coverage without proof and asserting a miss are **both** assertions; make neither. Keep the judgement, withhold the quote.
- **Justin's ruling, 2026-08-12:** *"flipping an unsupported claim to false would invent a gap and coach the rep on a fiction."*
- **A future session will notice the two sanitizers disagree and try to harmonise them. Do not.** The rule follows the CONSEQUENCE, not the shape: a field that drives nothing may fail closed; a field that drives coaching must not invent the thing it coaches on.

### ⚠ TWO DIFFERENT BARS FOR PROFILE MATERIAL — DO NOT "FIX" THEM INTO ONE (ruling 2026-08-11)
- **`usableProfileField` (≥15 chars AND >1 token) is the SELLING-CONTEXT bar** — it decides what background material the grader may read. Thin is fine there: a short offer is still true, just brief, and it can only help.
- **`lib/coaching-areas.js usableMaterial` is the RUBRIC bar, and it is DELIBERATELY HIGHER.** A coverage rubric ASSERTS what a buyer must demonstrate. Asserting that from 26 characters is fabrication, not brevity. It requires knowing what the offer demands OF A BUYER — so either `qualifications` or `script_raw` must be present, or an offer of ≥`MIN_OFFER_ONLY_CHARS` (200).
- **The live case that forced the split:** one user's ENTIRE offer is `"6 month real estate course"` (26 chars). It clears `usableProfileField`, and from those five words the model confidently produced five areas — *"time availability"*, *"current experience level"* — which are **general knowledge about real estate courses wearing the costume of a personalised rubric**. Josh's real offer is 506 chars plus criteria plus a 14,721-char script.
- **Justin's ruling, upheld 2026-08-11:** *"Background material can be thin and still help the grader; a RUBRIC asserts what a buyer must demonstrate, and asserting that from 26 characters is fabrication."* Live shape: **1 user derives areas, 7 are silent** — that is CORRECT, not a coverage bug. **A future session will notice the two bars differ and try to unify them. Do not.**

### SPEAKER IDENTITY — 6a/6b/6c/6d SHIPPED, 6e BUILT (2026-08-11). Supersedes the "blocked" note below.
- **6a — deterministic labelling (`66becf8`, live, `ANALYSIS_PROMPT_VERSION` v12→v13).** The closer is identified by **EXACT EQUALITY** between the per-turn `speaker.matched_calendar_invitee_email` and `fathom_connections.fathom_email`. Settled by live fetch: **only the recorded_by user's turns carry the email** (closer 47/47, 551/551, 653/653; prospects 0/180, 0/257) — a perfect discriminator. **NO name fallback, NO talk-time heuristic, NO fuzzy matching** — each returned the CLOSER as the prospect on real calls; the legacy `recorded_by`-name path is kept but deliberately unwired.
  - **RULING 1 — the email is resolved to a role at normalize time and DISCARDED.** `transcript_stored` turns are exactly `text · speaker · display_name · start_seconds`. Writing a personal email onto every turn would spread PII for no gain. **Cost, accepted knowingly:** a future repair can never re-derive attribution from stored data — it would have to re-fetch from Fathom while the recording and connection still exist. One-way door.
  - **Why it needed a version bump even though the TEMPLATE is unedited:** the prompt STRING changes materially (the closer is named; every transcript line is prefixed `CLOSER:`/`PROSPECT:` instead of a raw display name). `prompt_version` is the only way to tell which analyses were graded with MATCHED speakers vs GUESSED ones. It marks all prior analyses outdated — **cosmetic under new-calls-only; do NOT mass re-analyze.**
  - Verified on the hardest live call (**two people named Joshua**): Josh (prospect) 1214 turns → PROSPECT, Joshua Pinner 549 → CLOSER. `speaker_closer_name` populated for the first time (was NULL on all 83 rows).
- **⚠ `speaker_confidence` IS NOT A COLUMN** — the worker only RETURNS it. The persisted equivalent is `speaker_closer_name`, non-null **iff** confidence is `matched` (both set in the same normalizer branch). That invariant is pinned by `speaker-identity.test.js`; **do not decouple them** — `routes/kb.js` depends on it.
- **6b/6c — backfills, three-valued verdicts.** `call_highlights.speaker_verified` (migration **034**) and `closer_response_verified` (migration **035**) are **NULL / false / true** with **no default, deliberately**: NULL = never assessed (no closer identity — e.g. the 235 demo rows), false = assessed and NOT provable, true = proven. A default would collapse the first two. **Closer-side features MUST require `= true`.** Live state: highlights **440 proven / 524 unprovable / 235 never assessed**; harvested KB moments 48 proven / 63 refused.
  - **Corrected corpus numbers** (the originals were artifacts of the unsound locator and were reported as fact): highlight speaker error rate **8% → 3%** (13 of 439 provable; intro worst at **19%**), KB misattribution **18% → 6% of provable**.
- **6d — the rep-only "What worked" lane (`59c19ee`, live).** The section drilldown leads with the CLOSER's line and shows the prospect's preceding line as context. Only **proven** moments appear; unprovable and never-assessed are excluded and **counted separately on screen** (they are different messages). `closer_moments` is a SEPARATE field, never a filter on `good` — the call-review breakdown still shows everything. **RULING: prospect-side buying signals stay on the call-review page only — do NOT re-mix them into this lane.**
- **6e — objection responses (BUILT, local).** An objection is the PROSPECT's row with the rep's reply in `closer_response`, invisible to a CLOSER filter (257 responses vs 1 usable moment). **The field NAME is not evidence: of 53 responses that reconstruct, 3 were actually the prospect speaking.** Only `closer_response_verified = true` + `resolution='handled'` reaches the lane; the objection quote on the same row becomes EXACT context.
  - **⚠ THE YIELD CAME IN AN ORDER OF MAGNITUDE UNDER THE ESTIMATE — 1 → 3 moments, not the 30–80 predicted.** Three compounding reasons, all missed in the estimate: (1) only **24%** reconstruct (50 of 212 checkable), not the "third to a half" guessed; (2) only **12 of 50** are `handled` — `partial` dominates (38) and belongs in "what to fix"; (3) those 12 **scatter across sections** (NULL 5, close 3, discovery 2, **objection 2**) because an objection-TYPE highlight is tagged with the section where it OCCURRED. **Objection coaching is not viable on current data.**
  - **THE REAL BOTTLENECK, measured: the extractor paraphrases.** Of 88 sampled unreconstructible responses, **76 start verbatim then diverge mid-sentence**, 5 are wholly rewritten, and 7 ARE fully present (a small locator gap on responses split by an interjection). **Fixing this needs the extractor to emit VERBATIM `closer_response`, which is a prompt change + version bump and only helps NEW calls.** That single change is worth more than any further backfill.
- **v14 — VERBATIM QUOTING (built 2026-08-11). The single highest-yield change in this stage.** Every quoted field the models emit must be a **CONTIGUOUS character-for-character span of ONE transcript line**: extractor `quote` + `closer_response`, grader `why_outcome.quote` + `qualification_covered.evidence`. Scoped deliberately to ALL of them — fixing only `closer_response` would have repaired one section and left the identical defect capping stored highlights, the review page's decisive-moment quote, and harvested KB moments.
  - **WHAT ACTUALLY CAUSED THE DEFECT (both removed):** the prompt said **"trim filler"**, which licenses editing the words, and capped quotes at **5-30 words**, which forces compression of longer lines. Shortening is now **cut-from-the-ends only** — never stitching a beginning to a later ending.
  - **⚠ THE ADJECTIVE DOES NOT WORK — the rule must be MECHANICAL.** `closer_response` ALREADY said *"quoted verbatim from the transcript"* under v13 and still failed reconstruction **67% of the time**. Measured on 88 failures: **86% began verbatim and drifted mid-sentence**, 6% were wholly rewritten. Telling a model to be verbatim is not the same as telling it to COPY A SPAN. Do not "simplify" the HOW TO QUOTE block back into an adjective.
  - **MEASURED A/B on 8 real calls, both arms on the SAME call** (validated by RECONSTRUCTION RATE, never score deltas — the noise floor makes scores meaningless and reconstruction is directly checkable): **`quote` 67% → 93%**, **`closer_response` 33% → 91%**, **usable moments 35 → 51 (+46%)**.
  - **THE RISK THAT WAS MEASURED AND DID NOT MATERIALISE:** forcing verbatim could have made the extractor emit FEWER moments (no longer able to smooth a messy exchange by paraphrasing). It did the opposite — **52 → 55 moments, 6.5 → 6.9 per call**, and it emitted MORE `closer_response` values (6 → 11) because under v13 it often gave up and returned null. **If a future prompt change touches quoting, re-run this A/B rather than assuming the trade still holds.**
  - **Corpus baselines were re-measured with the interjection-tolerant locator FIRST**, so the prompt's gain is not confounded with the locator's: `quote` 45%→**48%**, `closer_response` 24%→**33%** from the locator alone.
- **`lib/quote-locate.js` tolerates interjections (2026-08-11).** Two accumulators: all turns in order, AND the anchor speaker's turns only. One person's continuous point interrupted by a short *"Right."* used to fail, because concatenating verbatim inserts the interjection INTO the candidate string — 7 of 88 unreconstructible responses failed for exactly that. Not a loosening: every word must still come from one speaker's own consecutive contributions, and a quote that only reconstructs by borrowing the other party's words is still reported `mixed`.
- **⚠ SUPERSEDED — the note below is kept for history only.**

### ⚠ CLOSER-SIDE FEATURES ARE BLOCKED ON SPEAKER LABELLING (recorded 2026-08-10 — RESOLVED by 6a above)
- **Root cause:** `analysis-worker.js` hardcodes `recorded_by: null` when building the meeting object, so the normalizer's fuzzy match never runs, `speaker_confidence` is `'unknown'` on **every** call, and `speaker_closer_name` is NULL on all 56 rows. **Speakers are INFERRED by the model from conversational cues, not matched.**
- **Consequence, measured:** of six real CLOSER-tagged discovery moments, **three are actually the prospect** (*"I'd definitely be interested to learn more about the role"*, *"I'm kind of sold on the actual work environment"*, *"I mentioned that in my Zoom video"*). A closer-only filter would present those to the closer as **"your good questions."**
- **Therefore: ANY closer-side feature waits on this.** The cheap display-only version **inherits the defect rather than avoiding it** — "start thin" is not a way around it.
- **The fix is the one that solved prospect names:** `fathom_connections.fathom_email` gives a reliable closer identity ("joshua@…" ↔ "Joshua Pinner"), and the raw transcript already carries `matched_calendar_invitee_email` per turn. Feed those into the normalizer so speaker assignment is deterministic. Prerequisite stage, not part of the feature.

### Recording Source Architecture
**ZOOM integration (2026-07-23, Zoom-only — Meet is OUT).** The pipeline is source-agnostic from `normalizeTranscript` onward (the `meeting` object at analysis-worker.js:710); a new source only reimplements OAuth + token store + sync + a transcript adapter that emits `[{speaker:{display_name}, text, timestamp:"HH:MM:SS"}]`.
- **Zoom app (Justin-created):** User-managed OAuth (General app, NOT Server-to-Server). Scopes `cloud_recording:read:recording` / `:list_recording_files` / `:meeting_transcript` + `user:read:user`. **OAuth Redirect URL = `https://www.scoutsystems.io/auth/zoom/callback`** (the www custom domain, NOT the Railway host — corrected 2026-07-23). Webhook (sub-stage 4): `recording.completed` + `recording.transcript_completed` → `https://www.scoutsystems.io/zoom/webhook`. **Env (Railway, set):** `ZOOM_CLIENT_ID`, `ZOOM_CLIENT_SECRET`, `ZOOM_REDIRECT_URI`, `ZOOM_WEBHOOK_SECRET_TOKEN` (sub-stage 4). State signing reuses `FATHOM_STATE_SECRET`. **PRODUCTION CREDENTIALS SWAP (beta-share, 2026-07-24 — submission is IN, ~72h review):** the beta auth flow runs on the app's PRODUCTION Client ID/Secret (Marketplace -> Production tab -> Basic Information), which are DIFFERENT values from the dev credentials Railway currently holds. The swap is a PURE ENV-VAR CHANGE — code verified to read ZOOM_CLIENT_ID/ZOOM_CLIENT_SECRET only via process.env (no hardcoded client id anywhere; token endpoint zoom.us/oauth/token is identical dev/prod). WHEN JUSTIN PASTES: after beta approval lands and BEFORE sharing the auth URL / before any tester connects. Sequence: (1) paste Production ZOOM_CLIENT_ID + ZOOM_CLIENT_SECRET into Railway Variables (triggers redeploy); (2) reconnect Zoom once from #account -> Connections (existing dev-minted tokens die when the client changes, so the connection must be re-established under Production creds); (3) verify boot + a fresh /users/me call succeeds; (4) dev credentials retired; (5) THEN distribute the beta auth URL to testers (they authorize under Production creds). ZOOM_REDIRECT_URI is unchanged. **External-user launch is gated on Zoom Marketplace beta-share (~100-install cap) then public review — build/test on Justin's own account first.** Cloud recording requires a PAID Zoom plan (Pro+); free tier has nothing to sync (sub-stage 4 detects+messages this). No native timestamp deep-link → clips are the parity mechanism.
- **Sub-stage 1 (BUILT 2026-07-23, local commits, awaiting approval):** migration 024 (`fathom_calls.source` fathom|zoom; unified `call_connections` table, composite PK (user_id,provider), RLS service-role-only) + `lib/zoom-client.js` (OAuth exchange/refresh — Basic-auth token endpoint, refresh tokens rotate like Fathom) + `lib/call-connections.js` (shared token store: `getValidAccessToken(provider)` refreshes + persists the rotated refresh_token) + `GET /auth/zoom/connect` + `GET /auth/zoom/callback` (mirror Fathom, write call_connections). **DEVIATION (justified): the approved sub-stage-1 scope included repointing Fathom's 8 `fathom_connections` sites into `call_connections`; the code argued against bundling that with the live Fathom token-refresh path, so the Fathom cutover is DEFERRED to its own contained step. Zoom is fully on `call_connections`; Fathom untouched; `call_connections` holds only zoom rows until the cutover.** **SCHEDULED DEBT (due date, not a resting state — Justin's ruling 2026-07-23): the Fathom cutover is its own contained step (own stop point) triggered AFTER sub-stage 2 proves the unified store against real Zoom traffic. It repoints Fathom's 8 `fathom_connections` sites (auth.js callback upsert, fathom.js ×8-incl. `_getValidAccessToken` refresh + `/sync-all` list + identity-picker `fathom_email`→`external_account_email`, analysis-worker.js Phase-3 token read) to `call_connections` filtered by provider='fathom', backfills existing Fathom rows, then drops `fathom_connections` after a rollback-safety release. Two parallel connection stores is DEBT WITH A DUE DATE — do not let it become permanent drift.** Tests: zoom-oauth.test.js (config gate, authorize-URL, Basic header, token-response validation, refresh-decision). **Sub-stage 2 (OPENED 2026-07-24: sync + VTT adapter → grade end-to-end).** MANDATORY FIRST TASK — token blocker: every authenticated Zoom API call in recon 401'd ('Invalid access token' 124) and a direct refresh 400'd ('Invalid Token'); we have NEVER confirmed a working Zoom API call from our flow (only ever tested REFRESHED tokens, all rejected; the connect token was never tested pre-refresh). Diagnostic discipline (ruling): after a FRESH reconnect, test the brand-new connect token against /users/me BEFORE any refresh (splits 'our tokens never worked' from 'rapid test refreshes broke the single-use chain'); then ONE controlled refresh + retest; root-cause before building sync. DESIGN RULING (adopted): Zoom token refresh is SERIALIZED per connection — single-flight, atomic-claim discipline, NO concurrent refreshes ever (Zoom's single-use rotating refresh tokens make this a correctness requirement, not an optimization); build it into lib/call-connections as part of sub-stage 2. NO scratchpad/test code touches live tokens outside that path from now on. Then per the approved design: zoom-client.listRecordings + lib/vtt-adapter (Zoom VTT: per-cue HH:MM:SS + inline 'Name:' speaker labels → the meeting.transcript shape) + POST /zoom/sync (mirror /fathom/sync, map → fathom_calls source='zoom') + the analyzeCall Phase-3/4 source-switch (call-connections.getValidAccessToken('zoom') + Zoom transcript → same meeting object at analysis-worker.js:711). First recon after the token fix: confirm Justin's Zoom account has a cloud recording + transcript to grade (couldn't verify in recon — API rejected auth). **Sub-stage 2 build progress (2026-07-24, local, unpushed):** (a) serialized single-flight refresh landed in lib/call-connections (getValidAccessToken joins one in-flight refresh per user+provider; double-checked re-read; TDD in call-connections.test.js) — the ruling fix for Zoom's single-use rotation. (b) lib/vtt-adapter.js: Zoom WebVTT → the meeting.transcript shape [{speaker:{display_name},text,timestamp:'HH:MM:SS'}] (supports 'Name: text' + <v Name> voice tags, ms→HH:MM:SS, merges consecutive same-speaker cues, skips NOTE/index lines; TDD in vtt-adapter.test.js against synthetic VTT — VALIDATE against a real Zoom recording once Josh connects post-approval). REMAINING for sub-stage 2: zoom-client.listRecordings + POST /zoom/sync (map → fathom_calls source='zoom') + the analyzeCall Phase-3/4 source-switch (call-connections.getValidAccessToken('zoom') + Zoom transcript via the adapter → same meeting object). **TOKEN BLOCKER RESOLVED (2026-07-24): our tokens + refresh WORK** — controlled diagnostic after a fresh reconnect: connect token → /users/me HTTP 200 (before any refresh), one controlled refresh → /users/me HTTP 200. The earlier 401s were the single-use refresh chain broken by rapid repeated test refreshes (confirms the serialized single-flight refresh ruling as a correctness requirement). **NEW BLOCKER — MISSING SCOPE: `cloud_recording:read:list_user_recordings`.** With a valid token, GET /users/me/recordings returns Zoom error 4711 naming exactly this missing scope. The app HAS list_recording_files/recording/meeting_transcript (fetch a KNOWN meeting's files) but NOT list_user_recordings (LIST the user's recordings — the polling-sync entry point). **Justin action before sub-stage-2 sync: add `cloud_recording:read:list_user_recordings` to the Zoom app scopes, then reconnect (re-consent to pick up the new scope).** (Webhook ingestion in sub-stage 4 delivers meeting ids directly and wouldn't need this, but sub-stage 2's polling sync does.) Gradeable-data check (recording + transcript present) is blocked until the scope lands. Feature env-dark until ZOOM_CLIENT_ID/SECRET present. **Connect-UI PULLED FORWARD from sub-stage 4 (product ruling 2026-07-23):** `GET /zoom/status` (routes/zoom.js, mounted /zoom) + a dashboard "Connect Zoom" section identical in shape to Fathom (same card/strip classes, connect+connected states, button → /auth/zoom/connect → popup → lands on ?zoom=connected → status reflects the call_connections row). Deeper polish (paid-plan detection messaging) stays in sub-stage 4. UI RESHAPE (ruling 2026-07-23): (1) the ONLY connection-management surface is a new #account 'Connections' section (all users, exempt from the managed lock like Password) — both providers with state (connected / email / since / not connected) + Connect/Reconnect/Disconnect. (2) Disconnect = DELETE /zoom/disconnect (call_connections) and DELETE /fathom/disconnect (fathom_connections): deletes ONLY the connection row, NEVER calls/analyses/highlights (history stays; new-call sync stops); confirm-before in the UI; sync degrades gracefully (Fathom /sync already 404s clean, cron skips absent connections). (3) the dashboard is FIRST-RUN-ONLY: connect cards render only when NO source is connected; once any source connects the dashboard shows only connected status strips and the other provider's connect button disappears (it lives in Connections). Pairing row from Justin's test was CLEARED (0 zoom rows; 250 calls/analyses intact) — he reconnects from his own login via the Connections section. ZOOM OAUTH TESTING GOTCHAS (2026-07-23, NOT bugs — don't chase): (1) AUTO-APPROVE — if the tester's browser has an active Zoom session AND the app was previously authorized, Zoom auto-approves instantly (popup closes immediately) and pairs the BROWSER's Zoom account regardless of which Scout login initiated. Standard OAuth. For multi-account testing use incognito or sign out of Zoom first. The already_linked guard (Zoom callback rejects a Zoom account already bound to a different Scout user -> ?zoom=already_linked) backstops the wrong-pairing case. (2) EXTERNAL ACCOUNTS BLOCKED — the Zoom app is unpublished (dev), so any Zoom account other than the developer's (Justin's) canNOT authorize it and Zoom shows its generic 'Something went wrong' page. Expected until Marketplace beta-share approval; build/test on Justin's own Zoom only. Correct current pairing after cleanup: Scout justin.schmidt23@yahoo.com <-> Zoom justinschmidtsales@gmail.com. **Deauthorization (2026-07-24, pre-submit, LOCAL/unpushed):** migration 025 adds call_connections.external_account_id (Zoom user id, captured at connect from /users/me); `POST /zoom/deauthorization` (unauth) verifies the app Secret/Verification Token (`ZOOM_WEBHOOK_SECRET_TOKEN`, Authorization header, timing-safe), answers the endpoint.url_validation CRC challenge, deletes the matching Zoom connection row(s) by external_account_id (tokens die; calls/analyses/history stay — same as in-app Disconnect), 200-fast, never 500. Endpoint URL → the Basic-Information 'Deauthorization Notification Endpoint URL' field at submission. TDD in zoom-deauth.test.js. Reviewer-seed data op prepared (scratchpad seed_reviewer.js) — HELD until reviewer@scoutsystems.io exists. **Acceptance PASSED (real refresh verified): Justin's round-trip paired Scout user josh@scoutsystems.io ↔ Zoom account justinschmidtsales@gmail.com (he tested while logged in as Josh) — tokens present, getValidAccessToken('zoom') refreshed + rotated + persisted cleanly.** **Deauth endpoint SHIPPED + live-verified 2026-07-24 (`e304a79`+`29b2bdb` pushed; migration 025 applied): against the real `ZOOM_WEBHOOK_SECRET_TOKEN` the live `POST /zoom/deauthorization` returns an `encryptedToken` matching `HMAC-SHA256(plainToken, secret)`, valid token→200, wrong token→401, connect routes alive, suite 67/67, health 200. Secret-token value was inadvertently printed once by a buggy shell echo during verification; rotation RECOMMENDED but DECLINED by Justin (accepted risk, logged).**

**ZOOM UNLISTED PUBLICATION — SUBMITTED 2026-07-24 (WAITING ON ZOOM; no Zoom work until external events).** Chose the UNLISTED route (Zoom confirmed in writing SSDLC/SAST/DAST evidence is NOT required for unlisted). Review phases: **completeness → functional** (reviewer uses `reviewer@scoutsystems.io`, seeded with `demo-rv-*` data — 18 calls/avg 64/$38,796 EOD cash/32 objection moments) **→ security** (TDD + OWASP + scope check). Responses arrive by email to justin@ + notes on the Publish page. **Post-approval choreography (STRICT ORDER):** (1) Justin manually activates (his click — activation is set to manual); (2) confirm the unlisted install link surfaced in-console; (3) Josh installs via link + records a cloud call with transcription; (4) validate the VTT adapter against his REAL recording; (5) finish sub-stage 2 — `zoom-client.listRecordings` + `POST /zoom/sync` + the `analyzeCall` source-switch; (6) THEN the scheduled Fathom→call_connections cutover comes due; (7) reviewer demo-data cleanup (documented SQL, `demo-rv-*`) runs ONLY after review FULLY completes — never before. **Standing state: deauth endpoint live; secret-token rotation declined (accepted risk, logged); Zoom stage WAITING on Zoom — do no Zoom work until approval/notes arrive.**

Fathom is the only recording source for now. Zoom and Google Meet will be added later.

**CRITICAL:** Before Phase 2 (the analysis pipeline) is built, a recording-source abstraction layer must be designed so the analysis pipeline is source-agnostic. The pipeline receives a **transcript object** regardless of where it came from. The transcript object shape MUST be defined before Phase 2 implementation begins. Locking the shape early is the only way to avoid a Zoom/Meet integration becoming an analysis-pipeline rewrite later.

Shape questions to answer at design time: speaker labels and identity resolution, turn-level vs sentence-level granularity, timestamp anchoring (absolute vs relative), confidence per segment, action items / highlights / summary as separate optional fields, transcript-language metadata, and whether each segment carries a back-reference to the source recording's playable URL + offset.

### Phase 2 Architecture — Post-Call Intelligence

**Pipeline (end-to-end, one row at a time):**

```
fathom_calls.sync_status='pending'
    ↓
analysis worker picks up the row
    ↓
fetch transcript + highlights + summary from Fathom with include_*=true
    ↓
normalize timestamps (HH:MM:SS strings + numeric seconds → numeric seconds)
    ↓
identify CLOSER vs PROSPECT (see Speaker Identification below)
    ↓
TWO PARALLEL CLAUDE CALLS:
  1. Section Grader      2. Highlight Extractor
    ↓                      ↓
results upsert into call_analyses + call_highlights
    ↓
fathom_calls.sync_status='processed'
```

**Two new tables (migration 010):**

`public.call_analyses` — one row per analyzed call.
- `id` uuid PK
- `fathom_call_id` uuid FK → `public.fathom_calls(id) ON DELETE CASCADE`
- `user_id` uuid FK → `auth.users(id) ON DELETE CASCADE`
- `overall_score` integer (0–100)
- `overall_summary` text
- For each of the 5 sections (`intro` / `discovery` / `pitch` / `objection` / `close`), three columns each: `<section>_grade` text, `<section>_score` integer, `<section>_notes` text. (15 columns total across the 5 sections.)
- `one_thing` text — the single actionable to do differently next time
- `follow_up_email` text — draft email body the closer can copy
- `transcript_stored` jsonb — the normalized turn array (`[{ speaker, role, text, start_seconds }, ...]`) kept on the row so the analysis is reproducible and the review page can render the timeline without re-fetching from Fathom
- `speaker_closer_name` text — what name the closer's voice was identified as in this transcript (free-form match against `recorded_by.name`)
- `analyzed_at` timestamptz
- `status` text — `pending` / `processing` / `done` / `error` (separate from `fathom_calls.sync_status`; this one tracks the *analysis* lifecycle within the row)

`public.call_highlights` — one row per highlight moment (typically 5–8 per call).
- `id` uuid PK
- `fathom_call_id` uuid FK → `public.fathom_calls(id) ON DELETE CASCADE`
- `user_id` uuid FK → `auth.users(id) ON DELETE CASCADE`
- `timestamp_seconds` integer — used in the Fathom deep link (`?t=<seconds>`)
- `speaker` text — `CLOSER` or `PROSPECT`
- `quote` text — exact words spoken at the moment
- `observation` text — one factual sentence describing what happened (not commentary)
- `type` text — `buying_signal` / `objection` / `missed_opportunity` / `strong_moment` / `rapport_moment` / `disqualify_signal`
- `sequence_order` integer — display order within the call (the timeline)

RLS on both tables mirrors `fathom_calls` (own / admin-managed / owner via `current_user_role()`). Backend writes via service-role; no INSERT/UPDATE policies.

**Two parallel Claude calls (one analysis pass = both fire concurrently):**

1. **Section Grader.** Inputs: normalized transcript, closer's uploaded script (if present in KB), top winning-call transcripts from KB. Output: structured JSON with `grade`, `score`, and `evidence` (quoted lines from the transcript) for each of the 5 sections (Intro / Discovery / Pitch / Objection Handling / Close). Plus `overall_score`, `overall_summary`, `one_thing`. Plus `follow_up_email` draft. Uses the Call Analysis Accuracy Requirements section as the prompting contract — every claim must cite a quoted transcript line.
2. **Highlight Extractor.** Input: normalized transcript only (no benchmark). Output: array of 5–8 highlights with `timestamp_seconds`, `speaker`, `quote`, `observation`, `type`. Tone explicitly calibrated as **film coach reviewing tape — factual, not cheerleader or critic.** Must connect patterns across related moments (e.g. "the same 'too expensive' objection appears at 00:12:04 and again at 00:34:51 — second one was harder because the first wasn't fully isolated"). No empty validation, no generic feedback.

**Timestamp handling.**

Per the SDK audit in this CLAUDE.md, Fathom's timestamps are inconsistent across types:
- `TranscriptItem.timestamp` → `"HH:MM:SS"` string
- `ActionItem.recording_timestamp` → `"HH:MM:SS"` string
- `Highlight.start_time` / `end_time` → numeric seconds
- `Meeting.recording_start_time` / `recording_end_time` → ISO datetime string (absolute)

The normalizer runs FIRST in the pipeline and converts everything to **numeric seconds from start of recording**. Numeric chosen because Fathom's own deep-link URL uses it: `https://fathom.video/calls/{recording_url_id}?t={seconds}`. All downstream code (analysis, UI, follow-up email "see at 00:12:04" anchors) reads from the normalized numeric form only.

**Speaker identification.**

The new CLAUDE.md SDK audit confirmed `TranscriptItemSpeaker.display_name` is fragile and `matched_calendar_invitee_email` is "Coming soon!" — currently never populated. Phase 2 resolves CLOSER vs PROSPECT this way:

1. Fetch `recorded_by.name` from the Fathom meeting data (the Fathom user who recorded — usually the closer).
2. Fuzzy-match against the set of `display_name` values present in the transcript turns.
3. Tag the matched speaker as `CLOSER`; all other speakers tagged as `PROSPECT`.
4. Store the matched name in `call_analyses.speaker_closer_name` so re-analysis is reproducible and the review page can label turns confidently.
5. **Fallback** when no fuzzy match clears a threshold: pass through raw `display_name` values to Claude and let it infer roles from conversational cues (who asks discovery questions, who pitches, who handles objections). Store the inferred name in `speaker_closer_name`.

Multi-prospect calls (more than one external participant) are out of scope for Phase 2 — first non-closer voice is treated as "the prospect" and a TODO is left for v1.4.0+ to handle panel calls.

**Trigger.**

Phase 2 fires automatically immediately after `/fathom/sync` completes for a row. Async via the existing fire-and-forget pattern (matches the post-call summary and adaptive-learning flows already in the desktop's `stop-session` handler). **The closer never waits.** Calls show up in the dashboard list as `"analyzing…"` (`status='processing'`) and then populate when done (`status='done'`). Errors flip to `status='error'` with an error message stored, and the dashboard surfaces a retry button.

**Call Review page (dashboard).**

When the user clicks a synced call from the library, the review page renders three sections **in this order** (deliberately — highlights come first, grades support):

1. **Call Highlights** — the lead section, most visual prominence. Timeline of the 5–8 highlights with: clickable timestamp link (`fathom.video/calls/{id}?t={seconds}` deep link, opens in a new tab), speaker label badge, exact quote, Scout's one-sentence observation. Pattern notes inline connect related moments. Footer: **"One thing to do differently"** (the `one_thing` field) called out in an accent box.
2. **Section Grades** — five collapsible cards, one per section, each showing the grade letter + score + evidence quotes. Supporting detail, not the focus — collapsed by default with a "show all" affordance.
3. **Follow-up Email** — generated draft with a copy-to-clipboard button. The closer sends manually; Scout never sends.

### CRM Integration Notes
- **Start with GHL (GoHighLevel) and Close.io.** Both have public APIs with OAuth (same UX pattern we built for Fathom in v1.2.0).
- **Read+write**: pipeline stages, activity logging, contact data, deal notes.
- **Write operations require closer confirmation before executing** — never auto-write without the user seeing exactly what will change. The diff-then-confirm pattern is non-negotiable; one wrong silent overwrite of a pipeline stage in a real CRM destroys trust in the entire product.
- **Closer connects CRM account via OAuth popup** — reuse the Fathom popup pattern (`/auth/<provider>/connect` returns `{url}`, popup posts result to opener, closes itself). The state-JWT scheme in `backend/routes/auth.js` is general enough to reuse with a per-provider secret.
- **HubSpot et al. come later** — only after GHL + Close.io are working in production for at least one paying customer.

### Call Analysis Accuracy Requirements
**This is the single most critical feature in Scout.** A wrong grade on a closer's call is worse than no grade — it teaches the wrong lesson and burns trust. The analysis pipeline must:

- **Know the source of the transcript** (Fathom, Zoom, Meet) and weight confidence accordingly. Fathom diarized transcripts get higher baseline confidence than a Zoom auto-caption export.
- **Flag low-confidence analysis** when transcript quality is poor — never hide uncertainty behind a confident grade. The dashboard surfaces a "transcript quality unclear" badge that adjusts how the grade is presented.
- **Reference specific transcript moments as evidence for every grade** — never make unsupported claims. Every assertion in a grade output ("missed the discovery on budget") must include a quoted prospect/closer line + timestamp anchor.
- **Use the closer's uploaded script** (if present in the KB) **as the benchmark** for what "good" looks like for that closer / offer.
- **Use winning-call transcripts from the KB as comparison examples** — the same three-tier KB diversity model that powers `/kb/search` (learned patterns → user uploads → seeded frameworks) extends naturally to "find the closest winning call to compare this one against."
- **Grade on a spectrum** — not pass/fail. Five-band scoring (or similar) so closers see directional progress over time, not a binary that flattens nuance.

### Fathom OAuth Credentials (Railway env vars)
```
FATHOM_CLIENT_ID
FATHOM_CLIENT_SECRET
FATHOM_REDIRECT_URI=https://sales-overlay-production.up.railway.app/auth/fathom/callback
FATHOM_STATE_SECRET
```

### Teleprompter (Future Architecture Note)
The original live-teleprompter feature **stays as an optional feature** in v2.0 — not deprecated, just no longer the lead product. Future: prompt mode only activates for **specific call sections** (objection handling, closing) — not during intro/rapport when narration interrupts rapport-building. Requires section detection in the live audio path. Not building now. The architecture (suggestion polling timer, prospect-response gate, closer-active guard) must be preserved so the section-aware mode can slot in without a rewrite.

### Desktop deferred work — persistent-login parity (ready-to-build, ships with next DMG)
The v1.3.1 persistent-login fix was applied to the **website only** (shared
`backend/web/js/scout-auth.js`: 401 refresh-retry interceptor + proactive timer +
unix-seconds `expires_at` handling). The Electron app is dormant (teleprompter
optional), so its parity work is deferred but designed and approved — build it
into the next DMG. What's there today: full session persisted to disk
(`userData/token.json`), `ensureFreshToken()` refreshes on launch and on
`get-token` (deduped via `_refreshInFlight`). Three approved additions:
1. **Proactive refresh timer (main process):** every ~4 min, if the stored
   session is within 5 min of `expires_at`, call `ensureFreshToken()` so the app
   never hands the renderer an about-to-expire token. Mirrors the web timer.
2. **401 IPC handling / retry:** the desktop proxy path routes through
   `get-token`→`ensureFreshToken`, but if a proxied request still returns 401
   (token expired mid-flight), the renderer should signal main to
   `ensureFreshToken()` and retry the request once — the desktop equivalent of
   the web fetch interceptor. Add a 401-retry wrapper in `src/lib/proxy-client.js`.
3. **Stale-token re-auth flow — distinguish transient vs genuine:**
   `ensureFreshToken()` currently calls `clearSessionFromDisk()` on ANY non-OK
   `/auth/refresh`. Change it to clear + reopen login ONLY on a genuine auth
   failure (HTTP 401 / `invalid_grant`); on transient failures (network / 5xx)
   keep the session and retry on next launch, so a Wi-Fi blip doesn't log the
   user out. JWT TTL is 3600s; refresh tokens rotate — the disk write after each
   refresh already persists the rotated token.

## Future Plans
- **Deactivation active-check fail-open is DELIBERATE (User Management, 2026-07-27).** `requireAuth` reads `user_profiles.active` after `getUser` and 401s if false — but on a DB error it FAILS OPEN (skips the gate, logs, proceeds). Accepted reasoning (Justin): the Supabase **ban** already blocks new login + token refresh, so a transient DB blip can only extend an ALREADY-ISSUED access token to its natural expiry (≤60 min) — it can never grant NEW access to a deactivated user. Fail-closed would instead risk locking out every legitimate user during a DB hiccup. So fail-open is the correct trade for a best-effort immediate-revoke layer sitting on top of the durable ban.
- **User-management confirms → proper modals (polish follow-up, logged 2026-07-27).** The `#team-members` add-rep flow is a modal, but email-change and delete use `prompt()` (delete requires typing the email). Accepted as a first cut; replace both with styled modals (delete: a typed-confirm modal like a real destructive-action dialog) in a polish pass.
- **Billing needs an ACCOUNT/ORG entity — FIRST TASK of the billing block (logged 2026-07-27, Justin's ruling).** Scout has NO company/account/org concept today — "teams" are only `user_profiles.managed_by` edges between individual users, and `billing_plan`/`billing_status` are PER-USER columns (migration 019/023). Real billing (a plan a whole company is on, one invoice, seat-based proration) requires a first-class account/org entity that users belong to. Until then: seat count = COUNT(active users), and "current plan" is shown per-user (`billing_plan`) — the User-Management block (2026-07-27) surfaces both but does NOT introduce an account object. **The billing block's first task is to design + add the account/org entity** (accounts table, membership, account-level plan/seats), then migrate the per-user billing columns onto it. Do not build charging/proration before this exists.
- **Abandoned auth accounts — KNOWN & ACCEPTED (logged 2026-07-27, Justin's ruling: NO deletion, NO filter for now).** Of 14 total auth users, **6 are abandoned public-signup attempts** (`aboude3311@gmail.com`, `imasouid@gmail.com`, `jdblobe15@gmail.com`, `kalvin.hadi@gmail.com`, `mmartinez@uiqus.com`, `paulrt60@gmail.com`) — no `user_profiles` row, 0 `fathom_calls`, 0 `call_sessions`, signed in once at creation, none assigned to a manager. The Calls-Picker scoping fix (2026-07-27) already keeps them out of every practical surface: unassigned → they only appear behind the owner-only "All users…" reveal. **Accepted as-is.** If the list ever gets noisy, the recorded option is an **"active users only" filter** = exclude accounts with (no `user_profiles` row AND 0 `fathom_calls` AND 0 `call_sessions`) — isolates exactly those 6, reversible, no deletion. Deletion would need an explicit go. (Separately: the 4 test/demo accounts — `demo-{ava,ben,cara}@scout-demo.dev`, `reviewer@scoutsystems.io` — keep their existing scheduled post-review cleanup; the other 4 accounts are real.)
- **Cash glance-tile → EOD once EOD is pivot-aware (follow-up, logged 2026-07-27).** A-1's coaching-overview Cash tile currently drills to the analyzed-calls list (uniform with the other tiles, safe for the `?user=` manager pivot). Point it at the EOD view instead once the EOD view accepts a pivoted user (`?user=`/`viewingUserId`) — today EOD is self-only, so a manager viewing a rep's Cash tile must not jump to their own EOD.
- **True calendar date picker (BACKLOG — logged 2026-07-27, "down the line" per Justin; do NOT build until scheduled).** Replace/augment the preset date-range controls (7/30/90d) PLATFORM-WIDE with an explicit start-date + end-date calendar picker so any view (Coaching, Calls, Team, EOD, digest history) can select an arbitrary range. Scope when it comes up: a shared date-range control component + wiring every `state.dateRange` consumer to it; the presets can stay as quick-set shortcuts alongside the calendar. Surfaced during the B-2.5 Manager Calls Picker block (which added 7/30/90 presets to the Calls view).
- **Adaptive Learning Engine (high priority, designed not built).** See "Current Status: What Works" for full architecture. Next desktop feature after v2.0 Fathom integration stabilizes.
- **Pre-existing item: Post-call prompt-compliance scoring (future).** Use session_logs to post-process each call: compare every Claude suggestion against the closer's transcripts that followed it (35% fuzzy-match). Output a per-call "delivery %" and per-stage breakdown. Scope: one backend post-call job + a UI surface.
- **Fix electron-builder DMG-notarization gap (high priority).** Discovered 2026-05-16 during v1.1.8 release: with `notarize: true` in `build.mac` config, electron-builder 25 notarizes the `.app` and staples the ticket, but never signs or notarizes the DMG container around it. Every release since at least v1.1.6 has needed manual `codesign + xcrun notarytool submit + xcrun stapler staple` per DMG to be shippable. Until fixed, the release workflow includes a mandatory manual DMG-notarization step (~15 min per release). Likely fix: add an `afterAllArtifactBuild` hook in `package.json` `build` config that auto-signs+notarizes+staples DMG artifacts, OR upgrade electron-builder, OR explicitly set `notarize.tool: "notarytool"` with target spec. Investigation should also verify the DMG-notarization step actually runs in CI/scripted contexts (it may be a known TTY-detection issue). See Resolved Issues for the full diagnosis and recovery sequence.
- Context-aware payment link surfacing: during Close / money objection, overlay shows the right payment option link based on prospect's financial situation (wired up after onboarding stores the links)
- Stripe billing fully wired (keys pending — see Railway env vars above)
- Diagnostics dashboard (Phase 3 — see BUILD-PLAN.md)
- **Migrate DNS from Namecheap to Cloudflare** — solves bare-domain HTTPS timeout via CNAME flattening + edge SSL. Free tier. Keep Namecheap as registrar, just change nameservers.
- Revoke old exposed GitHub PAT at github.com/settings/tokens (the Anthropic key is already gone from console.anthropic.com; only the PAT still needs manual revocation)
- See BUILD-PLAN.md for full 4-phase roadmap

### DATE-RANGE PICKER — rulings and named follow-ups (2026-08-16)
- **⚠ NAMED FOLLOW-UP: the picker has NO FOCUS TRAP.** Tab moves out of the open panel and the panel stays open until an outside click. **This is held deliberately, not missed.** The control re-renders on every keystroke and moves focus itself, so a naive `focusout`/`blur` handler fights the re-render and closes the panel spuriously. Doing it properly needs a render-aware guard (ignore focus events caused by the control's own re-render). **Do not "fix" this with a one-line blur listener** — that is the version that breaks. Escape-to-close, focus-on-open and focus-return-on-close all work today.
- **⚠ DIMMING IS NOT THE GREY THE NO-GREY RULE BANS (Justin's ruling 2026-08-16).** The picker's out-of-month days sit at **45% opacity** and its hint line / day-of-week headers at **85%**. That is allowed and must stay. **The distinction: the no-grey rule targets unreadable low-contrast BODY TEXT — numbers, labels and prose a user has to read. It does not target DEPTH CUES that make a dense control legible**, and flattening out-of-month days to full strength would make the current month hard to pick out. Every colour in the picker is already `var(--text)`; only opacity varies. **Do not tighten the no-grey guard test to cover opacity** — it would break a calendar that Justin has explicitly approved.
- **TWO LABEL CONVENTIONS, PERMANENTLY DIFFERENT.** Picker label **INCLUSIVE** ("Aug 3 - Aug 10" = both days in range, and `rangeToIso` ends the window at 23:59:59.999 so the label tells the truth); graph **BUCKET** labels **EXCLUSIVE** ("Aug 3 - Aug 9") so adjacent buckets never share a day. Both are correct for their own job; a guard test in `date-picker.test.js` pins both at once.
- **EVERY VIEW OWNS ITS OWN RANGE** (ruling 2026-08-16, wider than the original Calls question): `state.dateRange` was **shared** by coaching and team. The picker never reads app state — each instance registers a getter and a setter. Guard test fails on `state.` in the control's source.

### ⚠ COMPARE THE UNDERLYING VALUE, ROUND ONLY TO RENDER (2026-08-16)
**Comparing DISPLAY values rather than underlying ones produces a wrong answer that looks entirely right, and a deterministic tie-break makes it STABLE — so it never announces itself.**
- **The live case:** `weakestObjection` ranked categories on the ROUNDED handle rate. Josh's `timing` (3/42 = **7.14%**) and `partner` (2/27 = **7.41%**) both display as **7%**, so the comparison fell through to the tie-break and named **partner** his weakest objection when it is **timing**. Every part of the wrong card was real — a real category, a real rate, real counts — and it would have read the same way on every load.
- **Why it is worse than a crash:** rounding collapses distinct values into equal ones, the tie-break then picks deterministically, and the result is a stable, plausible, wrong answer. There is no flicker to notice and no error to catch.
- **THE RULE: sort, rank, compare and threshold on the exact value; call the rounding function only where the number is written into markup.** If a rounded value must be compared (e.g. against a stored rounded floor), say so explicitly at that line.
- Caught by a unit test whose expectation was written from the raw data rather than from the implementation — which is the argument for writing the expected value from the source numbers, not from what the code currently returns.

### ⚠⚠ A FABRICATED IDENTIFIER FAILS SILENTLY IN BOTH DIRECTIONS — NEVER RECONSTRUCT ONE FROM A DISPLAY PREFIX (2026-08-16)
**THIS IS WHY IT IS WORSE THAN AN OUTRIGHT WRONG VALUE: NEITHER DIRECTION RAISES AN ERROR.**
- **A WRITE scoped to a fabricated id** either inserts rows nobody can see, or **collides with a real row** that happens to match. The first is invisible; the second is corruption wearing a valid-looking id.
- **A READ or a DELETE scoped to one** simply matches nothing — and **reports success**. `0 rows affected` is indistinguishable from "there was nothing to affect".
So there is no failure to notice, no exception to catch, and no log line to grep. A wrong-but-real id would at least hit something someone would eventually question.

**A plausible-looking identifier is not an identifier.** Query output is routinely abbreviated for reading — `left(user_id::text,8)`, a truncated call id, a shortened commit hash — and rebuilding a full value from that fragment produces something that LOOKS right and refers to nothing.
- **The live case:** the demo-seed script was drafted with full UUIDs invented from the 8-character prefixes shown in earlier query results. They were well-formed, correctly shaped, and **completely fictional**. Caught in review before `--insert` ran.
- **Column constraints are not the safety net.** An FK to `auth.users` would reject a bad `user_id`, but a nullable or unconstrained column accepts it happily — so whether the failure is caught at all depends on which column you happened to target.
- **THE RULE, and it is the same discipline as destructive ops pinning to verified ids:** resolve the identifier from the source table by a stable natural key (email, `fathom_call_id`, label), **then assert every target exists before writing anything**. `backend/scripts/seed-demo-data-2026-08-16.js` does this — it selects the ids by email, re-queries `user_profiles` for all of them, prints `exists` per target, and **aborts if any is missing**.
- **This applies to every data script, not just destructive ones.** The existing rule ("a data-op checkpoint pins to VERIFIED IDS, never a fixed count") was written for deletes; inserts need the same guarantee for the same reason. **Print the verification in the run log** so the evidence is visible after the fact rather than assumed.

### PERFORMANCE SUMMARY IS SLOW BECAUSE IT IS A MODEL CALL — measured 2026-08-16
```
cache MISS   35,005 ms      cache HIT (before fix) ~1,196 ms      the model ~33,808 ms = 97% OF A MISS
```
- **The miss is irreducible.** 97% of the wall clock is Claude generating ~5,000 characters of JSON (`claude-sonnet-4-6`, `SYNTH_MAX_TOKENS = 2600`). **Do not go looking for slow database work — it is 3% of a miss.**
- **The practical pattern:** `to` is `new Date()` at click time, snapped to today's UTC midnight, so the cache key changes **once per day**. **First view of a range on a given day ≈ 35 s; every view after ≈ 0.8 s.** That is what "genuinely slow" means here.
- **THE FIX IS AN HONEST WAITING STATE, TIME-GATED.** Justin's copy — *"This will take a minute — go touch some grass"* — appears only after `PERF_LONG_WAIT_MS` (2 s). **Always-on would be a lie in the other direction** on a sub-second cached load, and would train people to ignore it.
- **⚠ A FASTER MODEL WAS CONSIDERED AND DECLINED (ruling 2026-08-16).** Haiku would cut the 34 s substantially. **This lane's entire value is the grounded claim/evidence pairing, and every time this project has weakened a synthesis to save latency it has cost more than it saved.** If it is ever wanted it is **a deliberate quality trade, not a performance tweak** — and it should be argued on output quality, with the delta measured, not slipped in as an optimisation.
- **The one real code defect found: the cache was checked AFTER all the data loading**, so a HIT paid for eleven columns across 154 rows that it then discarded. Now the key columns (`fathom_call_id, analyzed_at`) load first, the cache is checked, and the full select happens only on a miss. Median warm hit **1,196 ms → 824 ms**. ⚠ Both figures are noisy (observed 683–1489 ms) and the "before" was n=2 — **the direction is certain because less data is fetched; treat the magnitude as approximate.**
- **⚠ THE HIT RATE ON THIS LANE IS NOT YET MEASURABLE.** Only ONE row had been written since key-snapping landed, and **a rate cannot be computed from n=1**. Re-check after a few days of normal use; the number becomes meaningful then, not now.

### DEMO SEED DATA — WRITTEN 2026-08-16, AND HOW TO REMOVE IT
**Fabricated rows live in PRODUCTION on the three demo reps.** They exist because four features could never fire on real data: only one rep had prospects, only one cleared the objection-volume threshold, and only Josh had a coverage map.

**⚠ REMOVAL — one pass, cascades confirmed against the live schema:**
```sql
delete from fathom_calls where fathom_call_id like 'seed-2026-08-16-%';
delete from prospects     where display_name  like 'Seed %';
```
`call_analyses`, `call_highlights` and `eod_edits` are all `ON DELETE CASCADE` on `fathom_calls`, so the first statement clears them. `fathom_calls.prospect_id` is `SET NULL`, which is why prospects need the second. Script: `backend/scripts/seed-demo-data-2026-08-16.js` (`--plan` prints and exits; `--insert` writes).

- **WHAT WAS SEEDED:** 72 calls over 6 weeks across demo-ava / demo-ben / demo-cara — 26 prospects, 24 `fear` objections, 16 `what_mattered` rows. **Zero model calls.**
- **⚠ DIRECT INSERTION WAS CHOSEN FOR CORRECTNESS, NOT PRICE.** The demo reps' existing calls are COPIES OF JOSH'S TRANSCRIPTS, so a real analysis run would have put Josh's words under their names AND left the outcome up to the model — ~140 Claude calls with no guarantee the thresholds fired. Fabricated rows hit them deterministically, which was the entire objective.
- **Marker prefixes are deliberately DISJOINT:** `demo-%` (33 rows, old copies) and `demo-rv-%` (18 rows, Zoom reviewer) already existed. `seed-2026-08-16-%` matched 0 rows before this ran, so **the removal above cannot sweep the reviewer's data and a future `demo-%` cleanup cannot sweep the seed.**
- **`prompt_version` is stamped `seed-2026-08-16`**, not a real grader version, so seeded analyses can never be mistaken for graded calls or picked up by an outdated-count sweep.
- **ZOOM REVIEWER ISOLATION IS STRUCTURAL, verified not assumed:** `reviewer@scoutsystems.io` is role `user` with `managed_by = NULL`, and every cross-user route is `requireRole(['manager','owner'])`. That account 403s on all `/team/*` and `/admin/*` routes, cannot pivot with `?user=`, and sees only its own 20 calls.
- **JOSH WAS NOT TOUCHED — inserts only, scoped to the three demo user_ids.** Counts before and after: **calls 360 → 360, analyses 360 → 360, prospects 201 → 201.**
- **⚠ A DRAFT OF THE SCRIPT INVENTED THE REP UUIDs** from the 8-character prefixes visible in earlier query output. The script now fetches the real ids and **verifies every target id exists before writing a row**. Any future data script targeting users must do the same — a plausible-looking UUID is not a UUID.

### ⚠ A MIRROR TEST MUST PIN GEOMETRY, NOT JUST THRESHOLDS (2026-08-17)
**Where a value is duplicated between a module and an inline render, the pinned set has to cover everything that decides WHAT IS DRAWN — not only the numbers that look like business rules.**
- **The live case:** the speedometer face was rebuilt from a 180° sweep to 240°, with ticks every 2.5% instead of 10%. Those live in BOTH `lib/rep-gauges.js` and the inline copy in `dashboard.html`. The mirror test pinned the targets and thresholds but not the geometry — so a **240° module against a 180° render** would have placed the needle at an angle where the colour zones mean something different, **with no error, no failed test, and a confident wrong picture.** `SWEEP_DEG`, `TICK_STEP_PCT` and `MAJOR_EVERY_PCT` are now pinned alongside the targets.
- **The general form: if changing a constant changes the OUTPUT, the mirror pins it.** "It's only styling" is not an exemption — the needle angle is styling and it is also the entire claim the dial makes.

### ⚠ WHEN A METRIC DEPENDS ON A ROW'S CONTEXT, SEEDING IT MEANS CONTROLLING THE CONTEXT (2026-08-17)
- Under the handled-includes-closed ruling, an objection on a **closed** call counts as handled regardless of its own `resolution`. **The same row, moved to a different call, changes the rate — while the row itself looks identical.**
- A seed built to demonstrate the red/yellow/green **bands** therefore places its objections only on **non-closed** calls, so `handled == resolution` exactly and the intended band is the band that renders. Placed carelessly it would have demonstrated the wrong band and looked entirely correct doing it.
- Recorded in `backend/scripts/seed-demo-recent-2026-08-17.js`.

### ⚠ A "NOT YET LOADED" SENTINEL MUST BE THE VALUE THE LOADER ACTUALLY TESTS FOR (2026-08-17)
**`undefined` and `null` are both falsy and both mean "nothing here" to a reader. They are NOT interchangeable to a `=== null` guard, and the failure is silent.**
- **The live case:** the gauge panel's lazy kick is `if (state.teamGauges === null && !state.teamGaugesLoading) loadTeam('repGauges')`. The key was never declared in the state initialiser, so it was `undefined`, the condition was never true, **the lane never fetched, and the panel rendered a skeleton forever.** No error, no failed request, no console warning — just a spinner that never resolves, which reads as "slow" rather than "broken".
- **THE RULE: declare every lazy-load key with the exact sentinel its guard tests.** This codebase uses `=== null` for "not yet requested" and a separate `*Loading` boolean for "in flight", precisely so those two states are distinguishable. An undeclared key silently becomes a third state that matches neither.
- **How it was caught: by probing state in the browser after the first render came back empty** — not by reading the code, where the kick line looks perfectly correct. Same family as the dead-call-site rule: the code exists, it just never runs.

### ⚠ SHARED-CARRIER, LAYOUT EDITION — SPLITTING A SHARED CONTAINER REMOVES CONTROLS FROM PAGES THAT HAD NOTHING TO DO WITH THE CHANGE (2026-08-17)
**Third instance of the shared-carrier failure, and the first where the carrier is a LAYOUT CONTAINER rather than a value.**
- **The live case:** the speedometer panel had to sit between the page title and the date picker, so `teamHeaderHtml()` was split into a title row and a controls row. That function is rendered by **four** pages. The other three — team recommendations, team needs-work, manage members — carried the picker *inside* the header, so the split **silently removed the date picker from all three**. Nothing failed; those pages simply lost a control, on a change that was about a different page.
- **THE RULE is the existing one with the scope widened: after changing what a shared carrier CONTAINS, re-derive every other consumer's assumption — and a shared render function is a carrier.** The earlier instances were `state.dateRange` changing meaning and the URL hash gaining a range; the mechanism is identical.
- **Caught by rendering all four pages and asserting the control is present on each**, which is the layout equivalent of grepping the other readers.

### ⚠ A GUARD MUST SPLIT THE SAME WAY THE RULE SPLITS — BY QUESTION, NOT BY FILE (2026-08-17)
**When a rule divides call sites by the QUESTION they ask, a per-file guard is the wrong shape and will be wrong for a real file.**
- **The live case:** "handled" is read by fifteen places. TEN ask *"what is the rate?"* (credit objections on closed calls); FIVE ask *"was this a good moment?"* (must not). The obvious guard is a list of files. **`lib/team-synthesis.js` is in BOTH lists** — it computes an objection rate for its prompt *and* picks highlight-of-the-week candidates. `lib/objection-synthesis.js` and `lib/session-analytics.js` are the same shape.
- **`objection-synthesis` needed the split INSIDE A SINGLE LOOP**: it counts with the shared predicate and selects its "handled examples" on the moment's own resolution, because an example is evidence of good handling shown to a closer. A credited-but-unhandled moment held up as the model to copy is exactly backwards.
- **THE RULE: enforce per CALL SITE.** `test/handled-carrier.test.js` counts hand-rolled `resolution === 'handled'` comparisons per file against an explicit allowance, with the reason for each allowance written beside it. A file-level allowlist would have said "team-synthesis is exempt" and stopped noticing its rate lane entirely.

### ⚠ DO NOT DELETE A TEST BECAUSE ITS SCAFFOLDING WENT — CONVERT IT (2026-08-17)
**A test's SUBJECT often outlives its VEHICLE, and deleting it with the feature is how a property silently stops being covered.**
- Removing the What-Needs-Work money math failed 8 tests. Four were genuinely about the removed feature and were archived in place. **Four were converted**, because what they protected still exists:
  - *"linkage Δ counts TRUE objections only"* measured its property through the linkage — which is gone. The property is not: **logistical barriers and disqualifications must stay out of the math even when they sit in closed calls.** Re-expressed through `detail.objections` and bucket totals.
  - *"rate_gap when the linkage groups are too small"* → now pins `rate_gap` as the **only** state, since there is no longer a money state to fall back from.
  - The exports test now asserts the removed constants are **absent**, so reintroducing the money lane trips it rather than passing quietly.
- **THE TEST TO APPLY: is the assertion about the thing being removed, or about a property that merely used it to get at something?** Only the first should go.

### ⚠⚠ A ZERO THAT ARRIVES BY CONSTRUCTION IS THE SIGNATURE OF A TAUTOLOGY (2026-08-17)
**The most valuable measurement of the handled-definition block, and the reason two rulings had to ship in ONE commit.**
- **The ruling:** an objection counts as handled if `resolution === 'handled'` **OR the call's `outcome === 'closed'`**. Justin: *"objections are just barriers to a close, so if they side-step the barrier and still close, that's a win in my book."* Binary — `partial` scores zero.
- **What-Needs-Work's counterfactual multiplied by `P(closed | handled) − P(closed | not handled)`.** Under the new definition the not-handled group **cannot contain a closed call**, so:

| definition | P(closed \| handled) | P(closed \| not handled) | delta |
|---|---|---|---|
| old | 56.4% | 9.8% | **46.6 pts** |
| new | 67.6% | **0.0%** | **67.6 pts** |

- **That 0.0% is not data. It is arithmetic.** The claim degenerates to *"objections we credited BECAUSE the call closed correlate with the call closing."* Every dollar figure would have inflated **~45%** while still reading as a measurement — no error, no failing test, just bigger numbers.
- **THE GENERAL RULE: when a definition changes, look for a term that becomes exactly 0 or exactly 1 as a RESULT of the change.** A boundary value that arrives by construction rather than by measurement means the two sides of the comparison are no longer independent, and any statistic built on them is circular. **It is invisible in the output** — the number still renders, still looks plausible, and is still wrong.
- **Consequence:** the money math (counterfactual, `+$X at stake` chip, "How this is estimated" panel, team-borrowed coefficients) was removed **in the same commit** as the definition change. Justin had independently ruled the money out — but had he not, shipping the definition alone would have quietly corrupted it.
- **Also recorded: "handled" is read by FIFTEEN places and they do not all ask the same question.** TEN ask *"what is the rate?"* and credit closed calls. FIVE ask *"was this a GOOD moment?"* and must not — a moment inside a closed call is not automatically a good moment, and crediting it there files weak handling under "what worked". `lib/team-synthesis.js` is in **both lists**, which is why `test/handled-carrier.test.js` enforces the split **per call site, not per file**.
- **And a rate must reconcile with the counts printed beside it.** The Objections view shows handled · partial · unhandled, so a fourth **`credited (call closed)`** bucket was added. Without it a manager adds up what is on screen and gets a different number from the rate above it — which is how a page loses trust.

### ⚠⚠ `rgba()` CANNOT READ A HEX TOKEN — SO EVERY TINT WAS A **COPY** OF THE ACCENT, NOT A REFERENCE (2026-08-17)
**THIS IS THE FINDING. The one-line lesson underneath it is secondary.**
- **The mechanism:** `--accent` is a hex token. `rgba()` needs channels, so it cannot read it. Every soft tint on the dashboard was therefore written as `rgba(91, 158, 255, 0.14)` — a **hard-coded copy of the accent's value**, which does not move when `--accent` moves. **"Change one variable and all 65 sites follow" was only ever true for the sites using the HEX.** 23 tints were silently outside that story.
- **What shipped because of it:** (e) changed `--accent` to green, verified by grepping `#5b9eff` (0 hits), and **shipped green text sitting on blue chips to production** — several of those tints are backgrounds under `color: var(--accent)`.
- **THE ONE-LINE LESSON: a colour has more than one spelling — check every spelling.** Hex, `rgb()`, `rgba()`, and channel triples inside a data-URI SVG are all the same colour to a user and four different strings to a grep.
- **THE FIX is a second spelling of the same token, pinned to the first:**
```css
--accent:       #4ade80;
--accent-rgb:   74, 222, 128;              /* the same green, as channels */
--accent-quiet: rgba(var(--accent-rgb), 0.18);
```
`test/accent-palette.test.js` converts one to the other and asserts they match, so the two spellings cannot drift.
- **⚠ THIRD SIGHTING OF "A VALUE THAT LOOKS MAINTAINED AND ISN'T" — count it as the third.** (1) the stale load-bearing comment in `init()` (see its own entry below); (2) `.kb-promote-btn.is-team`, whose comment read `/* --accent #5b9eff at 10% */` beside a hard-coded blue — **the comment named the variable the code did not read**; (3) these 23 tints. **The shared shape: something that a reader scans as "this follows the token" while the machine sees a literal.** A comment pointing at a variable is weaker than a reference to it, and is worse than a bare literal because it stops the reader looking.

### PALETTE — THREE JOBS, AND NONE MAY BORROW ANOTHER'S HUE (rulings 2026-08-17)
```
SEMANTIC     --good / --mid / --bad   "this is good / a warning / bad"
INTERACTIVE  --accent                 "you can click this"
CATEGORICAL  REP_LINE_COLORS          "this is rep number four"
```
- **The categorical ramp borrows NONE of the others.** Amber and rose were dropped first: **a rep rendered in the warning hue reads as a warning**, when all the line means is "rep number four". **Green was then dropped for the same reason in the other direction** — it means "clickable" everywhere else, and a hue cannot do two jobs. Cyan leads.
- **SEVEN entries, not six.** The consumer is `REP_LINE_COLORS[i % length]`, so a six-colour ramp **draws rep 7 identically to rep 1 on the same chart**. Blue took the freed slot: it stopped meaning "clickable" the moment `--accent` moved off it.
- **KB category badges (`#93c5fd`) STAY** — a taxonomy palette on the Knowledge Base page, one hue per content category, not an interactive colour.
- **Also ruled: the `.user-select` chevron icon keeps its grey stroke** (the no-grey rule is about TEXT), and the Team drilldown's "Cash collected" **axis label** stays (it is a label, not a card — (m) removed cards).
- Guarded by `test/accent-palette.test.js`, which fails if the ramp contains any of the four reserved tokens and proves itself non-vacuous by re-adding the accent.

### ⚠⚠ A `z-index: -1` LAYER PAINTS BEHIND ITS OWN PARENT'S BACKGROUND — AND FAILS AS *ABSENCE*, NOT AS AN ERROR (2026-08-17)
**The (f) background motifs rendered NOTHING. Not faintly — nothing, at every opacity, including 1.**
- **The cause:** the layer is `body::before { position: fixed; z-index: -1 }`, and the stylesheet said `html, body { background: var(--bg) }`. **A negative-z-index child paints behind its own parent's background**, so BODY's opaque box covered the motifs. The CSS reads as completely correct and throws nothing.
- **The fix:** the page background lives on **`<html>` alone**. It propagates to the viewport canvas, so the page is pixel-identical. `test/background-motifs.test.js` fails if a background ever returns to `body`, and says why in the message.
- **⚠ WHY THIS ONE IS DANGEROUS: the symptom is ABSENCE, which presents as a DESIGN problem rather than a bug.** The natural read is "3.5% is too subtle on a near-black page, raise the opacity" — and the next step is to spend the architect's ruling on the wrong question. **It was caught by setting the opacity to 1 to check the layer painted at all, BEFORE reporting on how faint it was.** When a visual element is invisible, prove it is rendering before you tune it.

### BACKGROUND MOTIFS (f) — SAFETY IS THE LAYOUT, NOT THE OPACITY (2026-08-17)
Two inline-SVG data URIs (a plotted series with its nodes; the donut as concentric arcs) on a fixed `body[data-view]::before` at **3.5%**, `pointer-events: none`. No asset, no request, no dependency.
- **The failure mode — competing with the charts — is REMOVED BY THE LAYOUT rather than managed by being faint.** Every `.section` is an **opaque card** and `.page` is a 1200px column, so the layer can only show through the gutters and the gaps between cards. It is *structurally* incapable of sitting behind a chart, a number, or a line of text.
- Hidden under **1320px** (no gutter to live in) and under **`prefers-contrast: more`**.
- **Scoped by `body[data-view]`, stamped in `render()`** — the single dispatch point for every view, so the attribute cannot fall out of step with the screen.
- **⚠ THE ROLLOUT WAS MEASURED ACROSS ALL FIFTEEN VIEWS, NOT ASSUMED FROM THE FIRST.** Thirteen put every element inside an opaque card. **TWO do not** — `prospects` and `section` render a page subtitle and their empty/error states as **card-less full-width blocks** whose boxes reach into the motif's box. Those were **rasterised to a canvas and sampled**: **zero motif pixels** fall under those glyphs, at column widths equivalent to viewports from 1600px down to 1300px. **A bounding-box overlap is not an ink overlap, and the difference is worth measuring rather than reporting either way from geometry.** If either view gains RIGHT-ALIGNED card-less content, re-run that check.
- **A test DOM stub gained the `dataset` a real `<body>` always has, rather than production gaining `if (document.body)`.** A branch that can never be false in a browser is a worse artefact than an incomplete stub.

### ⚠ A POPUP RENDERING BEHIND THE PAGE IS A STACKING CONTEXT, NOT A z-index VALUE (2026-08-16)
**"Raise the z-index" is the obvious fix and it does nothing here.** The date picker's panel rendered behind the page content despite `z-index: 60`.
- **THE NON-OBVIOUS CAUSE: `.fade-in` is `animation: fadeIn … both`, and its keyframes animate `transform`.** With `fill-mode: both` the final `transform: translateY(0)` **persists forever** — measured on a settled page as `matrix(1, 0, 0, 1, 0, 4)`. **Any non-`none` transform creates a stacking context**, so EVERY `.page-header` and EVERY `.section` on this dashboard is its own stacking context at `z-index: auto`. The panel's z-index is therefore scoped INSIDE the header's context and can never rise above a later sibling, at any value.
- **THE FIX is on the ANCESTOR, not the popup:** give `.page-header` `position: relative; z-index: 50` so the whole context sits above the sections that follow it.
- **⚠ `document.elementFromPoint` NAMED THE REAL OCCLUDER IN SECONDS, AND BOTH PRIOR GUESSES WERE WRONG.** The hypothesis on both sides was the Chart.js canvas; the actual occluder was the **glance stat card**. Fixing the canvas would have changed nothing. **When something renders behind something else, ask the browser what is on top — do not reason about it from the CSS.** The same probe doubles as the acceptance test: iterate the popup's own cells and assert `elementFromPoint(centre) === cell`, which tests "readable and clickable" rather than "the CSS looks right".
- **CHECK EVERY SURFACE THAT MOUNTS THE COMPONENT.** Three views carry a picker; the bug was reported on one. Verified 42 day cells on each of Team, Coaching and Calls, with the panel confirmed to genuinely OVERLAP the graph — otherwise a layout that happens not to overlap passes as a fix.

### ⚠ SLICING SOURCE IN A TEST: PASS `fromIndex`, AND ASSERT THE LENGTH (four instances by 2026-08-16)
Tests here extract a function from `dashboard.html` by slicing between two markers. **Written the obvious way, the slice silently runs backwards and the test examines an EMPTY STRING** — then passes or fails for reasons unrelated to the code.

**THE MECHANICAL RULE — apply it every time, do not try to remember whether this case is safe:**
```js
const at = HTML.indexOf('function theThing');
const fn = HTML.slice(at, HTML.indexOf('END_MARKER', at));   // ← fromIndex, always
assert.ok(fn.length > 200 && fn.length < 4000, 'slice must cover the function: ' + fn.length);
```
- **`indexOf(end)` without `fromIndex` finds the FIRST occurrence in the whole file**, which is frequently before the function you are slicing. `HTML.slice(bigger, smaller)` returns `''`.
- **The length assertion is the load-bearing half.** It is what makes a backwards or truncated slice fail loudly instead of quietly testing nothing — a negative assertion (`assert.ok(!/x/.test(fn))`) passes trivially against `''`.
- **Four occurrences so far**, all found by accident rather than by the tests failing usefully: the `drillCalls` slice (`setCallsRange` defined earlier), the `sortRepsWorstFirst` mirror guard (`repCardsHtml` defined earlier), the `renderSectionView` slice (`var delta` occurs earlier), and one caught in review. **Prefer markers that are unique to the function**, and when the end marker is a common token, `fromIndex` is not optional.
- Same family as the other rules here: **the thing you are testing may not be the thing that runs.**

### ⚠ WHEN A SHARED CARRIER CHANGES MEANING, THE RISK IS IN ITS *OTHER* READERS (2 instances, 2026-08-16)
**A "carrier" is anything several places read: a URL/hash, a cache key, a state field, a request payload, a DB column.** When a change alters **what the carrier holds or what it means**, the code that breaks is **not in the file being edited** — it is in every other reader that still assumes the old meaning. Nothing fails; each reader keeps doing exactly what it always did.

**THE RULE: after changing what a carrier holds, grep every OTHER reader of that carrier and re-derive its assumption. The edited file is the one place you can be confident about.**

**Instance 1 — `state.dateRange` changed meaning.** Stage 2 moved Team onto `state.teamRange`, so `state.dateRange` silently stopped meaning "everyone's range" and started meaning "coaching's range". `openBucketEvidence` was not touched and kept windowing **TEAM** bucket evidence by **coaching's** range. It agreed by accident while they shared a field and would have diverged the moment the two ranges differed — with nothing on screen saying so. Caught only because the split forced a re-read.

**Instance 2 — the URL hash gained a range.** Stage 3 put `from`/`to` in the hash. `init()` was not touched, and its `setDateRange(30, true)` kept overwriting whatever `applyHashToState()` had just restored — destroying every picked coaching range on every boot, data included.

**What makes this class expensive:** both were invisible. No exception, no failed test, no log line — the carrier still parsed, the readers still ran, and the output was merely *wrong*. **The edit that causes it and the code that breaks are in different files, so review of the diff cannot find it.**

### 👁 OBSERVATION, NOT YET A RULE — "STALE LOAD-BEARING COMMENT" (1 instance, 2026-08-16)
**Logged so a SECOND instance can be recognised as a second instance.** One occurrence is not a pattern and this must not be treated as one — but the shape is specific enough to name now.

**THE SHAPE:** a comment that was **accurate when written**, states an assumption the code beside it **depends on**, and is **invalidated by a change in a DIFFERENT file** — so nothing ever prompts a re-read of it.

**The instance.** `init()` in `dashboard.html` read:
```js
// Restore the view from the URL hash BEFORE the first load ...
// the 30-day range applies underneath.
applyHashToState();
...
setDateRange(30, true /* preserveView — keep the hash-restored view */);
```
Correct for months: the hash carried a **view** and no range, so seeding a 30-day default underneath was right. Then the date-picker arc (stage 3) put **from/to in the hash** — a change to the picker, the router and the hash helpers, **none of them this file**. From that moment the comment was false and the line silently destroyed every picked coaching range on every boot. Nothing failed, nothing warned, and no test covered the boot sequence.

**Why it evades the existing rules.** It is not a dead call site, not a stale marker, not a wrong shape — the code *ran*, and did exactly what it said. **The defect was the assumption, and the assumption lived in prose.**

**⚠ THE COMMENT SHAPE AND THE CARRIER RULE ARE DIFFERENT ABSTRACTIONS OVER THE SAME EVENT, AND THEY ARE AT DIFFERENT INSTANCE COUNTS.** The carrier is the MECHANISM; the stale comment is one SYMPTOM of how that mechanism evades review. Counting them together would have hidden that the mechanism already has two instances. **Stale-load-bearing-comment: 1 instance. Shared-carrier: 2 — see the rule directly below, which is promoted on that basis.**

### ⚠⚠ THE DATE PICKER DIDN'T STICK — A CONTROL THAT REBUILT ITSELF MID-GESTURE (fixed 2026-08-17, `8103f8e`)
**Symptom:** pick a range, the URL never changes, nothing commits. On both the coaching and Team pages.

**ROOT CAUSE, in order:**
1. `datePickerHover` re-rendered the ENTIRE control via `host.outerHTML = …` on every `mouseenter` — that was how the range preview followed the cursor.
2. Moving the mouse from the start date to the end date therefore **replaced the node under the cursor**.
3. The user's second `mousedown` landed on a node the re-render had just **detached**.
4. The page-level outside-click handler asked `host.contains(e.target)`, got `false` for a detached node, concluded **"outside"**, and called `datePickerClose` — wiping the half-made selection and shutting the panel.
5. So the second click never reached `datePickerPick`. Nothing committed; the URL never moved.

**THE FIX:** the preview toggles classes on the cells already in the DOM (`paintRangePreview`, reusing `monthGridCells` so the preview logic keeps one definition), and the first click paints in place instead of re-rendering. Belt and braces: the outside handler ignores targets with `isConnected === false` and uses the panel's bounding rect as a second authority. **A control must not rebuild its own DOM mid-gesture — every assumption about node identity dies the moment it does.**

**⚠⚠ THE LESSON, AND IT INVALIDATED EVERY TEST THIS CONTROL HAD: `element.click()` dispatches a click with NO mousedown, NO mouseup and NO movement. It tests the HANDLER, not the CONTROL.** Every programmatic test passed for months while the control was unusable by a human, because the failure lives entirely in the parts a synthetic click skips. **A control's tests must drive it the way a user does — real events, including movement.**

**⚠ TWO FIXES SHIPPED AGAINST THIS SYMPTOM THAT WERE NOT THIS BUG.** Both were found by reasoning about state, because the harness could not click:
- **The coaching `init()` clobber** (`7af613e`) — `applyHashToState()` restored the range and the very next line overwrote it with the 30-day default. **Real, worth having, shipped.**
- **The team sub-page hash gap** — `#team-recs`, `#team-needs-work`, `#team-members` carry no range, so refreshing on one drops to the 7-day default. **Real, reproducible, and still unfixed at the time this was written.**

**HOW IT WAS FINALLY PROVEN:** not by this harness. `backend/scripts/try-picker-fix.js` serves the one patched file locally and proxies everything else to production, so Justin exercised the real gesture with zero deploy and confirmed it. **When a control run is load-bearing and the harness cannot deliver clicks, hand the gesture to a human** — see the input-path constraint directly below.

### ⚠⚠ SYNTHETIC CLICKS ARE DELIVERED ONLY BRIEFLY AFTER A FRESH NAVIGATION — A STANDING LIMIT (3 investigations corrupted, 2026-08-17)
**Browser-automation clicks land reliably for a short window after a page loads, then silently stop being delivered.** The tool still reports `Clicked at (x, y)`; the page receives nothing. This is not a Scout problem and **cannot be fixed from here**.
- **Observed:** runs early in a page's life recorded 1 and 3 `mousedown` events and behaved correctly; a run issued after a later navigation recorded **0 events across 3 clicks** while reporting success. It has now corrupted **three separate investigations** of the date-picker bug.
- **THE RULE: every conclusion drawn from clicking must be paired with a DELIVERY COUNT.** Install `document.addEventListener('mousedown', …, true)` and report the count beside the result. **A zero-delivery run is unusable evidence in BOTH directions** — it neither confirms nor refutes anything, and must be discarded rather than interpreted.
- **⚠ THE ASYMMETRY THAT MATTERS: a passing run and a failing run are not equally trustworthy.** "It worked" with clicks delivered is real. "It failed" may only mean the clicks never arrived. A fix that appears to work twice **is not proven without a failing control**, and if the control cannot be delivered, the honest statement is *"works under the conditions I could test; necessity unproven"*.
- **✅ WHAT SURVIVES THE DEAD INPUT PATH — RELOAD EVIDENCE IS STILL TRUSTWORTHY.** The constraint above kills conclusions drawn from *clicking*; it does **not** kill verification generally. **A page reload IS the user action for anything about restoring state, and it needs no synthetic click:** trigger it with `location.reload()` or a fresh navigation, then read the URL and the restored state directly. Both are directly observable and neither depends on an event being delivered.
  - **The class this still proves:** anything about **boot, routing, the URL/hash, restored state, cache keys, or what a fresh load fetches**. The team sub-page range gap (2026-08-17) was fully verified this way — a real reload from `#team-members?from=2026-06-03&to=2026-06-19` came back on that view with the range intact — **in the same session where a pick attempt recorded ZERO clicks delivered.**
  - **The class it does NOT prove:** anything requiring a *gesture* — mid-drag behaviour, hover, mousedown/mouseup ordering, focus following a pointer. That is where a human is still required.
  - **State which one you used.** "Verified by reload" and "verified by clicking, N events delivered" are different strengths of evidence and should not be reported as the same thing.
- **WHERE A CONTROL RUN IS LOAD-BEARING, HAND THE GESTURE TO A HUMAN.** Justin can click reliably; this harness cannot. Spending a third session fighting the input path is worse than a five-second manual check. `backend/scripts/try-picker-fix.js` is the pattern: serve the one patched file locally and proxy everything else to production, so a human can exercise a change with zero deploy.

### ⚠ VERIFY AGAINST THE REAL ENTRY POINT AND THE REAL DATA SHAPE (2026-08-16)
**A test that drives the wrong function, or feeds an invented fixture shape, passes or fails for reasons unrelated to the code under test.** Both faults appeared in one block (10e) and both would have reported success while exercising nothing:
- **WRONG ENTRY POINT.** The harness drove `renderTeamView` while the feature renders in `renderOverview`. Zero charts were built; the assertion failed, but for a reason that had nothing to do with the feature. Had the assertion been a negative one ("this must NOT appear"), it would have PASSED — vacuously.
- **INVENTED FIXTURE SHAPE.** `analytics2` was written from memory rather than copied from `lib/session-analytics`. The real render threw `Cannot read properties of undefined (reading 'analyzed')` before ever reaching the code under test.
- **THE RULE: take the entry point and the data shape FROM THE SOURCE, not from memory.** Copy the empty-shape literal out of the lib that produces it; call the function the app actually calls. If a test needs a fixture, derive it from the producer rather than approximating it.
- **This is the same family as** the deploy-marker rule, the hash-navigation rule, the dead-call-site rule and the slice-boundary faults: **the thing you are testing may not be the thing that runs.** Every instance so far has been caught by exercising the real path — never by reading the code.

### ⚠⚠ A VERIFIER MUST BIND THE CLAIM TO THE EVIDENCE, NOT MERELY TO ITS PRESENCE (2026-08-16)
**A check of the form "is there a claim where there is no evidence?" passes ANYTHING once evidence exists.** That is a hole with a specific shape, and it is the most valuable finding of the 10d block.
- **The live case:** 10d's `verifyWhySentence` refused a causal claim when the rep had no tier-2 evidence. Josh HAS tier-2 evidence, so the check did not run — and the fabricated cause *"because he rushes past rapport"*, which has nothing to do with the counted area (`income goal and motivation`), **was accepted**. The guard was satisfied by the mere existence of *a* cause rather than by the sentence stating *the counted* one.
- **THE RULE: verify that the claim MATCHES the evidence, not that evidence exists.** The fix is one line in shape — a causal claim must name the counted area — but the class is general: any "we have proof for this kind of statement" gate must check the statement against the specific proof, or it degrades into a presence check.
- **⚠ FIXTURES STRUCTURALLY CANNOT FIND THIS.** The guarded path only exists on data that qualifies, and the qualifying tier exists for **one rep in eight**. It was found by running the guards against LIVE facts — the same reason the in-situ discipline exists for Phase 7b and the quote locator. **When a guard has tiers, exercise the tier that only real data produces.**
- Related, same family: the [deploy-marker] and [hash-navigation] rules — *the thing you are testing may not be the thing that is loaded*. Here: the branch you are testing may not be the branch that runs.

### MANAGER REP CARDS + "WHY" PROSE (10c/10d) — rulings 2026-08-16
- **⚠⚠ THE CAUSE CLAUSE IS COUNTED, NOT INFERRED. THE MODEL IS NEVER ASKED WHY.** Justin's worked example — *"John scores low on discovery because he isn't challenging prospects enough"* — contains a judgement no number produces, and "let the model explain why" is exactly the drift this ruling guards against. It does not have to be inferred: **7d's `what_mattered` already stores, per call, the highest-priority UNCOVERED discovery area, verified at write time** (the area exists for that rep, was marked uncovered, and the prospect's quote reconstructs). **JS counts the modal area; the model only turns the count into a sentence.** Measured on Josh: `income_goal_and_motivation` on **93 of 130** calls. The output sentence is *"Discovery is his weakest section at 51. On 93 of his last 130 calls the ground he left uncovered was the prospect's income goal and motivation."*
- **TWO TIERS, because the structured cause exists for ONE REP IN EIGHT.** Coaching areas derive only from a rep's own offer/criteria/script (the standing asymmetry already recorded for 8c/8d): Josh has 130 `what_mattered` rows, the three demo reps have **zero coverage maps at all**. So — **TIER 1 is numbers only and is always available**; **TIER 2 adds the counted cause** and appears only when the modal area covers **≥40% of that rep's calls that have a `what_mattered`, with ≥10 such calls**. The model is handed the modal area and its count, **or nothing**. It is never asked to supply a cause when the evidence is absent — that is the wrong-label-worse-than-none failure in a new costume.
- **⚠ NO INTENSIFIERS THE DATA DOES NOT CARRY.** *"Discovery is his weakest section"* is supported. *"Constantly scores low"* is NOT: **19 of 146 of Josh's calls (13%) are post-sale onboarding or mid-process follow-ups where discovery legitimately should not happen**, and excluding them discovery moves 47 → 51. The RANKING survives (still below pitch 65 and objection 64) so the weakest-section claim is safe, but the adverb would be describing call-type mix, not the rep.
- **TEAM COMPARISON THRESHOLD: state "lowest on the team" only when ≥3 reps have ≥6 objections in that category** (consistent with the existing `MIN_BUCKET = 6`); otherwise show the rep's own rate with its counts and no ranking. Measured: only ONE rep currently clears any threshold (josh fear 47 / timing 42 / partner 27 / logistical 16; everyone else 1-5), and **a ranking off n=1 reads as a finding and means nothing.**
- **CARD SORT: closing rate ASCENDING, reps with NO prospects LAST.** A rep with zero prospects is not worst, they are unmeasured.
- **⚠ THE CARD MUST READ `close_score_earned`, NOT `close_score`.** Migration 027 forces the DISPLAYED close score to 100 on closed calls; the section drilldown already reads the earned column for that reason, but `lib/team-analytics.js` selects the inflated one. On live data this flips a weakest-section result at the margin (demo-ben ties under one column and not the other).
  - **⚠ QUOTE THE MEASURED MAGNITUDE, NOT THE SHORTHAND (2026-08-17).** This was being described in review as *"displayed makes close 2nd BEST, earned makes it 2nd WORST"*. **Measured on Josh's 153 analysed calls it is an EIGHT-POINT score swing — earned 57 (rank 3 of 5) vs displayed 65 (rank 4 of 5) — one rank position, not a leap from one end to the other.** The shorthand overstated a real effect, which is its own kind of wrong: it invites someone to check, find a one-position shift, and conclude the whole warning is overblown. **The eight points are what matters** — they are the difference between close reading as level with `intro` and close reading as level with the two strongest sections.

## Preferences
- **Process rule — reporting & artifacts (standing, 2026-07-24).** Full work-block reports go to `~/Desktop/scout-findings.md`, **OVERWRITTEN each block** so it always holds exactly the current block's report (recon findings, diff summary, self-review, verification output, open questions) — the architect (Justin) reads that file directly via file access. **Chat pastes of long reports are RETIRED**: keep chat to a two-line status pointing at the file. History lives in git + CLAUDE.md, not in the findings file. Evidence artifacts (SAST/DAST scan reports, etc.) live in `~/Desktop/scan-reports/`. One-off architect handoffs are named `.md` files on `~/Desktop`. **Back-to-back blocks (2026-07-24 refinement): the findings file must HOLD an UN-REVIEWED block's report until the architect has read it — only a REVIEWED block's report may be overwritten. If a second block completes before the first is reviewed, do NOT overwrite; keep the un-reviewed report (append the new block above/below it, or wait) so nothing un-reviewed is lost.**
- **⚠ STANDING CONVENTION — QUOTE ATTRIBUTION REQUIRES FULL RECONSTRUCTION (2026-08-11).** Any code that decides *"who said this quote?"* — backfills, verification passes, closer-side filters, future analytics — MUST use `lib/quote-locate.js` and obey its contract. **Never write a second, looser locator.**
  - **Anchor on the quote's OPENING words**, then walk forward through **CONSECUTIVE turns** until the **whole quote is reconstructed**. The speaker who opens the quote is the attributee.
  - **REFUSE if it cannot be reconstructed.** Refusal is the expected outcome, not a failure: on the live corpus **526 of 1,200 highlights and 63 of 111 harvested KB moments are not reconstructible** (the extractor stitches and lightly paraphrases). Flag them `speaker_verified=false`; never fall back to a guess.
  - **REFUSE on ambiguity** — if two DISTINCT speakers can each reconstruct the quote, refuse. Live case: the closer and the prospect both say *"I don't care if you're a multi-millionaire"* on the same call; first-wins would decide that on a coin flip.
  - **NEVER accept a substring match in either direction**, and above all never *"the turn's whole text sits inside the quote."* That is the specific shortcut that caused the damage below.
  - Anchor length adapts 8→4 words. Shortening is safe **ONLY** because full reconstruction is still required — a quote beginning mid-turn (*"…but my dad's got cancer."* + *"I just found out."*) has no single turn holding its first 8 words, and a fixed anchor refused that real CLOSER moment. **The reconstruction requirement, not the anchor length, is what makes this sound** — do not keep the short anchor while relaxing reconstruction.
  - **WHY THIS IS A STANDING RULE — it already shipped wrong once.** The either-direction locator fired on **16 of 17** KB repair rows (not one matched strictly) and returned the FIRST such turn in transcript order — routinely an unrelated short turn by the other speaker. **5 of the 10 independently re-derivable rows were labelled WRONG**, and the pass was reported as "17 repaired, 0 misattributed". It also corrupted two corpus measurements that were reported as fact: the highlight error rate (**reported 8%, actually 3%**) and KB misattribution (**reported 18%, actually 6% of provable rows**).
  - **HOW IT WAS CAUGHT — the diagnostic worth reusing.** Not by code review. By asking a model to describe the moment from the transcript window around the located turn: it kept replying *"that line does not appear in the excerpt."* **A locator that cannot show you its own evidence in context is lying to you.** When attribution logic is suspect, make it produce the surrounding turns and check the quote is actually there.
  - Governing principle (shared with prospect names): **a WRONG attribution is worse than NO attribution.** It files the prospect's words as the closer's own winning material and reads plausibly enough that nobody questions it.
- **Process rule — no push without approval (standing, 2026-07-22).** Never `git push` sales-overlay `main` without explicit approval. Commit locally, report to the findings file, and WAIT at every stop point. Pushes auto-deploy to Railway (production).
- **Process rule — NEVER push during an analysis drain (standing, 2026-08-01).** A `git push` to `main` auto-redeploys Railway, and a redeploy **kills the in-flight fire-and-forget analysis drain** (`/fathom/sync`, `/fathom/reanalyze`, `/fathom/update-analyses`, `/zoom/sync`) — the loop isn't durable across a dyno restart, so remaining calls are left stranded (`pending`, sometimes a row stuck at `processing`). Before pushing, confirm no drain is running (dashboard shows no "analyzing…"/pending batch; or check `call_analyses.status='pending'`/`'processing'` for the affected user). If a backfill/re-analysis is needed alongside a code change: **push the code FIRST, let the deploy settle, THEN trigger the drain** — never the reverse, and never a push mid-drain. **⚠ HOW TO RUN THE CHECK (added 2026-08-03 after the rule failed a THIRD time):** the drain check MUST be **its own command whose output you read before deciding**. On 2026-08-03 the check was `&&`-chained to the `git push` in a single command — `supabase db query "…processing…" && git push` — so the result (`processing = 1`, last analysis **25 seconds** old) came back only AFTER the push had already gone. **The rule was already written, was known, and still failed, because a check chained to the action it gates is not a check — it is narration.** No damage that time (the in-flight call completed in ~44s before the container swapped; 0 rows stranded), but the safety came from luck, not from the process. Never `&&` a gate to the thing it gates; run it, read it, then decide. **Incidents: this has cost a run twice** — the 2026-07-20 deploy-race (re-analysis batch graded under the outgoing prompt version) and the 2026-07-31 Call-Review backfill (stalled at 38/101 when a Part-1b API-fix push redeployed mid-drain). Pairs with the deploy-race mitigation already logged in Session Learnings.
- **Process rule — `/health` 200 does NOT mean your deploy landed; poll a NEW MARKER (standing, 2026-08-03).** Railway does a **rolling** deploy: the OLD container keeps serving until the new one is ready, so `GET /health` returns 200 **the entire time** — including before your code exists on the server. A post-deploy check that only asserts 200 is asserting "some container is alive", which was already true before you pushed. **Always verify against a marker that ONLY the new build can produce**, and poll until it appears:
  - a **new route flipping 404 → 401/200** (e.g. after 2a, `PATCH /kb/:label/scope` returned 404 pre-deploy and 401 post-deploy — unambiguous, and 401 also proves the auth gate is wired);
  - **new markup/JS in the served page** (`curl -s https://www.scoutsystems.io/dashboard | grep -q '<newFunctionName>'`) — HTML is served `Cache-Control: no-cache`, so a miss is a real miss, not a cache artifact;
  - a **version probe** (`/fathom/status`.`prompt_version`) for grader bumps — **⚠ BUT SEE THE CAVEAT BELOW BEFORE PLANNING ON IT.**
  - **⚠ `/fathom/status` CANNOT be used as an unauthenticated marker (learned 2026-08-11, cost: 30 wasted polls).** It sits behind `requireAuth`, so an unauthenticated poll returns `{"error":"Missing auth token"}` **forever** — before, during, and after the deploy — while `/health` sits at 200 the whole time. It looks exactly like a deploy that never lands. It is only usable as a version probe from an **authenticated** session, which a headless verification run does not have. **For a grader-version bump, verify one of these instead:** (a) **the deployed container's own logs** — `railway logs -s sales-overlay` and look for `[analysis] start call … prompt_version=vNN`, which is the strongest proof available because it shows the SERVER running the new prompt on a real call; or (b) **a `call_analyses` row stamped with the new version whose analysis the server ran** (not one you invoked locally — check it was dispatched by sync/cron). On 2026-08-11 the v13 verification came from (a) after the `/fathom/status` route proved unpollable.
  - **The byte-length of the served page is a useful secondary signal.** During the 6d deploy the response held steady at 372,012 bytes for three polls and then jumped to 376,361 with the new function present — the old bundle was demonstrably still serving while health was green.
  **⚠ STRIP COMMENTS IN DEPLOY VERIFICATION TOO, NOT ONLY IN TESTS (2026-08-17).** This codebase archives removed code **in place** as `/* … */` per the standing convention — so a marker string very often survives inside a comment after the live code is gone. A `grep` over the served page then reports the wrong answer **in either direction**: a shipped removal looks un-shipped (observed: the Cash-collected tile checked as still present when it had gone), and worse, a marker that only ever existed in an archived block would report a change as shipped when it never was. **Pipe the served page through a comment stripper before matching**, exactly as the guard tests do:
```bash
curl -s https://www.scoutsystems.io/dashboard | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const live=s.replace(/\/\*[\s\S]*?\*\//g,"");console.log((live.match(/YOUR_MARKER/g)||[]).length);})'
```
The (d) deploy check read **163 → 0** across the boundary once stripped — unambiguous, where the raw grep had read `1 → 1`. **Prefer a marker that CANNOT appear in an archive** (a new function name, a new CSS custom property) over one that is merely absent from the render path.
- **⚠ THE TOKEN VARIABLE IS `RAILWAY_API_TOKEN`, NOT `RAILWAY_TOKEN` (cost 10 min of blind polling, 2026-08-15).** `API Keys.md` stores it as `RAILWAY_API_TOKEN`; the CLI expects it as `RAILWAY_TOKEN=<value>`. A `grep '^RAILWAY_TOKEN='` matches **nothing**, and an EMPTY token makes `railway status` fail in a way that looks exactly like a deploy that never lands — every poll returned `? ?` for ten minutes while the deploy had in fact already succeeded. **Echo the token length before polling.** Also note `railway status` takes **no `--service` flag**, and the deployment node sits at `.environments.edges[0].node.serviceInstances.edges[0].node.latestDeployment` (`status`, `meta.commitHash`) — walk the JSON rather than guessing the path.
- **⚠ SAME FAMILY, CLIENT SIDE — A HASH-ONLY NAVIGATION DOES NOT RELOAD THE DOCUMENT (2026-08-16).** Navigating from `/dashboard` to `/dashboard#team?from=…` changes only the fragment, so the browser makes **NO REQUEST AT ALL** and keeps running the previously loaded bundle. `Cache-Control: no-store` cannot help — there is nothing to cache-bust. During stage 2 this reported the new date picker as "missing" when it was simply not in the loaded document. **Diagnosed by probing for a symbol the new build introduces** (`typeof ensureTeamPicker` → `undefined`), and fixed by re-navigating with a **changed query string** (`/?v=2#team?…`) to force a real load. **THE GENERAL RULE, shared with the deploy-marker rule above: the thing you are testing may not be the thing that is loaded. Confirm the document/container actually contains the code under test BEFORE drawing any conclusion about behaviour** — on the server that means a deployment record whose COMMIT HASH matches (a stale deployment also reads `SUCCESS`, observed the same day), and in the browser it means probing for a new symbol.
- **Cost history:** this exact illusion burned **three rebuild/test cycles on the OAuth-popup bug**, where a fix that had never actually deployed looked deployed because health was green and the page loaded — the team debugged the code instead of the deploy. Observed again 2026-08-03 during the 2a push (first check showed the old bundle with health already 200). Pairs with the A-2.3 rule below: **200 is not verification, and neither is a page that renders — exercise the new element.**
- **Process rule — docs travel with the milestone.** CLAUDE.md must be updated in the **same session** as every shipped milestone — the docs commit lands alongside or immediately after the feature commits. And keep it honest: document what actually shipped vs. what's still pending (credit-gated, deferred, designed-not-built) — never mark a milestone "shipped" for features that don't exist yet.
- **Push back when warranted.** Justin doesn't want an agreeable AI. If an idea has a flaw — logical, technical, UX, strategic — call it out and explain why before implementing. If experience says something won't work, say so. Challenge, disagree, say no with reasoning. Only implement a user idea after confirming it actually makes sense. Never water down a concern to seem easier to work with. Justin is the expert on his business; Claude is the expert on the code and product patterns — push back from that expertise.
- Justin is not a developer — explain things simply, provide exact terminal commands
- Always use universal sales framework structure, never client-specific content
- Project lives in TWO places on Justin's Mac:
  - `~/Library/Mobile\ Documents/com~apple~CloudDocs/sales-overlay/` (iCloud canonical copy — `npm start` for dev)
  - `~/sales-overlay/` (local build dir — `npm run build` here for signed + notarized DMGs; watch for drift, especially in `build/`)
- **Signed + notarized build (use the script, not inline-paste):** `bash ~/sales-overlay/build-release.sh` — gitignored script (mode 0755, never commit) that wraps the build with the Apple creds on backslash-continued env-var prefix so they always reach electron-builder. Inline-pasting `APPLE_ID=... APPLE_APP_SPECIFIC_PASSWORD=... APPLE_TEAM_ID=... npm run build` into the terminal has silently failed 3+ times across the project history. If creds rotate, edit `~/sales-overlay/build-release.sh`. **Important:** the script reliably notarizes the `.app` but NOT the DMG container — every release currently needs manual DMG sign+notarize+staple (see Resolved Issues for the exact recovery commands). Until the electron-builder config gap is fixed, treat the DMG manual recovery as part of every release.
- Unsigned dev build fallback: `npm run build:unsigned` then `xattr -cr dist.nosync/mac-universal/Scout.app && codesign --force --deep --sign - dist.nosync/mac-universal/Scout.app`
- When making code changes, always run `node -c <file>` to syntax check before declaring done
- Push to GitHub: `git add <files> && git commit -m "message" && git push`
- **All secrets live in `API Keys.md` (gitignored). DO NOT paste keys into `CLAUDE.md`, `.env` that's tracked, or any other committed file** — GitHub push protection will block the push.
- **Build path (mandatory):** Always build Scout releases from `~/sales-overlay` (the local copy), never from the iCloud folder. The build command is `npm run build` run from `/Users/justinschmidt/sales-overlay`. iCloud's extended attributes cause codesign failures when building from the iCloud path. Claude must reference this rule before writing any build or release command. If the local copy is out of date, rsync from iCloud first (exclude node_modules and dist), then build. (this rule applies to npm run build only, not to normal development, code editing, or SQL operations)
- **v1.1.0 build plan:** Features built one at a time in this order: (1) Role system, (2) Role-based dashboard system on website (`/dashboard` for users + redesigned `/admin` for admins+owners with user management; the original raw log viewer remains as Sections B+C of the admin page), (3) Script upload in app, (4) Call boundary detection, (5) Post-call summary. Each feature fully committed and approved before the next starts. Justin receives one feature prompt at a time from the architect — Claude Code never plans ahead.
- **Session start:** cd into the project root at the start of every session: cd '/Users/justinschmidt/Library/Mobile Documents/com~apple~CloudDocs/sales-overlay'. Note: Supabase MCP is not currently configured — migrations must be run manually via the Supabase dashboard SQL editor. To enable Supabase MCP in future, add it to .mcp.json and start a fresh session.
