# Scout — standing rules

**This file is rules only.** Every rule that has reasoning behind it ends with an `H###` pointer into `SCOUT-HISTORY.md` (repo root) — the full record of rulings, incidents, measurements and worked examples, moved there verbatim on 2026-09-01 when this file hit 1.28M chars and stopped loading. To read the reasoning: `grep -n "\[H050\]" SCOUT-HISTORY.md`. **Never `@import` the history file** — it is read on demand only.

**The rule for what goes where:** if acting against it would be a mistake, it is a rule and lives here; if it explains why a rule exists, it is history. New rules are added here in one to three lines; their reasoning is appended to `SCOUT-HISTORY.md` with a new `H###` id (next free id: see the end of that file's table of contents). Keep this file well under 150,000 characters — check with `wc -c CLAUDE.md` after editing.

**Load check token** (so a session can prove this file loaded — see §3): `SCOUT-RULES-LOADED-2026-09-01`.

**The design system is listed once in `SCOUT-DESIGN.md`** (rules as pointers to this file, the token blocks and the guards — never restated there; H693). **The queue is `BUILD-LIST.md`** (repo root): open work, rulings needed, filed items. Update it after every push, alongside this file (H533).

---

## 1 · Who and what

- **Justin Schmidt** (justinschmidtsales@gmail.com) is building Scout as a personal side project (not for Net Revenue LLC). **Not a developer** — explain simply, give exact terminal commands, no external dev until revenue. Never use Net Revenue client content (SSI, GlobalBanks); universal sales-framework structure only. (H002–H004, H006)
- **Scout Systems** · scoutsystems.io · product name TBD ("Sales Overlay" is the working title). (H021)
- **The product today:** post-call intelligence for high-ticket closers. Fathom/Zoom recordings sync in, a grader + highlight extractor + coaching pass analyse each call, and managers/reps read the results at `/dashboard`. **The desktop overlay and teleprompter are DEAD (Justin, 2026-09-03: "we've completely removed the thought of adding another overlay and teleprompter") — not parked, not coming back; the orphaned code is listed in `BUILD-LIST.md` and stays until its removal is its own block.** (H031–H032, H116, H689)
- **Where the code lives:** iCloud copy `~/Library/Mobile Documents/com~apple~CloudDocs/sales-overlay/` is canonical for dev and commits; `~/sales-overlay/` is the local BUILD dir for signed DMGs only (rsync from iCloud first). `node_modules`/`dist` are `.nosync` symlinks. `sales-overlay/` nested folder is an old copy — ignore. (H023, H558)
- **Session start:** `cd` into the iCloud root. Supabase MCP is not configured; use the Supabase CLI (§7). (H558)

## 2 · How to work with Justin (process)

- **Push back when warranted.** He does not want an agreeable AI. Flaws (logical, technical, UX, strategic) get named with reasons before implementing; say no with reasoning; never water down a concern. He is the expert on his business, Claude on the code. (H558)
- **If the premise of an instruction is false, say the instruction should not proceed — do not footnote it and comply.** Refuse BEFORE an irreversible action. (H252)
- **Reports go to `~/Desktop/scout-findings.md`, overwritten per block, chat stays to a two-line status.** Hold an UN-REVIEWED block's report until Justin has read it; never overwrite one he has not seen. Evidence artefacts to `~/Desktop/scan-reports/`. (H558)
- **A report may only describe state that exists when it is written.** If the push has not happened, say so in the first line. **Commit the moment work is complete** (a commit deploys nothing); a blocked push is an escalation, not a wait. (H086)
- **Stop reporting cost.** Ask for approval BEFORE spending; never narrate prices after. Applies to Claude's own reports too. (H058)
- **Spend needs approval before it is spent** — dry-run then confirm; "do not run it, Justin rules." An approved spend item is a claim about the data: re-measure the target set right before running it, and confirm each row CAN succeed. (H058, H486)
- **When a request names something you cannot find, say so — never bind it to the nearest match.** (H188, H436)
- **A ruling has a scope: the property it names, not the object it touches.** Two rulings on one artefact rarely conflict; if they genuinely do, ask. (H130)
- **A new ruling that reverses an old one must be shown the old one.** Before implementing a definition change, grep this file and the history for the previous ruling on that definition. (H658)
- **When a ruling contradicts a request the same person made a block ago, state both readings and change nothing until asked.** (H560)
- **When a ruling lands, every place that states the question closes in the SAME edit** — this file, `BUILD-LIST.md`, the code comment. A stale open row gets re-asked; a stale "not ruled" note stops a block. (H069, H482, H489)
- **Before starting any queue row, verify the row is still true** — two minutes of grep/curl against half a block of work. "Is it wired" (call sites), not "does it exist". (H484)
- **A row may restate a finding, never strengthen it** ("unobserved" must not become "broken"). (H054)
- **Docs travel with the milestone**, in the same session; document what shipped vs what is pending, never mark shipped what does not exist. (H558)
- **Secrets live only in `API Keys.md`** (gitignored). Never in this file, `.env` that is tracked, or any committed file. `ANTHROPIC_API_KEY` in `.env` is STALE (401) — `API Keys.md` wins. (H558, H425)
- **Commit messages: `git commit -F - <<'MSG'`** (quoted heredoc) — backticks in a double-quoted `-m` execute. Never force-push to repair a message. (H381)
- **Syntax-check before declaring done: `node -c <file>`** — and know its limits (§6). Run the inline-script extraction for `dashboard.html`. (H558, H203)
- **Never enter credentials or mint a login session for another person's account.** When a boundary cannot be reached with legitimate credentials, forge the actor at the layer below the credential in a server-side test (`test/not-sales-http.test.js` pattern). (H355)
- **A fix and a cover-up look identical from outside — diagnose before extending, delaying or hiding anything.** (H210)
- **Report before wiring; a recon that returns "extend an existing thing" instead of "build a new one" is the point of reporting first.** (H400, H281)

## 3 · Git, deploy and the drain

- **A push deploys production** (`main` → Railway, ~60s). Push only verified work with no drain running (next rule) and verify the deployed commit afterwards. Justin has said not to ask for confirmation on routine commits and pushes (memory, later than the 2026-07-22 "no push without approval" rule in H558) — so do not ask, but never push mid-block unverified work, and never force-push `main` (five commits were lost that way once). (H558, H019)
- **NEVER push during an analysis drain.** A redeploy kills the fire-and-forget analyze loop (`/fathom/sync`, `/reanalyze`, `/update-analyses`, `/zoom/sync`) and strands rows. The drain check is **its own command whose output you read before deciding** — never `&&`-chained to the push. A drain is `processing > 0` OR `pending` falling across polls; a static `pending` across several polls is NOT a drain. Push code first, let it settle, then dispatch. (H558, H358)
- **A dispatched run cannot be recalled** — the only kill switch is a redeploy. Treat every dispatch as irreversible for the length of its list; drain → re-admit → re-fire, never "wind it back". (H220, H251)
- **Concurrency ceiling on grading loops: SIX.** Twelve rate-limited Fathom's transcript fetch and errored 153 calls. The saturation abort counts SATURATION (429/overloaded), never "any error". Never sacrifice data quality or completeness for speed; if a deadline cannot be met at a safe rate, say so. After any run: report errored count and confirm re-admission. (H250, H251, H155)
- **`/health` 200 does not mean your commit deployed.** Verify the DEPLOYED COMMIT HASH (`RAILWAY_TOKEN=<value of RAILWAY_API_TOKEN in API Keys.md> railway status`; walk `.environments.edges[0].node.serviceInstances.edges[0].node.latestDeployment`) and a marker only the new build produces; a served-page marker check prints RAW and COMMENT-STRIPPED counts side by side (this page is ~45% comment). `backend/scripts/verify-deploy.js` requires every marker to declare its artefact (page vs server). Poll loops that exhaust are FAILURES — escalate at the limit. Check status.railway.com before diagnosing a deploy failure; one retry, then wait for the all-clear. (H558, H217, H051, H059, H512)
- **`/fathom/status` cannot be an unauthenticated marker** (401 forever). For a grader bump use Railway logs (`[analysis] start call … prompt_version=vNN`) or a server-dispatched `call_analyses` row. (H558)
- **A hash-only navigation does not reload the document**; a stale tab and a failed deploy are indistinguishable to the user — diff the old bundle and name a visible difference. (H558, H567)
- **The cron is best-effort:** `0 */2 * * *` has never run on the hour (observed 1h40–2h25). Derive windows from `gh run list`, not the crontab. (H187)
- **`git log --since` filters on AUTHOR date.** When a window matters, print both dates; when a date-bounded search returns nothing, re-run unbounded on the artefact. (H499)
- **Data-ahead-of-code windows:** when a backfill must prove a rule before the rule ships, name the window and close it in the same session. (H093)
- **Backend tests:** `cd backend && npm test` (Node runner; use the quoted glob form, bare directory misbehaves under iCloud). (H042)
- **Releases: there are none.** The desktop app is dead (H689); the build chain (`~/sales-overlay`, `build-release.sh`, DMG notarization, `gh release`) is orphaned. The `electron` devDependency at the repo root STAYS — the rendered guards run on it. (H025, H029, H558 are history.)

## 4 · Product rulings — Justin's, do not re-litigate

### 4a · Metrics and numbers
- **One definition, one computation, every surface reads the same one.** Where surfaces legitimately differ, the difference is the WINDOW and the surface says which window it shows. Three surfaces showing three closing rates "looks like amateurs". Enforce with one shared computation plus a guard that fails if a surface computes the rate itself. (H450, H451, H452)
- **The window is part of the definition.** Team pages default to 30 days, matching Coaching; the gauges are a fixed 7 days and say so. A 7-day closing rate is mostly noise. (H659, H655)
- **Close rate = closed PROSPECTS ÷ ALL prospects** (open counts as not closed; follow-ups collapse into their prospect; a prospect is closed if ANY of their calls closed). Rates render with their counts beneath ("12 of 37 prospects"). Merges are proposals only, never automatic. A prospect leaves the denominator only when EVERY one of their calls was a no-show or a DQ (`hadAConversation`). (H020, H449, H454)
  - **RULED 2026-09-01: PROSPECTS STANDS.** The 2026-08-30 "calls taken, not prospects" remark was about the DATE RANGE and calls taken versus BOOKED, not the denominator; the contradiction was raised at the time and prospects was confirmed. Do not re-open. The legacy per-call `close_rate` field is rendered nowhere and should be deleted. (H020, H658, H453)
- **No volume suppression on a closing rate** — "I don't care if he takes 1 call, grade it." `MIN_BUCKET` guards a COMPARISON, never a count. (H660)
- **Pooled, never mean-of-rates**, so the counts printed beneath a rate reconcile with it. (H124)
- **Zero is a measurement, absence is not.** An unmeasured rep is named ("no offer price saved"), never plotted or averaged as zero. A filter on a nullable field has THREE groups and the third is printed with its count so the numbers reconcile. Any group nobody sees (e.g. `credited (call closed)`) gets its own visible bucket. (H189, H247, H319, H062)
  - **Exception surfaces (badges, alerts) show NOTHING at zero** — that is the surface's job, not a violation of the rule above. (H170, H483)
- **Cash collected is EOD-only.** Still extracted and stored (`call_analyses.cash_collected`, drives EOD), rendered on no other surface, never cited in findings or used as evidence. (H182)
- **Justin's definition (2026-09-02): "An objection is someone's reasoning for not being able to purchase. This only comes at the end of a call, after pitch and price drop."** The extractor already types pre-price doubt as `risk_signal` (v37 prompt). **The COUNT is ONE function — `lib/objection-strict.js countsAsObjection` — called at every site** (`team-analytics`, `routes/team.js`→`rep-series`, `team-objections`, `session-analytics`), each site selects `objection_class`, and the exclusions run BEFORE the denominator is incremented. Pinned per site by `test/objection-counting-carrier.test.js`, which executes each computation and was proven under both plants. **The price gate ("after the price") is RULED NOT BUILT (Justin, 2026-09-02) — do not re-litigate:** the extractor already requires the price to have been stated (390 objections in the close, one in discovery), so a gate is a second filter behind one already doing the job; and it would let a weaker signal overrule a stronger one — the price detector matches phrasing and is SILENT on calls it cannot read, while the extractor reads the whole conversation in context; a gate needs an answer on every call and absence is not an answer. **What would change his mind: a hand sample showing pre-price objections actually being counted** — a sampling question, not an architecture one. **The zero-model price backfill is DECLINED (Justin, 2026-09-02) — not run, not re-proposed; both this and the gate are CLOSED, not pending.** (H674, H675, H676)
- **Objection handle rate uses the STRICT standard:** disqualifications and logistical barriers are not coachable objections and are excluded, via the `objection_class` field the extractor writes at analysis time (v37); NULL (pre-v37) counts as an objection so history degrades in the direction it already had. "Handled" for RATES = `resolution='handled'` OR the call closed; for "was this a good moment" selection it is NEVER credited by the close. Guard is per call site (`test/handled-carrier.test.js`). (H378, H446, H447, H319, H138)
- **The handle rate will drift downward for weeks after a taxonomy change; that is the metric becoming truer.** Broken looks like: the tile blanks, the needs-work card sits at insufficient persistently, or the rate falls below ~20% and keeps going. (H106)
- **The manager is a team member on every team surface.** Use `lib/team-membership.js` (`withBoardOwner` / `membersByManager`), never a hand-rolled `managed_by` rule — nine copies have now been found wrong. `resolveTeam` returns `memberIds`. (H260, H544)
- **Direction is a declared property of a metric** (`direction`/`targetDirection`), never a comparison at a call site; a wrong direction has no wrong number so review cannot catch it. Mirror a threshold by distance, never by division. Avg call time is a CEILING (60 min max; caption "Max", "at or below"). (H125, H124)
- **Band metrics:** avg call time ideal 35–45 / acceptable 20–60; time to price 20–45 (lower edge measured, upper ruled). `lib/metric-band.js` is the one source; rows state their SIDE; reps inside the band tie and are listed, never ranked; bars are coloured by band. A ranked view needs a ruled direction; a number card does not. (H518, H521, H514)
- **Compare the underlying value, round only to render.** (H120)
- **Inactive/deleted users do not render as rows; the scope is untouched** (their numbers stay). Default is `active !== false`. (H062)
- **Synthetic rows are marked by id prefix** (`fathom_call_id LIKE 'seed-%'` / `'demo-%'`); `lib/real-calls.js realCallsOnly` on EVERY cross-user `fathom_calls` query (`.in('user_id',…)` is the tell). Never a user-level rule (the reviewer owns real calls too). Demo reps carry copies of Josh's calls: any count over "all rows" double-counts him. (H369, H388, H387, H546)
- **`not_a_sales_call` and `duplicate_of` exclusions are always PAIRED** (21 sites); the library shows the row with a badge, the counts exclude it. `countUserHistory` is deliberately unfiltered — it gates deletion, not a metric. Marking is retroactive: un-harvest KB moments and clear derived output when a call is marked; an automatic mark never overrides a human one (`not_sales_marked_by`). (H180, H350, H439, H469)
- **Scores are coloured only when they cross a band** (`--good/--mid/--bad`, ≥70/≥50); a colour that cannot vary is not a colour. `.review-why` keeps its outcome colour and left border — the one place semantic colour is correct. (H657, H650, H654)
- **EOD:** the Payment row is removed (outcome prefill composes "Closed - PIF/Payment plan/Financed"); edits win over analysis values; the view carries sync freshness ("still filling up") and complete means a sync ran after the day ended. (H037, H177)

### 4b · Objections, coaching, discovery
- **Canonical objection set is FIVE: Fear · Timing · Partner · Logistical · Other** (stored `uncategorized`). Price is NOT a category — it folds into fear or logistical. One source `lib/objection-categories.js`, mirrored inline in `dashboard.html` and guarded; ORDER is load-bearing. **Every lib file takes the stored set from `STORED_OBJECTION_CATEGORIES` — never a literal (seven copies removed 2026-09-02; a category line in a prompt is prompt text, so a change there bumps that lane's version).** (H082, H088, H090, H680)
- **The boundary, three-way, and the prospect's stated excuse is never the classification:** ask first whether they WANT it (a stated reason the offer does not apply = DQ, `disqualify_signal`); then whether they CAN buy — cannot afford = financial DQ (not an objection, not coachable); externally blocked = LOGISTICAL; able but hesitant, any wrapper = FEAR (the coachable one); must consult anyone = PARTNER. "Trust/proof/skepticism" = fear. (H082, H085, H398, H428, H465)
- **A true objection is post-price/post-ask resistance.** `risk_signal` = earlier doubt; `barrier` = external constraint. The decision test: can the prospect change it by deciding differently? Yes → objection, no → barrier. Questions are not automatically resistance. No precedence rule between types. Speaker-anchoring guard rejects (never retypes) a barrier/risk/objection on a closer line. (H092, H100)
- **Coaching is principles, not word tracks — for everything in sales, including Justin's own examples.** Strip the quoted line: if the advice still stands it was directional. Name what each answer tells the rep. Fear = find out what the fear is, then ask for commitment conditional on it; partner = warm, the coachable part is isolation and next step; logistical/money = direct. Never diminish the closer's work (no "but" after a count of closes). Never invent, paraphrase or intend anything about the PROSPECT; the observation outranks the raw quote. Never name the prospect in coaching. Never coach a rep out of isolating ("money aside" is a technique). Never explain WHY a technique works unless the team's own KB says so. Open with the outcome and its cause; assemble mechanical parts (timestamp, quote) in code, not in the prompt. They/them for the prospect. (H414, H410, H408, H420, H412, H525, H429, H424, H427, H430, H413, H415, H605, H417)
- **The definition of done on a coaching prompt includes Justin reading real output** — a judgement cannot be certified by scalars. Unattended coaching that invents, names a prospect, leaks internals or coaches out of correct technique is STOP-AND-REPORT. (H423, H396)
- **Nothing infers a call-level fact from a moment-level field** ("you lost the sale" from `resolution='unhandled'`). (H604)
- **Fine Tune Coaching (FINISHED 2026-09-02; surfaces ① the Call Review moment rows, ② the Team → Coaching insights, ③ the Objections "Why" evidence moments — ONE `fineTuneCoaching(target)` helper, three callers, `given_on.surface` from the closed set `call_review_moment | team_coaching_insight | team_objections_why`; insights and Why evidence carry `highlight_id` + call id, each payload change bumped its lane version; "Noted ✓" survives a reload through ONE team-scoped `POST /kb/noted-moments` per page, keyed on the ids rendered — never a query per moment): a manager's correction becomes a `coaching_correction` row in the TEAM knowledge base** (`lib/coaching-corrections.js`; `scope='team'`, `team_owner_id`, `uploaded_by` = the manager, `content` = the concept, `metadata.given_on` = call + moment + rep, `concept_hash` for dedupe). Confirm before storing; the manager's wording wins; a failed extraction stores their words verbatim and says so. ONE extraction call on lane `fine-tune-coaching`. The coaching pass (7c) reads the team's notes oldest-first as heavily weighted examples that OUTRANK defaults but are not hard rules, and writes `coaching_applied_notes` on the highlight (migration 061) — the checkable field. **The grader and the extractor never see a note** (both selling-context filters; `test/fine-tune-coaching.test.js`). Reps read them on the KB page as "Team coaching notes". **Three coaching syntheses read the notes too** — Team Recommendations, the Objections "Why", the Performance Summary — each folding the notes hash into its cache key and rendering the ONE `promptLane()` wording (five lanes, one text; `loadCorrectionsSafe` never fails a page). **Needs-work deliberately does not:** its only model call classifies phrases into buckets, which sets a RATE — a note there would let a manager move a metric (substitution, not suppression; guarded per lane). The aggregate surfaces — digest, Coach Summary tile, section-rank card, Performance Summary — stay OFF deliberately: no single moment behind them. (H708, H709)
- **`why_outcome` never names the prospect (v34)**; observation timestamps render `[HH:MM:SS]`, never raw seconds. The synthesis evidence id is validated against the reps the claim names (`evidenceMismatch`), and a mismatch drops the evidence. "Fine-tune coaching" (a manager correction stored as a concept in the KB) is filed as a MAJOR — the durable fix for prompt-level coaching defects. (H397, H424, H641, H639)
- **Any change to what a synthesis STORES (prompt, caps, payload shape) bumps that lane's version INSIDE its cache key in the same commit** — otherwise it ships nothing. The tone rule is one module shared by four lanes. (H442, H630, H625, H525)
- **Closer-side lanes require `closer_response_verified === true`** — never null, never the sentinel. `__no_reply__` and `__moment_is_closer__` are results, not nulls; `displayCloserResponse`/`provenCloserResponse` gate them. (H078, H611, H109)
- **Quote attribution = full reconstruction via `lib/quote-locate.js`; refuse rather than guess.** (H558)
- **Six discovery items are fixed areas in `coverage`** (PAIN, GOALS, CURRENT_SITUATION, DECISION_MAKERS, WHY_NOW, FINANCIAL_RESOURCES) plus derived ones; pain has a logical-sale caveat; evidence verbatim from one line, verified at write time, NULL rather than a guess, drives no score. Justin's definitions: pain = current situation misaligned with goals; discovery exists to DQ or close, both correct. (H399, H404, H419, H418)
- **`coverage` keeps `covered:true` and withholds an unprovable quote; `qualification_covered` flips to false.** Different on purpose — one drives coaching, one drives nothing. (H107)
- **Qualification check:** three verdicts (`undetermined` is common and honest), one-way downgrade, prospect-spoken evidence only, criteria are free text per company — never a hardcoded threshold. (H084)
- **Grader noise: ±14 per section, ~6 overall between rounds, some sections bimodal.** Validate prompt changes on a CHECKABLE field (reconstruction rate, structured yes/no), never by score deltas smaller than that; gate on the field the change targets; state the noise floor beside every delta; choose samples that exercise the change; run the control arm to n≥6 before calling a flip attributable; stop at two attempts — a third rewording is tuning. (H045, H406, H462, H401, H458, H405)
- **Outcome and section grades come from ONE grader pass, so any criterion change can move outcomes.** Watch `lost:follow_up` (baseline 0.137 at v32). (H402, H404)
- **Prompt edits ship with `ANALYSIS_PROMPT_VERSION` in the same commit; new calls only; never mass re-analyse; a backfill is a spend decision.** Two token caps: grader 4500, extractor 3000 — measure the six longest calls BY TURN COUNT before adding a field. One transcript sent twice is refused on quality; do not merge the calls. (H042, H104, H248, H044)
- **Manual outcomes are frozen by SOURCE (`manualLocked`)**; `disqualified` is manual-only and counts as work but leaves every rate. (H454)
- **Prospect names:** a wrong name is worse than no name → NULL/"Unknown prospect"; couples are one prospect; precedence grader → diarized → cleaned title → NULL; never assume the closer talks most; agreeing sources keep the fuller name; raw titles stay on scanning surfaces (objections feed, digest). (H020)
- **Speaker identity is exact equality** (Fathom invitee email; Zoom display name) — no name fallback, no talk-time heuristic. `speaker_verified` is three-valued with no default. (H109, H143, H195)
- **KB rulings:** harvested material never reaches the grader (two independent filters, `learned_pattern` + `call_moment`); managed rep's personal KB stays out of their own grading; harvest every call (the closed-only gate is superseded), quality via the proven-closer-line bar; ~2 per section cap on auto-harvest, none on manual; manager Add-to-KB is the promotion path and lives in the SHARED row renderer, role-gated; rep button removed; `POST /kb/from-highlight` stays mounted because its tests pin ruling 1; harvest writes to team scope must be readable by reps (shared predicate `lib/kb-scope.js`). Embedding: batched and paged, never per-item; seed scripts must embed or refuse; provider ceiling is a paid-tier property. (H037, H038, H282, H456, H281, H472, H312, H315)
- **Selling context budget lanes** (`lib/selling-budget.js`) and **two different bars** for profile material (`usableProfileField` ≥15 chars for context; `usableMaterial` far higher for a rubric). Do not unify. Coaching-area keys are join keys; re-derivations are shown existing keys. (H046, H108, H047)
- **Held on DATA, not unfinished — do not build them to "finish" the work:** the barrier→uncovered-ground causal link (needs a meaningful share of calls with 2+ uncovered areas), a role-inversion threshold (n=2 is not a sample), the confronts-vs-steers coaching wording; the 9a missed-cue pairing lives in the worker with NO model decline step by ruling; the LLM residue tier for the one unresolvable prospect name is skipped by ruling. (H105, H047, H098, H099, H020)

### 4c · Users, companies, recording sources
- **Deactivate leaves the numbers; delete deletes** — calls, analyses, prospects, KB personal rows — via `lib/user-purge.js` (one path for users and companies), owner-only at the ROUTE. Global KB rows survive. The pre-ruling tombstones (3 users, 117 calls) are Justin's decision, not a tidy-up. (H221, H070, H176)
- **Moving a rep does NOT move their history** — calls stay with the company where earned (needs a team stamp at call creation; filed). (H070, H259)
- **A company = a manager with reps OR an explicitly named head; role is never the key.** Name lives in `user_profiles.team_name` on the head's row; fallback "Unnamed company", never derived. "Company" in admin, "team" everywhere else. Head renders a static label, not a dropdown. Company-by-name picker needs the Director role first; visibility = every level sees below it and nothing beside it. (H161, H162, H168, H169, H171, H160)
- **The offer axis** (what a closer sells for) is independent of the org tree — filed, not a priority. (H159)
- **Every grade cites a quoted transcript moment; low-confidence transcripts are flagged, never hidden behind a confident grade; CRM writes (when built) are diff-then-confirm, never silent.** (H114, H113)
- **One active recording source per user**; a deliberate Switch-provider action; cross-provider duplicates matched by time overlap (never `meeting_id` or duration), suppressed via `duplicate_of`, never deleted. (H143, H178, H179)
- **Fathom identity:** no `/me`; candidates = paged `/team_members` ∪ `recorded_by` on `/meetings`, matched by EXACT equality to the Scout login, ambiguity refuses, picker is the fallback; `recorded_by` is stamped on new rows and audited read-only; disconnect never deletes calls. Never send an epoch `created_after`; first sync backdates 30 days explicitly (Zoom too). (H165, H606, H608, H609, H610, H041, H166, H175)
- **Zoom:** grades equivalently to Fathom; cloud recording + transcription must be ON (the sync-result copy already says so — do not add a second notice); 3301 requeue is live; one-speaker collapsed transcripts are refused before grading (`not_a_sales_call` + `exclusion_reason`); clip extraction is unbuilt and the privacy policy wrongly says otherwise. Zoom creds exist only on Railway. (H142, H158, H471, H545, H185, H485)
- **Grading controls:** first sync auto-grades the newest 20 only; steady-state grades everything; all-time is OWNER-only (403, never truncate), 30 days for everyone else; the reset excludes live claims; **EVERY grading act is OWNER-ONLY — self-serve, cross-user, reanalyze, every window** (Justin, 2026-09-02, going past the same-day admin-only ruling: "no I DON'T want closers or anyone but admins be able to touch reanalyze"). One gate `ownerOnlyGrading` called first at every route; the all-time cap inside the runner was REMOVED as dead (every caller is already an owner). The control is off the page for everyone else, a rep's own Calls page included, and the count line says `N not graded yet — grading is handled by an admin` (Calls, Connections, Get Started). Never a count with no statement of who can act (H706, H707); the control sits where the problem is named (Calls page + Connections) and the onboarding card must not vanish while a backlog remains; ungraded, retryable-failed and permanently-failed are three separate counts. (H037, H263, H060, H237, H262, H153, H254)
- **A backfill/history pull must not inherit steady-state grading** (pass `lastSyncAt=null` so the first-sync cap applies); pulling calls and grading calls are separate budgets. An upstream default (Zoom's 30-day window) is not a decision — state it locally even when it matches. (H167, H175)
- **Support:** submit is universal, the list is owner-only, the snapshot is captured at raise time and the ticket lands even if the snapshot fails; reference is derived hex; no in-app replies until a notification system is built deliberately; attachments private, 5 MB, images, path ownership checked. Health snapshot has NO UI. (H236, H222, H156, H229, H231)
- **`canMarkNotSalesCall` and `canTagOutcome` deliberately differ:** a managed rep may mark their own call not-a-sales-call (it cannot flatter them) but may not tag their own outcome (inflatable). Do not unify. (H352)
- **The daily digest is a day behind by design** (one model call per manager per day on the cron); legal/liability content is suppressed everywhere. (H261, H070)
- **Accounts:** public self-signup is CLOSED (403); creation is manual/invite via `provisionUser` (atomic, reclaims orphans); the 6 abandoned auth rows and the orphans are left alone by ruling; welcome emails go through Resend; forced password change and self-serve reset are still owed. **No organisation entity is needed for per-user customisation** (ruled 2026-08-20) — billing alone still wants an account entity. (H019, H118, H037)
- **Manual `confirm()`/`prompt()` are now `scoutConfirm`/`scoutPrompt` — every call must be `await`ed** (a promise is always truthy). (H283)
- **Role labels are RULED (2026-09-02): `user` → Rep · `manager` → Manager · `owner` → Admin.** The stored values do not change — never rename them in the database. The fix is ONE shared map every surface calls (My Team table, the pivot header pill, `/coaching`, and `admin.html`'s partial `roleLabel`), never three string edits. Built 2026-09-02 as `lib/role-labels.js` + verbatim mirrors on the dashboard, admin and coaching pages, each EXECUTED against the lib by `test/role-labels-mirror.test.js`. (H673, H681)

### 4d · Surfaces and language
- **Anything a customer can see is written for them:** what happened and what to do; no internal names, mechanism, or vocabulary (`chunk`, `embedding`, `fingerprint`…); remove internal words, never translate them; the cost warning is gone and stays gone. Guarded by `test/customer-language.test.js`. (H064, H591)
- **If the user has to do something, the page they are on says so, obviously — and that state must not be able to disappear.** (H245, H153)
- **A third state (blocked-on-you) must render; a completed action is never re-offered; a successful action invalidates the caches it changed; progress is honest (no fake %); a control that vanishes on success needs an inverse.** (H145, H146, H147, H526)
- **Absent and excluded must never look alike:** dim AND badge; three-valued statuses; NULL means "not checked". (H354, H109)
- **A label must name its destination**; a button's label is the ACTION, a badge the STATE; hover previews the state the click produces. (H523, H360, H368)
- **Call review shows Call Highlights only** — three panels removed from the render path, their data still written. Put new detail inside the section that owns it, never in a new panel. **Their CSS (`review-trace`/`trace-*`, `review-risk`/`rs-*`, `review-mattered*`) is RETAINED BY RULING (2026-09-02) — parked, not dead; a cleanup must not "finish the job".** (H097, H445, H669)
- **The prospect-merge page and `GET /me/prospects/merge-candidates` are RETIRED (ruled 2026-09-02) — do not rebuild.** It existed for duplicate PROSPECTS; cross-provider duplicate CALLS are solved by overlap suppression under one-active-source; what remains is single-provider name variation, which inflates a close rate nobody has complained about, and 355 hand-reviewable proposals is not work worth creating. Auto-merge stays refused. Its 500 (586 ids in one unchunked `.in()`) is closed by the retirement; removal of the code is its own queue row. (H057, H664)
- **A page that renders nothing from a lane must not fetch it** (especially a model lane). (H651)
- **Every lane waits IN WORDS where its content will be, through `laneWaitHtml(which, copy, longCopy, minHeight)`** — a spinner plus honest copy, the longer sentence only after `PERF_LONG_WAIT_MS` (armed by `armLaneWait`, cleared by `clearLaneWait`, one map `state.laneWaitLong` for team and personal lanes). A bare `skeleton` box on a loading lane is the recorded defect: loading and broken looked identical. A lane that FAILS renders words, never `''`. The view you deep-linked to paints BEFORE the personal lanes return (`reloadAll`). Guard: `test/lane-wait.test.js`. (H701)
- **Controls inherit by CAPABILITY, never by enumeration:** `button, input, select, textarea { font: inherit; color: inherit }` plus one field rule for every field-like type; a per-type list (`input[type="text"]`) is how an email box became 22px of white Arial. Guarded as RENDERED with the inverse check (a control equal to its `all: revert` clone). The outside pages share `/css/style.css`, which mirrors the dashboard's tokens (executed) and ratchets literal size/weight/radius at zero; index inlines the same tokens by ruling. (H688)
- **The background switch:** "Background", a square, green ON / black OFF, a 1px `--edge-white` edge ALWAYS (the product's ONE white — never declare a second), top bar between Admin and My Account, `role="switch"` with an accessible name; the Account checkbox is the same setting's second door and the one setter syncs both. The white date-range picker is Justin's own ruling (`.dp` tokens) — not a defect. (H688)
- **Reveal a dialog before rendering into it;** a native `<select>` cannot stay open — use the custom control with full keyboard/aria. (H570, H344)
- **Deleted/dead views normalise at every ENTRY point** (`setView` AND the hash map), never in a renderer; redirect, do not delete (bookmarks). (H379)

### 4e · Design rulings (the 2026-08/09 design pass)
- **Green carries three meanings at once — good, brand, and the association between them — by design. Never raise it as a collision again.** The only green issue worth raising is legibility (green on green), answered by contrast and FORM (border, fill, weight), never by hue. (H364)
- **Scout green is `#09e046`** (a correction to the asset, not a preference); `--accent-rgb` exists because `rgba()` cannot read a hex token, and every tint derives from it. `--green-dark` is derived by ratio. (H321, H326)
- **Palette has three jobs — semantic, interactive, categorical — and the categorical rep-line ramp borrows no hue from the other two.** Seven entries; a second cycle is DASHED, not more hues (the hue space is measured out). (H327, H052)
- **Type scale: seven sizes, closed.** Named exceptions only: `--fs-gauge-value` 24px and `--fs-company` 24px, declared after the scale and pinned; a third exception landing on 24 re-opens the scale rather than adding a fourth carve-out. When the scale does not fit, something else moves (a bigger dial), not the scale. (H288, H508, H497)
- **Typeface is Saira, self-hosted variable file, body 450, `tabular-nums` on numeric surfaces only** (Chart.js canvas labels cannot). Login stays 300. The wordmark is an IMAGE (`web/scout-wordmark.png`) — the typeface question is retired; a 2x needs a real export, not an upscale. (H345, H349, H539, H540)
- **Safety is layout, not faintness.** Text is protected by an opaque surface (the page ground — `--bg`, content-column wide, no border, no radius), never by dimming artwork. The ground goes on BEFORE cards come off. Every page that loses its cards needs the ground, and a new view inherits nothing — add it to every per-view list. (H286, H292, H637, H559)
- **No grey body text.** `--muted` may not be referenced for text; dim with `opacity` on `var(--text)` instead (that is a depth cue, allowed — the picker's 45%/85% is approved). (H280, H119)
- **The mesh shows in BOTH gutters and the sidebar is a floating card** (ruled 2026-09-02, Josh on a 24" monitor). Geometry lives in ONE place: `--rail-inset` 16 · `--rail-gap` 24 · `--page-left` / `--page-w` feed the `.page` margin, the `.page` width AND the mesh mask, so the artwork is cut exactly where the column is and text is never on it. The column centre stays black on every view. The right band is ≥24px BY CONSTRUCTION above the 900px cut, so there is no width floor to hand-pick (the 1495 floor is retired). **The rail is sized by its CONTENT, never the viewport** (`bottom: auto`; ends 16px below the last nav item with Team expanded; a viewport-derived `max-height` scrolls inside the card on a short window; the Team menu animates 0fr→1fr so the card never jumps — ruled 2026-09-02). The rail is OPAQUE `--bg-elevated` — a translucent panel is nav text on a non-uniform mesh; if it must be tried, measure contrast against the brightest node behind EACH label. `--motif-mesh` is gone; reviving it needs a reader too. (H537, H535, H503, H509, H663)
- **The top bar is STICKY, not fixed, and the WORDMARK is its only logo** (ruled 2026-09-02). Measured before pinning: the bar never scrolled away — it held `top: 0` at every scroll position — so the "bounce" was the `backdrop-filter` on a sticky element (Chrome lags it a frame; removed, the gradient is opaque anyway) plus macOS rubber-band (`overscroll-behavior-y: none` on `html`). Sticky is kept because the rail top (51 + 16 = 67) and the ground `min-height: calc(100vh - 61px)` are derived from the 51px bar in flow; a fixed nav re-derives both. The nav logo is `scout-wordmark.png` at `height: 22px` (`width: auto`, intrinsic `width="1038" height="138"` declared, NEVER upscaled), replacing the "Scout" text — one logo, one file shared with login and the welcome overlay, alt "Scout Systems", linked to `/dashboard`. The small SVG mark is NOT the logo and is sub-pixel at nav size. On DPR 2 the wordmark is a 3.1× downscale (44 device px) and reads crisp. (H700)
- **One loud number per page, fixed per page; some pages honestly have none** (Calls, My Team). (H653)
- **The KB scope badge (`GLOBAL / TEAM / PERSONAL`) and the review speaker badge (`PROSPECT / CLOSER`) render their stored words on purpose (ruled 2026-09-02)** — machine words that ARE the product's words; a later sweep must not "finish" them with a label map. (H673)
- **Attribution before the quote:** claim → evidence → attribution → quote; `spoke` is recorded at the producer from `call_highlights.speaker`, never derived from which field was used; a row with no `spoke` renders UNLABELLED, never labelled. Timestamps render `hh:mm:ss` everywhere. (H623, H640, H642)
- **Store the DEVIATION, not the default** (dashboard layouts: no row until customised, default lives in code, unknown keys dropped on READ and left in the row); **store the HIDDEN set, never the visible one** (preferences). Rename routes through a dedicated one-field route, never through save. (H584, H317, H530)
- **Average call time on a board: the catalog entry's `lower_is_better` is the NUMBER CARD's ceiling caption; the ruled BAND travels on the wire (`publicMetric` attaches it) and the ranked view uses it — verified by executing the click path, both plants fail the pin (H678).** Do not "fix" the entry's direction; do not type a band on the entry.
- **A widget's view list is DERIVED from what the metric has** (target → gauge, history → trend…), intersected with what the builder can draw; never a hand-written list. Units live on the metric. An unavailable metric is named with the right sentence (`no_data` vs `no_card`), never greyed. (H583, H572, H574, H582)
- **Removing a card/control: enumerate what it CONTAINED and what it was the sole caller of** (the Fathom card took the reanalyze controls; a section held the only Back link); check where a removed control's destination went; removing a control does not remove its EFFECTS (stored settings) or bring back its CSS when revived. (H173, H340, H627, H298, H594)
- **A card that links to a navigation item is a duplicate.** (H507)
- **The rep cards are trading cards, treatment C (ruled 2026-09-02): the five section grades are the picture** — name band with the movement chip (beside the GRADE, never beside the closes), closing rate with its counts at `--fs-gauge-value` (Justin reversed "go big": eight cards at 48 is eight loud numbers, so `--fs-display` keeps its one meaning), objections handled, avg call time stating its side, the five section bars with the weakest lit (the Close bar draws `close_earned`), the weakest objection in the foot. Unmeasured is a sentence in the value's slot, never a dash or a zero; grades are uncoloured (one band across the team). **Gauges → cards → graphs**; the Team Overview / Closing Score / Objection Handling Score lists are retired (their figures are on the card; ranking one metric is the `by_rep` widget), and Performance no longer fetches the `why` lane. **One `repCardHtml`, two placements, unchanged**: the catalog's `person` entry (`rep_card`, carries `rep`) is drawn by the same function; the slot is clamped to TWO columns — the card's minimum, a constraint, never a narrower variant. Monogram, no photos (no avatar column; upload is its own item). Guard `test/rep-card.test.js`. (H705)
- **The rep card has a WHITE 1px edge and 16px corners — a RULED EXCEPTION to the borders-removed pass (Justin 2026-09-02: a playing card), the same standing as the verdict border; never remove it in a sweep.** `--rep-card-edge` carries the ruling; `test/rep-card-border-rendered.test.js` measures the RENDERED edge in Electron (`test/helpers/electron-render.js` is the renderer for any guard that must see a computed style — and it must render under the host's `data-view` attribute and ancestor chain read from the live DOM, never a bare body: the first version passed while the live page had no border). (H686)
- **Weights: the scale's three and nothing above 500** (Justin, 2026-09-03 — the 600 nav and the 700 family came down to `--fw-emphasis`); **radii: the three ruled tokens** (6 → `--radius-sm`, 10 → `--radius`, 3/4/5 → `--radius-sm`); **pills (99/999px) and circles (50%) stay — a pill is a shape, not a radius**; **one dialog shell** (`.kb-modal-dialog`'s surface, hairline edge, `--radius`, 520, 28px — the Need-help card, the confirm/prompt dialog and the card picker wear it; what each puts inside is its own). Literal weights and radii in `dashboard.html`, `scout-modal.js` and `style.css` are ratcheted at zero. (H689)
- **One loud number per page, at 48/300:** on Objections it is the HANDLE RATE (its two counts sit at 20/400; `objStatCard`'s `loud` flag is passed by the caller, never inferred); on Knowledge Base it is the counter. **The landing page carries two NAMED exemptions on its own `:root` — `--fs-hero` 58 at 300 and `--fs-section` 32 — declared with their reason; a landing page is not a dashboard, and a sweep must not "finish" them.** Admin's badges are the green pill, never a hue outside the palette. **`--muted` on the outside pages stays grey BY RULING** (the measured ceiling for the login mark). (H690)
- **A vector wordmark exists (`Scout Brand Assets/scout-wordmark.svg`, an auto-trace of the raster, verified the same artwork, fill = the token; H694). When Justin rules the swap: strip the c2pa `<metadata>` first (17.5 KB → 9.8 KB), set the nav to `height: 25.71px` (measured — the cap-matched height that keeps the 165px width) or trim the viewBox, and apply the glow as a CSS filter only if he chooses it. `scout-glyph.svg` is the favicon source.**
- **The wordmark's face is NOT Saira and is not in the repo** (measured 2026-09-03: C=U=T widths, no overshoot, a straight-spined squared S, a short-vertex M — H691); its mark IS the inline nav vector at ~5.6×; its green is #5DC64D (off-token) with a ~17px glow. A vector rebuild waits on Justin naming the face or supplying outlines — never draw the letters in Saira and call it the wordmark. (H691)
- **The brand assets live in `~/Library/Mobile Documents/com~apple~CloudDocs/Scout Brand Assets/` (thirteen PNGs, no vector; H692).** Nothing from there is copied into the repo until Justin has seen the side-by-side; the transparent wordmark is the same raster 5% larger without the glow, and a straight swap shrinks the nav wordmark 13% at 22px. (H692)
- **The wordmark is a 1038×138 screenshot with no original: never draw it above half its native width on a retina surface** (the login/set-password lockups at 1038 CSS px are a 2× upscale — reported, not changed; a sharper asset is Justin's). The nav's 22px is a 3.1× downscale and stays. No page links a favicon. (H690)
- **Coming-soon items are `<span>`s** — inert by tag, not by style. (H534)
- **Bars encode state only if nothing else on screen already does** — strip the encoding and ask what the reader no longer knows. (H506)

## 5 · Engineering invariants

### 5a · Data and Supabase
- **An unchunked `.in()` FAILS outright above ~395 ids** (URL length; measured 2026-09-02: 390 ok, 400 `fetch failed`, no PostgREST error object) — a smaller ceiling than the 1,000-row cap, and both are live today (merge-candidates 586 ids; owner KB list 2,272 rows). Chunk every `.in()` at 100; page every read of a growing table. `backend/sweep/detect-unbounded-reads.js` lists the unpaged reads. The section drill-down was the first site fixed against THIS ceiling (fix #3, H677) — its pin executes the route against a fake wire that refuses >395 ids; copy that shape for the next unchunked site. **For the 1,000-row cap the shape is fix #6's / H683's: page with `.range` inside the request, every page before the answer, a hard cap, `total_rows`/`truncated` on the payload, a pin with a range-aware fake that fails on the unpaged code.** (H663, H677, H679, H683)
- **The `.in()` chunk size is ONE constant — `lib/chunk.js CHUNK` (100) — never a literal.** Every `slice(i, i + N)` in `lib/` and `routes/` names it, every user requires it outside a comment, and `CHUNK ≤ 390`; `test/duplicated-constants.test.js` enumerates by capability. A scripted require insert can land inside a header comment and `node -c` will not notice — the pin does. (H685)
- **Check `error` on every Supabase call.** An unknown column (or a derived API field used as a column) EMPTIES the whole select; an implausible empty result is a statement about the query. `.in()` chunks at 100 (URL length); default 1000-row cap on unbounded selects; `.neq` drops NULLs — filter in JS; a comma inside `.or()` breaks the parser; `.ilike` on a uuid returns null. (H656, H607, H067, H042, H425)
- **Write the null.** An absent row and a row saying "nothing here" are identical to a query; a placeholder that is a valid value of its own type (`0`, `'__no_reply__'`, `{_error}`) defeats every presence check — test for the placeholder, and normalise fallback shapes so `undefined` never reaches a `=== null` branch. (H094, H048, H634)
- **Migrations:** `supabase link --project-ref vkprybqmryiuwbdwdlpk` once, then `supabase db query --linked --file backend/migrations/NNN.sql`; `supabase/` is gitignored. Apply a migration BEFORE deploying code that writes the column. Never rely on RLS alone — always an explicit user filter. (H019)
- **Never reconstruct an identifier from a display prefix; resolve by natural key and assert every target exists before writing.** Destructive/expensive data ops pin to printed, verified IDS, never a count; never during a drain; recount to zero after. (H121, H096, H186)
- **Seeding a metric that depends on row context means controlling the context** (objections on non-closed calls). Demo seed removal: `delete from fathom_calls where fathom_call_id like 'seed-2026-08-16-%'` + `prospects where display_name like 'Seed %'`. (H133, H123)
- **Foreign keys first:** every history table cascades on `auth.users`; `knowledge_base` has no FK and must be purged first; `profiles` (vestigial) blocks the auth delete. (H382)

### 5b · Pipeline and model calls
- **`analyzeCall(fathomCallId, userId)` — two arguments;** one strands the row at `processing` with no error recorded. A runner resolves every owner up front and refuses to start otherwise; a CAPABILITY failure (missing provider creds) aborts the whole run immediately — a local run inherits the local environment, and the credential is only needed on a token REFRESH. (H076, H487, H485)
- **Claims:** `claimAnalysisRun` is the concurrency guard; `CLAIM_STALE_MS` 10 min; a stale claim is reclaimable; `analyzed_at` doubles as the claim stamp so it advancing is not proof of progress. Resets exclude live claims and fail closed. (H042, H060, H155)
- **Retry classification follows the CONSEQUENCE, not the shape:** Fathom transcript fetch retries by ATTEMPT COUNT (temporary HTTP codes requeue, permanent ones fail); the model call has its own counter; Zoom bounds by call AGE; `lib/failure-class.js` fails OPEN (unknown = retryable) with "no transcript" permanent and a human retry clearing both counters. A count that can never reach zero stops being read — split actionable from permanent. (H257, H253, H311, H254)
- **Failure reasons live in `overall_summary` (latest state only) and `highlight_error`;** a programmer error (`ReferenceError`/`TypeError`) is logged WITH its stack and class, an operational one without; a background pass that writes nothing says so in words. (H255, H501, H477)
- **Every model call goes through the one `getAnthropic()` seam with `usageFor(lane)`** logging to `model_usage`; `routes/proxy.js` is exempt (an orphan of the dead desktop app, H689). One coaching call per call, never per moment. (H615, H394)
- **Caps are derived from the input** (bucket max tokens from phrase count; a text cap sits ABOVE the prompt's own length rule and cuts at a sentence boundary); a derived limit is where the thing breaks, not where it should sit — leave margin. (H066, H603, H624, H331)
- **A cache keyed on the data cannot skip reading the data** — a hit is as fast as its queries; a warm-up must build the SAME key and the SAME output inputs (name map). Cache entries accumulate per hash. (H372, H597, H361)
- **Independent round trips run at once (`Promise.all`), never one after another** — the "warm floor" of every team page was ordinary Supabase queries awaited in sequence (~12 deep on `/team/overview`). Chunked fetches issue every chunk together; processing keeps its order. Never five dangling promises: a rejection while another read is pending must be handled. `auth.admin.listUsers` is read through `lib/email-map.js` (one map, 60s TTL per process), never per request. Guard: `test/lane-parallel.test.js`, `test/email-map-cache.test.js`. (H702)
- **The recommendations warm-up runs AFTER the drain, from the end of each analyze loop (`lib/warm-after-drain.js`): a live claim means step aside, none means warm.** sync-all warms only when it dispatched nothing — warming right after dispatch warmed a key the analyses then changed (evidenced 2026-09-01). The key is untouched; only the timing moved. (H703)
- **One bucketing model call per phrase set in flight** (`getBucketMapping` shares the in-flight promise; not a cache) **and the Objections summary starts only after the grid has landed** (`gridLanded`), so a cold window pays one bucketing call, not two. (H704)
- **A faster model for a synthesis lane is a quality trade, not a performance tweak** — declined for the performance summary (the model call is 97% of a miss; the fix was an honest time-gated waiting state). No lane has ever been evaluated on a smaller model; if one is, argue it on output quality with the delta measured. (H122, H612)
- **A require cycle can make a shared constant `undefined` at import time — resolve lazily and throw on a bad value.** (H061)
- **The JSON control-character repair runs only after a normal parse fails** (both extractors). (H083)
- **Persist highlights insert-new-then-delete-old;** highlight ids are reissued on re-analysis (dedupe by content hash, never id). (H037)
- **A temporary refusal from an external service must never permanently destroy a record;** `.in()`-style chunking, bounded requeues, fail-closed audits. (H257)
- **Column selected at every consumer:** a field read at five sites and selected at none is `undefined` everywhere with no error — guards pin column lists; a field dropped from the render must also leave the PROMPT. (H448, H279, H590)
- **Three-valued verification columns have no default** (`speaker_verified`, `closer_response_verified`). (H109)

### 5c · Front-end state and routing (`dashboard.html`)
- **`TEAM_PAGES` is the one list of team views;** `isTeamView` derives from it; every dispatcher names every case (a catch-all `else` hides a missing one); the hash READ and WRITE sides round-trip; each page owns its own range key; a page that renders a control owns that control's state. (H299, H304, H302, H305, H301)
- **Scoped state resets by OPT-IN SURVIVAL** (`INITIAL_STATE`, `PIVOT_KEEP`); `TEAM_LANE_SCOPE` declares each lane `both`/`team` (default `team`); a team epoch discards stale responses; data and its loading flag clear from the same map; both pivot doors (`setUser`, `setCallLibraryUser`). (H242, H238, H049, H055)
- **A lane must not depend on a value another lane happened to load;** every fetch that can show another user chooses its scope (`isSelf()`), allowlisted exceptions only; arrive COLD (deep link) to verify. (H371, H479, H243)
- **A `null` sentinel must be the value the loader tests for** — declare every lazy-load key. Loading fallbacks may fall back to a neutral placeholder, never to the viewer's own identity. (H134, H467)
- **Date-range picker:** every view owns its own range (the control never reads app state); picker labels are INCLUSIVE, graph bucket labels EXCLUSIVE; no focus trap, deliberately (a naive blur listener breaks it); it must not rebuild its own DOM mid-gesture. (H119, H553)
- **A `_forbidden` result is neither `_error` nor empty** — `laneProblem(d)` is the one predicate; problem branch before success branch; the message host survives when the toolbar is stripped. (H569)
- **Stacking contexts, not z-index values:** `.fade-in`'s persisted transform makes every section a context; ask `elementFromPoint`; the nav sits at 70 (above page content, below modals 100). Equal z-index is a loss for the earlier element. (H548, H296)
- **Chart.js:** emit a slot and keep the canvas outside the render cycle, then `update('none')`; hidden-set keyed by `user_id`, handler writes to `state` first; a replaced default must re-supply `datasetIndex`; date-axis thinning keys on px per label, not day count. (H192, H126, H129, H618)
- **Event handling:** the capture phase runs first — test containment (`closest`), never rely on `stopPropagation`; a control must not rebuild its own DOM mid-gesture. (H343, H553)
- **`<a>` without `href` is not focusable** — give promoted controls `tabindex`, keys, Escape and focus return. Shared components carry `var(--token, #fallback)` because `admin.html` lacks the dashboard tokens. (H297, H284)
- **One CSS class per design; rename, never merge.** `.sec-note` is the section page's italic caveat, `.sec-moment-note` the manager's bordered note on a closer moment — two authors once shared the name and the browser merged them into a design neither wrote. Pin a selector's CARDINALITY. **A group rule's declaration that one member always restates is dead for that member and live for the rest — split it per member, never delete it from the group;** measure every member before and after. (H685)
- **CSS is last-wins and searches are first-hit:** assert CARDINALITY of a selector/declaration, not presence; a `border: 0` shorthand kills a deliberate side; opacity composites multiplicatively (scope the dim, never override it); a `var()` with a fallback makes the rendered value unknowable from source. (H267, H268, H654, H365, H537)
- **A raster/asset's colour: read the source bytes** (data URI, PNG), never a neighbouring property or a canvas round-trip. (H200, H596)

## 6 · Verification and testing discipline

These are the rules behind most of the history file. The families recur; each rule below is the family, with its instances as pointers.

- **A GUARD PROTECTING MONEY, ACCESS OR SCOPE MUST EXECUTE THE ROUTE AND ASSERT THE CONSEQUENCE — never assert that a line of code exists. And it is proven by planting the defect TWICE: once REMOVING the call, once KEEPING the call and DISCARDING ITS EFFECT (gate result ignored, comparison `&& false`, confirm answer dropped, scope on a dead lookup). A guard that survives the second plant guards nothing.** Standing rule, Justin 2026-09-02, earned by sweep block 2: eight such guards survived the second plant with the full suite green. "Executes something" is not "executes the thing" — a sibling that executes the helper does not cover the route. (H665, H666)
- **Execute the real entry point.** Testing a helper in isolation and grepping for its name are the same check twice — both prove it EXISTS, neither that it RUNS. Drive the real render/route/pivot with injected state; assert the CALL SITE and the CONSEQUENCE (the branch, the slot it needs), not the mention. **Proved at scale 2026-09-02: eight spend/permission/scope guards stayed green — with the FULL suite, 2132/2132 — when the gate was called and its result ignored, the comparison made `&& false`, the confirm awaited and its answer dropped.** The plant that proves a guard is "call kept, effect removed", never only "call removed". (H665) The dead-call-site family: H019, H235, H239, H564, H644, H571, H193, H228, H236.
- **Restore the defect and count the failures.** A guard is proven only when the exact defect makes it fail; a mismatch between defects restored and guards failed is the signal. Write a removal guard BEFORE the removal and see it fail. Back up every changed file (`git stash -u`), and assert the revert took effect before trusting the result. (H148, H510, H103, H313)
- **Every anchor asserted present before it is compared** (`indexOf` returns −1 and `−1 < x` is true); assert the slice length with a per-function floor and pass `fromIndex`; select by CONTENT never by position (`all[0]`, `.pop()`, first match); anchor past `<body>` and past archived copies. (H190, H528, H549, H149, H079, H633)
- **Derive guards from the source of truth; never pin a literal, an encoding, a version, a loop-variable name, or the shape of a list.** A guard that fails on the correct change is pinned to an implementation, not a property — re-pin, and assert the old form ABSENT. A guard may enshrine the defect it was written to prevent. (H325, H511, H527, H565, H587, H645, H529)
- **Scope equals claim.** A check's verdict is narrower than its claim in three ways — SCOPE (what it looked at), INVENTORY (targets it enumerated), RANGE (one point on a continuous axis). State the scope beside the verdict; enumerate by CAPABILITY (the operation nothing can avoid), not by the string you expect; a search built from the hypothesis cannot disconfirm it. A count over a file is not a per-site assertion. (H278, H276, H213, H303, H324, H164, H390, H516, H455, H387)
- **A check that can pass by finding nothing needs a floor** — assert the sample size, that the control moves, that the fixture exercises the branch, that the probe returns different answers for cases that differ, that a planted defect is found. A control that can decline to exercise its property is not a control. (H332, H080, H357, H300, H578, H416, H463)
- **When a check reports a problem, confirm the check is sound first** — the cheapest test is whether its output contradicts something already visible (a green mark read as white ink; a screenshot vs a pixel count; two facts in one result that cannot both be true). Read the right property, in the right coordinate space, in the right region. (H200, H513, H566, H206, H287, H367, H595)
- **The shared stripper is string-aware and EXECUTED against the five killers and the live dashboard on every run (`test/one-stripper.test.js`, H682); the count of private copies is a ratchet — it may fall, never rise. Convert a private copy by hand (they slice, count and collect as well as strip), never by script; the eleven block-first files go first.**
- **A detector is scored on planted positives AND negatives before its output is read, and prints RAW line numbers** (`backend/sweep/`, `sweep/raw-line.js`; the one stripper is `sweep/strip.js`). A measurement quoted in a finding uses the code's EXACT filter. (H663)
- **Strip comments — line comments FIRST, then block; leading AND trailing — before any text match, in tests AND in deploy checks;** this codebase archives removed code in place, so a raw grep reports the documentation of a rule as a violation of it (11 guards carried the ordering bug). Strip for matching; run the RAW source for execution. Never quote removed syntax in the prose explaining a removal. (H131, H184, H233, H306, H128, H342)
- **Measurement is not a composition test; a subjective target needs a LOOK.** Ask first: "if all my numbers came back perfect, would that prove it is right?" Screenshot the worst instance, not the default one; sample the RENDER, not the source; for anything about appearance enumerate `getComputedStyle` over the live page — a stylesheet grep cannot show a style that does not live in it, and predicting classes has been wrong three times. A block's `getBoundingClientRect` cannot see overflow (`scrollWidth` vs `clientWidth`) or ancestor clipping. (H209, H208, H205, H206, H646, H649, H163, H543)
- **Verify across the range a layout moves through;** derive values from the constraint (`fontSize = width / k`, k measured per face and per weight/tracking), never pick a number that passes today; a criterion is not a constant; a derived value inherits the case it was derived from; a stroke is a solve, not a division. (H213, H216, H199, H270, H272, H273)
- **Fixtures come from the producer, never from memory** — six invented shapes produced six false defects; check `cached` before reading a before/after; a hash-only navigation keeps module state; forge the actor at the layer below the credential. (H555, H480, H351, H150, H632, H355)
- **`node -c` proves a file parses, nothing more:** it cannot see an unresolved identifier, a nested declaration at the wrong brace depth, a missing import, or a truncated-but-valid file. Count references per identifier; append parameters, never insert mid-list; assert an edit did not shrink the file (per-edit cap) and that brace counts balance; every edit script writes AND prints; no optional-prefix regexes; scope scripted edits by enclosing function and assert the match count; never `node -e` with quotes in the payload. (H385, H476, H389, H102, H330, H495, H588, H622, H341, H203, H522)
- **Width claims come from a same-origin `<iframe>` at that width, never from `resize_window`** (a no-op on a maximised window that still reports success — `innerWidth` is the check); a hash-only `navigate` keeps the old document AND its stylesheet, so a deploy is confirmed only after a path change. (H687)
- **The browser harness:** synthetic clicks are delivered only briefly after a fresh navigation — pair every click conclusion with a delivery count; `computer` takes SCREENSHOT-SPACE coordinates (scale by `screenshotWidth / innerWidth`); a hidden tab freezes the animation CLOCK, so pause before capturing and scrub `getAnimations()` rather than waiting; native `confirm()`/`prompt()` cannot be driven; reload evidence (URL, restored state) is trustworthy, gesture evidence needs a human; the unauthenticated-iframe recipe suppresses `init()` and cannot verify anything `init()` does. (H554, H366, H541, H386, H619)
- **Reporting:** a number in a report is a claim until re-derived — including your own; a caveat carries its evidence or its uncertainty; a mid-run measurement is not a result; sort-by-size is not sample-by-frequency; a mean over a bimodal field describes neither mode; a sample cannot move a per-user statistic; a rate is only actionable per group; state the growth term of an estimate; state which question a measurement answers. (H290, H295, H152, H473, H207, H075, H151, H218, H592)
- **Diagnosis:** a clearance is only as good as the symptom it was tested against — re-open every clearance when a sharper report arrives; establish which failure it is before scoping (display filter vs incomplete delete); enumerate by capability then execute rather than reason from an ordering model; two changes in one block invite a false causal story — measure whether the fix explains the symptom; a fix shipped beside the thing it breaks reads as a relapse. (H053, H071, H050, H568, H411, H054)
- **Tests survive their scaffolding: convert, do not delete;** a test may pin a known defect with its reason and expiry inside the test; a converted test's TITLE is part of the conversion; a conversion aimed at another dead thing is not one; a guard's floor moves with its population with the reason at the assertion; one exemption in a guard is doing real work — do not become the second. (H191, H095, H524, H586, H602, H580, H579)
- **A comment that asserts an ordering or a parity the code does not enforce is worse than none** — establish it structurally or in a test, or write "assumed". The stale-load-bearing-comment family: H551, H552, H049, H452, H629.
- **When a value's MEANING changes but its TYPE does not, RENAME it** so every reader fails loudly (`repIds`→`memberIds`, `at_or_above`→`meeting`, `hard`→`purge`). When a shared carrier (state field, hash, container, word) changes meaning, the risk is its OTHER readers — grep them all. (H125, H544, H221, H550, H135, H136, H137)
- **A constant typed twice is pinned by IDENTITY where an import is possible (`===` on the same array — a copy that agrees fails) and by EXECUTING the page's literal as a set where it is not.** The four ③ pairs (objection classes, coachable/negative types, worker/page handling types, `by_rep`/`bar_rep`) are wired that way; a guard that pinned the literal `100` was the drift it claimed to prevent. (H685)
- **Replace a guard by routing around it, never widening it;** a rule implemented at the call site is a rule most call sites do not have — put it in one module and guard that no site hand-rolls it. (H237, H544)
- **A published claim about unbuilt behaviour is a commitment nobody chose** — decide, build, then write the sentence. (H196, H545)

## 7 · Reference (compact — details in the history)

### 7a · Stack and layout
- **Backend:** Node ≥20, Express, `backend/` deployed to Railway via `backend/Dockerfile` (NOT nixpacks; there is no `railway.json` or `Procfile`). Static web at `backend/web/` (not `public/` — Railpack would deploy Caddy). Supabase (`vkprybqmryiuwbdwdlpk`) + pgvector; Claude `claude-sonnet-4-6` on every lane (`claude-haiku-4-5` only in the dormant desktop proxy); Voyage `voyage-3-lite` 512-dim server-side; Resend for email over HTTPS (Railway blocks outbound SMTP). Chart.js 4 via CDN on the dashboard. (H004, H025, H037)
- **Desktop app: DEAD (H689).** Electron 41 overlay + control windows, Deepgram, `/proxy/*`, release v1.1.2 — all orphaned; nothing is preserved for a revival. Its history stays in H007–H016, H116, H117 as a record.
- **Key backend files:** `lib/analysis-worker.js` (grader + extractor + Phase 7b harvest + 7c coaching; `ANALYSIS_PROMPT_VERSION`), `routes/fathom.js` / `routes/zoom.js` (sync, first-sync cap, update-analyses), `lib/team-*.js` (analytics, digest, synthesis, needs-work, objections, objection-summary, membership, averages), `lib/prospect-entity.js` (close rate), `lib/objection-handled.js`, `lib/objection-categories.js`, `lib/metric-band.js`, `lib/real-calls.js`, `lib/kb-scope.js` + `lib/kb-entry.js` + `lib/kb-harvest.js`, `lib/quote-locate.js`, `lib/closer-side.js`, `lib/coaching.js`, `lib/coaching-tone.js`, `lib/model-usage.js`, `lib/user-purge.js`, `lib/company.js`, `lib/failure-class.js` / `fathom-retry.js` / `model-retry.js` / `zoom-retry.js`, `lib/selling-context.js` + `selling-budget.js`, `lib/discovery-areas.js`, `web/dashboard.html` (one ~18k-line SPA, ~45% comment, inline JS), `web/admin.html`, `web/login.html`, `web/js/scout-auth.js`. Route surface: `/auth/*`, `/me/*`, `/admin/*`, `/team/*` (15 routes, manager+owner), `/fathom/*`, `/zoom/*`, `/kb/*`, `/eod`, `/support/*`, `/proxy/*`, `/download`, `/health`. (H005, H025, H037)
- **Roles:** `owner` (platform admin) · `manager` · `user`; "has assigned reps" (not role) grants the manager experience; `managed_by` edges are the only team structure; `requireAuth` fails OPEN on a DB blip (deliberate). (H037, H118)
- **Auth on the web:** `scout-auth.js` — cross-tab refresh lock, transient failures keep the session; `dashboard.html` must route through `window.ScoutAuth`, never its own 401 handlers. (H034, H036)

### 7b · Environments, tools, credentials
- **Railway:** service `sales-overlay`, project caring-empathy/production, public URL `https://sales-overlay-production.up.railway.app`, custom domain `www.scoutsystems.io` (CNAME → `vdy3qiy5.up.railway.app`; bare domain is an HTTP-only Namecheap redirect). CLI: `RAILWAY_TOKEN=<RAILWAY_API_TOKEN from API Keys.md> railway logs|status|variables` (`whoami` always fails with a project token; echo the token length before polling). Healthcheck path/timeout are dashboard settings; our bind is ~117ms. Env var names: `DEEPGRAM_API_KEY, ANTHROPIC_API_KEY, VOYAGE_API_KEY, SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, STRIPE_*, DEEPGRAM_PROJECT_ID, FATHOM_CLIENT_ID/SECRET/REDIRECT_URI/STATE_SECRET, ZOOM_CLIENT_ID/SECRET/REDIRECT_URI, ZOOM_WEBHOOK_SECRET_TOKEN, CRON_SECRET, RESEND_API_KEY, BACKEND_URL, SKIP_BILLING, SKIP_AUTH`. (H025, H026, H558)
- **Cron:** GitHub Actions `.github/workflows/fathom-sync.yml` every ~2h → `/fathom/sync-all` then `/zoom/sync-all` with `X-Cron-Secret`; the digest and the team-recommendations warm-up ride the post-sync pass; the repo PAT needs the `workflow` scope to touch that file. (H036, H597)
- **Supabase CLI** for DDL and ad-hoc SQL (`supabase db query --linked "<sql>" -o csv`); the service-role key cannot do DDL. `objection_synthesis_cache` columns: `id, user_id, from_ts, to_ts, analysis_set_hash, synthesis, generated_at, synthesis_type`. (H019, H361)
- **Throwaway scripts:** run from `backend/` (or bootstrap `backend/node_modules` onto `Module.globalPaths`), load `API Keys.md` before `.env`, resolve repo paths via a helper. (H425)
- **DNS / Google Workspace / SPF / DMARC** and the Namecheap quirks: H027, H028, H019.
- **Zoom Marketplace:** unlisted app APPROVED; production creds on Railway; redirect `https://www.scoutsystems.io/auth/zoom/callback`; deauth endpoint live; reviewer data (`demo-rv-*`, `reviewer@scoutsystems.io`) still present — cleanup is a deliberate reopen, as is the Fathom→`call_connections` cutover. (H111, H142, H144)
- **Fathom API:** 8 endpoints, no identity endpoint, `recorded_by[]=` (literal brackets) is the owner filter, transcript via `/recordings/{id}/transcript`, page size hard-coded at 10, single-use rotating refresh tokens (a probe that refreshes is a WRITE). (H041, H165, H166, H219)

### 7c · Where things stand (state, not rules — check before relying)
- Grader/extractor at **v37** (objection_class); coaching pass live (v30+); model usage logged (migration 059); `recorded_by` stamped (058); the DQ-routing "want it first" fix is designed, not wired (v36 held). Open product questions and the queue: `BUILD-LIST.md`. The retired desktop analytics tables (`call_sessions`, `session_logs`) are kept but unwritten. (H398, H457, H615, H610)
