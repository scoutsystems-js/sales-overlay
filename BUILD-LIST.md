# SCOUT — BUILD LIST

**Created 2026-08-20.** Seeded from the live-site audit and the current repo, **not** from `BUILD-PLAN.md` (19 April, four months stale — treat it as history).

**⚠ STANDING PROCEDURE: update this file after every push, alongside `CLAUDE.md`.**

**⚠ ANYTHING UNVERIFIED IS MARKED `UNKNOWN`.** An invented entry is worse than a gap: a gap gets asked about, an invention gets built.

## ⚠⚠ HOW THIS FILE IS ORDERED (Justin's ruling 2026-08-28)

**THE ORDER IS THE PRIORITY: 🐛 BUG → ▪ MINOR → ▰ MAJOR.** Within the feature sections his stated queue order stands: **admin view rebuild part 2 → first-time password page → Zoom → role hierarchy.**

| tag | means |
|---|---|
| `🐛 BUG` | something that does not work and needs to |
| `▪ MINOR` | a button, a graph, a small capability |
| `▰ MAJOR` | Marketing Insights, Scout AI, whole pages |

**⚠⚠ THIS SUPERSEDES THE PREVIOUS NOTE THAT SAID "TYPE IS A SECOND AXIS — THE FILE IS NOT RE-SECTIONED BY IT, DELIBERATELY."** That note argued sectioning by type would force rows into two places. Justin ruled the other way on 2026-08-28 because the state-based sections had become unreadable. **The old note is not kept as an alternative — it is wrong now, and a file carrying both would be a second live answer.**

**Counts after the 2026-08-28 reorder — `🔴 NEEDS JUSTIN` 18 · `🐛 BUGS` 12 · `▪ MINOR` 52 · `▰ MAJOR` 9 · `⏳ TRIGGERED` 6 · `🗄 CLOSED` 50 · `✅ LIVE` 63.** **All 205 original rows carried over, none duplicated — verified mechanically, not by eye.**

**⚠ THE RULE THAT MATTERS MORE THAN THE ORDER: A ROW APPEARS EXACTLY ONCE.** If a row needs Justin, it is in **NEEDS JUSTIN** and nowhere else. Anything ruled, shipped or superseded is **out of the forward-looking sections** and lives in HISTORY at the bottom.

---

## 🔴 NEEDS JUSTIN — decisions and spend, nothing else

**Everything here is blocked on him, not on work.** Each row says what the question is; the reasoning is intact.

### ⚠ THE ONE GENUINELY OPEN QUESTION HE HAS NOT BEEN ASKED PLAINLY

| question | why it is being asked |
|---|---|
| **The "who has not connected a recording source" badge — once a manager dismisses it, should it come back?** Three options: (1) never again, (2) again tomorrow, (3) **only when someone NEW is unconnected** *(recommended)*. | ⚠ **Justin did not recognise this row, which means the question was never put to him in one line.** It is here as one line. The trade: *never again* means a rep onboarded in three weeks never triggers it and the badge quietly stops working; *tomorrow* is a daily nag that gets ignored on sight; *per-person* keeps "I have dealt with this" and "something new happened" distinguishable. **The full reasoning is in the row directly beneath.** |

| the feature this question blocks | state | what it needs |
|---|---|---|
| `▪ MINOR` **🔺 HIGH — badge: who has not connected a recording source** | ✅ ruled 2026-08-26 — **HIGH: above Zoom and the password page, below whatever is in flight.** **FILED, NOT BUILT** | A dismissible badge at the **top of the Manager view** naming reps with no recording source connected. Justin: *"Not an emergency priority but definitely a high priority."* ⚠⚠ **THE DISMISSAL BEHAVIOUR IS THE WHOLE DESIGN AND IT IS NOT YET RULED.** Dismissed **forever** → the manager onboards a rep in three weeks, never sees it, and **the badge quietly stops working with nobody noticing**. Dismissed **until tomorrow** → it nags, and a nag that returns daily gets ignored on sight. **ARCHITECT'S RECOMMENDATION: dismissal is PER UNCONNECTED PERSON, not per badge** — gone for people the manager has already been told about, returns for anyone NEWLY unconnected. **Justin has not ruled; do not decide it while building.** ⚠ **A fully connected team must see NOTHING.** A badge rendering empty, or saying "0 unconnected", is noise. ⚠ **It reports a fact about OTHER PEOPLE, so it is manager-and-above, enforced SERVER-SIDE — not by hiding it client-side.** ⚠⚠ **IT IS A BADGE ON A PAGE, NOT A NOTIFICATION. Scout has no notification system, and that gap is already filed as blocking EOD manager approval — this must NOT pull that work in.** |

### 💰 SPEND

| item | state | what it needs |
|---|---|---|
| **⚠ 102 of Godwin's 123 calls are ungraded** | ⛔ **~$25, NOT approved** — but he can now SEE them | The backlog is no longer invisible: his card names both numbers and the grading control renders. **Whether to spend the ~$25 is Justin's call**, and Godwin can now make it himself. |
| **✅ yazan's ORIGINAL backlog complete — but 183 MORE calls arrived after** | ✅ done 2026-08-26 for the 292 that existed; ⚠ **181 newly-synced calls are now ungraded (~$50, NOT approved)** | Two passes, six loops, **`saturation=0` in both** (130 in 33 min, then 25 in 14 min with `errored=0`). ~$46 against a ~$51 estimate. ⚠ **The 3 remaining are NOT retry candidates** — `fetched 0 raw turn(s)`, i.e. Fathom returns an empty transcript, so there is nothing to grade. **Named rather than reported as zero.** ⚠⚠ **AT 19:56 HIS CALL COUNT WENT 292 → 475** — something pulled more of his history, unrelated to the run. **181 of the new ones are ungraded.** ⚠ **The cause could not be established: my own docs deploys restarted the container and Railway logs do not survive a restart.** That is a standing observability limit — **a question asked ten minutes too late about a deploy-adjacent event has no answer**, and it is one more argument for the health-snapshot work now at the top of the queue. |
| **153 errored calls left unrepaired on Josh's account** | ⛔ deliberate — spend not approved | 151 of them are Fathom 429 casualties from the twelve-loop incident; nothing retries them. Re-admitting is ~$35 on old history that appears on no surface Justin shows. **Justin has not approved it — do not re-admit without a ruling.** The 30-day pitch surface is unaffected (163 of 165). |
| **180 calls no control can reach** | ⛔ **reported, not fixed — spend decision** | `/fathom/update-analyses` finds its pending block with `.eq('sync_status','pending')`. Measured on Josh's main account: **271 pending (reachable), 180 `synced_unanalyzed` (NOT), 3 error (NOT)** — so an **"All time" run grades 267 and silently leaves 183 behind while the label says all time**. The holdback is deliberate (the first-sync cap), but the Update-analyses dropdown is documented as the backfill path for exactly that tail and cannot reach it. ⚠ Admitting them adds **~$43 to every all-time run** — Justin's call, not an implementation detail. |
| 4 | **Cost scales linearly with no ceiling** | filed as a **PRICING input**, not an engineering task | ~$0.23/call, ~20 calls/day/closer ≈ **~$140/month per seat** in Anthropic spend alone; 100 closers ≈ **$14k/month** before anything else. ⚠ **The obvious lever is closed:** the one-transcript-one-send merge measured 46% input saving and was **REJECTED on quality** (it dropped objection moments) — see the OPEN row below. It reopens only if someone finds a way to keep both outputs intact. |

### ⚖ DECISIONS

| item | state | what it needs |
|---|---|---|
| **⚠ 3 users tombstoned BEFORE the ruling still hold 117 calls** | ⛔ **REPORTED, NOT DECIDED — Justin's call** | They sit on Josh's board, still counted in his team numbers, showing as *"Deleted user (…)"*. **They were deleted under the old contract where the person was told the calls stay.** Purging them removes 117 calls from every period his team has worked. Three options: **purge** (numbers move), **leave** (they keep showing), or **hide from the roster only** (non-destructive, numbers unchanged). |
| `🐛 BUG` **⚠⚠ Update-analyses was pressed on Josh's account — 128 graded calls reset** | ⛔ **filed 2026-08-26 — needs a ruling, NOT touched** | Railway log: `Update-analyses queued for user 40616e16… queued=152 remaining=58 (outdated=158, pending=52)`. **128 calls that were already `done` are now `pending`**, and **23 have already been re-graded at v28** — roughly **$40 if the rest run**. ⚠ **Nothing Claude did caused this.** ⚠⚠ **AND IT IS NOT RUNNING AWAY — `batch_limit=10` means it grades TEN PER PRESS, not continuously.** Verified static (122 pending / 35 done / 1 in flight, unchanged across 40s), so **the spend only continues if the control is pressed again.** ⚠ **This is the exact cost the v28 bump was flagged for**: the prompt bump marks every prior analysis outdated, and the control resets them. **Either leave it or clear the pending rows back — Justin's call.** |
| 1 | **🚨🚨 THE ANTHROPIC RETRY GAP — ANSWERED 2026-08-26. YES, PARTIALLY.** | **⚠ READING DONE, FIX NOT BUILT — awaiting Justin.** Still top of the list. | **ANSWER: the TRANSIENT half is already protected; the PERMANENCE half is not.** `@anthropic-ai/sdk` 0.27.3 is constructed in **11 places with no override**, so defaults apply: **maxRetries 2, 10-minute timeout, exponential backoff honouring `retry-after`**. It retries **429, 409, 408, all 5xx (incl. 529 overloaded) and connection/timeout errors**. ⚠⚠ **BUT once retries are exhausted the row is permanently `error` and NOTHING moves it back to `pending`** — enumerated on the comment-stripped tree, the only requeue in the worker is `requeueTranscript`, which is the **Fathom TRANSCRIPT path**; the model path did not inherit it. `/fathom/update-analyses` cannot reach an errored row either (its batch is `sync_status='pending'` ∪ `status='done'`). **Exactly the Fathom gap.** ⚠ **Unparseable JSON at the cap is a 200, so the SDK does not retry it at all** — grader → permanent error; extractor → non-fatal, grades ship with zero highlights. ⚠⚠ **AND THE 'RETRY WOULD CHANGE NOTHING' PREMISE IS WRONG: it is length-CORRELATED, not length-determined.** `c1097d61` failed on it today and parsed fine on both paths in the merge experiment, and the live error count went 2→1 when a loop re-claimed one and it succeeded. **A retry would genuinely recover some.** ⚠ **SCALE:** limits are per ORGANISATION — the SDK's 3 attempts absorb a brief spike, but a sustained burst (100 closers on one sync tick) exhausts them and lands every call in permanent error across every account at once, with no alerting (#5). **FIX, NOT BUILT:** reuse `lib/fathom-retry.js` + `requeueTranscript` one layer down — classify the SDK error, requeue within a bound, fail past it. ⚠ Not wholly trivial: the unparseable case is a 200 and needs its own branch, and `transcript_attempts` (migration 047) would need a sibling since it currently means transcript attempts. |
| `▪ MINOR` Objection-handling name collision | "Objection Handling" names 3 different metrics on the team page | observation filed 2026-08-18, with Justin |
| `▪ MINOR` EOD divergence | Parked deliberately | do not touch without a ruling |
| `▪ MINOR` **When the honest answer is "not enough data", the panel must SAY SO** | ⛔ filed 2026-08-27 — **copy is Justin's call, not ours** | *"Godwin Ona's objection handling is even across types — no type is more than 5 points below their own average"* is the INSUFFICIENCY path written as a finding. Justin's word: **unacceptable.** ⚠ Confirmed it is that path, not a computation fault. ⚠ **Downstream of grading Godwin's 99** — with 23 objections across 19 of his 118 calls there is genuinely not enough to say anything. ⚠ The fix is saying *not enough data yet* plainly; **dressing insufficiency as an even distribution is the failure.** |
| `🐛 BUG` **(f) The "why" text gives the not-enough-data answer as a finding** | ⛔ **filed — copy is Justin's call** | *"objection handling is even across types"* is the INSUFFICIENCY path written as a conclusion. Justin's word: **unacceptable.** ⚠ Confirmed previously as that path, not a computation fault. **The honest fix is saying "not enough data yet" plainly.** |
| `🐛 BUG` Colour ramp collision at 8+ reps | 7 colours cycling, so rep 8 shares rep 1's hex | measurement deferred; options for Justin owed |
| `▪ MINOR` **🔺 HIGH — tell closers what Zoom needs, AT CONNECT TIME** | ✅ filed 2026-08-26 — **recommended HIGH, near the unconnected-source badge. Justin decides.** **NOT BUILT** | Scout says "Connected: Zoom" and nothing else; **two people have now raised the same ticket**. Zoom exposes **CLOUD** recordings only, and *cloud recording enabled* vs *recordings actually going to the cloud* are different settings — **only the second produces anything Scout can read.** ⚠ **QUICK VERSION: say it on the screen where they connect**, plain language, before they click Sync. Mention auto-record (Justin runs it deliberately). ⚠⚠ **BETTER VERSION — CONNECT THEN CHECK.** Scout can ask Zoom how many cloud recordings exist and knows within seconds: *"Zoom is connected, but we can't see any cloud recordings — here's what to switch on."* ⚠⚠ **THE GODWIN INCIDENT IS THE ARGUMENT FOR CHECK OVER TELL: his settings were ALREADY CORRECT (121 recordings synced), so a connect-time instruction would have told him to fix something that was not broken.** The check covers both outcomes; the instruction covers one and gets the other actively wrong. ⚠ **Zero recordings and a failed check must not look the same** — "we found none" vs "we could not ask". ⚠ **Zoom ALREADY HAS the honest sync states** (`no_recordings`, not-connected, failure-with-reason, success-with-count, a distinct syncing state) — **verify before rebuilding them.** |
| UNKNOWN | Anything Justin is tracking that is not listed here | ⚠ this list was seeded from the repo and the live site; if he has items that live only in conversation, they are not here and should be added rather than assumed |

---

## 🐛 BUGS — ordered by consequence

**⚠ Ordered by what breaks, not by date.** Anything needing a ruling is in NEEDS JUSTIN instead.

| item | state | what it needs |
|---|---|---|
| `🐛 BUG` **⚠⚠ COACHING EVIDENCE IS THE WRONG KIND OF MOMENT — 69%, STILL NOT FIXED** | ⛔ **attempted 2026-08-28 and the fix DID NOT WORK. Still the most serious item.** | ⚠⚠ **TWO CORRECTIONS TO THE FIRST DIAGNOSIS.** (1) The type was NEVER thrown away — `c.type` has been on every candidate line since July, so all 414 quotes were generated with it visible; I had read a truncated line. (2) The evidence RULE shipped and **did not move the number: 67% before, 75% after** on the real function. ⚠ **The cache version is deliberately NOT bumped** — regenerating every synthesis at real cost for a change measured not to help is paying to look busy. ⚠⚠ **THE REAL FIX, from the survivors:** every one is a claim about the CLOSER attached to the PROSPECT'S words. A `buying_signal` moment usually has no `closer_response`, so **the only text it can offer is the prospect's line — the evidence structure gives the model no way to quote the closer's failure.** Give the candidate BOTH sides. ⚠ Sharper metric: **69%**, not 61% — only 1% is the closer's own line, so the original number was UNDER-counting. |
| `🐛 BUG` **Prospect grouping is broken — one person, three prospect rows** | ⛔ **filed 2026-08-26 — blocks any follow-up-aware feature** | Abu is split across `Abu` (1 call), `Abu and Jelani` (2), `Abu Askia and Shakrea` (3). ⚠⚠ **This is the prerequisite for (b) above:** the grader could be told "this is call 3 of 4 with this prospect", but on today's data it would say "first call" for most of them — **a confident wrong answer is worse than none**, which is this project's standing rule on names. ⚠ The merge-review surface exists (`/me/prospects/*`) but is **caller-scoped**, so a manager cannot fix a rep's prospects. |
| `🐛 BUG` **⚠⚠ The Manager Daily Digest made a LEGAL AND COMPLIANCE matter the day's coaching focus** | ✅ **RULED 2026-08-28 — BUILDABLE, no longer an open question.** A rep told a prospect to overstate income on a financing application; the digest built **"today's focus"** around it. | **JUSTIN'S RULING: SUPPRESS ENTIRELY.** *Scout has zero context on liability and must never base coaching on it* — and it does not surface legal or liability matters **ANYWHERE**, not as coaching and not on any other surface. ⚠⚠ **THE OPEN QUESTION IS CLOSED AND THE ANSWER IS (a).** The file previously carried it unresolved — *"Scout noticing this is arguably right and a manager would want to know; suppress entirely, or surface it away from coaching?"* — and **that question is answered: suppress. DO NOT BUILD A SECOND SURFACE FOR IT**, and do not let it pull in the notification work. ⚠ **What to build: a filter in the digest's own moment SELECTION**, so a compliance/liability moment can never be chosen as a notable moment or as the day's focus. ⚠⚠ **WHY THIS RANKS AS A BUG RATHER THAN A NICETY:** it is *a data problem must never render as good news* in its most expensive form — it rendered as an **INSIGHT**, confidently, as the day's top item, and a manager acting on it coaches the wrong thing. Same family as the Abu entry, different cause. |
| `🐛 BUG` Deleted users and companies still appear in the admin view and on the team page | ✅ **RULED 2026-08-28 — BUILDABLE.** Justin: **a deleted user's data is removed from the site completely.** | ⚠⚠ **DIAGNOSED FIRST, AND IT IS NEITHER OF THE TWO FAILURE MODES I FLAGGED.** Not a surface reading a cached set, and **NOT a delete path that missed a table** — measured 2026-08-28: **0 orphan auth rows**, so `lib/user-purge.js` leaves no debris and any NEW delete already removes everything. **The materially-worse possibility is ruled out.** **What actually remains is two things: (i)** three legacy tombstones from the SUPERSEDED contract (`deleted-49711e7d`, `deleted-8bda1aac`, `deleted-e3ae475c`), all `active=false`, all still `managed_by` josh, all rendering as *"Deleted user (…)"* on the Scout Systems board; **(ii)** **nothing anywhere filters `active`** — `routes/team.js:66` selects members on `managed_by` alone and `getAllowedUserIds` in `routes/admin.js` has no `active` filter either. ⚠⚠ **THE RULING REACHES THE THREE TOMBSTONES, BUT PURGING THEM IS A SEPARATE DECISION AND IT IS IN NEEDS JUSTIN** — it destroys **117 calls / 117 analyses / 87 highlights / 56 prospects** currently counted in Josh's team history, and they were deleted under a contract that promised the calls would stay. ⚠ **Once they are purged this bug is gone**, because there will be no deleted users left and future deletes remove everything. What is left is then purely about **DEACTIVATED** users — and ruling (a) says their numbers stay, so only the *row* question remains, not the numbers. |
| `▪ MINOR` **🚨 The digest gave confident, wrong coaching** ⚠ *tagged MINOR, not BUG: the BUG half (a) shipped in v27. What REMAINS is (b), a capability the grader has never had — and it is blocked on the prospect-grouping BUG above.* | ✅ **INVESTIGATED 2026-08-26 — (a) FIXED in v27, (b) is a CAPABILITY GAP** | The 2026-08-24 digest built the day's highest-leverage coaching point, and TODAY'S FOCUS, around enrolling a prospect who was a **financial DISQUALIFICATION** on a **FOLLOW-UP call**. **Two independent blind spots:** (a) **a DQ read as a winnable close** — Scout already has a disqualification concept and the objection surfaces exclude DQs as non-coachable, but the digest does not apply it; (b) **a follow-up treated as a first call** — "enroll them on the spot" is first-call advice, and **whether the grader knows a call is a follow-up at all is the open question** (prospect grouping exists, so the information is in the system; whether it reaches the grader is not established). ⚠⚠ **The failure mode is the point: not a wrong number but confident, specific, actionable advice that is wrong, rendered as the day's top insight.** A manager acting on it coaches the wrong thing. ⚠ **Not general unreliability** — Josh confirmed the other items were accurate. **✅ ANSWERED:** (a) **was the boundary, and the DQ was never identified at all** — the extractor prompt said the opposite of the ruling, so *"I can't afford it"* had no route to a disqualification. **Fixed in v27.** ⚠⚠ **Scout HAD the disqualifying quote and wired it to nothing:** `qualification_covered = {financial:true, evidence:"My personal credit score right now is about 60 or something like that"}`, beside Josh's stored criterion **"640 or above credit score"** — both in front of the model, nothing compares them. (b) **NOT KNOWABLE** — `buildSectionGraderPrompt` receives no prospect and no prior calls; `prospect_id` is only ever written. **And Abu is split across THREE prospect rows**, so wiring the current one in would ship a confident wrong answer. **Two new rows below.** |
| `🐛 BUG` Blank graphs on refresh onto `#team` | Pre-existing, not a regression from `87f09d3` (proven by in-memory revert) | the guard that swallows it is still unidentified |
| `🐛 BUG` Team sub-page hash gap | `#team-recs` / `#team-needs-work` / `#team-members` carry no range, so a refresh drops to the 7-day default | real, reproducible, unfixed |
| `🐛 BUG` `/kb/upload` batch embeddings | Still embeds chunk-by-chunk; `getVoyageEmbeddings` drops straight in | ⚠ becomes load-bearing the moment similarity search surfaces harvested moments |
| `🐛 BUG` Team page `<h1>` reads "Team" on a deep link, "My team" when reached via the Team page | The heading comes from the team-overview lane, which a deep link never loads | ⚠ cosmetic and **pre-existing**, not introduced by the drilldown. Noticed while fixing the drilldown's own version of this (its grid note had the same dependency and now carries `board_size` in its own payload). Same fix shape if it is ever wanted: have the view read a value its own request returns |
| `🐛 BUG` Date-picker focus trap | Tab leaves the open panel; panel stays open until an outside click | held deliberately — a naive blur handler fights the re-render |
| `🐛 BUG` Bare-domain HTTPS | `https://scoutsystems.io` times out (Namecheap redirect has no 443) | needs Cloudflare DNS migration |
| `🐛 BUG` DMG notarization gap | Every release needs manual DMG sign+notarize+staple | electron-builder config |

---

## ▪ FEATURES — MINOR

**⚠ Justin's queue order leads this section: admin rebuild part 2 → password page → Zoom.** Everything after those three is unordered.
### 1 · ADMIN VIEW REBUILD, PART 2 — around companies (filed 2026-08-24)

**⚠⚠ PART 1 IS SHIPPED (`e4679e8`, `a7ad0c4`). PART 2 IS NOT.** What remains is listed at the bottom of this block. **⚠ It is no longer blocked: the ruling it was waiting on landed 2026-08-28 (deactivate leaves the numbers, delete deletes) — all of part 2 is now BUILDABLE.**

**SHIPPED:** storage for the name (migration 043, `user_profiles.team_name`) · company blocks with the name editable top centre · three tabs (Companies / Single Users / All Users) with tab state in the URL · the company name on the Team view, reading the same stored value · the owner team picker now labels by company name, server-enforced.

**NOT BUILT:** deactivate/delete a whole company (§5 — **blocked on a ruling, see below**).

**⚠⚠ RULING — A COMPANY IS A RENAMED TEAM. ADMIN VIEW ONLY. NO NEW TIER, NO SCHEMA CHANGE.** Everything outside the admin view keeps saying "team". The open question this file previously carried — *"is a company a team, or a new tier above teams?"* — **is answered and has been removed.** It is a **relabel in one view**, so `resolveTeam`, `managed_by` and every team-scoped query are untouched.

**⚠⚠ RULING — THE REBUILD SHIPS WITH TODAY'S THREE ROLES.** Blocks are ordered **Manager then Users**. **Director is NOT part of this build** and slots in later without a second rebuild — so the dependency on the filed role-hierarchy item is also gone.

**ORDER OF WORK AFTER THIS BLOCK (Justin, 2026-08-24):**
**1. admin view rebuild → 2. first-time password page → 3. Zoom → 4. role hierarchy.**

| item | the ruling | what it actually needs |

| item | the ruling | what it actually needs |
|---|---|---|
| **Admin view rebuild — companies** | The admin view is reorganised around **COMPANIES** rather than a flat user list. ⚠ Each company gets its own **BLOCK**, ordered top-down: **Manager > Users** (Director is a later tier, explicitly out of this build). ⚠ The company **NAME is editable at the top centre of its block**, and that name then appears at the **top centre of the TEAM view** for that company's manager. ⚠ **DEACTIVATE and DELETE for a whole company and all its users.** ⚠ **THREE TABS**: Companies, Single Users, All Users. ⚠ An admin can go to **Team view and use a dropdown to cycle through every company** and see their sales data. | ⚠ **RESOLVED 2026-08-24: a company IS a team, renamed in the admin view only** — no schema change, no new tier, `managed_by` unchanged. ⚠ **Director is OUT of this build** (three roles today, blocks ordered **Manager then Users**), so the role-hierarchy dependency is gone too. ⚠ **STILL REAL: teams have no NAME today.** The editable company name has nowhere to live — a team is currently just the set of users whose `managed_by` points at one manager, identified by that manager's email. Storing a name is the one piece of persistence this needs. ⚠ **Deleting a company deletes many users at once**, so the tombstone-vs-remove decision that shipped for single users (`b535fb5`) must apply consistently at company scale — a company delete that hard-deletes N users would destroy N people's call history and rewrite every historical figure they contributed to. |

### ✅ RULED 2026-08-28 — MOVING A REP DOES **NOT** MOVE THEIR HISTORY

**⚠ This was carried as "SECOND QUESTION FOR JUSTIN" and it is ANSWERED. The heading is corrected rather than left reading as open** — a decided question sitting under a question heading is exactly what gets re-asked. The measurement below is unchanged and is why the ruling costs work.

**`fathom_calls` carries only `user_id`, and team scope resolves `managed_by` at query time.** So reassigning a rep **retroactively moves every past quarter's calls, cash, close rate and objections** from one company's totals to the other. Nothing records which team a call belonged to when it happened.

**Reported and NOT changed** — the fix is either freezing history (snapshot the team on each call) or accepting the retroactive move, and that is a **reporting decision**. ⚠ It also compounds with the naming caveat: a client with two sales managers is already two companies, so "move a rep between managers" and "move a rep between companies" are the same action today.

**⚠⚠ SUPERSEDED 2026-08-28 — JUSTIN HAS RULED, AND THE ANSWER IS THE OPPOSITE OF WHAT THIS FILE PREVIOUSLY RECORDED.** **MOVING A REP DOES NOT MOVE THEIR HISTORY: call data STAYS WITH THE COMPANY WHERE IT WAS EARNED.** The 2026-08-25 entry that accepted *copying* history to the new team — and accepted the resulting double-count — **is dead. Do not implement it, and do not leave both readings on file.**

**The consequence is unchanged and is the whole cost:** `fathom_calls` carries only `user_id` and team scope resolves `managed_by` **live**, so honouring the ruling needs **a team stamp written at call creation** plus **a sweep of every team-scoped query** to key on the stamp rather than on today's membership; backfill the stamp from current mapping as a best guess. ⚠ It is the same mechanism the **offer axis** needs — whoever builds either should build both.

### ⚠⚠ WHAT REMAINS — AND THE RULING THAT UNBLOCKS IT

**⚠⚠ RULED 2026-08-28: DEACTIVATE LEAVES THE NUMBERS. DELETE DELETES. AT COMPANY SCALE EXACTLY AS AT USER SCALE — no new rule, no exception.** Both rows below were BLOCKED ON A RULING and are now **BUILDABLE**. Reuse `lib/user-purge.js`; do not write a second deletion path.

| item | state | what it needs |
|---|---|---|
| **Deactivate a whole company** | ✅ **RULED 2026-08-28 — BUILDABLE. Deactivating a company LEAVES THE NUMBERS, exactly as it does for a single user.** | ⚠⚠ **DEACTIVATE AND DELETE ARE NOT THE SAME THING AND MUST NOT BE CONFLATED.** Measured 2026-08-24, here is what deactivate does for a SINGLE user today, so the company-level question is concrete rather than invented: it **bans the auth account** (`ban_duration` ~100y) and sets `user_profiles.active = false`, so **they cannot log in**; it is **fully reversible** (Reactivate lifts both); and — the part that matters — **NO team aggregate filters on `active`**, so **their calls, prospects, cash and objections all still count in every company figure.** ⚠⚠ **ANSWERED: YES — the numbers stay.** Justin ruled the company case is the user case with no exception, so **no new rule and no special-casing**: keep the existing behaviour (ban + `active=false`, fully reversible) and **do NOT add an `active` filter to team aggregates as part of this work.** Leaving them in keeps history honest; taking them out would silently rewrite past figures, which is the thing the delete work was careful to avoid. |
| **Delete a whole company** | ✅ **RULED 2026-08-28 — BUILDABLE, no longer gated.** Delete deletes, at company scale as at user scale. | ⚠ **MUST reuse the single-user deletion that shipped this session** (`b535fb5`) — same tombstone, same scrub, same calls-survive rule. **Do not write a second deletion path.** ⚠ The confirmation must NAME THE COST: the company name, how many users, how many calls, and that the calls remain while the people do not. ⚠ **Report before/after on a throwaway company; historical figures MUST NOT MOVE.** ⚠ The current single-user confirmation uses a native `prompt()`, which **this harness cannot click** — if that pattern is reused, the final click is UNVERIFIED and must be reported as such. | ⚠⚠ **THE CALLS-SURVIVE RULE NAMED IN THIS ROW IS SUPERSEDED** — the 2026-08-26 ruling made a single-user delete DESTRUCTIVE, so a company delete destroys its data too. **Reuse `lib/user-purge.js`** (which is what the single-user path now uses); the tombstone described here is history.

### 2 · FIRST-TIME PASSWORD PAGE (filed 2026-08-24)

**⚠ FILED ONLY. Justin's ruling.** The page is `web/set-password.html`, reached from the welcome email's one-time link.

| item | the ruling | what it actually needs |

| item | the ruling | what it actually needs |
|---|---|---|
| **First-time password page** | Add the **Scout logo used on the login page**; show the **password rules beneath the field**; add a **reveal-password control**. | ⚠ **THE LOGO IS `web/scout-wordmark.png`**, not a text lockup and not `src/assets/icon.png` — see the wordmark ruling in `CLAUDE.md`. Login sizes it `min(96vw, 1038px)`; **1038px is the asset's native width**, above which the browser upscales. ⚠ **The reveal control is a real a11y surface**, not a styling job: it needs an accessible name that changes with state, and it must not live only in a `title` attribute. |

**⚠⚠ THE CAVEAT ANSWERED — THIS IS A DISPLAY CHANGE, NOT A BEHAVIOUR CHANGE, *FOR THE RULE THAT EXISTS TODAY.*** Measured 2026-08-24, the question the architect flagged:

```
POST /auth/set-password      password.length < 10  -> 400   ENFORCED SERVER-SIDE
                             password.length > 200 -> 400   ENFORCED SERVER-SIDE
POST /auth/change-password   same two bounds                ENFORCED SERVER-SIDE
set-password.html            placeholder "At least 10 characters" + a client check
dashboard.html (#account)    the same placeholder + the same client check
```

So **"at least 10 characters" is already the rule and is already enforced** — writing it under the field only stops it being a placeholder the user loses the moment they type. **Nothing changes behaviourally.**

**⚠ BUT ANY RULE BEYOND THOSE TWO IS NEW ENFORCEMENT.** An uppercase / number / symbol requirement is **not** enforced anywhere today, server or client. Adding it to the displayed list means implementing it in **both** password endpoints — and it would **lock out no existing user** (the rules apply on set, not on login), but it **is** a behaviour change and must be ruled on as one. **Display only what is enforced, or enforce what you display; never show a rule the server does not apply.**

---


### 3 · ZOOM — A FIX, NOT A REBUILD (diagnosed 2026-08-26)

**⚠ The original 2026-08-22 headline ("Zoom produces no usable data") was DISPROVEN. It is kept as history at the bottom of this file and must never sit above this section.**


| feature | Zoom | evidence |
|---|---|---|
| **Grading** | **WORKS** | same meeting, both providers: 67/63 · 74/74 · 73/73 · 70/71 · 67/67 · 55/56 |
| **EOD figures** | **WORKS** | outcome + `eod_summary` on 48/48 |
| **Team analytics** | **WORKS** | same tables |
| **Prospect name** | **DEGRADED** | **81%** (34/42) vs Fathom 99% — 20 diarized, 20 grader, **0 from title** |
| **Highlights** | **DEGRADED** | fine under an hour; **7 of 8 calls over 60 min have ZERO** (Fathom: 0 of 176) |
| **Objections** | **DEGRADED** | 0.43/call vs 0.93 — downstream of highlights |
| **Clips** | **DEGRADED** | `recording_url` on 53/53, but no timestamp deep-link |
| **Speaker split** | **DEGRADED** | 6 of 39 calls collapse into one turn |

**⚠ The transcript is NOT empty and IS parsed** — 775 chars/min vs Fathom's 841 (92%). The difference is granularity (177 chars/turn vs 53), and **that does NOT hurt quote accuracy: Zoom verifies at 97%, Fathom 92%.**

**⚠⚠ THE LONG-CALL HIGHLIGHT FAULT DOES NOT REPRODUCE** — re-running the extractor on the two worst calls returned 8 moments each. Ruled out by measurement: empty/unparsed transcript, input size, timestamps past the hour, newlines, output truncation, rate-limit burst. **The mechanism is undeterminable because an extractor failure records NO reason — that gap is the blocker, and it is already filed below.** Re-analysing those 8 calls would recover their highlights; **not done, spend not approved.**

### ORDERED — hardest first. ⚠ Items 1-3 and 5 are ENGINEERING; item 4 depends on how the closer runs meetings.
1. **Clip extraction** — **neither the download nor the cutting step exists.** Zoom has no jump-to-a-moment link, so this is the only route to parity. ⚠ **The published privacy policy already describes it as if it were built.**
2. **Record why an extraction failed** — until this exists the long-call fault can only be guessed at.
3. **Stop a one-speaker call collapsing into one turn** — Zoom labels every cue with one name on some calls; Fathom's equivalent still yields 38 turns.
4. **The Personal Meeting Room** *(not engineering)* — 45 of 53 calls share one title, so the title can never name a prospect. Booking through a calendar link fixes it at source; the transcript route already covers 81%.
5. **Turn granularity** — measured NOT to hurt quality. **Leave it alone.**

---

<details><summary>(superseded) original filing, 2026-08-22</summary>


### 4 · SCALE RISKS — filed, not built

**⚠ #1 (the Anthropic retry gap) and #4 (cost) are DECISIONS and live in NEEDS JUSTIN. #6 and #7 have since SHIPPED and are in HISTORY. What remains is engineering nobody has started.**

**⚠⚠ THE THROUGH-LINE, AND IT IS THE REASON THESE ARE FILED TOGETHER: EVERY DEFECT THIS WEEK WAS SILENT.** The inverted duplicate filter, the manager-missing digest, the Fathom 429s, a dead sync — none of them errored, none alerted, and every one was found by Justin noticing a wrong number or by querying the database by hand. **The product has no way to say something is wrong.**

| # | item | priority | what it needs |
|---|---|---|---|
| 2 | **The 2-hourly sync is a thundering herd** | filed | Every connection syncs on the same tick. Fine at 10 accounts; at 200 they all dump into one queue and reach for transcripts simultaneously — **the same shape that rate-limited Fathom tonight with only twelve loops**. Stagger by account, or queue with a global concurrency cap. |
| 3 | **No analysis concurrency in normal operation** | filed | Tonight's six loops existed only because they were started by hand. There is no equivalent in the running product, so a backlog **queues with no way to drain it and no visibility that it is growing**. |
| 5 | **🚨 Monitoring and alerting — NOTHING EXISTS** | filed | Errored-call count crossing a threshold (per account **and** globally) · a sync that has not succeeded in N hours · `last_sync_at` NULL or stale (**this already hid a broken sync for days**) · model-call error and 429 rate · **a digest generating zeros for an account that has calls** (tonight's bug, silent for weeks). |

### Other MINOR items — unordered

| item | state | what it needs |
|---|---|---|
| `▪ MINOR` **No way to retry a SINGLE failed call** | 👁 observation, filed 2026-08-26 | The grading control is window-scoped, so retrying one failure runs its whole window — measured on live data, **80 calls ≈ $19 to retry one**. A real consequence of extending the existing control rather than adding a per-call action (which was the right call: two controls that both grade calls is how they diverge). Worth a per-call retry on the call review page if failures become common. |
| `▪ MINOR` **No failure HISTORY — the reason lives in `overall_summary`** | ⛔ filed | Every failure path writes a durable reason on the row (verified: 5 failed calls, 0 without one) — but into **the same column the real call summary uses**. So **a call that fails and later succeeds loses all trace**, and one that fails twice keeps only the second reason. ⚠ **This gets worse now the retry ships**, since a recovered call overwrites its own evidence by design. ⚠ Also: **"too long" and "model returned junk" are the SAME message** — neither transcript length nor output size is recorded, and they have different fixes. |
| `▪ MINOR` **Qualification verdicts cannot be aggregated yet** | 👁 filed 2026-08-26 — per-call recording is what was asked for | The criterion is stored as the model's own verbatim text, so *"how often does credit fail"* has no stable key to group on — the same non-determinism already recorded for coaching-area keys, where a re-derivation silently orphaned tagged moments. **A stable key is a prerequisite for any trend view**, and inventing one now without a consumer would be guessing at its shape. |
| `▪ MINOR` **Three concurrent analyze loops on one account — file against the six-loop ceiling** | 👁 observation, filed 2026-08-27 | The window in which 21 of josh's 42 Zoom calls extracted zero highlights is the same one in which **three grading loops were found running at once** on that account. ⚠⚠ **THREE IS INSIDE THE RULED CEILING OF SIX, so if this is the cause the ceiling is wrong for this path** — the ruling was derived from Fathom transcript-fetch saturation, a different bottleneck. ⚠ **UNPROVABLE NOW and deliberately not claimed** — six explanations were tested and disproven. Recorded so that if it recurs with `highlight_error` populated, the connection is already written down. |
| `▪ MINOR` **"On no team" is a real state — never a silent empty** | ✅ ruled 2026-08-27, **already applied to the rep graph** | An unmanaged rep resolves as a **board of one** rather than an empty result. ⚠ **THE RULE, not a patch for one panel:** a silent empty is what this whole arc exists to remove, and it applies to any scope resolution that can legitimately find nobody. |
| `▪ MINOR` **A message must have ESTABLISHED any cause it names** | ✅ ruled 2026-08-27 | Generalised from the objection-graph copy, which blamed the date range on a rep with 19 objections in every window. ⚠⚠ **A wrong reason is worse than no reason — it sends someone to change the one thing that cannot help**, and it reads as a considered explanation. Applies to every empty state and error message. |
| `▪ MINOR` **Shared styling creates an implied affordance** | ✅ **swept 2026-08-28 — no other instances** | Anything that LOOKS interactive must be interactive or look different. ⚠ Swept by capability rather than by eye: every element carrying a class whose CSS sets `cursor: pointer`, checked for a handler. **The Per-Closer grid was the only one, and it is fixed.** |
| `▪ MINOR` **Rep pages show the email, not the person's name** | ⛔ filed 2026-08-27 — cosmetic, Justin's request | Use first + last name wherever a rep is identified on their own page. `lib/display-name.js` already owns the join-and-fallback rule — reuse it, do not re-derive. |
| `▪ MINOR` **Closer names in the Per-Closer Objection grid should link through** | ⛔ filed 2026-08-27 — cosmetic, Justin's request | Clicking a closer in that grid opens that closer's objection page. The `?user=` pivot already exists; this is a link, not a new route. |
| `▪ MINOR` **The offer price is set by MANAGERS AND ABOVE, not by reps** | ✅ **RULED 2026-08-26 — PER MANAGER, and a manager may set a DIFFERENT price per rep.** Managers and above set it; a **single user** sets their own, having no manager | ⚠⚠ **IT IS A PERMISSIONS CHANGE, NOT A DATA CHANGE — and neither of the two options I originally reported is it.** Per-rep values under manager control means **`user_profiles.price_pif` STAYS EXACTLY WHERE IT IS, on the individual**: **no migration, no moved column, and no fallback rule in the analysis worker.** All that changes is *who may write it* — a rep loses the ability to set their own, and their manager (and above) gains it. ⚠ **The read site is untouched:** `analysis-worker.js` (~L1466) already reads the rep's own `price_pif`, and it still will. ⚠ **THIS CORRECTS A RECOMMENDATION I MADE:** I reported that the 8 reps *"need to save their offer price in Account"*. Wrong instruction — they should never have been asked. ⚠ **The per-COMPANY vs per-MANAGER question is CLOSED — it is per manager.** ⚠ **LIVE CONSEQUENCE, and it is NOT a bug:** until this ships, Time to Price stays unmeasured for **8 of 9 reps**, and the graph correctly reads **UNMEASURED** rather than slow. |
| `▪ MINOR` Customize View — the real feature | Let a manager choose what the team page shows | per-user, not per-org (no org entity needed) |
| `▪ MINOR` **Login page + post-login animation — bring in line with the site** | Two of the three open items are now CLOSED | ✅ **(1) the wordmark ruling shipped** — it is Justin's logo image on both surfaces (`0073af9`). ✅ **(2) login body weight 450 shipped** (`31c446d`). ➡ **REMAINING: only (3)**, and it is a recorded choice rather than work — the animation's motion vocabulary is deliberately unlike the site's (420–760ms and expo + 1.8 overshoot, against the site's 0.15s `cubic-bezier(0.4,0,0.2,1)`). **Recommended UNCHANGED: correct for a ceremony, wrong for a button.** ⚠ Standing observation: the three surfaces use three different geometries — concentric arcs (login), a radar dial (welcome), a node mesh (dashboard) — and only the first is the brand mark |
| `▪ MINOR` **Nav shows the wordmark, not just "Scout"** | Blocked on a redrawn small-size mark — **SPEC BELOW, not built** | ⚠⚠ **SCALING CANNOT FIX THIS — it is a resolution-of-construction limit.** Measured on the shipped asset: the smallest dot is **8px at native 1038px**, so **2.5px at the welcome overlay's 327px** and **0.2px at nav size**. **SPEC FOR A DESIGNER: (1) at most THREE elements** — one outer arc, one inner arc, one dot; drop the middle ring and the two smaller dots. **(2) stroke >= 1/8 of the mark's height**, so it survives at 16px as a 2px line. **(3) the single dot >= 1/5 of the height** (>=3px at 16px) — a filled dot stops reading below ~2-3px. **(4) minimum usable size 16px tall**; below that drop to the outer arc alone. **(5) no glow** — it is invisible under ~24px and only muddies the edges. ⚠ Deliver as **SVG**, not a raster: at these sizes a raster cannot be shared across 1x and 2x. ⚠ Same limit already bites the welcome title below ~618px vmin, where its smallest dot goes under 2px |
| `▪ MINOR` **Harvest gate — capture from almost every call** | Justin 2026-08-20: *"there's always a coaching moment you can take from a call"* | ⚠⚠ **SUPERSEDES KB RULING 4**, which gates auto-harvest on `outcome='closed'` ALONE. ⚠ OPEN: extra model calls per call? ⚠⚠ OPEN: **what stops a weak moment being kept once the closed filter goes** — if that filter was the de facto quality gate, removing it needs a replacement, not just a wider net. ⚠ The KB counter card copy ("Scout only collects from calls that close") becomes FALSE and must change in the same commit |
| `▪ MINOR` **New Scout Systems logo** | Bold square wordmark, ring-and-dot mark replacing the O | ⚠ **SHIPS AS SVG, not text in a font** — removes the identification and licensing questions entirely. ⚠ The **"5." in the source image is a SLIDE NUMBER**, not part of the logo. ⚠⚠ **SMALL-SIZE VARIANT NEEDED FIRST**: the Logo sweep ruling records that at nav size concentric rings vanish and a dot renders under a pixel — **this mark has exactly that construction**. ⚠ Also fixes the **missing favicon**: measured 2026-08-20, `rel="icon"` appears in ZERO of the served pages |
| `▪ MINOR` Fathom → `call_connections` cutover | Move Fathom's 8 connection sites onto the unified store, drop `fathom_connections` | ⚠ **The stated reason for this hold is STALE (corrected 2026-08-24): Scout is APPROVED, so no reviewer is mid-assessment.** The hold itself has NOT been lifted here — that is Justin's call, not a side effect of a records correction. Reopen it deliberately |
| `▪ MINOR` Reviewer demo-data cleanup | Remove `demo-rv-*` rows | ⛔ same hold |
| `▪ MINOR` Shared constants module | Payment-structure allowlist + sync-cap constants are duplicated across route files | hardening pass |
| `▪ MINOR` Section drilldown surfacing | Surface harvested KB moments under their weakest section | needs the batch-embedding fix first |
| `▪ MINOR` Self-serve forgot/reset | No reset UI exists anywhere | front-door arc |
| `▪ MINOR` Onboarding sequence for a first-run user | | front-door arc |
| `▪ MINOR` Slack connect | EOD one-click send + manager digests | |
| `▪ MINOR` Modals for user-management confirms | Email-change + delete still use `prompt()` | polish |
| `▪ MINOR` Settings toggle — turn the background off | Per-user; plain black instead of the raster | ⚠ per-user by the 2026-08-20 ruling that customisation needs no org entity |
| `▪ MINOR` Cash tile → EOD | Retarget once EOD accepts a pivoted user | EOD is self-only today |
| `▪ MINOR` Delete orphan auth accounts | 6 abandoned public-signup rows | accepted as-is; needs an explicit go |
| `▪ MINOR` Zoom webhook + polling + connect UI (sub-stage 4) | `recording.completed` ingestion, paid-plan detection messaging | ⚠ connect-flow must tell a user WHICH Zoom setting is wrong on an empty sync |
| `▪ MINOR` 8c/8d barrier → uncovered-ground link | "the lender approved $5k and financial qualification was never established" | ⛔ held on DATA: needs ≥2 uncovered areas per call; currently 0/1/1 |
| `▪ MINOR` Role-inversion detector | Deterministic closer-question-share threshold | ⛔ held on DATA: n=2 labelled calls |
| `▪ MINOR` Shape (b) missed cue | "the closer DID dig and the prospect had nothing" | needs a purpose-built signal + a KB-harvest-gate ruling |
| `▪ MINOR` Desktop persistent-login parity | Proactive refresh timer, 401 retry, transient-vs-genuine re-auth | ships with the next DMG |
| `▪ MINOR` **⚠⚠ The exposure sweep is unguarded** | Nothing re-runs it, so a NEW view whose text sits outside an opaque container lands on a full-brightness photograph with no test catching it | **Not cheap.** A faithful sweep needs computed styles across 15 *rendered* views — i.e. a headless-browser devDependency this repo does not have (`node --test` only), plus a harness that boots the dashboard and drives all 15 views. The four carded classes are pinned statically, which covers today's containers but cannot cover a class that does not exist yet. A "border but no background" heuristic was considered and rejected — it fires on badges, buttons and inputs that are legitimately inside carded parents. |
| `▪ MINOR` **The `[team]` catch shape cannot distinguish a programmer error from an operational one** | All 13 catch blocks in `routes/team.js` log `err.message` without a stack and return a generic 500. A `ReferenceError` and a DB timeout are indistinguishable to both the client and the log | **Reported, not rewritten** — 13 blocks is its own blast radius. `/team/why-prose` sat broken since it was written because of this. ⚠ The generic CLIENT message is correct and should stay; the fix is to make programmer errors loud on the SERVER — log the stack, and consider classing `ReferenceError` / `TypeError` separately from operational failures. ⚠ Same shape almost certainly exists in the other route files |
| `▪ MINOR` **⚠ Synthetic rows are identified by an ID PREFIX, and for `demo-` rows that is the ONLY signal there is** | `lib/real-calls.js` filters `seed-` / `demo-` prefixes. Measured 2026-08-22: real 420 calls / 420 recording_url / genuine grader versions; seed 102 / 0 / `seed-2026-08-16`; **demo 33 / 33 / v4,v5,v8 — byte-level COPIES of Josh's analyses, so nothing in the row says "synthetic" except the id we gave it** | **The durable fix is an `is_synthetic boolean` column on `fathom_calls`**, written at insert time by the seeding scripts — then the filter reads a property instead of parsing a name, and a seed row that forgets the prefix stops being invisible. ⚠ `recording_url IS NULL` was considered as a second mechanism for seed rows and **rejected**: two mechanisms for one concept is two things to keep in sync, and that property is a coincidence of today's data — a genuine call that failed to sync a `recording_url` would be silently discarded as synthetic. Until the column exists, **the prefixes ARE the contract** |
| `▪ MINOR` **Old "Objection Handling Focus" panel — NOT archived, deliberately** | Nothing links to it any more (all team entry points now open the drilldown), but the view still exists and works if reached directly | It carries **three things the drilldown does not**: an LLM surface-label taxonomy ("Spouse / partner approval", "Needs time / think it over") rather than the 4 stored categories; a **true-objection denominator** that excludes disqualifications and logistical barriers **and says so**; and clickable bucket → per-call evidence. ⚠ The card on the team page stays too — it is the only place that context line renders there. **Justin's call whether to fold any of it into the drilldown or restore an entry point** |
| `👁 OBSERVATION` **Godwin half-handles objections — 74 of 138 come out PARTIAL** | 👁 **coaching, not a defect — filed 2026-08-28** | The real finding behind the zero. ⚠⚠ **AND A PROPERTY OF THE METRIC WORTH KNOWING:** an objection counts as handled **when the call closes**, so a rep's handle rate tracks their closing rate — **it will always look like a cliff in a week they close nothing.** He closed 1 of 29 that week against 15 earlier. **That is the number behaving as designed, not a fault**, and it is why the weekly figure and the all-time figure disagree so sharply. |
| `⛔ BLOCKED` **(a) Time to Price shows only Josh and the team average** | ⛔ **NOT A BUG — blocked on the offer-price permissions change** | 8 of 9 reps have no `price_pif` saved, and Justin ruled **managers and above set it, not reps.** `lib/price-moment` only runs when the rep's own price is on file, so the graph correctly reports **UNMEASURED rather than inventing a number.** ⚠ **Resolves when the offer-price row does — cross-referenced, not separately fixable.** |

---

## ▰ FEATURES — MAJOR

**⚠ Justin's queue order puts ROLE HIERARCHY fourth, after the admin rebuild, the password page and Zoom.** It leads this section for that reason.

| item | state | what it needs |
|---|---|---|
| `▰ MAJOR` **Role hierarchy — A HIERARCHY WITH INHERITED VISIBILITY, not a rename** | ⚠⚠ **AMENDED 2026-08-26 — read this cell before the old one.** Target shape: **Company > Head of Sales/CSO** *(NOT needed yet — shape only)* **> Director > Manager > Closer**, at agency scale roughly **9 managers and 63 closers under ONE company**. ⚠⚠ **THE VISIBILITY RULE (Justin): *"A manager can only view closers under him. Directors can see all teams below him. Head of sales can see all teams in the company."* — EVERY LEVEL SEES EVERYTHING BENEATH IT AND NOTHING BESIDE IT.** ⚠⚠ **THIS IS NOT AN ADMIN-VIEW RULE.** It governs **every number Scout produces** — team averages, digests, objection drilldowns, needs-work, rankings, EOD, every synthesis. **Two directors in the same company must never see each other's figures.** ⚠ **CONSEQUENCE 1 — "a company is a renamed team" stops being true at this scale**; here a company is the WHOLE TREE. That ruling is correct today and is **flagged, not superseded** — same expiry the `team_name` storage entry already names. ⚠ **CONSEQUENCE 2 — scope resolution becomes a TREE WALK, not a lookup.** `resolveTeam` (`routes/team.js:111`) is ONE step off `managed_by`; a director is TWO, a head of sales THREE — on every page load, for 63 closers. **Recorded as a design question; do NOT design it now.** Original framing follows: Top-down **Executive** (currently `admin`) > **Director** (NEW TIER) > **Manager** > **Closer** (currently `user`) | ⚠⚠ **THIS IS A DATA MIGRATION, NOT A RELABEL.** The role strings `admin` and `user` are **stored values, not display text** — every permission check, every query that filters or branches on role, every test that asserts a role, and every stored row carries them. ⚠⚠ **DIRECTOR IS A GENUINELY NEW TIER, not a rename.** Manager today is a flat "manager of a team"; Director introduces **managers-under-a-director**, a second level of hierarchy the data model has never had. **OPEN: does a Director see every team beneath them aggregated, each separately, or both?** ⚠ **TOUCHES THE PERMISSION WORK SHIPPED 2026-08-20** — `canMarkNotSalesCall` and `canTagOutcome` **deliberately differ and must not be unified**; both branch on role and both need revisiting under a four-tier model. **Whoever picks this up must read that ruling first.** ⚠ **OPEN QUESTIONS, carried so they are not rediscovered:** does a Director inherit a Manager's permissions exactly, or a superset? · can a Director sit above another Director? · does "Executive" still mean account-wide, or organisation-wide? · what happens to existing rows during the rename — a mapping migration, or dual-read during a transition? ⚠ **AGREED, NOT STARTED — Justin has said the team objection drilldown is the priority.** |
| `▰ MAJOR` **Invite-based onboarding — each level invites the level below** ⚠ **WHEN THIS LANDS, ADD A BILLING/ACCESS TICKET CATEGORY.** There is deliberately none today — nobody can be invited or billed, so it would be an option nobody could pick, and an unpickable option teaches people the list is wrong. | ✅ **Justin's design, ruled 2026-08-26.** A director onboards and enters their managers; each manager onboards and enters their closers. **The tree is built top-down as people arrive, so nobody at Scout ever touches a customer's org chart.** | ⚠⚠ **THIS IS A BILLING FEATURE WEARING AN ONBOARDING ONE, AND THAT IS THE WHOLE REASON IT IS MAJOR.** An invite that adds a paid seat needs a card on file, a subscription that can grow and shrink, and **a company that exists as a billable thing**. ⚠⚠ **HARD PREREQUISITE: `Account/org entity` (this list, QUEUED). You cannot bill a company that is not an entity — do not design it here.** ⚠⚠ **SECOND PREREQUISITE: `Role hierarchy` (bottom of this list). "Directors invite managers" presumes Director EXISTS, and it does not.** <br><br>**THE RULINGS, all three settled:** **(1) A MANAGER CAN INVITE REPS, and doing so ADDS A PAID SEAT** billed to the company's stored card. **(2) ONLY DIRECTORS AND ABOVE CAN INVITE MANAGERS**; only admins — and eventually Head of Sales — can invite directors. **A manager can NEVER invite a peer.** **(3) ASK FOR THE ROSTER AFTER ONBOARDING, NOT DURING IT.** They land on their team page and *"add your reps"* is the first thing there. Same data, after they have seen what they signed up for — **a manager typing eight names during signup abandons it.** **(4) CARD DECLINED = THE INVITE CANNOT BE SENT.** Not sent-then-blocked at login: **blocking at the invite is the honest failure; the alternative strands a person who was told they had access.** **(5) REMOVING A REP FREES THE SEAT. There is NO seat cap** — a company can have as many as it can pay for. <br><br>⚠⚠ **OPEN — CARRIED, NOT ANSWERED:** **(a) What happens to a person's calls and history when a rep is removed and their seat freed.** The deletion and deactivation rulings already cover the DATA; what is open is whether *removing-for-billing* means **deactivate** (numbers stay, reversible) or **delete** (history destroyed) — **very different consequences.** **(b) Does an invited person who never accepts still occupy a paid seat?** **(c) Does a manager see the billing consequence BEFORE inviting** — *"this will add a seat at $X/month"* — **or discover it on the invoice?** <br><br>⚠ **OVERLAPS TO RECONCILE, NOT BUILD TWICE:** the **unconnected-source badge** exists because managers cannot see their own team's state, and **forced password change / self-serve reset** are the same front-door arc. **All three are the same underlying thing: managers owning their own roster.** |
| `▰ MAJOR` **Replying to a ticket in-app needs the NOTIFICATION SYSTEM** | ⛔ **filed 2026-08-27 — say so rather than half-build it** | There is no notification system: **no table, no delivery, no read state.** Already a hard prerequisite for EOD manager approval. ⚠⚠ **A notification system built as a side effect of a support tool is how it ends up wrong for the three other things that need it.** **Replies happen by email until it is built deliberately.** |
| `▰ MAJOR` Account/org entity | Real billing needs it (seats, one invoice) | ⚠ per-user customisation does NOT wait on this — ruled 2026-08-20 |
| `▰ MAJOR` CRM integration (GHL, Close.io) | Read then write, diff-then-confirm on writes | after call analysis is proven |
| `▰ MAJOR` Stripe billing | `SKIP_BILLING=true`; keys pending | needs the account/org entity first |
| `▰ MAJOR` Zoom clip extraction (sub-stage 3) | download → ffmpeg-cut → discard source → store clips | the only part needing hosted storage |
| `▰ MAJOR` Adaptive Learning Engine (desktop) | Post-call pattern extraction into the KB | desktop app dormant |
| `▰ MAJOR` Teleprompter section-mode | Prompt only during objection/close | architecture preserved, unscheduled |

---

## ⏳ TRIGGERED — deferred with the condition that makes them due

| item | trigger |
|---|---|
| **Exposure-sweep guard** | ⚠ **See OPEN below — currently unguarded.** Due before another full-bleed/brightness change |
| Zoom VTT adapter validation on a real call | Josh records a cloud call with transcription on |
| Zoom 3301 requeue live-exercise | A transcript fetch actually races |
| Non-English prospect-name handling | A non-English account is onboarded |
| Empirical grader calibration | 50+ outcome-tagged calls exist |
| Objection handle-rate drift watch | Rate falling below ~20% and continuing = real failure, not the v17 narrowing |
---

## 🗄 CLOSED, RULED OR SUPERSEDED — history, not a queue

**⚠ These are here so a decided question cannot be re-asked.** Nothing in this section is work.

### ⚖ Rulings recorded 2026-08-28

| ruling | what it closes |
|---|---|
| **(a) DEACTIVATE LEAVES THE NUMBERS. DELETE DELETES.** At company scale exactly as at user scale — no new rule, no exception. | Unblocks **Deactivate a whole company** and **Delete a whole company**; both are now BUILDABLE and sit under the admin rebuild in MINOR. Reuse `lib/user-purge.js`. |
| **(b) MOVING A REP DOES NOT MOVE THEIR HISTORY.** Call data stays with the company where it was EARNED. | ⚠⚠ **SUPERSEDES the 2026-08-25 entry that accepted COPYING history to the new team and accepted the resulting double-count.** Corrected here **and in `CLAUDE.md`** — both readings are not left on file. The consequence is unchanged: needs a team stamp at call creation plus a sweep of every team-scoped query. |
| **(c) THE DIGEST COMPLIANCE QUESTION: SUPPRESS.** Scout does not surface legal or liability matters **anywhere** — not as coaching, not elsewhere. | The open question ("suppress entirely, or surface away from coaching?") is **CLOSED, answer (a)**. **Do not build a second surface for it.** The row is now a buildable BUG. |
| **(d) THE DAILY DIGEST STAYS A DAY BEHIND.** Deliberate. | Closes the row. Generation rides the sync cron's post-sync pass, so each ET day is built ~00:45 the following morning and the newest tab is always yesterday. The two alternatives (generate on read; a much shorter cron) were costed and are not wanted. |
| **(e) A DELETED USER'S DATA IS REMOVED FROM THE SITE COMPLETELY.** | ⚠⚠ **ESTABLISHED BEFORE SCOPING, AND IT IS NEITHER OF THE TWO FAILURE MODES.** Not a cached surface, and **not a delete path that missed a table** — measured 2026-08-28, **0 orphan auth rows**, so `lib/user-purge.js` leaves no debris and any NEW delete already removes everything. What remains is **(i)** three legacy tombstones from the superseded contract and **(ii)** nothing anywhere filters `active`. ⚠⚠ **The ruling REACHES the three tombstones, but purging them destroys 117 calls / 117 analyses / 87 highlights / 56 prospects currently counted in Josh's team history, and they were deleted under a contract that promised the calls would stay — so that consequence is a SEPARATE decision and sits in NEEDS JUSTIN.** ⚠ Once they are gone the reported bug goes with them, and what is left is purely about DEACTIVATED users, whose numbers ruling (a) says should stay. |

**⚠⚠ THE PROCESS FAILURE THIS BLOCK EXISTS TO FIX, AND IT MATTERS MORE THAN THE FIVE ROWS: FIVE OF THESE HAD BEEN RULED BEFORE AND THE FILE STILL CARRIED THEM AS OPEN.** Justin answered three of them **twice**. **A decided question sitting in an open list gets re-asked.** Standing rule from now on: **when a ruling lands, the OPEN row is closed in the SAME edit** — the ruling and the closure are one change, never two.

### 🎫 The support arc — DESIGN RULINGS, now SHIPPED

**⚠ Kept because the design reasoning outlives the build.** The health snapshot (`a298652`), the ticket control and admin list (`b52fafb`), a person seeing their own tickets (`049d628`) and categories/screenshots (`c8d2f96`) all shipped 2026-08-27.

**⚠⚠ THESE NOW SIT ABOVE ZOOM, THE PASSWORD PAGE, THE ROLE HIERARCHY AND THE UNCONNECTED-SOURCE BADGE.** The evidence is this week's own record: **two support tickets out of a handful of beta users — Josh's Zoom, then Godwin's — and EVERY ONE needed the architect or Claude to query the database by hand to answer.** Godwin's took a server-log read to establish that **nothing was broken at all**. That does not scale past a handful of users, and it is not a reporting nicety — it is the only way a ticket gets answered.

**⚠ THE DEPENDENCY ORDER IS UNCHANGED AND IS THE POINT: the HEALTH SNAPSHOT IS BUILT FIRST. A ticket without it is just a message box.**

**⚠⚠ THE SNAPSHOT HAS NO UI (Justin's correction, standing).** It is **generated on demand and ATTACHED TO A TICKET** — the ticket list is the only surface a human ever sees. Do not design a health dashboard.

### ⚠⚠ THE SUPPORT CONTROL IS ON EVERY PAGE (Justin, 2026-08-26)
His words: *"it should be a tech support, submit a ticket or whatever you want to call it on EVERY PAGE they see."* Four things follow, and they decide the design:
1. **PERSISTENT CHROME — ONE CONTROL RENDERED ONCE AND SHOWN EVERYWHERE, NEVER COPIED ONTO EACH PAGE.** ⚠ This codebase has paid for the copy-per-surface mistake repeatedly and recently: **the objection label lists had to be unified TWICE** (a sweep missed three more), and **the company picker rendered on two surfaces and would have shown TWO on one page.** A control placed on ~15 pages by copying is that failure at maximum scale.
2. **THE TICKET CAPTURES WHICH PAGE THEY WERE ON.** ⚠ **Free context the app already has** — it turns *"X isn't working"* into *"X isn't working ON THE CALLS PAGE"*, and it costs nothing to record.
3. **IT MUST NOT OBSTRUCT ANYTHING.** A control on every page is on every page **forever** — it cannot cover content, steal a click target, or sit over a scroll region.
4. **⚠⚠ EVERY ROLE, NOT ADMINS. Closers are the ones who hit problems.** ⚠ **This is the opposite of the TICKET LIST, which stays ADMIN-ONLY because it exposes other companies' data** — same feature, two different permission answers, and conflating them leaks data. **The health snapshot still attaches automatically.**

**⚠ DEPENDENCY ORDER IS UNCHANGED: #6 before #7** — a ticket without the snapshot is just a message box. **Neither is urgent at 2 accounts; both become urgent at the first real customer.** Justin has given a priority only to #1; the rest sit at the bottom per his standing rule.

| # | item | priority | what it needs |
|---|---|---|---|
| 6 | **Account health SNAPSHOT — a function, NOT a page** | filed, prerequisite for #7 | ⚠⚠ **CORRECTED BY JUSTIN 2026-08-26: HE DOES NOT NEED A HEALTH REPORT SCREEN.** No admin page, no dashboard, nobody maintains it. It is a **function that generates a snapshot on demand when a ticket is raised**, and the snapshot is **attached to the ticket**. ⚠ That makes it **much smaller work than a page**, and **removes the admin-only permission surface a health page would have needed**. **Contents:** calls synced / graded / pending / errored + the **last error TEXT** · connection status and last successful sync per provider, with `last_sync_at` NULL or stale flagged loudly · whether the digest generated and whether it produced **zeros against real calls** · recent failures in enough detail to tell a **PROVIDER problem from a SCOUT problem**. ⚠⚠ **THE QUESTION IT EXISTS TO ANSWER: "is this ONE BROKEN ACCOUNT or a BROKEN PRODUCT?"** — John reports X at Company A and Jim reports Y at Company B on the same day; shared cause or two faults? Today that needs an engineer querying Supabase by hand. |
| 7 | **🎫 Tech Support ticket — THE ONLY VISIBLE SURFACE** | filed, **depends on #6** | A **Tech Support** control in Scout: describe the issue, attach screenshots, optionally paste a Loom link. ⚠⚠ **The TICKET LIST is the visible feature and the only thing Justin ever sees** — there is no health screen. ⚠ **What makes it worth building: the health snapshot is auto-attached to every ticket.** "X isn't working" is nearly useless; the same ticket carrying sync status, graded/pending/errored counts and the last error is often **self-answering**, because most reports will be a stalled sync, ungraded calls or a dropped connection. ⚠ The submitter is logged in — identity, company, role and account state are known. **NEVER ask the user for information Scout already has.** ⚠ **The ticket list stays ADMIN-ONLY, enforced server-side** — it exposes another company's data. ⚠ **ARCHITECT-READABLE (Justin's request):** he wants to say "check support" and have the architect read them, so open tickets must be **pullable to a file** (same pattern as `~/Desktop/scout-findings.md`) with **screenshots downloadable locally**; a Loom link is just a URL. ⚠ **Notification: Scout has NO notification system** — already filed as blocking EOD manager approval. This depends on that or on something simpler (email). |

### Previously open, now closed by measurement or by shipping

| item | state | what it needs |
|---|---|---|
| ~~Failed calls: no aggregate view, no retry button~~ ✅ **DONE `1af6cd1`** — the HISTORY half remains open below | ✅ shipped | ⚠ **No control can reach a failed call** — the ungraded batch is `sync_status='pending'` ∪ `status='done'` and an errored row is in neither, so **a human who knows a call failed has no button that retries it**. Visible one call at a time (red badge + red banner with the reason), **invisible in aggregate** — there is no "3 calls failed today". **RECOMMENDED: count failed calls beside ungraded ones and let the existing control retry them** — no new screen, no alerting. ⚠ Caveat: some can never succeed (a Zoom call with no transcript), so show those separately or the count never reaches zero. |
| **One transcript, one send — REFUSED on quality** | ⛔ measured 2026-08-26, **do not ship** | Merging the grader + extractor calls saves **46-47% of input tokens** (real API usage) but only **~37% of cost** (output unchanged, priced 5×), and less on short calls (31.9%). **Refused because quality dropped:** fewer objection moments on both calls that had any (4→2, 3→2), two of five grader sections missing on another, and merged output smaller every time. ⚠ **The three LONGEST calls looked perfectly equivalent** — sampling only those would have shipped it. ⚠ Also would have worsened failure coupling (today an extractor failure is non-fatal; merged, one parse failure loses both). The output ceiling is NOT the blocker. |
| **Josh's backfill STOPPED on cost at 495/581** | ✅ ruled 2026-08-25 — stopped deliberately, **do not resume without a ruling** | Justin halted at ~$50 in the final hour, $200+ for the day. **30-day surface is 167 of 168 — complete for every default view.** Residue is **78 pending** (never dispatched) + **6 stranded claims** (loops killed mid-call by the redeploy) + **2 errors** (1 Zoom no-transcript, permanent; 1 grader unparseable-JSON). ⚠ **All old history, on no surface he shows — a known deliberate residue, not silent casualties.** ⚠ **The only kill switch for a dispatched run is a REDEPLOY**; verify a stop by `max(analyzed_at)` not advancing, never by `processing` reaching 0. |
| **The willing/able boundary is THREE-WAY** | ✅ **BUILT 2026-08-26 (v27, `eadd4af`)** | Justin verbatim: *"LOGISTICAL IS WHEN THEY WANT TO BUY BUT PHYSICALLY CANT FOR SOME REASON"* and *"I NEED TO TALK TO MY WIFE/PARTNER/PET GOLDFISH IS SPOUSE-PARTNER. AND I WANT TO I JUST CANT AFFORD IT IS A FINANCIAL DQ"*. **Can't afford → FINANCIAL DQ (not an objection at all)** · **externally blocked → LOGISTICAL** · **able but hesitant → FEAR**. Discriminator: **willing-but-unable vs able-but-hesitant**, and unable splits by why. **Spouse/Partner = anyone they must consult**, defined by the need to consult, not the relationship. ⚠ **My flag about `logistical` being both named and excluded is WITHDRAWN — that is correct and intentional**: a genuine inability is not a coaching failure, so it is counted and shown but kept out of the handle rate. ⚠ **CROSS-REF: this is the likely cause of the Abu failure above** — the DQ boundary being unclear to the grader, not a digest-only bug. |
| **"Trust / Proof / Skepticism" maps to `fear`** | ✅ **BUILT 2026-08-26 (`d15a5d5`)** | Closes the open question on Justin's canonical six (Fear · Price · Timing · Spouse/Partner · Logistical · Other). ⚠ **It is a CLASSIFICATION PRINCIPLE, not a label mapping** — *"When someone is worried about paying because of whatever excuse, it's fear. Prospects will never say 'I'm scared'."* **Hesitation about paying is `fear` regardless of the wrapper** (proof, legitimacy, needing to check, wanting to think). Belongs in the extractor's classification guidance, **not a rename in a naming table**. ⚠⚠ **SUPERSEDED 2026-08-26 — PRICE IS NOT A CATEGORY.** Justin: *"Price is either fear or logistical so it can fold into one of those depending on the circumstance."* **The canonical set is FIVE: Fear · Timing · Spouse/Partner · Logistical · Other.** ⚠⚠ **This removes the re-classification problem entirely** — Price was the only item that was not a rename, so there is now **nothing to re-grade and no migration**, and the naming work is **purely a rename plus one source of truth for both surfaces**. Price folds into the three-way boundary: hesitant about money → **fear**; cannot move money now → **logistical**; genuinely cannot afford it → **financial DQ**, not an objection. **✅ SHIPPED:** the coercion maps it explicitly (`Trust / Proof / Skepticism → Fear`, `Price / too expensive → Fear`), pinned in tests. |
| **🚨 Nothing checks a disclosed qualification against the rep's threshold** | ✅ **BUILT 2026-08-26 (v28, migration 049)** | Josh's criteria (**"10k saved, not living paycheck to paycheck, 640 or above credit score"**) reach the grader through SELLING CONTEXT, and `qualification_covered` already stores the prospect's disclosure **with the verbatim quote**. **Nothing compares the two.** On the Abu call the stored evidence is *"My personal credit score right now is about 60 or something like that"* — below the threshold, on the row, unevaluated. ⚠ `qualification_covered` records that the ground was **COVERED**, never whether the prospect **PASSED** — measurement-only by design (v12). ⚠ **v27's boundary lets the model call an affordability failure a DQ when the prospect says so; it does NOT make Scout check a number against a threshold.** **✅ NOW BUILT:** the grader returns a verdict per criterion, verified against the prospect's own words at write time. ⚠ **Two of Josh's three criteria are numeric bars and can be checked; "not living paycheck to paycheck" is a JUDGEMENT with no threshold and will usually come back `undetermined` — that is correct, not a gap to close.** ⚠ **Future calls only**; nothing re-graded. |
| **⚠ A new account has 186 ungraded calls** | ⛔ **filed 2026-08-26 — needs a spend decision** | `yazan@soberlivingriches.com` synced **292 calls**; **105 graded, 186 ungraded, 1 stuck**. The backfill was running when the objection-category deploy went out — **pushed 18:03:22, last analysis written 18:03:45, then it stopped.** ⚠ **Causation not proven** (a container swap takes a minute or two and the job may have ended on its own), but **nothing was left mid-analysis** — the in-flight count did not move, so any interruption was cleanly between calls. ⚠ The rows are `pending` and recoverable through the grading control; **re-running them costs money, so it is Justin's call.** ⚠ **The lesson is already filed above: commit immediately, push deliberately — and a blocked push is an escalation, not an indefinite wait.** |
| `▪ MINOR` **Assign people by COMPANY NAME, not manager email** | ✅ ruled 2026-08-26 — **BOTTOM of the list** unless Justin says otherwise. **FILED, NOT BUILT** | The admin Company column **already moves people between companies — it just does not read like it does.** It lists MANAGER EMAILS, so moving someone into Scout Systems means picking `josh@scoutsystems.io` and already knowing that address IS Scout Systems. **Justin could not find the feature at all.** ⚠ Justin: *"That's just to assign it to a different manager's email. If I could drag and drop or have an actual company dropdown that would be better. Then it auto adds them under whatever manager(s) are on that team."* → **pick a company BY NAME and let Scout resolve the manager; drag-and-drop preferred, a company-name dropdown is the minimum.** ⚠ **Drag-and-drop is materially more work than a dropdown — say so when this is picked up.** ⚠⚠ **THE MULTI-MANAGER QUESTION IS ANSWERED (Justin, 2026-08-26) — IT IS NO LONGER A BLOCKER, IT IS A DEPENDENCY.** His words: *"If a company has more than 1 manager they will have a director to fold up into. That's what that role is for."* **A company with several managers points at its DIRECTOR, and the managers sit underneath — so picking a company is never ambiguous.** ⚠⚠ **CONSEQUENCE: THE ROLE HIERARCHY IS A PREREQUISITE FOR A CLEAN VERSION OF THIS, not merely a rename exercise sitting elsewhere on the list.** Without Director there is nothing for multiple managers to fold into, so a company-name picker built first would work only while every company has exactly one manager and would need revisiting the moment one does not. ⚠ **The `team_name` storage entry records the same expiry from the storage side** — one problem, two surfaces; reconcile both when either is built. ⚠ **SECOND ODDITY, SAME ROOT CAUSE, FIX BOTH OR NEITHER: the HEAD of a company shows "None — single user".** Josh Pinner heads Sober Living Riches and his own Company column reads as if he were in no company, because `managed_by` is null for a head. **Both faults are the screen showing a DATABASE RELATIONSHIP instead of a fact about the business** — which is exactly what the ruling is asking to stop. |
| **⚠ gabriel synced 129 calls today — 123 ungraded** | ⛔ filed 2026-08-26 — **~$30, not approved** | A sixth Sober Living account came online mid-session (`fetched=129 inserted=129 pages=13 analysis_dispatched=20`). ⚠ **The survey earlier in the same session showed him at ZERO** — new accounts are still arriving, so any cost total has a short shelf life. **Finishing everyone except yazan is now ~$125.** |
| **✅ Sober Living Riches graded — everything reachable from here** | ✅ done 2026-08-26 — **192 graded, ~$98, `saturation=0` across both passes** | **nick 117/117 · dre 34/34 · yazan 291/294 · josh.n 7/8 · gabriel 89 (40 untranscribable).** **ZERO calls left waiting — verified twice.** ⚠ **The first verification found 2 of gabriel's still `pending`, so "nothing is waiting" was briefly wrong.** Running them returned the same empty-transcript result as his other 38 — **they were never retryable, merely unattempted**, which is exactly the distinction a bare pending count cannot make. ⚠⚠ **THREE GROUPS COULD NOT BE DONE FROM HERE, and none is a failure:** **joshua 175** (Fathom token expired 42 min before the run; **renewing needs client credentials that live only on Railway** — the site's own control works) · **godwin 102** (Zoom-sourced; **Zoom credentials are also Railway-only**) · **yazan 174 Zoom** (`zoom connection not found` — he has 181 Zoom calls and **no Zoom connection**; **reconnecting fixes all 174 at once, nothing else will**). ⚠ **I stopped the first attempt 20 calls in** rather than let it stamp 176 of Josh's calls `error` for a reason about MY environment. **The runner now classifies that as `runner_missing_credentials`, never as a permanent property of the call** — a call that fails because a secret is absent locally is not a call that can never be graded. |
| **✅ ONE graded call hid the whole backlog — the onboarding card removed itself** | **SHIPPED 2026-08-26** | The card said *"Your recent calls are graded"* and deleted itself — true of 20 of 121, read as finished. Now says **"19 of your 123 calls are graded, the other 102 are waiting"**, links to the existing Calls control rather than building a second one, and **only leaves when something is graded AND nothing is waiting** (a fully-graded user still sees it go). ⚠⚠ **AND THE EARLIER FILING WAS WRONG ON THE KEY POINT:** it said a Zoom-only user reaches the count and control fine. **The COUNT is source-agnostic; the CONTROL was gated on `fathomStatus.connected`** — so Godwin saw "102 not graded yet" with **no button anywhere**, and the progress poll had the same gate and would have reported a healthy run as dead. Fixed by moving the counts to **`GET /me/grading-backlog`**, which is where the answer actually lives. |
| **✅ Time to Price showed one rep while Closing % showed many** | **SHIPPED 2026-08-26** | **Not the drop-empty bug it looked like — 8 of 9 reps have no `price_pif` saved**, and `lib/price-moment` only runs when the rep's own offer price is on file. So Yazan reads 0 priced calls out of 290 graded: not slow to price, **UNMEASURED**. The chart now names the excluded reps and keeps the two reasons apart — *no offer price saved* (can never be measured until they save one) vs *no data in this window*. ⚠ **One function, four graphs** — `repSeriesChart` drops empty reps for every metric; it only showed on the sparse one. |
| **✅ Deleting a user now deletes their calls and history** | **SHIPPED 2026-08-26** | Justin's ruling — the tombstone is **superseded, not a bug**, and it is recorded that way in three places so nobody restores it. Same blast radius as delete-company and **reuses that exact path** (`lib/user-purge.js`, one implementation, two callers). **DEACTIVATE is the safeguard** — keeps every number, fully reversible — **and the reason only admins may delete**, enforced server-side rather than by hiding a button. Confirmation names who, how many calls, that there is no undo, and points at Deactivate. Verified on a throwaway: every table returned to its exact pre-test count, no other user's figures moved. ⚠ **`deletePlan` mode renamed `hard` -> `purge`** so the meaning change could not pass silently. |
| **The daily digest is a day behind by design** | ✅ **RULED 2026-08-28 — DELIBERATE, ROW CLOSED. It stays a day behind.** | `GET /team/digest` is a **cache read only**; generation rides the sync cron's post-sync pass, so each day is built **~00:45 ET the following morning** and today's never exists during today. Verified in cache: `08-24` written `08-25 04:47Z`. ⚠ Not a timezone bug — ET is used correctly. **The two alternatives and their cost:** generate **on read** = one Claude call per manager per page load (seconds of latency, repeated per view unless cached within the day); or a **shorter cron** = one call per manager per interval, most producing nothing new. Today's design costs **one model call per manager per day**. Justin has not ruled; neither was built. | ⚠⚠ **RULING: this is intended and is not a defect.** The two alternatives were costed before asking: generating **on read** is a Claude call per manager per page load (seconds of latency, repeated per view), and a **much shorter cron** is a call per manager per interval with most producing nothing new. The current design deliberately rides the 2-hourly sync so the cost is **one model call per manager per day**. **Do not build either alternative.**
| **Company picker: move it, and give it a chevron** | ✅ **DONE 2026-08-26** | Justin: the picker moves to the **TOP RIGHT of the company name card**, at the top of the page. It currently sits in the controls row; **it works and switches data correctly** — this is placement, not function. ⚠ **Fold in the existing chevron observation rather than doing this twice:** beside "All objections" (which has one) it renders as a static text field, and **two people concluded it had never shipped**. **✅ MOVED** to the top right of the company card, and it is now rendered in **exactly one place** — it was in the controls row AND in the drilldown's own strip, and since the drilldown also renders the header, leaving both would have shown TWO pickers. ⚠⚠ **THE CHEVRON WAS NEVER MISSING**: `.user-select` has had a data-URI SVG chevron since the v2 rewrite (`c4dd0f7`), measuring 5.45:1 — **nothing was added, because adding one would have produced two**. The likeliest explanation for the reports is that the picker returns empty for a non-owner, so what was being looked at was the company NAME. ⚠ **Placement NOT VERIFIED visually** — no browser responded. |
| **The grading control could be fired repeatedly** | ✅ fixed for the new control, ⚠ note the evidence | Three concurrent analyze loops were found running on Josh's account when this block started — the old control's guard reset as soon as the request returned. The new one holds a run state across the whole dispatch, and `claimAnalysisRun` was already preventing double-spend per call. |
| **Admin dropdown cycling companies** | ✅ **already works** | The owner team picker exists, is **server-enforced** (a manager passing `?team=` gets their own board back — proven with a forged actor, plus a non-vacuity case showing an owner CAN pivot), and now labels by **company name** rather than `"<email>'s team"`. ⚠ **Not visually demonstrable yet: there is exactly ONE company**, so there is nothing to cycle through. |
| **A team has no NAME beyond the head's row** | ✅ solved | `user_profiles.team_name`. A column, not a table — see CLAUDE.md for why that choice is load-bearing. |
| **Customize View (team page)** | `soon` tag in the team controls row, where the real control will live | **tag SHIPPED `4d6d9e6`** and verified in the render (SPAN, tabIndex -1, dimmed). The feature itself is not started — see AGREED, NOT STARTED |
| ✅ **Delete a user even when they have data** | **SHIPPED 2026-08-26 (`9009679`)** — and the ruling went further: a delete now destroys their calls and history too. Reuses the delete-company path (`lib/user-purge.js`). The block quoted below is the SUPERSEDED design, kept so nobody restores it. |
| ✅ **A rep pivot showed another person's data** | **FIXED 2026-08-27 (`31516d8`)** | Enumerated by capability: **18 of 22 rep-scoped lanes survived a pivot**, not the four noticed. Fixed AT THE PIVOT with **opt-in survival** — state is snapshotted at boot and anything not in `PIVOT_KEEP` resets, so a lane added tomorrow cannot inherit it. ⚠ **TWO doors, not one:** the Calls-page rep picker had the same defect, so switching rep there and opening Coaching showed the old rep's numbers. Proven by executing the real pivot functions — the original code fails 3 tests including both end-to-end ones. |
| ✅ **"No objection data for this rep" on reps who HAVE data** | **FIXED 2026-08-27 (`f391bd9`)** | `/team/rep-series` is now rep-aware: it resolves the VIEWED rep's board, not the caller's. Proven on production — unnamed, the board holds 5 and dre is absent; named, it resolves his manager's board of 9 and includes him. ⚠ **The copy was the other half** — it blamed the date range, and dre has 19 objections in 7d, 30d and 90d. A message must not name a cause it has not established. |
| ✅ **Account health snapshot** | **BUILT 2026-08-27 (`a298652`)** — `GET /admin/users/:id/health`, owner-only | Attaches to a ticket; **not a dashboard, no page**. Leads with WHAT THE USER CAN SEE, because both real tickets were working syncs misread as broken. Returns Godwin as *"WORKING, BUT INCOMPLETE ON SCREEN — 21 of 124 graded, 101 waiting"*. ⚠ **Migration 051 stores what the last sync did** — the one fact that settled his ticket and previously existed only in a log. ⚠ Cost: head-counts + 2 small selects, no model call, **1.8-4.2s** measured. ⚠ **Ticket submission + the ticket list are still to build.** |
| ✅ **Support tickets — raise from any page, admin-only list** | **BUILT 2026-08-27 (`b52fafb`)**, migration 052 | One control rendered once outside `#content`; every role can submit, only an owner can read the list. **The snapshot attaches AT RAISE TIME**, never on read. ⚠ **The ticket lands even if the diagnostics break** — proven by making the snapshot throw. ⚠ Exercised end to end on production; test tickets removed. **~5.7s to submit.** |
| ✅ **A person can see their own tickets** | **BUILT 2026-08-27 (`049d628`)** — status only | ⚠⚠ **A SEPARATE QUERY, not the admin list filtered** — that list carries other companies' account state and one forgotten filter would leak every snapshot. ⚠ **The snapshot columns are not even SELECTED**, which is stronger than not rendering them: not-fetched cannot leak. ⚠ Also fixed: **the reference was displayed nowhere an admin could see**, so a code read out on a call could not be found. It now leads every row, with search. |
| ✅ **Ticket categories + screenshot + video link** | **BUILT 2026-08-27 (`c8d2f96`)**, migration 053 | Five approved categories wired; free text still required, the category not — **a category with no message is a ticket nobody can action.** ⚠ **Storage was already provisioned and unused (0 buckets, 0 objects) — no paid tier was touched.** Private bucket, images only, 5 MB, enforced on the BUCKET as well as in code. ⚠ Upload is its own request so Send stays at ~6s, and **the ticket sends even if the upload fails.** ⚠ Access gated in the query: owner or uploader, five-minute signed URL, never a public one. |
| `👁 REPORTED` **(e) Godwin 0 of 36 handled in one week — THE ZERO IS REAL** | ✅ **investigated 2026-08-27, no fix needed** | ⚠ **NOT a Zoom detection failure.** Aug 21-27: 29 graded calls, **40 objections extracted and EVERY ONE carries a resolution (0 null)** — detection ran. 20 partial, 20 unhandled, 0 handled. Transcripts read normally: avg **315 turns, 52 minutes, score 60.** ⚠⚠ **WHY THE WIDER RANGE LOOKS BETTER:** his raw all-time handled rate is **11 of 138 (8%)**; the UI's higher figure includes objections **credited because the CALL CLOSED**. He closed **1 of 29** that week against **15 earlier** — so almost nothing got credited. ⚠ **The real finding is that `partial` dominates (74 of 138). That is a coaching fact, not a defect.** |
| ✅ **(b)+(d) Per-Closer grid rows are clickable** | **FIXED 2026-08-27** | ⚠⚠ **ONE FAULT REPORTED TWICE.** The grid rows share `.team-detail-row` with the score list, which carries `cursor: pointer` and an accent hover — **they inherited the clickable look without the behaviour.** A row that invites a click and does nothing is worse than one that never invited it. Now opens that closer's objections, reusing `setUser` rather than adding a second pivot door. |
| ✅ **(c) The team view forgot the company on refresh** | **FIXED 2026-08-27** | ⚠⚠ **IT FORGOT BECAUSE IT WAS NEVER STORED — not because something reset it.** `teamSelected` lived only in module state; a refresh rebooted it to null, which means "default team". Its sibling the rep filter has persisted to localStorage all along. ⚠ **The stored key is VALIDATED against `/team/context`** — a stale or no-longer-permitted team would otherwise give an empty board with nothing saying why. |
| ✅ **(g) "Objection Handling" removed from the date-picker row** | **FIXED 2026-08-27** | Justin: *it makes no sense there.* It sat beside the date picker, which reads as a filter, so a navigation button looked like a way to narrow the data. ⚠ **The drilldown is still reachable from the gauge and the per-closer card** — removing the only way in has cost a capability here twice. |
| ✅ **Managers and above can grade a REP's backlog from the Calls page** | **BUILT 2026-08-27 (`225e741`)** | The proper fix for handing a customer written steps. ⚠⚠ **The self-only guard was REPLACED, NOT REMOVED** — `/fathom/update-analyses` still passes `req.user.id` and cannot name anyone else; the cross-user case is a separate role-gated route. ⚠ **Boundary decided and enforced:** a manager may grade only their own team, an owner anyone. All-time stays owner-only on the SIGNED-IN role — one cap, not two. ⚠ **Proof:** dry runs on production — manager→rep priced **95**, manager→self priced **0**. Different numbers is the evidence. ⚠ Confirmation names whose calls and whose money. **NOTE: viewing another rep's CALL LIST already worked; this block added the grading.** |
| ~~Wordmark → Saira wide (`wdth 125`)~~ | **RETIRED 2026-08-21** — superseded by the image ruling | The wordmark is Justin's logo image; the mark replaces the O, so no typeface can draw it. ⚠ Kept as a line rather than deleted so the next reader does not re-open it: the A/B/C typeface question is CLOSED, not deferred, and its measured k values live in `CLAUDE.md` as history |
| ~~`🐛 BUG` Forced password change on first login~~ | ✅ **NOT A BUG — RETIRED 2026-08-26. Justin was right and my row was wrong.** | ⚠⚠ **THE CLAIM WAS THAT WELCOME EMAILS CARRY A PERMANENT CREDENTIAL. THEY DO NOT.** The email carries a Supabase **recovery-type action link**, and it is **single-use and time-limited**. **SINGLE-USE IS PROVEN FROM LIVE DATA, not from the docs:** of the three users ever sent one, `josh@scoutsystems.io` used his and his `auth.users.recovery_token` is **cleared**, while two unused links still hold theirs — consuming the token wipes it. **EXPIRY is enforced by the platform:** `web/set-password.html` handles Supabase's `otp_expired` explicitly (*"expired or already been used"*). ⚠ **HONEST LIMIT: the configured expiry VALUE is not verifiable from here** — it is a GoTrue setting readable only in the Supabase dashboard or via the Management API, and no Supabase PAT exists on this machine. `CLAUDE.md` records it as 24h, set during the welcome-email work. **Single-use alone already defeats the "permanent credential" claim.** ⚠ **THE TEMP PASSWORD IS NOT THE NORMAL PATH:** the console shows it **only when the email fails or Resend is unconfigured** (`admin.html` hides the box on `invite_sent`). Production is configured and the invite path is what runs. ⚠⚠ **KEPT SEPARATE FROM THE COSMETIC SET-PASSWORD PAGE ROW** (its own section above: logo, password rules, reveal toggle). **Conflating the two is what produced this false bug.** ⚠ A bug list containing a non-bug devalues the tag, so this row is struck rather than quietly re-worded. |
| ~~`loadTeamWindow` counts SYNTHETIC rows~~ | ✅ **DONE 2026-08-24** (`7a83b2a`+`32982cb`+`dda8739`) — and it was SIX consumers, not three, plus five more aggregators that bypassed the loader entirely. See LIVE. | The remaining durable fix is an `is_synthetic` column so the rule reads a property instead of parsing a name |
| ~~Login page: 5 unconfirmed renders~~ | ✅ **CLOSED 2026-08-20** | All five looked at on the real login document (fetched same-origin into an exact-sized iframe with only `checkExistingSession()` neutralised, because a signed-in browser redirects away from `/login`). 1 no faux-bold — every element computes 300, a weight Saira's 100–900 axis really has · 2 mark box derived, `--mark-top` = 0 and height = viewport − gap at all 8 sizes · 3 viewBox cropped to ink (`0.464 2.464 19.072 17.105`, intrinsic 400×359, aspect matches) · 4 already confirmed by Justin · 5 first ring at the top confirmed at every viewport |
| ~~`8bd06ec` owed checks~~ | ✅ **CLOSED 2026-08-20 — the ink sweep is done and the answer did not move** | Swept 8 viewports at the raised position: 11 exposed elements enumerated **from the DOM**, worst case **4.58:1 on `--muted` every time**, AA holds, **0.21 stands for the fourth time**. ⚠ The sweep's floor check caught an instrument fault first — walking ancestors to `<html>` found BODY's opaque background and marked all 11 "shielded", reporting a flawless **zero exposed while looking at nothing** |
| ~~The published privacy policy describes Zoom clip handling that does not exist~~ | ✅ **RULED 2026-08-22 — Justin: BUILD IT, do not soften the policy.** Moved to the Zoom block at the top of this file | The statement stays false until the build lands, knowingly. Nothing to decide here any more; the work is filed with the Zoom item |

### ⚠ A note kept from the old ordering, because it still decides tag choices

| |
|---|
| **⚠ WHERE A ROW IS GENUINELY BOTH, the tag is the one that decides its PRIORITY, and the row says why.** Live example: *"the digest gave confident, wrong coaching"* is tagged `▪ MINOR` because the BUG half shipped in v27 and what remains is a capability the grader has never had. |

### (superseded) the original Zoom filing, 2026-08-22

**⚠⚠ THE HEADLINE BELOW IS WRONG.** It was disproven on 2026-08-26 by measurement against 48 real analysed Zoom calls — the current Zoom section above is the live answer. **Kept only so the original observation is not lost; it must never sit above the current one.**

<details><summary>original filing — DISPROVEN, history only</summary>

**⚠⚠ FILED, NOT INVESTIGATED. Justin's ruling: recon happens AFTER this session, and Zoom stays PINNED until he lifts it. Do not measure, diagnose or fix any part of this from the entry below — it is a report of what he observed, not a set of findings.**

> **⚠ JUSTIN'S REQUIREMENT, and it is why this sits above everything else:**
> **"Zoom HAS TO work as good or better than Fathom, because not every closer is going to use Fathom."**

Since Josh connected Zoom, Zoom calls appear in the call library **alongside** Fathom ones, and three things are wrong at once:

| # | observed | note |
| # | observed | note |
|---|---|---|
| **a** | **No prospect name.** Zoom rows are titled *"Josh's Zoom Meeting"* / *"Josh's Personal Meeting Room"* where Fathom rows carry the prospect's name. | ⚠ Cross-reference the **PMR title finding**: a Personal Meeting Room gives *every* call an identical, non-distinguishing title — which is very likely why no prospect name is resolved. The prospect-name resolver's precedence is grader → diarized → cleaned title → NULL, and a PMR title carries no candidate. |
| **b** | **No analysis.** A **57-minute call marked Closed** shows *"No highlights were extracted for this call"*, a section grade of **F on Intro**, and coaching text reading **"This recording contains no sales conversation to coach against."** | ⚠ Open: **is the transcript parsing at all, or arriving empty?** A grader returning "no sales conversation" on a real 57-minute sales call is being handed nothing — that reads as an ingestion or adapter failure, not a grading one. ⚠ Open: **does a re-analyse trigger fix existing rows, or is the ingestion itself broken?** |
| **c** | **BOTH sources are ingesting the same meetings.** | ⚠ This is exactly the duplicate exposure the **ONE ACTIVE SOURCE PER USER** ruling was written for — and that ruling was **never built**. See it in `CLAUDE.md`: prevent new dual connections, do not force-disconnect, provide a deliberate *Switch provider* action, and **if enforcement keeps the FIRST source active the incumbent always wins and the newcomer can never generate the evidence it is being asked for** — for Josh the active source must be **Zoom**. |

### ⚖ JUSTIN'S RULING — BUILD ZOOM CLIP EXTRACTION SO THE PUBLISHED POLICY BECOMES TRUE (filed 2026-08-22)

| item | ruling | what it actually involves |
| item | ruling | what it actually involves |
|---|---|---|
| **Zoom clip extraction — make the privacy policy true** | **Justin's ruling: BUILD IT, do not soften the policy.** His reasoning: it was judged doable when originally reconned. | ⚠ **NEITHER STEP EXISTS TODAY** — verified: `pickTranscriptFile` selects only `file_type='TRANSCRIPT'`, `downloadFile` runs only on that URL, and there is **no ffmpeg, no `.mp4`, no `createWriteStream`, no `os.tmpdir`** anywhere in the backend. This is **download + cut + store + serve**, not "add ffmpeg". ⚠ The policy states it in the **PRESENT TENSE**, so **until the build lands the published statement is false today, and Justin is carrying that risk knowingly.** ⚠ **CORRECTED 2026-08-24: Scout is APPROVED — the "reviewer mid-assessment" urgency that used to sit here is STALE and has been removed. The statement being false still stands on its own; the deadline pressure does not.** ⚠ It also claims **short-lived signed URLs** and **permanent deletion of the full recording** — both must actually be implemented, not just the cutting. |

**⚠ DO NOT BUILD ANY OF IT YET. Zoom stays PINNED until Justin lifts it.**

**⚠ Known-relevant context already on file, so recon does not rediscover it:** the VTT adapter has **never been exercised on a real-length call** (everything it has parsed is ≤120 seconds); the 3301 requeue has **never fired live**; and every Zoom row that existed before Josh connected belonged to the **security reviewer** and was a 2-minute test meeting. Check **whose** a Zoom row is and **how long** it is before drawing any conclusion from it.

</details>

</details>

---

## ✅ LIVE — shipped. **The largest section, and it is history, so it sits at the bottom.**

| item | one line | evidence |
|---|---|---|
| **v29 — BOTH SIDES OF EVERY MOMENT** (`d420916` + **`3fa1c05`**, 2026-08-28) | `closer_response` on **every** citable moment type. ⚠⚠ **`d420916` ALONE WAS INERT — the prompt asked and the SANITIZER discarded it for five of eight types.** Caught by a 30-call sample that wrote **0 of 137**; `3fa1c05` hoists the assignment and the same 30 calls now write **134 of 134 (100%)**. | ⚠⚠ **THE FULL RE-GRADE IS STILL NOT RUN — ~1,331 calls, ~$320–340. Justin rules.** Sample: 105 spans hand-checked, **76 (72%) independently proven to be the closer**, 7 proven the **prospect**, one reply **121 turns** from its moment — all excluded by the existing `closer_response_verified` gate. No regression (59/60 on the three types that always worked). ⚠ **`__no_reply__` fired 0 times across 51 real opportunities — that sub-feature does not work and should be fixed BEFORE any large re-grade.** ⚠ **Zoom is UNVERIFIED** (creds are Railway-only; tokens had 18 min left and a local run would rotate single-use refresh tokens against two live customers). ⚠ The 69% cannot be moved by a sample — it is per-user-per-range and the sample is 3–4% of any window |
| **Raw newline in grader JSON killed whole analyses** | One stray line break threw away an entire call's grading | shipped with v28, **PRE-EXISTING and found by the truncation gate**. ⚠⚠ **The cause is v25's own paragraph-break instruction** — we ask for blank lines in `follow_up_email`, the model emits LITERAL newlines, and JSON forbids them inside a string, so `JSON.parse` rejects the ENTIRE response. ⚠ **Not truncation** — `stop_reason=end_turn`, 2618/4500, braces balanced; every length check passes. Deterministic on the longest call (2 of 2 runs, 10 raw newlines). **2 production analyses sit in `error` for this.** ⚠ **The repair runs ONLY after a normal parse has already failed, so it is a provable no-op on healthy responses**; applied to both extractors. Recovering the failing call yields `outcome=closed` with its paragraph breaks intact. ⚠ **The 2 casualties were NOT re-admitted — retrying one costs its whole window; spend not approved.** |
| **Every objection category is listed, including empty ones** | Five rows in both places, not three | shipped `c7541c1`. The per-closer grid showed five columns while "Handle rate by objection type" below it showed three — empties were dropped at **both** ends (built lazily server-side in `bucketTotals`, then filtered again client-side). ⚠ **Zero is a measurement, absence is not**: a missing category reads as "does not exist", 0/0 reads as "nothing came up this period". ⚠ **An empty category sorts LAST rather than being ranked** — n/0 is NaN and NaN comparisons make a sort silently incoherent. ⚠ **Enumerated the rest by capability**: no other category list drops empties; the remaining lazy accumulators are time buckets and the needs-work ranking, which already states insufficiency explicitly. **Verified on the served page.** |
| **Team view and Coaching Review now agree on objection names** | `Fear · Logistical · Timing · Partner · Other`, one list, one order | Justin: *"I need the obj handling category on the teams view to match this in the coaching review."* **The team view moved to match the Coaching Review**, not the reverse. `Spouse/Partner → Partner`, `Uncategorized → Other` (his written ruling). ⚠⚠ **The previous "one source" sweep had MISSED THREE MORE LISTS** in the Coaching Review — that is why the two still disagreed after a block whose whole purpose was to make them agree. ⚠ **Order is load-bearing**: they already shared four of five names and still read as different. ⚠ **The guard asserts the DERIVATION, not the strings** — a literal list that happens to match today is how this drifts a third time. ⚠ `NEEDS_WORK_LANE_VERSION v3→v4`, because the labels live inside the cached payload. **Display only — no grader change, nothing re-graded.** |
| **Company picker on the company card** | One picker, top right of the company name card | Moved out of the controls row **and** the drilldown strip — the drilldown renders the header too, so leaving both would have shown two. `teamSelectHtml()` now has one definition and **one** caller. ⚠ Absolute, **scoped to `.page-header--company`**, so the centred title stays centred and no other view is re-laid-out; a reserved lane stops a long name running under it, both dropping away under 860px. ⚠ Admin-only gate unchanged (returns empty for a non-owner, and it is enforced server-side). ⚠ **Markup verified by rendering; the visual placement is NOT VERIFIED — no browser responded.** |
| **Qualification check — the comparison that was missing** | The grader now compares what the prospect said against the rep's OWN criteria, per criterion | shipped v28, migration 049. `qualification_covered` already stored the disclosure verbatim and the criteria already reached the grader — **nothing joined them**. ⚠⚠ **THREE verdicts** (`passed`/`failed`/`undetermined`), never two: a prospect who never mentioned money is not a prospect who failed. ⚠⚠ **A decided verdict requires the PROSPECT'S OWN WORDS** — measured, 55 of 286 reconstructible qualification quotes are the CLOSER asking (*"is your credit shot?"*), and deciding on those would let his own question disqualify a buyer. Costs almost nothing: 99% of real calls have matched speakers. ⚠ **The downgrade is ONE-WAY** — an unprovable quote turns a decided verdict into `undetermined`; nothing can ever turn anything INTO `failed`. ⚠ **ONE notion of a DQ**, fed into v27's existing outcome rule; no server-side forcing of `outcome`. ⚠ **Criteria are free text per company** — nothing hardcodes 640, and only 1 of 8 profiles has any, so the prompt is byte-identical for everyone else. **Truncation gate run on the two longest calls (2123/2097 turns) with production inputs: 2684 and 2419 of 4500, no truncation.** NO UI yet, by the standing store-then-read pattern |
| **One set of objection names, site-wide** | Fear · Timing · Spouse/Partner · Logistical · Other, from one list | shipped `eadd4af` + `7984fc0` + `d15a5d5`. `lib/objection-categories.js` is the only list; `dashboard.html` mirrors it inline (a browser cannot `require()`) and the mirror is **guarded**. ⚠⚠ **The two schemes were not two hardcoded lists** — the Focus page's labels were **INVENTED BY THE MODEL every period**, which is why they kept diverging. ⚠⚠ **The truncation was server-side, not CSS:** `str(bk.label, 30)` cut *"Needs To Consult Spouse/Partner"* (31 chars) mid-word. Labels are now **coerced to a closed vocabulary**, longest 14 chars, so the cut **cannot fire**. ⚠ **`other` is the existing NULL** — the CHECK still allows only the four, so this is a **pure rename: no migration, no re-grade**. ⚠⚠ **The `NEEDS_WORK_LANE_VERSION` bump is load-bearing** — the labels live inside the cached payload, so without it the rename would have looked shipped and rendered the old names indefinitely. Coercion honours Justin's named mappings (`Trust / Proof / Skepticism → Fear`), 13 pinned against the REAL extracted function. ⚠ **The two surfaces still have different DENOMINATORS on purpose** — Focus counts true objections only, the drilldown counts every moment |
| **Justin's objection boundary, in the grader** | Cannot afford = a disqualification, not a coachable objection | shipped `eadd4af`, `ANALYSIS_PROMPT_VERSION` v26 → **v27**. The prompt previously said the OPPOSITE of the ruling, so an affordability failure had **no route to a DQ**. Now one question precedes categorisation — *can the prospect buy if they decide to?* ⚠ **The DQ carve-out reuses `disqualify_signal`, an EXISTING type** — no enum, no migration. Shared guidance string means the extractor and the surface bucketer **cannot drift**. ⚠ **NO output-token gate needed**: input text plus a redirect between two existing types, output shape byte-identical. ⚠⚠ **The bump marks every prior analysis outdated and the control will offer a large re-grade — that count is COSMETIC. Do not click it.** Already-graded calls keep their old classifications by design |
| **Failed calls surfaced and retryable** | Two counts beside the ungraded one; the existing button now reaches failures | shipped `1af6cd1`. Before, a failed call was in **no count** and **no button could retry one**. ⚠⚠ **Split into retryable (red, an action) and permanent (neutral + a title saying why)** — folded together the number never reaches zero, and a number that never reaches zero stops being read. `lib/failure-class.js`: permanent = missing or rejected (no transcript / 404 / credentials); retryable = everything else **including exhausted automatic attempts**, since a human retry is a different act and gets a fresh budget. ⚠ **Fails OPEN, unlike model-retry and fathom-retry** — the failure direction follows the consequence (one wasted click vs hiding a recoverable call forever). ⚠ `zoom_no_transcript` still decided by `zoom-retry`'s age bound, not a second rule. **Verified on production:** both counts render; dry run reports `failed: 1`, so the retryable one is in and the permanent one is out. ⚠ **The full retry was NOT run** — the control is window-scoped, so retrying that one call meant 80 calls ≈ $19 and re-grading 79 that are fine |
| **Model-call retry** | A rate limit no longer permanently ungrades a call | shipped `263c3de`, migration 048. 429/5xx/529/connection/timeout → requeue (bounded 3); 400/401/403/404/422 → still permanent; **unusable output gets its own branch** (a 200, bounded 2, tighter because it is length-correlated and costs a full transcript per try). Clean run resets both counters. ⚠ **The SDK already retries 3× with backoff** — this sits on top, for a sustained burst not a blip. ⚠ **Separate counter from `transcript_attempts`** or a transcript flake spends the model's budget and the two become indistinguishable. ⚠ `requeueTranscript` reused (one requeue, three callers); `fathom-retry` NOT reused — it parses a status out of a string, the SDK gives one structurally. Tests drive the REAL branches; disabling them fails 3 of 7 |
| **Digest day navigation** | Prev Day / Next Day replaces the date chip row | shipped `9a96d62`, duplicate-date fix `190a421`. ⚠⚠ **The walk is BOUNDED to the 7 days the chips offered** — 35 digest days exist back to 07-21 and days 8+ predate the manager fix, reading "quiet day · 0 calls" for days Josh worked. **The 7-day cap was accidentally shielding the reader from 28 stale days.** Both directions; disabled not hidden at each end; Next cannot reach a day with no digest by construction. **Verified by clicking on production:** back 24→23→22→21, forward 21→22, stats matching the DB at each step (8, 1, 6, 13). ⚠ A duplicate date under the control was caught by the SCREENSHOT, not the tests |
| **Fathom transcript retry** | A 429 or 5xx used to mark a call `error` **permanently**, with nothing retrying it | shipped `582acbb` + migration 047. **Latent defect, not a consequence of the twelve-loop incident** — one bad minute on Fathom's side would silently hole any customer's data. ⚠ **Zoom had this fixed and Fathom did not**: `lib/zoom-retry.js` exists for the same class. ⚠ **Deliberately diverges** — Zoom bounds by CALL AGE (its two cases are textually identical); Fathom's failures carry an HTTP status, so age would permanently fail old calls on a transient blip. Bound is **attempt count**, cap 5, reset on a clean fetch. Retry-After parsed and clamped to 300s. Unclassifiable = permanent. **Proved by forcing a 429 through the REAL extracted branch**; disabling the retry fails all 7 |
| **Justin's history ruling filed** | A call belongs to the company where it was EARNED; moving a rep does not move their past | ruled 2026-08-25. Copying history to a new team is **accepted even though it double-counts** in all-companies views. **Delete-a-company destroying its data is intended**; deactivate is the non-destructive option. The old "unsafe to delete a company that received a rep" warning is **withdrawn**. ⚠ **Not yet honourable in code**: `fathom_calls` carries only `user_id` and team totals resolve `managed_by` live, so "which company earned this call" is unanswerable today. Needs a team stamp at creation plus a sweep of every team-scoped query. **Filed, not built** |
| **Daily digest counts the manager** | It rendered "quiet day · 0 calls" for a day Josh took 8 real calls | shipped with `b27b250`. `generateDailyDigests` built its rep list from `managed_by` alone, which **cannot contain the manager** — and Josh's four reps are 3 demo accounts + a test user with no real calls, so the countable set was empty **every day**. ⚠ **Ninth site of the defect `9a27979` fixed at eight endpoints**; it never got the fix because it hand-rolls the rule. ⚠ **`resolveTeam` could not be called** — it takes an Express `req` and this runs from the cron with no request — so the shared thing is the RULE: new `lib/team-membership.js` (`withBoardOwner` + `membersByManager`), which `routes/team.js` now imports instead of keeping its own copy. **Enumerated by capability: only 5 live sites build a user set from `managed_by` and this was the only one that dropped the manager** (a 6th in `team-needs-work.js` is inside an archived comment). Failing test reproduces the 0 first |
| **Grading control on the Calls page** | The backlog control moved onto the line that already said "N not graded yet"; still in Connections | shipped `e56b78a`. It lived ONLY in Account -> Connections and **Justin could not find it**, while the Calls page named the problem and offered no fix. **Two render sites, ONE implementation** — a guard fails if a second window-option list appears. **⚠ It deliberately restates no count**: three "ungraded" numbers are live at once, each correct for its own window (Coaching tile = the date range, Calls note = the library query, grader = all time), so a count on the control would sit inches from a different count with the same label. `isSelf()`-gated — on an admin pivot it would grade the VIEWER's calls. **Verified by clicking on production** (1 mousedown delivered): confirm panel -> 267 calls / ~$64 -> Start Grading -> progress moved, both sites showing the same live state |
| **All-time grading is owner-only** | Everyone else capped at 30 days (Justin, API cost) | shipped `e56b78a`. **403, refused not truncated** — answering an all-time request with 30 days spends less than asked and reports success. Gated **before any lookup**, `dry_run` included; **fails closed** because `requireAuth` fails OPEN on a DB error. **⚠ The architect read the trial account as a `user`; measured it is a `manager`**, so `!== 'user'` would have granted all-time to exactly the account the ruling caps — the check is positively `=== 'owner'`. A capped user is told why. Proven over HTTP with a forged non-owner, plus non-vacuity cases (owner passes, 7d/30d pass) |
| **A dying grading run says so** | Polled progress + a stall detector, instead of a spinner that never resolves | shipped `e56b78a`. The analyze loop is fire-and-forget and **does not survive a redeploy**. Remaining is re-read from `/fathom/status` every 15s and the bar moves only when it actually falls. **⚠ The stall window is derived**: measured per-call time is p50 50s / max 247s, so a shorter one reports a healthy run as dead. A failed status fetch counts as a quiet tick, not a failure. **⚠ Repaints only the control host** — a full re-render every 15s replays every `.fade-in` |
| **fix: switching company left the previous company's GAUGES on screen** | Header, graphs, rep list and totals switched; Team Averages did not | shipped `e56b78a`. Measured on production: picking "Sober Living Riches" kept Scout Systems' 21% / 8-of-39 and its "4 not enough calls" (Scout Systems' rep count) under the other company's name, with no error. `resetTeamData()` missed `teamAverages` — **third lane it has missed**. ⚠ Cleared in `pickTeam`, NOT `resetTeamData`: the gauges are range-independent and their own label says the date filter does not affect them, so clearing them on a range change would make them reload right after that promise |
| Fathom OAuth + auto-sync | Connect Fathom, 2-hourly GitHub-Actions cron syncs calls | 381 calls synced on Josh |
| Post-call analysis pipeline | Grader + highlight extractor per call, 2 Claude calls | `ANALYSIS_PROMPT_VERSION` currently v17-era |
| Call library + call review | List of analysed calls; per-call grades, highlights, follow-up email | |
| Coaching Dashboard | Glance tiles, coach summary, what-needs-work, performance summary | default landing for all roles |
| Objections view | Metrics, per-category breakdown, moment feed, coaching synthesis | synthesis is a ~34s model call, cached per day |
| EOD Report | Per-call editable report + copy-for-Slack | |
| Knowledge Base | Upload/list/delete, auto-harvest from closed calls (Phase 7b) | manual Add-to-KB removed 2026-08-18 |
| Team layer | Team view, rep cards, rep line graphs, team averages gauges, daily digest | |
| Prospects + close rate | Prospect entity, merge/unmerge, closed-prospects ÷ total | |
| Date-range picker | Calendar picker; every view owns its own range | |
| Welcome sequence | One-shot dial animation after fresh login | never a gate; 6s watchdog |
| Site-wide background | Team raster on every view, `cover`, `50% 50%`, **opacity 1**; eased smoothstep ramp 22%→96% | shipped `9d297a8`, faded `6adb774`, **banding fixed `9f5d2d9`** — no line visible at 1440 or 1920 |
| Objection focus states | `insufficient` split into no_volume / thin_types / even_performance; focus_set + full ranking | shipped `9f5d2d9`; thresholds untouched |
| Sync Now in Connections | Manual sync for both providers in #account, verified by clicking | shipped `6adb774`; healthy dashboard cards removed with it |
| Week labels in Scout green | Weekly team-graph axis labels `#09e046` | shipped `864bd49` |
| Rep filter dropdown | Custom multi-select listbox: stays open across picks, colour swatches, keyboard + focus | shipped `9265131`, capture-phase fix `0fbde98`; **verified by clicking** — 3 reps toggled without reopening, persistence across reload |
| Zoom OAuth + deauthorization | Connect/disconnect, deauth endpoint live | |
| Zoom speaker identity | Byte-identical display-name match, collision detector | `lib/zoom-identity.js` |
| Welcome emails / set-password | Invite flow via Resend | |
| Account page | Profile, password, billing badge, Connections | |
| **Mark a call "not a sales call"** | Closer on their own call, manager on a team call; excluded from every metric, stays visible and flagged | shipped `6129fed`, library rows `ae19d22`. Verified by clicking on BOTH surfaces: mark from a row in place, opens, un-mark from either side, no row overflow at 1440/1920. ✅ **DONE** (`87383ab`). Marked row recoloured to Justin's ruling: red badge = state, green button = action. Hover states + un-dim shipped `2f6514f`: unmarked hovers RED (previewing the state the click produces), marked hovers a brighter green; the row dim moved to `.library-card-main` so badge and button render at full strength. Earlier (`eceb31c`): Button inverted to Justin's ruling (neutral unmarked / red marked); the cache question closed — entries accumulate, so the pre-mark entry is neither evicted nor overwritten and the hash round-trips exactly. Not structural; no cache behaviour changed. |
| **Site-wide font: Saira** | Self-hosted variable woff2 (96.4 KB), dashboard + login, body 450, tabular numerals | shipped `284cfdb`/`39faa05`. Verified on the live site: face loads on both pages (registered count + proven control), weight 650 renders natively, no overflow, no login wrap, no nav collision at 1920 |
| **Wordmark is the logo image** | `web/scout-wordmark.png` (1038x138, 71.8 KB) replaces the text lockup on login AND in the welcome overlay | shipped `0073af9`. Source plate keyed out on GREENNESS not brightness — the slide-background arcs peak BRIGHTER than the glow, so no luminance cut could separate them; arcs end at alpha 3-9, glow preserved at 41. One constant colour, alpha carries the shape (224KB → 71.8KB). Dead `archivo-expanded-700.woff2` + both `@font-face` blocks + every derived lockup constant DELETED. Verified on production: both pages carry it, asset serves 200, dead font 404s, no upscaling, no overflow at 8 viewports (420x720 → 1920x1080). ⚠ Motion untouched; the title now fades rather than settling its tracking, because an image has no letter-spacing — reported, not worked around |
| **`/team/why-prose` fixed** | It had never worked — `getAdminClient()` is undefined in `team.js`, and the args to `resolveTeam` were reversed | shipped `1d8ae80`. Failing test first, over HTTP through the real handler; it reproduced the exact production symptom `500 "Failed to load rep summaries"`. Post-fix assertion is **503 (not configured)** because the route cannot reach `getAdmin()` while it is dying on an undefined symbol — the two codes separate precisely what is under test. **Verified on production with content:** 5 rep summaries, Josh's reads *"closed 11 of 41 prospects (27%) … partner objections unhandled in all 7 cases … weakest section discovery (42)"*, cross-checking his rep card exactly |
| **Manager is on their own team board** | `resolveTeam` returns `memberIds` (reps + the board owner); the rule had been hand-rolled at 2 of 10 endpoints and absent from the other 8 | shipped `9a27979`. Josh was missing from rep cards, totals, trends, why-prose, recommendations, needs-work, highlights and digest — present only on the gauges and the rep graphs. **Verified on production:** per_rep 4→5, calls_analyzed 6→60, cash 7,500→66,300; Josh's card reads 54 calls, 27% closing (11 of 41), 7% objection handling. Field renamed `repIds`→`memberIds` so a stale reader fails loudly rather than silently dropping the manager |
| **Team objection drilldown (steps 1+2)** | Instance list + per-closer × per-category grid, manager-only, under `/team/objections` | shipped `23dc454`, note `b265626`, `board_size` `9d58911`. **NOT the coaching summary** — that is a separate block. Justin's 3 rulings honoured: manager view only (gated by `teamGate`, forged-closer 403 pinned over HTTP), the EXISTING date picker (range rides the hash, no new component), closers NAMED. **`lib/real-calls.js` is the load-bearing piece** — without it the grid shows Josh 4× under demo names. **Verified on production by clicking:** category 177→55 moments matching the grid's Timing cell `4/55`; range 7d 28 → 90d 177 with buckets reconciling to ALL exactly (65+23+55+34=177); 177 clip links, **171 Fathom "▶ Clip" / 6 Zoom "▶ Open Recording", 0 mislabelled**. This view was the LAST hardcoded `▶ Watch Clip` in the file |
| **Synthetic rows excluded from EVERY team surface** | Demo copies and seeded rows no longer reach any cross-team aggregation | shipped `7a83b2a` / `32982cb` / `dda8739`. **Josh's board before → after: objection universe 280 → 179** (now identical to the drilldown), **prospect denominator 258 → 202** (the 56 seeded prospects gone), and the demo reps went from fabricated figures to honest empty states. ⚠ Took THREE rounds: the shared loader, then five more aggregators that never used it (rep cards, gauges, rep graphs, why-prose), then the close rate in its own module. ⚠ The rule is the ID PREFIX and that is a convention, not a property — a user-level rule is WRONG because `reviewer@scoutsystems.io` owns 18 synthetic AND 6 real calls |
| **Fathom identity auto-capture + choose your sync window** | The prompt is now a fallback, and the user picks 30d / 90d / All time | shipped `2fcaea2`, cap fix `083f89f`. **Probed live: Fathom has NO identity endpoint** (`/me` etc all 404, `/users` 403) and its token is **workspace-scoped** (5 distinct recorders in one unfiltered page), so `recorded_by[]` is required — but `/team_members` + an EXACT Scout-email match removes the prompt for the common case. **"Only 30 days" was a CALL-COUNT cap**: page size is hard-coded at 10, `MAX_PAGES` was 20 = 200 calls, Josh got exactly 200 (38 days) while his real history is 560 back to 2021. **The older tail was unreachable** because every sync after the first passes `created_after=last_sync_at`. **Verified on production: 560 fetched, 160 new, 432 → 592 calls, back to 2021-09-16, `truncated:false`, 15.5s — and 159 of 160 left ungraded, so the grading cap held** |
| **Admin: add a company, cycle guard, diagnostics removed** | Create a company, reassign people, and the reset-diagnostics surface is gone | shipped `01b5283`. **"Add company" = promote an existing SINGLE USER** (a company IS a manager's team, so it needs a head; `+ Create user` already makes people). ⚠ **The definition had to widen to "has reps OR is named"** — a reps-only rule makes creating one impossible, since a new company has no members. Reclassifies nobody. ⚠ **That superseded the no-reps naming guard shipped hours earlier**; it became a ROLE check and its test was CONVERTED, not deleted. **Reset diagnostics removed — UI AND endpoint** (confirmed only two files referenced it; endpoint now 404s). **Verified by clicking: Companies 1→2, Single Users 3→2, All Users still 8** — counts consistent, nobody lost. ⚠ **The cycle guard is NOT live-exercised** — the pre-existing role check fires first in current data |
| **Company lifecycle + backlog grading + creation moved into onboarding** | Deactivate/reactivate/delete a company; a reachable control for ungraded calls; company creation inside "+ Create user" | shipped `319d06b`, `982d259`. **Deactivate keeps the data, delete destroys it** (Justin). Migration 045's `deactivated_with_company` is what stops a company reactivate resurrecting someone deactivated on purpose. **Delete verified on a throwaway: 3 users / 12 calls removed, every other account BYTE-IDENTICAL before and after, zero residue.** ⚠ **Global KB rows are KEPT deliberately** — one account owns 583 and every other company can see them. ⚠ **KB deletion is UNPROVEN** (the fixture's KB insert silently failed, so the path was never exercised). **Backlog: `renderFathomStripConnected` was orphaned by `6adb774` — defined, never called — so a healthy account had NO way to grade ungraded calls.** Replaced by a Connections row; the window picker now shows for a pure backlog, not only for outdated work |
| **EOD says when the day is still filling up** | "3 calls, he took 6" — the 3 was correct | shipped `0deadd9`. Causes: two calls **not synced yet** (last sync 22:34, ET day runs to 04:00Z) and one that exists **only in Zoom**, which that account has no connection to. **(a) grading ruled out** — EOD counts SYNCED calls, not analysed. Fix is the WORDING, not the number. Both exits carry it, including the empty-day return |
| **Cash is EOD-only; Calls refreshes on entry** | Justin's ruling + the "Show more" cause found | shipped `f662b1c`. **Cash removed from every display and derivation except EOD** (tile, rep line, drilldown, summary payloads, team + session analytics) — the column and grader extraction are untouched. **"Show more" was the FIRST RENDER**: entering Calls re-rendered a list cached earlier in the session, so `hasMore` was stale; it now refetches on entry (call-review excluded, so returning from a call keeps your place). ⚠ **A latent comment-stripper ordering bug in 11 guards detonated on this edit** — two reported a missing query and a missing import that were both present. Fixed the order in all 11 |
| **Cross-provider duplicates suppressed** | 23 duplicates on Josh's account; cash was 16% overstated | shipped `abc8f77`. Matched on **interval OVERLAP**, not duration delta (Zoom reports whole minutes, and a delta is scale-blind) and not `meeting_id` (a Personal Meeting Room reuses one id for every meeting). Thresholds sit in the **empty band** measured on his corpus: genuine 71-99%, ambiguous 42%, known false pair 0%. **Suppress, never delete** — `duplicate_of` points at the survivor; Fathom wins as a **flippable preference**. All 21 counting surfaces paired, guarded on the PAIRING. Library shows duplicates badged. **Josh: calls 596→573, cash $274,431→$230,331, objections 208→199, prospects 215→204** |
| ~~"Show more" — NOT REPRODUCED~~ | ✅ **RESOLVED 2026-08-25** — it was the first render, not the query. See the row above | The `.range(0, 9999)` 10,000-row ceiling found during that investigation is still filed and still unfixed |
| **30-day default on a new connection** | Both providers backdate 30 days on a first sync; the History picker still offers 90d / all time | shipped `6cf9d53`. **Fathom had NO date limit** (pulled to the 200-call page cap); **Zoom already pulled ~30 days but by inheriting Zoom's API default** — now sent explicitly, because an upstream default is not a decision. ⚠ The Fathom default is applied **only when no window exists**: the connect handler is an upsert, so writing it unconditionally would silently narrow anyone reconnecting who had chosen all-time. Grading cap untouched |
| **Admin view rebuild, part 1 — companies** | Storage for the name, company blocks, three tabs, the name on Team view | shipped `e4679e8`. **A company is a RENAMED TEAM** — migration 043 adds ONE nullable column (`user_profiles.team_name`) rather than a `companies` table, because a team is already uniquely identified by its head and **creating the entity is how "renamed team" quietly becomes a tier**. Fallback **"Unnamed company"**, deliberately NOT derived from the head (a derived label reads as real, so nobody ever sets the real one). ⚠ It replaced an **email dressed up as a company** — `"josh@scoutsystems.io's team"` — which two separate generators were producing. **A company is defined by HAVING REPS, never by role**: live data has a `manager` with zero reps who is correctly a Single User. Exactly-one bucketing is guaranteed by construction, including the reps-AND-a-manager shape the schema allows. Grouping runs **server-side** so the page cannot grow a fourth mirrored predicate. **Verified on production by clicking:** tabs read 1 / 3 / 8 matching the DB; typed a name, saw `…` → `✓`, confirmed it persisted server-side, and saw it render top-centre on Team view |
| **fix: the admin user list was clipped** | `max-height: 320px` hid rows behind an inner scrollbar | shipped `a7ad0c4`. **Pre-existing** — All Users was already truncated at ~6 of 8 — and company blocks made it visible by dropping 2 of a company's 5 members. ⚠⚠ **Every measurement said it was fine**: all 5 rows were in the DOM and `getBoundingClientRect` reported them visible, because they DO have size; they sat inside a clipped container. **The screenshot showed the section ending after row 3 and the numbers did not.** A DOM check cannot see clipping by an ancestor |
| **fix: a connected Fathom account could not sync, and the page said "not connected"** | A new trial account connected Fathom, clicked Sync **five times**, saw nothing — no calls, no progress, no error | shipped `7682173`. **The connection SAVED and the sync RAN**; it was refused at the identity gate (`fathom_email` NULL), which is CORRECT — without `recorded_by[]` a sync pulls the whole team's recordings. ⚠ **`needs_identity` is neither a success nor an error**, so no notice fired. **Three faults, all needed together:** (1) `showFathomIdentityPrompt()` ended `if (state.view === 'overview')` and the only manual Sync a healthy account has lives in **#account** — plus the same defect in all 3 renders of `saveFathomIdentity`, where it made a **failed save silent**; (2) the prompt's only MARKUP was the overview strip, so even a correct render call had nowhere to draw; (3) the Calls empty state emitted "Connect a Recording Source" whenever the list was empty, **with no connectivity check at all**. Also: both sync handlers now invalidate the cached call list, or a successful sync re-renders the cached EMPTY list. **Verified on production by clicking** (1 mousedown delivered): the identity prompt opens on the Calls page with 6 real Fathom identities. ⚠ **Proving non-vacuity caught a gap in my own tests** — 10 of 12 stayed green when the fix was reverted, because they drove `callLibraryEmptyHtml` directly and nothing asserted `renderCallLibrary` **calls** it |
| **fix: add-user was completely broken** | `ReferenceError: normalizeName is not defined` — every attempt to add a user threw | shipped `0dec7f7`. **PRE-EXISTING, introduced by `20ab18c`** (proven with a worktree at `b535fb5^`, which fails identically) — that commit added two `normalizeName()` calls to `POST /admin/users` and never added the import, so add-user has been dead since it shipped. ⚠ **`node -c` PASSES on that file** — an identifier that resolves to nothing is only found by RUNNING the line, and no test ever invoked the handler. ⚠ The call sat **above the route's own try/catch**, so the throw escaped: no log line, bare failure. Fixed + the try now covers the whole handler + the real reason is surfaced. **Verified on production by clicking**: rep created, name normalised "click"→"Click", invite sent. Swept every route/lib for the same class — no other instances |
| **Admin can delete a user who has calls** | Two modes: no history → hard delete; has history → **tombstone** (access revoked, identity scrubbed, calls kept) | shipped `b535fb5`. ⚠ **The architect's premise was not what the schema does** — every history table CASCADEs on an `auth.users` delete, proven on a throwaway (2 calls in, 0 out), and the `user_id` columns are NOT NULL so `SET NULL` needs a migration. The tombstone keeps the auth row so all ~12 FKs hold, with **no migration**. **Verified on an isolated throwaway team: reps 2 · calls 3 · cash 5000 · avg 65 before and IDENTICAL after.** Surviving rows render as "Deleted user (436a87e8)". Gate proven over HTTP with forged actors — **closer 403, manager 403, owner clears** (non-vacuity). Two-step typed confirmation restored: it was dropped in July as redundant *because of the zero-history gate*, which this ruling removes |
| **Strict objection standard + rep filter on the drilldown** | Disqualifications and payment/logistical barriers no longer count toward any rate; the rep filter now works on this page | shipped `94c67dc` / `ca12930` / `30a39e5` / `26ed0e7`. **Josh's rates moved: ALL 20% (35/177) → 17% (26/155)**, biggest single move logistical 43% (10/23) → 56% (5/9). Excluded and stated on screen: **13 disqualifications + 9 payment/logistical barriers**. ⚠ The classification is an LLM call over the distinct surface phrases — cold ~20s, cached ~2s, and **non-deterministic across cold runs** (two concurrent cold requests gave 15/8 and 13/9); stable once cached. A classifier failure reports `strict:false` and the panel SAYS the numbers are not the standard, because without the exclusion the rate reads HIGHER than the truth. Rep filter reuses the team-graph control (roster + apply parameterised, not copied), hides the rep from grid, moments AND summary, persists per team across a refresh — **verified by clicking, 3 clicks delivered, menu stayed open, survived a full reload** |
| **Old "Objection Handling Focus" page ARCHIVED** | `#team-needs-work` now redirects to the drilldown | shipped `30a39e5` / `26ed0e7`. Safe to archive only once the drilldown gained **rates per sales-language bucket** — the last thing it lacked. ⚠ Archived as a **redirect, not a deletion**: a bookmark is a link too. ⚠ Normalised at the ENTRY POINTS (setView + the hash map), because the view was dispatched from **three** renderers and a fix in one of them shipped looking correct while production kept serving the old page |
| **Drilldown refinements (Justin's six)** | Routing, in-card controls, reused picker, team-average row, no Manage Members / Customize View, bare labels | shipped `3c1e3a5`. **Verified by clicking on production, 3 clicks delivered:** the averages gauge and the focus card both land on `#team-objections` (the gauge carrying its fixed 7-day window); the picker is the SAME component and its selection survives leaving and returning; controls sit inside the card; the two team-only buttons are gone; badges elsewhere in the app kept their fills. ⚠ The personal objection surfaces are **deliberately not retargeted** — the drilldown is manager-only, so retargeting them would 403 every closer. ⚠ Team average is **omitted with a stated reason below two closers** ("with one, it would just repeat the row above") |
| **Coaching summary (drilldown step 3)** | Per-closer "Why", named at any team size, explaining the MECHANISM behind the rate — not restating it | shipped `991b597`, output budget `966d963`. One Claude call for the whole board; state model from `team-needs-work` so a data shortage never reads as good news; reads through `computeTeamObjections` so the grid and the paragraph cannot disagree and the not-a-sales-call/synthetic filters are inherited rather than rebuilt. **Verified on production by looking at the panel**, and the cache proven both directions: mark → `cached:false`, 55→53, text regenerated; un-mark → `cached:true`, 55 back with the original text byte-identical. **Cost: miss ~10.6s, cached ~1.8s, of which ~1.8s is the query the cache cannot skip** — ~1,360 input tokens for one closer, ~960 more per additional closer |
| **Login body weight 450** | Login was the last surface still at 300; it now matches the site | shipped `31c446d`. **Edited in place, not overridden** — one weight declaration per selector, so the file cannot acquire a third contradiction. Verified on production: **57 elements compute 450, zero offenders**, 450 comes from Saira's real axis (inside the declared `100 900`, ink mass distinct from 300 and 900), advance unchanged so nothing reflows. The trailing `.brand-name` 700 was removed as **redundant** — it holds 700 by specificity `(0,2,0)` vs the catch-all's `(0,1,0)`, confirmed in the browser after removal |
